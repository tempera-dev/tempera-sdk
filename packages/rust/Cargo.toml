[package]
name = "tempera-sdk"
version = "0.13.0"
edition = "2024"
license = "UNLICENSED"
repository = "https://github.com/tempera-dev/tempera-sdk"
description = "Generated contract client for authenticated Tempera product APIs"

[lib]
path = "src/lib.rs"

# Keep this publishable SDK package independent of an unrelated parent Cargo
# workspace when it is checked out inside a larger developer directory.
[workspace]
