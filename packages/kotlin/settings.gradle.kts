// The Kotlin SDK is its own Gradle build: nothing else in this repository is
// built by Gradle, and consumers depend on the published artifact rather than
// on a composite build.

rootProject.name = "tempera-sdk-kotlin"
