Vendored from tempera-gym repo: contracts/gym-api.openapi.yaml
Origin path: /home/user/tempera-gym/contracts/gym-api.openapi.yaml
Deterministic JSON rendering of the upstream YAML contract (json.dumps(yaml.safe_load(...), indent=2)) because this gate reads JSON specs; the upstream repo's tests/test_service.py drift-gates the YAML against its ROUTES. Do not hand-edit; refresh by re-converting the upstream committed spec.
