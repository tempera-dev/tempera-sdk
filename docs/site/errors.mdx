---
title: "Errors"
description: "One TemperaApiError across seven wire error shapes, plus MCP gateway errors."
---

{/* Generated by scripts/gen-sdk-docs.py from surface.json -- do not hand-edit. */}

Every product speaks its own wire error shape; the SDK normalizes all of
them into one **`TemperaApiError`** with identical fields in every
language (Python uses `request_id`/`retry_after`; the HTTP-less Rust crate
normalizes `status`/`code`/`message`/`request_id`/`retryable` from response
bodies via `normalize_error_body` and carries no request context):

| Field | Meaning |
|---|---|
| `status` | HTTP status code of the failed response. |
| `code` | Machine-readable error code, when the wire shape carried one (else null/None). |
| `message` | Human-readable error message; never empty. |
| `requestId` | Server request id, from the body or the `x-request-id` response header. |
| `product` | Product key of the client that made the call. |
| `operation` | Operation id of the typed call (null/None for passthrough requests). |
| `body` | The raw parsed response body, untouched. |
| `retryable` | Server-declared retryability (`body.error.retryable`) when the object wire shape carried a boolean, else null/None. Opt-in client retry also treats HTTP 429/502/503/504 as retryable when the body declares nothing. |
| `retryAfter` | Parsed numeric `Retry-After` response header, in seconds (TypeScript `retryAfter` / Python `retry_after`; the header-less Rust normalization omits it). |

## Normalization rules

Applied in order to the parsed error body:

1. If body.error is an object: code = body.error.code, message = body.error.message, requestId = body.error.request_id (cradle, remi, data-engine, tempera-gym; data-engine bodies always carry request_id).
2. Else if body.error is a string and body.message is present: code = body.error, message = body.message (control plane, palette).
3. Else if body.error is a string: code = null, message = body.error (tempo).
4. Else: code = null, message = the HTTP status text.
5. requestId also falls back to the x-request-id response header when the body carries none (remi echoes it).
6. retryable = body.error.retryable when the object shape carries a boolean (cradle, data-engine, tempera-gym), else null/None; retryAfter = the numeric Retry-After response header in seconds, when present (TypeScript and Python; the header-less Rust normalization surfaces retryable only). The TypeScript and Python clients use these for opt-in bounded retry of idempotent (GET/DELETE) operations, also treating HTTP 429/502/503/504 as retryable when the body declares nothing.

## Wire shapes by product

| Product | Wire error shape |
|---|---|
| auth-hub | `{"error": "<code>", "message": "<text>"}` |
| palette | `{"error": "<code>", "message": "<text>", "status": <int>}` |
| tempo | `{"error": "<message>"}` |
| cradle | `{"error": {"code", "message", "status", "request_id", "retryable", "details"}}` |
| remi | `{"error": {"code": "<code>", "message": "<text>"}}` |
| data-engine | `{"error": {"code", "message", "status", "request_id", "retryable", "details"}}` |
| Tempera Gym | `{"error": {"code", "message", "retryable"}}` |

## TemperaMcpError

JSON-RPC errors from the [MCP gateway](/mcp-gateway) raise `TemperaMcpError`
(TypeScript and Python) with the gateway's integer `code`, its `message`, and
any `data` member. The Rust crate returns the same information as `McpError`
(`code`, `message`) from `parse_mcp_error(body)`. A non-conformant error
without an integer code is reported uniformly as code `0`.

## MCP gateway error codes

| Name | Code | Meaning |
|---|---|---|
| `planLimit` | `-32002` | Plan limit exceeded (`plan_limit_exceeded`): the workspace's metered `mcp_invocations` allowance is exhausted; upgrade the plan or wait for the meter to reset. |
| `invalidRequest` | `-32600` | The payload is not a valid JSON-RPC 2.0 request. |
| `methodNotFound` | `-32601` | Unknown JSON-RPC method (or unknown tool name on `tools/call`). |
| `invalidParams` | `-32602` | The method was called with invalid or missing parameters. |
| `internalError` | `-32603` | The gateway or an upstream product MCP server failed. |
