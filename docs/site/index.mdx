---
title: "Tempera SDK"
description: "One SDK, three languages, every Tempera product \u2014 generated from a single manifest."
---

{/* Generated by scripts/gen-sdk-docs.py from surface.json -- do not hand-edit. */}

The Tempera SDK exposes **one uniform surface in three languages** — TypeScript
(`@tempera/sdk`), Python (`tempera-sdk`), and Rust (`tempera-sdk`) — covering every
Tempera product: the same product registry, the same operation names and
descriptions, and the same error model. All of it is generated from a single
manifest, `surface.json`, and gated in CI so the packages (and these docs)
cannot drift apart.

## Quickstart

Authenticate once — a central `tp_` API key works as a bearer at every product —
and call any product through the unified client.

<CodeGroup>

```typescript TypeScript
// npm install @tempera/sdk
import { TemperaAuth, createTemperaClient } from "@tempera/sdk";

const auth = new TemperaAuth({
  issuerUrl: "https://api.tempera.dev",
  apiKey: process.env.TEMPERA_API_KEY, // tp_... works at every product
});
const client = createTemperaClient({ auth, environment: "production" });

const traces = await client.palette.listTraces({ tenant_id: "org_1", limit: 20 });
const session = await client.tempo.createSession({ url: "https://example.com" });
```

```python Python
# pip install tempera-sdk
import os
from tempera_sdk import TemperaAuth, TemperaClient

auth = TemperaAuth(
    issuer_url="https://api.tempera.dev",
    api_key=os.environ["TEMPERA_API_KEY"],  # tp_... works at every product
)
client = TemperaClient(auth=auth, environment="production")

traces = client.palette.list_traces({"tenant_id": "org_1", "limit": 20})
session = client.tempo.create_session({"url": "https://example.com"})
```

```rust Rust
// cargo add tempera-sdk
use tempera_sdk::{TemperaAuth, TemperaClient};

// The Rust crate is HTTP-less: it builds fully-described RequestSpecs
// (method, URL, headers, JSON body) for your own HTTP client to send.
let auth = TemperaAuth::new("https://api.tempera.dev").with_api_key(api_key);
let client = TemperaClient::new().with_auth(auth);

let spec = client.build_request(
    "palette",
    "list_traces",
    &[("tenant_id", "org_1".into()), ("limit", 20i64.into())],
)?;
// spec.method / spec.full_url() / spec.headers / spec.body_json
```

</CodeGroup>

Method naming is mechanical across the languages: the manifest's camelCase
operation id in TypeScript (`listTraces`), snake_case in Python (`list_traces`),
and `build_request(product, "list_traces", params)` in Rust. Parameters use wire
names (snake_case) in every language.

## Products

Every product in the registry — including the ones without typed operations
yet — gets a client in all three languages.

| Product | Audience | Env var | Repository | Operations | Description |
|---|---|---|---|---|---|
| [auth-hub](/products/control-plane) | — | `TEMPERA_CONTROL_PLANE_URL` | [auth-hub](https://github.com/tempera-dev/auth-hub) | 38 | Tempera control plane: unified accounts, OAuth issuance, workspaces, teams, API keys, billing, usage metering, and the unified MCP gateway. |
| [palette](/products/palette) | `palette` | `TEMPERA_PALETTE_URL` | [palette](https://github.com/tempera-dev/palette) | 15 | Agent observability: trace and span ingestion, search, datasets, evals, experiments, gates, and human review. |
| [tempo](/products/tempo) | `tempo` | `TEMPERA_TEMPO_URL` | [tempo](https://github.com/tempera-dev/tempo) | 18 | Agent-native browser daemon (tempod): structured observation, batched actions, sessions, runs, and human handoff. |
| [cradle](/products/cradle) | `cradle` | `TEMPERA_CRADLE_URL` | [cradle](https://github.com/tempera-dev/cradle) | 10 | Capability sandbox daemon (cradled): synchronous and job-based sandboxed execution plus browser admission control. |
| [remi](/products/remi) | `remi` | `TEMPERA_REMI_URL` | [remi](https://github.com/tempera-dev/remi) | 12 | Temporal memory server: remember, project, query, and maintain an agent memory graph. |
| [data-engine](/products/data-engine) | `data-engine` | `TEMPERA_DATA_ENGINE_URL` | [data-engine](https://github.com/tempera-dev/data-engine) | 19 | Domain-portable label-emergence engine: deterministic ingestion, sandboxed verification in cradle, and RL/eval/SFT dataset emission. |
| [human-data](/products/other-products) | `human-data` | `TEMPERA_HUMAN_DATA_URL` | [human-data](https://github.com/tempera-dev/human-data) | passthrough | Human data operations: expert annotation and review pipelines. Passthrough client only; no typed operations yet. |
| [temp.js](/products/other-products) | — | `TEMPERA_TEMPJS_URL` | [temp.js](https://github.com/tempera-dev/temp.js) | passthrough | Durable JavaScript runtime bridge for Tempera agents. Passthrough client only; no typed operations yet. |
| [tempOS](/products/other-products) | — | `TEMPERA_TEMPOS_URL` | [tempOS](https://github.com/tempera-dev/tempOS) | passthrough | OS/runtime admission, policy, and receipt layer for agents. Passthrough client only; no typed operations yet. |
| [Arrha](/products/other-products) | — | `TEMPERA_ARRHA_URL` | [arrha](https://github.com/tempera-dev/arrha) | passthrough | Settlement, chain, credits, and indexer layer for agent payments. Passthrough client only; no typed operations yet. |

112 typed operations across 6 products, plus a raw
`request()` escape hatch on every product client for endpoints the surface
tables do not cover yet.

## Where to go next

- [Authentication](/authentication) — the unified account model, OAuth2 + PKCE, and `tp_` API keys.
- [Environments](/environments) — presets and base-URL resolution.
- [Errors](/errors) — one `TemperaApiError` across six wire shapes.
- [MCP gateway](/mcp-gateway) — every product as namespaced MCP tools.
- [Rollout](/rollout) — how an endpoint change propagates into the SDK.
