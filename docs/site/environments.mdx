---
title: "Environments"
description: "The four environment presets, per-product base-URL env vars, and resolution precedence."
---

{/* Generated by scripts/gen-sdk-docs.py from surface.json -- do not hand-edit. */}

> **Private access.** Hosted Tempera services are available through design-partner onboarding.
> Use the issuer, credentials, environment, and product URLs provisioned for your workspace;
> start in staging unless Tempera explicitly approves another target.

The SDK ships four versioned environment presets. A preset records target URLs;
it does not assert that a deployment is live or generally available. Private
onboarding tells you which preset and overrides to use. Begin with `staging`; use
`production` only after Tempera explicitly provisions and approves production access.

## Presets

| Field | `local` | `preview` | `staging` | `production` (reserved) |
|---|---|---|---|---|
| `authIssuerUrl` | `http://localhost:8787` | `https://preview-api.tempera.dev` | `https://staging-api.tempera.dev` | `https://api.tempera.dev` |
| `authJwksUrl` | `http://localhost:8787/.well-known/jwks.json` | `https://preview-api.tempera.dev/.well-known/jwks.json` | `https://staging-api.tempera.dev/.well-known/jwks.json` | `https://api.tempera.dev/.well-known/jwks.json` |
| `controlPlaneUrl` | `http://localhost:8787` | `https://preview-api.tempera.dev` | `https://staging-api.tempera.dev` | `https://api.tempera.dev` |
| `cradleApiUrl` | `http://127.0.0.1:8088` | `https://preview-cradle.tempera.dev` | `https://staging-cradle.tempera.dev` | `https://cradle.tempera.dev` |
| `dataEngineApiUrl` | `http://127.0.0.1:8090` | `https://preview-data-engine.tempera.dev` | `https://staging-data-engine.tempera.dev` | `https://data-engine.tempera.dev` |
| `mcpGatewayUrl` | `http://localhost:8787/mcp` | `https://preview-api.tempera.dev/mcp` | `https://staging-api.tempera.dev/mcp` | `https://api.tempera.dev/mcp` |
| `paletteApiUrl` | `http://localhost:8080` | `https://preview-mcp.tempera.dev` | `https://staging-mcp.tempera.dev` | `https://mcp.tempera.dev` |
| `paletteMcpUrl` | `http://localhost:8080/mcp` | `https://preview-mcp.tempera.dev/mcp` | `https://staging-mcp.tempera.dev/mcp` | `https://mcp.tempera.dev/mcp` |
| `publicSiteUrl` | `http://localhost:3000` | `https://tempera-public-site-git-preview-tempera.vercel.app` | `https://staging.tempera.dev` | `https://tempera.dev` |
| `temperaCodeApiUrl` | `http://127.0.0.1:8789` | `https://preview-code-api.tempera.dev` | `https://staging-code-api.tempera.dev` | `https://code-api.tempera.dev` |
| `temperaGymUrl` | `http://127.0.0.1:8091` | `https://preview-gym.tempera.dev` | `https://staging-gym.tempera.dev` | `https://gym.tempera.dev` |
| `tempoApiUrl` | `http://localhost:7878` | `https://preview-tempo.tempera.dev` | `https://staging-tempo.tempera.dev` | `https://tempo.tempera.dev` |

## Per-product base-URL env vars

Every product has a `TEMPERA_*_URL` environment variable, and only the
control plane, palette, and tempo have preset URLs — the other products
must get their base URL from the env var or an explicit override.

| Product | Env var | Preset field |
|---|---|---|
| auth-hub | `TEMPERA_CONTROL_PLANE_URL` | `controlPlaneUrl` |
| palette | `TEMPERA_PALETTE_URL` | `paletteApiUrl` |
| tempo | `TEMPERA_TEMPO_URL` | `tempoApiUrl` |
| Tempera Code | `TEMPERA_CODE_GATEWAY_URL` | — |
| tempera-llm | `TEMPERA_LLM_URL` | — |
| cradle | `TEMPERA_CRADLE_URL` | — |
| remi | `TEMPERA_REMI_URL` | — |
| data-engine | `TEMPERA_DATA_ENGINE_URL` | — |
| human-data | `TEMPERA_HUMAN_DATA_URL` | — |
| temp.js | `TEMPERA_TEMPJS_URL` | — |
| tempOS | `TEMPERA_TEMPOS_URL` | — |
| Arrha | `TEMPERA_ARRHA_URL` | — |

## Base-URL precedence

For each product, the client resolves the base URL in this order (first hit
wins); if nothing resolves, calls fail with a configuration error before any
request is sent:

1. **Explicit override** — `baseUrls.<product>` (TypeScript),
   `base_urls["<product>"]` (Python), or `with_base_url("<product>", url)` (Rust).
2. **The product's env var** — `TEMPERA_*_URL` from the table above.
3. **The environment preset** — TypeScript and Python only, and only for the
   control plane, palette, and tempo. The Rust crate takes no `environment`
   option; use an explicit `with_base_url` or the env var (the preset table is
   still available as `tempera_sdk::ENVIRONMENTS`).

Trailing slashes on configured base URLs are trimmed in every language.
