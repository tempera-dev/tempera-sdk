---
title: "palette"
description: "Agent observability: trace and span ingestion, search, datasets, evals, experiments, gates, and human review."
---

{/* Generated by scripts/gen-sdk-docs.py from surface.json -- do not hand-edit. */}

> **Private access.** Hosted Tempera services are available through design-partner onboarding.
> Use the issuer, credentials, environment, and product URLs provisioned for your workspace;
> start in staging unless Tempera explicitly approves another target.

Agent observability: trace and span ingestion, search, datasets, evals, experiments, gates, and human review.

| | |
|---|---|
| Repository | [palette](https://github.com/tempera-dev/palette) |
| Base URL env var | `TEMPERA_PALETTE_URL` (see [Environments](/environments)) |
| Audience | `palette` |
| Auth | bearer minted for audience palette, central tp_ API key, or product x-palette-api-key header |
| Client | TypeScript `client.palette` · Python `client.palette` · Rust `build_request("palette", ...)` |
| Typed operations | 61 |

Anything this page does not cover is reachable through the raw passthrough:
`client.palette.request(path, options)` (TypeScript) /
`client.palette.request(path, ...)` (Python).

## Operations

### health

`GET /health`

Check palette API liveness; returns \{ok: true\}.

- **Producer operationId:** `health.check`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.palette.health()` · Python `client.palette.health()` · Rust `build_request("palette", "health", params)` · Go `BuildRequest("palette", "health", params)` · C `tempera_build_request(..., "palette", "health", ...)`

<CodeGroup>

```typescript TypeScript
const result = await client.palette.health();
```

```python Python
result = client.palette.health()
```

```rust Rust
let spec = client.build_request("palette", "health", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "health", map[string]any{})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "health",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### alertsEvaluate

`POST /v1/alerts/{tenantId}/{projectId}/traces/{traceId}/webhook`

Call POST /v1/alerts/\{tenant_id\}/\{project_id\}/traces/\{trace_id\}/webhook.

- **Producer operationId:** `alerts.evaluate`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.alertsEvaluate()` · Python `client.palette.alerts_evaluate()` · Rust `build_request("palette", "alerts_evaluate", params)` · Go `BuildRequest("palette", "alertsEvaluate", params)` · C `tempera_build_request(..., "palette", "alertsEvaluate", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |
| `traceId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `input` | yes | — |
| `policy` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.alertsEvaluate({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  traceId: "<traceId>",
  input: "<input>",
  policy: "<policy>",
});
```

```python Python
result = client.palette.alerts_evaluate({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "traceId": "<traceId>",
    "input": "<input>",
    "policy": "<policy>",
})
```

```rust Rust
let spec = client.build_request("palette", "alerts_evaluate", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("traceId", "<traceId>".into()),
    ("input", "<input>".into()),
    ("policy", "<policy>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "alertsEvaluate", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "traceId": "<traceId>",
    "input": "<input>",
    "policy": "<policy>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
    {"traceId", "<traceId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"input\":\"<input>\",\"policy\":\"<policy>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "alertsEvaluate",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### createApiKey

`POST /v1/api-keys/{tenantId}/{projectId}/{environmentId}`

Mint a palette-scoped API key; the secret is returned exactly once.

- **Producer operationId:** `apiKeys.create`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Scope:** `admin`
- **Call as:** TypeScript `client.palette.createApiKey()` · Python `client.palette.create_api_key()` · Rust `build_request("palette", "create_api_key", params)` · Go `BuildRequest("palette", "createApiKey", params)` · C `tempera_build_request(..., "palette", "createApiKey", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |
| `environmentId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `scopes` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.createApiKey({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  environmentId: "<environmentId>",
  scopes: "<scopes>",
});
```

```python Python
result = client.palette.create_api_key({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "environmentId": "<environmentId>",
    "scopes": "<scopes>",
})
```

```rust Rust
let spec = client.build_request("palette", "create_api_key", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("environmentId", "<environmentId>".into()),
    ("scopes", "<scopes>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "createApiKey", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "environmentId": "<environmentId>",
    "scopes": "<scopes>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
    {"environmentId", "<environmentId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"scopes\":\"<scopes>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "createApiKey",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### revokeApiKey

`POST /v1/api-keys/{tenantId}/{projectId}/{environmentId}/{apiKeyId}/revoke`

Revoke a palette API key.

- **Producer operationId:** `apiKeys.revoke`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Scope:** `admin`
- **Call as:** TypeScript `client.palette.revokeApiKey()` · Python `client.palette.revoke_api_key()` · Rust `build_request("palette", "revoke_api_key", params)` · Go `BuildRequest("palette", "revokeApiKey", params)` · C `tempera_build_request(..., "palette", "revokeApiKey", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |
| `environmentId` | yes | Substituted into the URL path (percent-encoded). |
| `apiKeyId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.revokeApiKey({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  environmentId: "<environmentId>",
  apiKeyId: "<apiKeyId>",
});
```

```python Python
result = client.palette.revoke_api_key({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "environmentId": "<environmentId>",
    "apiKeyId": "<apiKeyId>",
})
```

```rust Rust
let spec = client.build_request("palette", "revoke_api_key", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("environmentId", "<environmentId>".into()),
    ("apiKeyId", "<apiKeyId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "revokeApiKey", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "environmentId": "<environmentId>",
    "apiKeyId": "<apiKeyId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
    {"environmentId", "<environmentId>"},
    {"apiKeyId", "<apiKeyId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "revokeApiKey",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### archiveQuerySpans

`GET /v1/archive/{tenantId}/{projectId}/spans`

Call GET /v1/archive/\{tenant_id\}/\{project_id\}/spans.

- **Producer operationId:** `archive.querySpans`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.archiveQuerySpans()` · Python `client.palette.archive_query_spans()` · Rust `build_request("palette", "archive_query_spans", params)` · Go `BuildRequest("palette", "archiveQuerySpans", params)` · C `tempera_build_request(..., "palette", "archiveQuerySpans", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `environmentId` | no | — |
| `traceId` | no | — |
| `spanId` | no | — |
| `kind` | no | — |
| `status` | no | — |
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.archiveQuerySpans({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  environmentId: "<environmentId>",
  traceId: "<traceId>",
});
```

```python Python
result = client.palette.archive_query_spans({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "environmentId": "<environmentId>",
    "traceId": "<traceId>",
})
```

```rust Rust
let spec = client.build_request("palette", "archive_query_spans", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("environmentId", "<environmentId>".into()),
    ("traceId", "<traceId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "archiveQuerySpans", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "environmentId": "<environmentId>",
    "traceId": "<traceId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "environmentId=<environmentId>&traceId=<traceId>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "archiveQuerySpans",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### archiveTrace

`POST /v1/archive/{tenantId}/{projectId}/{traceId}`

Archive a trace to Parquet and return the archive manifest.

- **Producer operationId:** `archive.archiveTrace`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Scope:** `trace:read`
- **Call as:** TypeScript `client.palette.archiveTrace()` · Python `client.palette.archive_trace()` · Rust `build_request("palette", "archive_trace", params)` · Go `BuildRequest("palette", "archiveTrace", params)` · C `tempera_build_request(..., "palette", "archiveTrace", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |
| `traceId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.archiveTrace({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  traceId: "<traceId>",
});
```

```python Python
result = client.palette.archive_trace({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "traceId": "<traceId>",
})
```

```rust Rust
let spec = client.build_request("palette", "archive_trace", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("traceId", "<traceId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "archiveTrace", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "traceId": "<traceId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
    {"traceId", "<traceId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "archiveTrace",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### auditList

`GET /v1/audit/{tenantId}/{projectId}`

Call GET /v1/audit/\{tenant_id\}/\{project_id\}.

- **Producer operationId:** `audit.list`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.auditList()` · Python `client.palette.audit_list()` · Rust `build_request("palette", "audit_list", params)` · Go `BuildRequest("palette", "auditList", params)` · C `tempera_build_request(..., "palette", "auditList", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.auditList({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.palette.audit_list({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("palette", "audit_list", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "auditList", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "pageSize=<pageSize>&pageToken=<pageToken>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "auditList",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### calibrationsRun

`POST /v1/calibrations/{tenantId}/{projectId}/{datasetId}/versions/{versionId}`

Call POST /v1/calibrations/\{tenant_id\}/\{project_id\}/\{dataset_id\}/versions/\{version_id\}.

- **Producer operationId:** `calibrations.run`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.calibrationsRun()` · Python `client.palette.calibrations_run()` · Rust `build_request("palette", "calibrations_run", params)` · Go `BuildRequest("palette", "calibrationsRun", params)` · C `tempera_build_request(..., "palette", "calibrationsRun", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |
| `datasetId` | yes | Substituted into the URL path (percent-encoded). |
| `versionId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `evalReportId` | no | — |
| `evaluatorVersionId` | no | — |
| `passThreshold` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.calibrationsRun({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  datasetId: "<datasetId>",
  versionId: "<versionId>",
  evalReportId: "<evalReportId>",
  evaluatorVersionId: "<evaluatorVersionId>",
  passThreshold: "<passThreshold>",
});
```

```python Python
result = client.palette.calibrations_run({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "datasetId": "<datasetId>",
    "versionId": "<versionId>",
    "evalReportId": "<evalReportId>",
    "evaluatorVersionId": "<evaluatorVersionId>",
    "passThreshold": "<passThreshold>",
})
```

```rust Rust
let spec = client.build_request("palette", "calibrations_run", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("datasetId", "<datasetId>".into()),
    ("versionId", "<versionId>".into()),
    ("evalReportId", "<evalReportId>".into()),
    ("evaluatorVersionId", "<evaluatorVersionId>".into()),
    ("passThreshold", "<passThreshold>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "calibrationsRun", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "datasetId": "<datasetId>",
    "versionId": "<versionId>",
    "evalReportId": "<evalReportId>",
    "evaluatorVersionId": "<evaluatorVersionId>",
    "passThreshold": "<passThreshold>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
    {"datasetId", "<datasetId>"},
    {"versionId", "<versionId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"evalReportId\":\"<evalReportId>\",\"evaluatorVersionId\":\"<evaluatorVersionId>\",\"passThreshold\":\"<passThreshold>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "calibrationsRun",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### connectGetStatus

`GET /v1/connect/status/{tenantId}/{projectId}`

Call GET /v1/connect/status/\{tenant_id\}/\{project_id\}.

- **Producer operationId:** `connect.getStatus`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.connectGetStatus()` · Python `client.palette.connect_get_status()` · Rust `build_request("palette", "connect_get_status", params)` · Go `BuildRequest("palette", "connectGetStatus", params)` · C `tempera_build_request(..., "palette", "connectGetStatus", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.connectGetStatus({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
});
```

```python Python
result = client.palette.connect_get_status({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
})
```

```rust Rust
let spec = client.build_request("palette", "connect_get_status", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "connectGetStatus", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "connectGetStatus",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### connectorsList

`GET /v1/connectors/{tenantId}/{projectId}`

Call GET /v1/connectors/\{tenant_id\}/\{project_id\}.

- **Producer operationId:** `connectors.list`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.connectorsList()` · Python `client.palette.connectors_list()` · Rust `build_request("palette", "connectors_list", params)` · Go `BuildRequest("palette", "connectorsList", params)` · C `tempera_build_request(..., "palette", "connectorsList", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.connectorsList({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.palette.connectors_list({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("palette", "connectors_list", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "connectorsList", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "pageSize=<pageSize>&pageToken=<pageToken>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "connectorsList",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### connectorsConnect

`POST /v1/connectors/{tenantId}/{projectId}/connect`

Call POST /v1/connectors/\{tenant_id\}/\{project_id\}/connect.

- **Producer operationId:** `connectors.connect`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.connectorsConnect()` · Python `client.palette.connectors_connect()` · Rust `build_request("palette", "connectors_connect", params)` · Go `BuildRequest("palette", "connectorsConnect", params)` · C `tempera_build_request(..., "palette", "connectorsConnect", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `toolkit` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.connectorsConnect({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  toolkit: "<toolkit>",
});
```

```python Python
result = client.palette.connectors_connect({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "toolkit": "<toolkit>",
})
```

```rust Rust
let spec = client.build_request("palette", "connectors_connect", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("toolkit", "<toolkit>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "connectorsConnect", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "toolkit": "<toolkit>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"toolkit\":\"<toolkit>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "connectorsConnect",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### connectorsInvokeTool

`POST /v1/connectors/{tenantId}/{projectId}/invoke`

Call POST /v1/connectors/\{tenant_id\}/\{project_id\}/invoke.

- **Producer operationId:** `connectors.invokeTool`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.connectorsInvokeTool()` · Python `client.palette.connectors_invoke_tool()` · Rust `build_request("palette", "connectors_invoke_tool", params)` · Go `BuildRequest("palette", "connectorsInvokeTool", params)` · C `tempera_build_request(..., "palette", "connectorsInvokeTool", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `arguments` | no | — |
| `tool` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.connectorsInvokeTool({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  tool: "<tool>",
  arguments: "<arguments>",
});
```

```python Python
result = client.palette.connectors_invoke_tool({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "tool": "<tool>",
    "arguments": "<arguments>",
})
```

```rust Rust
let spec = client.build_request("palette", "connectors_invoke_tool", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("tool", "<tool>".into()),
    ("arguments", "<arguments>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "connectorsInvokeTool", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "tool": "<tool>",
    "arguments": "<arguments>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"arguments\":\"<arguments>\",\"tool\":\"<tool>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "connectorsInvokeTool",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### connectorsGetSkills

`GET /v1/connectors/{tenantId}/{projectId}/skills`

Call GET /v1/connectors/\{tenant_id\}/\{project_id\}/skills.

- **Producer operationId:** `connectors.getSkills`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.connectorsGetSkills()` · Python `client.palette.connectors_get_skills()` · Rust `build_request("palette", "connectors_get_skills", params)` · Go `BuildRequest("palette", "connectorsGetSkills", params)` · C `tempera_build_request(..., "palette", "connectorsGetSkills", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `toolkit` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.connectorsGetSkills({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  toolkit: "<toolkit>",
});
```

```python Python
result = client.palette.connectors_get_skills({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "toolkit": "<toolkit>",
})
```

```rust Rust
let spec = client.build_request("palette", "connectors_get_skills", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("toolkit", "<toolkit>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "connectorsGetSkills", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "toolkit": "<toolkit>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "toolkit=<toolkit>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "connectorsGetSkills",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### connectorsStatus

`GET /v1/connectors/{tenantId}/{projectId}/status`

Call GET /v1/connectors/\{tenant_id\}/\{project_id\}/status.

- **Producer operationId:** `connectors.status`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.connectorsStatus()` · Python `client.palette.connectors_status()` · Rust `build_request("palette", "connectors_status", params)` · Go `BuildRequest("palette", "connectorsStatus", params)` · C `tempera_build_request(..., "palette", "connectorsStatus", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `toolkit` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.connectorsStatus({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  toolkit: "<toolkit>",
});
```

```python Python
result = client.palette.connectors_status({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "toolkit": "<toolkit>",
})
```

```rust Rust
let spec = client.build_request("palette", "connectors_status", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("toolkit", "<toolkit>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "connectorsStatus", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "toolkit": "<toolkit>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "toolkit=<toolkit>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "connectorsStatus",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### connectorsListTools

`GET /v1/connectors/{tenantId}/{projectId}/tools`

Call GET /v1/connectors/\{tenant_id\}/\{project_id\}/tools.

- **Producer operationId:** `connectors.listTools`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.connectorsListTools()` · Python `client.palette.connectors_list_tools()` · Rust `build_request("palette", "connectors_list_tools", params)` · Go `BuildRequest("palette", "connectorsListTools", params)` · C `tempera_build_request(..., "palette", "connectorsListTools", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `toolkit` | yes | — |
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.connectorsListTools({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  toolkit: "<toolkit>",
  pageSize: "<pageSize>",
});
```

```python Python
result = client.palette.connectors_list_tools({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "toolkit": "<toolkit>",
    "pageSize": "<pageSize>",
})
```

```rust Rust
let spec = client.build_request("palette", "connectors_list_tools", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("toolkit", "<toolkit>".into()),
    ("pageSize", "<pageSize>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "connectorsListTools", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "toolkit": "<toolkit>",
    "pageSize": "<pageSize>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "toolkit=<toolkit>&pageSize=<pageSize>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "connectorsListTools",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### createDataset

`POST /v1/datasets/{tenantId}/{projectId}`

Create a dataset for curating cases from traces.

- **Producer operationId:** `datasets.create`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Scope:** `dataset:write`
- **Call as:** TypeScript `client.palette.createDataset()` · Python `client.palette.create_dataset()` · Rust `build_request("palette", "create_dataset", params)` · Go `BuildRequest("palette", "createDataset", params)` · C `tempera_build_request(..., "palette", "createDataset", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `name` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.createDataset({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  name: "<name>",
});
```

```python Python
result = client.palette.create_dataset({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "name": "<name>",
})
```

```rust Rust
let spec = client.build_request("palette", "create_dataset", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("name", "<name>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "createDataset", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "name": "<name>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"name\":\"<name>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "createDataset",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### promoteTraceToCase

`POST /v1/datasets/{tenantId}/{projectId}/{datasetId}/cases/from-trace`

Promote a trace (or one span of it) into a dataset case.

- **Producer operationId:** `datasets.promoteCaseFromTrace`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Scope:** `dataset:write`
- **Call as:** TypeScript `client.palette.promoteTraceToCase()` · Python `client.palette.promote_trace_to_case()` · Rust `build_request("palette", "promote_trace_to_case", params)` · Go `BuildRequest("palette", "promoteTraceToCase", params)` · C `tempera_build_request(..., "palette", "promoteTraceToCase", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |
| `datasetId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `reference` | no | — |
| `spanId` | no | — |
| `traceId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.promoteTraceToCase({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  datasetId: "<datasetId>",
  traceId: "<traceId>",
  reference: "<reference>",
  spanId: "<spanId>",
});
```

```python Python
result = client.palette.promote_trace_to_case({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "datasetId": "<datasetId>",
    "traceId": "<traceId>",
    "reference": "<reference>",
    "spanId": "<spanId>",
})
```

```rust Rust
let spec = client.build_request("palette", "promote_trace_to_case", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("datasetId", "<datasetId>".into()),
    ("traceId", "<traceId>".into()),
    ("reference", "<reference>".into()),
    ("spanId", "<spanId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "promoteTraceToCase", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "datasetId": "<datasetId>",
    "traceId": "<traceId>",
    "reference": "<reference>",
    "spanId": "<spanId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
    {"datasetId", "<datasetId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"reference\":\"<reference>\",\"spanId\":\"<spanId>\",\"traceId\":\"<traceId>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "promoteTraceToCase",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### createDatasetVersion

`POST /v1/datasets/{tenantId}/{projectId}/{datasetId}/versions`

Snapshot a dataset into an immutable version for evals and experiments.

- **Producer operationId:** `datasets.createVersion`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Scope:** `dataset:write`
- **Call as:** TypeScript `client.palette.createDatasetVersion()` · Python `client.palette.create_dataset_version()` · Rust `build_request("palette", "create_dataset_version", params)` · Go `BuildRequest("palette", "createDatasetVersion", params)` · C `tempera_build_request(..., "palette", "createDatasetVersion", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |
| `datasetId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `caseIds` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.createDatasetVersion({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  datasetId: "<datasetId>",
  caseIds: "<caseIds>",
});
```

```python Python
result = client.palette.create_dataset_version({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "datasetId": "<datasetId>",
    "caseIds": "<caseIds>",
})
```

```rust Rust
let spec = client.build_request("palette", "create_dataset_version", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("datasetId", "<datasetId>".into()),
    ("caseIds", "<caseIds>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "createDatasetVersion", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "datasetId": "<datasetId>",
    "caseIds": "<caseIds>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
    {"datasetId", "<datasetId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"caseIds\":\"<caseIds>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "createDatasetVersion",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### evalsRunDeterministic

`POST /v1/datasets/{tenantId}/{projectId}/{datasetId}/versions/{versionId}/evals/deterministic`

Call POST /v1/datasets/\{tenant_id\}/\{project_id\}/\{dataset_id\}/versions/\{version_id\}/evals/deterministic.

- **Producer operationId:** `evals.runDeterministic`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.evalsRunDeterministic()` · Python `client.palette.evals_run_deterministic()` · Rust `build_request("palette", "evals_run_deterministic", params)` · Go `BuildRequest("palette", "evalsRunDeterministic", params)` · C `tempera_build_request(..., "palette", "evalsRunDeterministic", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |
| `datasetId` | yes | Substituted into the URL path (percent-encoded). |
| `versionId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `agentReleaseId` | yes | — |
| `codeHash` | no | — |
| `evaluatorId` | yes | — |
| `evaluatorVersionId` | yes | — |
| `kind` | yes | — |
| `promptVersionId` | no | — |
| `wasmHash` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.evalsRunDeterministic({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  datasetId: "<datasetId>",
  versionId: "<versionId>",
  agentReleaseId: "<agentReleaseId>",
  evaluatorId: "<evaluatorId>",
  evaluatorVersionId: "<evaluatorVersionId>",
  kind: "<kind>",
});
```

```python Python
result = client.palette.evals_run_deterministic({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "datasetId": "<datasetId>",
    "versionId": "<versionId>",
    "agentReleaseId": "<agentReleaseId>",
    "evaluatorId": "<evaluatorId>",
    "evaluatorVersionId": "<evaluatorVersionId>",
    "kind": "<kind>",
})
```

```rust Rust
let spec = client.build_request("palette", "evals_run_deterministic", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("datasetId", "<datasetId>".into()),
    ("versionId", "<versionId>".into()),
    ("agentReleaseId", "<agentReleaseId>".into()),
    ("evaluatorId", "<evaluatorId>".into()),
    ("evaluatorVersionId", "<evaluatorVersionId>".into()),
    ("kind", "<kind>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "evalsRunDeterministic", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "datasetId": "<datasetId>",
    "versionId": "<versionId>",
    "agentReleaseId": "<agentReleaseId>",
    "evaluatorId": "<evaluatorId>",
    "evaluatorVersionId": "<evaluatorVersionId>",
    "kind": "<kind>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
    {"datasetId", "<datasetId>"},
    {"versionId", "<versionId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"agentReleaseId\":\"<agentReleaseId>\",\"evaluatorId\":\"<evaluatorId>\",\"evaluatorVersionId\":\"<evaluatorVersionId>\",\"kind\":\"<kind>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "evalsRunDeterministic",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### evalsRunJudge

`POST /v1/datasets/{tenantId}/{projectId}/{datasetId}/versions/{versionId}/evals/judge`

Call POST /v1/datasets/\{tenant_id\}/\{project_id\}/\{dataset_id\}/versions/\{version_id\}/evals/judge.

- **Producer operationId:** `evals.runJudge`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.evalsRunJudge()` · Python `client.palette.evals_run_judge()` · Rust `build_request("palette", "evals_run_judge", params)` · Go `BuildRequest("palette", "evalsRunJudge", params)` · C `tempera_build_request(..., "palette", "evalsRunJudge", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |
| `datasetId` | yes | Substituted into the URL path (percent-encoded). |
| `versionId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `agentReleaseId` | yes | — |
| `codeHash` | no | — |
| `evaluatorId` | yes | — |
| `evaluatorVersionId` | yes | — |
| `kind` | yes | — |
| `promptVersionId` | no | — |
| `providerSecretId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.evalsRunJudge({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  datasetId: "<datasetId>",
  versionId: "<versionId>",
  agentReleaseId: "<agentReleaseId>",
  evaluatorId: "<evaluatorId>",
  evaluatorVersionId: "<evaluatorVersionId>",
  kind: "<kind>",
  providerSecretId: "<providerSecretId>",
});
```

```python Python
result = client.palette.evals_run_judge({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "datasetId": "<datasetId>",
    "versionId": "<versionId>",
    "agentReleaseId": "<agentReleaseId>",
    "evaluatorId": "<evaluatorId>",
    "evaluatorVersionId": "<evaluatorVersionId>",
    "kind": "<kind>",
    "providerSecretId": "<providerSecretId>",
})
```

```rust Rust
let spec = client.build_request("palette", "evals_run_judge", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("datasetId", "<datasetId>".into()),
    ("versionId", "<versionId>".into()),
    ("agentReleaseId", "<agentReleaseId>".into()),
    ("evaluatorId", "<evaluatorId>".into()),
    ("evaluatorVersionId", "<evaluatorVersionId>".into()),
    ("kind", "<kind>".into()),
    ("providerSecretId", "<providerSecretId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "evalsRunJudge", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "datasetId": "<datasetId>",
    "versionId": "<versionId>",
    "agentReleaseId": "<agentReleaseId>",
    "evaluatorId": "<evaluatorId>",
    "evaluatorVersionId": "<evaluatorVersionId>",
    "kind": "<kind>",
    "providerSecretId": "<providerSecretId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
    {"datasetId", "<datasetId>"},
    {"versionId", "<versionId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"agentReleaseId\":\"<agentReleaseId>\",\"evaluatorId\":\"<evaluatorId>\",\"evaluatorVersionId\":\"<evaluatorVersionId>\",\"kind\":\"<kind>\",\"providerSecretId\":\"<providerSecretId>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "evalsRunJudge",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### importTemperaBundle

`POST /v1/eval-results/{tenantId}/{projectId}/tempera/bundles`

Import one RFC 8785-canonical, detached-Ed25519-signed official Tempera result bundle and return its minimal evidence receipt.

- **Producer operationId:** `evalResults.importTemperaBundle`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.palette.importTemperaBundle()` · Python `client.palette.import_tempera_bundle()` · Rust `build_request("palette", "import_tempera_bundle", params)` · Go `BuildRequest("palette", "importTemperaBundle", params)` · C `tempera_build_request(..., "palette", "importTemperaBundle", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `canonicalJson` | yes | — |
| `publicKeyPem` | yes | — |
| `signatureBase64` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.importTemperaBundle({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  canonicalJson: "<canonicalJson>",
  publicKeyPem: "<publicKeyPem>",
  signatureBase64: "<signatureBase64>",
});
```

```python Python
result = client.palette.import_tempera_bundle({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "canonicalJson": "<canonicalJson>",
    "publicKeyPem": "<publicKeyPem>",
    "signatureBase64": "<signatureBase64>",
})
```

```rust Rust
let spec = client.build_request("palette", "import_tempera_bundle", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("canonicalJson", "<canonicalJson>".into()),
    ("publicKeyPem", "<publicKeyPem>".into()),
    ("signatureBase64", "<signatureBase64>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "importTemperaBundle", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "canonicalJson": "<canonicalJson>",
    "publicKeyPem": "<publicKeyPem>",
    "signatureBase64": "<signatureBase64>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"canonicalJson\":\"<canonicalJson>\",\"publicKeyPem\":\"<publicKeyPem>\",\"signatureBase64\":\"<signatureBase64>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "importTemperaBundle",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### recordTemperaDecision

`POST /v1/eval-results/{tenantId}/{projectId}/tempera/decisions`

Import one RFC 8785-canonical, detached-Ed25519-signed preregistered Tempera A/B decision and return its minimal evidence receipt.

- **Producer operationId:** `evalResults.recordTemperaDecision`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.palette.recordTemperaDecision()` · Python `client.palette.record_tempera_decision()` · Rust `build_request("palette", "record_tempera_decision", params)` · Go `BuildRequest("palette", "recordTemperaDecision", params)` · C `tempera_build_request(..., "palette", "recordTemperaDecision", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `canonicalJson` | yes | — |
| `publicKeyPem` | yes | — |
| `signatureBase64` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.recordTemperaDecision({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  canonicalJson: "<canonicalJson>",
  publicKeyPem: "<publicKeyPem>",
  signatureBase64: "<signatureBase64>",
});
```

```python Python
result = client.palette.record_tempera_decision({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "canonicalJson": "<canonicalJson>",
    "publicKeyPem": "<publicKeyPem>",
    "signatureBase64": "<signatureBase64>",
})
```

```rust Rust
let spec = client.build_request("palette", "record_tempera_decision", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("canonicalJson", "<canonicalJson>".into()),
    ("publicKeyPem", "<publicKeyPem>".into()),
    ("signatureBase64", "<signatureBase64>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "recordTemperaDecision", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "canonicalJson": "<canonicalJson>",
    "publicKeyPem": "<publicKeyPem>",
    "signatureBase64": "<signatureBase64>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"canonicalJson\":\"<canonicalJson>\",\"publicKeyPem\":\"<publicKeyPem>\",\"signatureBase64\":\"<signatureBase64>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "recordTemperaDecision",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### getTemperaEvidence

`GET /v1/eval-results/{tenantId}/{projectId}/tempera/{kind}/{externalId}`

Fetch one tenant/project-scoped Tempera evidence receipt without returning its raw signed payload.

- **Producer operationId:** `evalResults.getTemperaEvidence`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.palette.getTemperaEvidence()` · Python `client.palette.get_tempera_evidence()` · Rust `build_request("palette", "get_tempera_evidence", params)` · Go `BuildRequest("palette", "getTemperaEvidence", params)` · C `tempera_build_request(..., "palette", "getTemperaEvidence", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |
| `kind` | yes | Substituted into the URL path (percent-encoded). |
| `externalId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.getTemperaEvidence({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  kind: "<kind>",
  externalId: "<externalId>",
});
```

```python Python
result = client.palette.get_tempera_evidence({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "kind": "<kind>",
    "externalId": "<externalId>",
})
```

```rust Rust
let spec = client.build_request("palette", "get_tempera_evidence", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("kind", "<kind>".into()),
    ("externalId", "<externalId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "getTemperaEvidence", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "kind": "<kind>",
    "externalId": "<externalId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
    {"kind", "<kind>"},
    {"externalId", "<externalId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "getTemperaEvidence",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### experimentsRunDeterministic

`POST /v1/experiments/{tenantId}/{projectId}/{datasetId}/versions/{versionId}/deterministic`

Call POST /v1/experiments/\{tenant_id\}/\{project_id\}/\{dataset_id\}/versions/\{version_id\}/deterministic.

- **Producer operationId:** `experiments.runDeterministic`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.experimentsRunDeterministic()` · Python `client.palette.experiments_run_deterministic()` · Rust `build_request("palette", "experiments_run_deterministic", params)` · Go `BuildRequest("palette", "experimentsRunDeterministic", params)` · C `tempera_build_request(..., "palette", "experimentsRunDeterministic", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |
| `datasetId` | yes | Substituted into the URL path (percent-encoded). |
| `versionId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `baselineOutputs` | yes | — |
| `baselineReleaseId` | yes | — |
| `candidateOutputs` | yes | — |
| `candidateReleaseId` | yes | — |
| `evaluatorId` | yes | — |
| `evaluatorVersionId` | yes | — |
| `gatePolicy` | no | — |
| `kind` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.experimentsRunDeterministic({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  datasetId: "<datasetId>",
  versionId: "<versionId>",
  baselineOutputs: "<baselineOutputs>",
  baselineReleaseId: "<baselineReleaseId>",
  candidateOutputs: "<candidateOutputs>",
  candidateReleaseId: "<candidateReleaseId>",
  evaluatorId: "<evaluatorId>",
  evaluatorVersionId: "<evaluatorVersionId>",
  kind: "<kind>",
});
```

```python Python
result = client.palette.experiments_run_deterministic({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "datasetId": "<datasetId>",
    "versionId": "<versionId>",
    "baselineOutputs": "<baselineOutputs>",
    "baselineReleaseId": "<baselineReleaseId>",
    "candidateOutputs": "<candidateOutputs>",
    "candidateReleaseId": "<candidateReleaseId>",
    "evaluatorId": "<evaluatorId>",
    "evaluatorVersionId": "<evaluatorVersionId>",
    "kind": "<kind>",
})
```

```rust Rust
let spec = client.build_request("palette", "experiments_run_deterministic", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("datasetId", "<datasetId>".into()),
    ("versionId", "<versionId>".into()),
    ("baselineOutputs", "<baselineOutputs>".into()),
    ("baselineReleaseId", "<baselineReleaseId>".into()),
    ("candidateOutputs", "<candidateOutputs>".into()),
    ("candidateReleaseId", "<candidateReleaseId>".into()),
    ("evaluatorId", "<evaluatorId>".into()),
    ("evaluatorVersionId", "<evaluatorVersionId>".into()),
    ("kind", "<kind>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "experimentsRunDeterministic", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "datasetId": "<datasetId>",
    "versionId": "<versionId>",
    "baselineOutputs": "<baselineOutputs>",
    "baselineReleaseId": "<baselineReleaseId>",
    "candidateOutputs": "<candidateOutputs>",
    "candidateReleaseId": "<candidateReleaseId>",
    "evaluatorId": "<evaluatorId>",
    "evaluatorVersionId": "<evaluatorVersionId>",
    "kind": "<kind>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
    {"datasetId", "<datasetId>"},
    {"versionId", "<versionId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"baselineOutputs\":\"<baselineOutputs>\",\"baselineReleaseId\":\"<baselineReleaseId>\",\"candidateOutputs\":\"<candidateOutputs>\",\"candidateReleaseId\":\"<candidateReleaseId>\",\"evaluatorId\":\"<evaluatorId>\",\"evaluatorVersionId\":\"<evaluatorVersionId>\",\"kind\":\"<kind>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "experimentsRunDeterministic",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### experimentsRunJudge

`POST /v1/experiments/{tenantId}/{projectId}/{datasetId}/versions/{versionId}/judge`

Call POST /v1/experiments/\{tenant_id\}/\{project_id\}/\{dataset_id\}/versions/\{version_id\}/judge.

- **Producer operationId:** `experiments.runJudge`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.experimentsRunJudge()` · Python `client.palette.experiments_run_judge()` · Rust `build_request("palette", "experiments_run_judge", params)` · Go `BuildRequest("palette", "experimentsRunJudge", params)` · C `tempera_build_request(..., "palette", "experimentsRunJudge", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |
| `datasetId` | yes | Substituted into the URL path (percent-encoded). |
| `versionId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `baselineOutputs` | yes | — |
| `baselineReleaseId` | yes | — |
| `candidateOutputs` | yes | — |
| `candidateReleaseId` | yes | — |
| `evaluatorId` | yes | — |
| `evaluatorVersionId` | yes | — |
| `gatePolicy` | no | — |
| `kind` | yes | — |
| `providerSecretId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.experimentsRunJudge({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  datasetId: "<datasetId>",
  versionId: "<versionId>",
  baselineOutputs: "<baselineOutputs>",
  baselineReleaseId: "<baselineReleaseId>",
  candidateOutputs: "<candidateOutputs>",
  candidateReleaseId: "<candidateReleaseId>",
  evaluatorId: "<evaluatorId>",
  evaluatorVersionId: "<evaluatorVersionId>",
  kind: "<kind>",
  providerSecretId: "<providerSecretId>",
});
```

```python Python
result = client.palette.experiments_run_judge({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "datasetId": "<datasetId>",
    "versionId": "<versionId>",
    "baselineOutputs": "<baselineOutputs>",
    "baselineReleaseId": "<baselineReleaseId>",
    "candidateOutputs": "<candidateOutputs>",
    "candidateReleaseId": "<candidateReleaseId>",
    "evaluatorId": "<evaluatorId>",
    "evaluatorVersionId": "<evaluatorVersionId>",
    "kind": "<kind>",
    "providerSecretId": "<providerSecretId>",
})
```

```rust Rust
let spec = client.build_request("palette", "experiments_run_judge", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("datasetId", "<datasetId>".into()),
    ("versionId", "<versionId>".into()),
    ("baselineOutputs", "<baselineOutputs>".into()),
    ("baselineReleaseId", "<baselineReleaseId>".into()),
    ("candidateOutputs", "<candidateOutputs>".into()),
    ("candidateReleaseId", "<candidateReleaseId>".into()),
    ("evaluatorId", "<evaluatorId>".into()),
    ("evaluatorVersionId", "<evaluatorVersionId>".into()),
    ("kind", "<kind>".into()),
    ("providerSecretId", "<providerSecretId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "experimentsRunJudge", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "datasetId": "<datasetId>",
    "versionId": "<versionId>",
    "baselineOutputs": "<baselineOutputs>",
    "baselineReleaseId": "<baselineReleaseId>",
    "candidateOutputs": "<candidateOutputs>",
    "candidateReleaseId": "<candidateReleaseId>",
    "evaluatorId": "<evaluatorId>",
    "evaluatorVersionId": "<evaluatorVersionId>",
    "kind": "<kind>",
    "providerSecretId": "<providerSecretId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
    {"datasetId", "<datasetId>"},
    {"versionId", "<versionId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"baselineOutputs\":\"<baselineOutputs>\",\"baselineReleaseId\":\"<baselineReleaseId>\",\"candidateOutputs\":\"<candidateOutputs>\",\"candidateReleaseId\":\"<candidateReleaseId>\",\"evaluatorId\":\"<evaluatorId>\",\"evaluatorVersionId\":\"<evaluatorVersionId>\",\"kind\":\"<kind>\",\"providerSecretId\":\"<providerSecretId>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "experimentsRunJudge",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### gatesCreate

`POST /v1/gates/{tenantId}/{projectId}`

Call POST /v1/gates/\{tenant_id\}/\{project_id\}.

- **Producer operationId:** `gates.create`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.gatesCreate()` · Python `client.palette.gates_create()` · Rust `build_request("palette", "gates_create", params)` · Go `BuildRequest("palette", "gatesCreate", params)` · C `tempera_build_request(..., "palette", "gatesCreate", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `datasetId` | no | — |
| `evaluatorVersionId` | no | — |
| `gateId` | yes | — |
| `inconclusivePolicy` | no | — |
| `name` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.gatesCreate({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  gateId: "<gateId>",
  name: "<name>",
  datasetId: "<datasetId>",
});
```

```python Python
result = client.palette.gates_create({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "gateId": "<gateId>",
    "name": "<name>",
    "datasetId": "<datasetId>",
})
```

```rust Rust
let spec = client.build_request("palette", "gates_create", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("gateId", "<gateId>".into()),
    ("name", "<name>".into()),
    ("datasetId", "<datasetId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "gatesCreate", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "gateId": "<gateId>",
    "name": "<name>",
    "datasetId": "<datasetId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"datasetId\":\"<datasetId>\",\"gateId\":\"<gateId>\",\"name\":\"<name>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "gatesCreate",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### gatesRun

`POST /v1/gates/{tenantId}/{projectId}/{gateId}/run`

Call POST /v1/gates/\{tenant_id\}/\{project_id\}/\{gate_id\}/run.

- **Producer operationId:** `gates.run`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.gatesRun()` · Python `client.palette.gates_run()` · Rust `build_request("palette", "gates_run", params)` · Go `BuildRequest("palette", "gatesRun", params)` · C `tempera_build_request(..., "palette", "gatesRun", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |
| `gateId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `experimentRunId` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.gatesRun({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  gateId: "<gateId>",
  experimentRunId: "<experimentRunId>",
});
```

```python Python
result = client.palette.gates_run({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "gateId": "<gateId>",
    "experimentRunId": "<experimentRunId>",
})
```

```rust Rust
let spec = client.build_request("palette", "gates_run", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("gateId", "<gateId>".into()),
    ("experimentRunId", "<experimentRunId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "gatesRun", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "gateId": "<gateId>",
    "experimentRunId": "<experimentRunId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
    {"gateId", "<gateId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"experimentRunId\":\"<experimentRunId>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "gatesRun",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### importSource

`POST /v1/import/{tenantId}/{projectId}/{environmentId}`

Import spans from a named external source payload.

- **Producer operationId:** `ingest.importSource`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Scope:** `trace:write`
- **Call as:** TypeScript `client.palette.importSource()` · Python `client.palette.import_source()` · Rust `build_request("palette", "import_source", params)` · Go `BuildRequest("palette", "importSource", params)` · C `tempera_build_request(..., "palette", "importSource", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |
| `environmentId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `durability` | no | — |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `payload` | no | — |
| `source` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.importSource({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  environmentId: "<environmentId>",
  source: "<source>",
  payload: "<payload>",
});
```

```python Python
result = client.palette.import_source({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "environmentId": "<environmentId>",
    "source": "<source>",
    "payload": "<payload>",
})
```

```rust Rust
let spec = client.build_request("palette", "import_source", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("environmentId", "<environmentId>".into()),
    ("source", "<source>".into()),
    ("payload", "<payload>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "importSource", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "environmentId": "<environmentId>",
    "source": "<source>",
    "payload": "<payload>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
    {"environmentId", "<environmentId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"payload\":\"<payload>\",\"source\":\"<source>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "importSource",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### ingestReplayDeadLetter

`POST /v1/ingest/{tenantId}/{projectId}/dead-letters/{messageId}/replay`

Call POST /v1/ingest/\{tenant_id\}/\{project_id\}/dead-letters/\{message_id\}/replay.

- **Producer operationId:** `ingest.replayDeadLetter`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.ingestReplayDeadLetter()` · Python `client.palette.ingest_replay_dead_letter()` · Rust `build_request("palette", "ingest_replay_dead_letter", params)` · Go `BuildRequest("palette", "ingestReplayDeadLetter", params)` · C `tempera_build_request(..., "palette", "ingestReplayDeadLetter", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |
| `messageId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `resetAttempts` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.ingestReplayDeadLetter({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  messageId: "<messageId>",
});
```

```python Python
result = client.palette.ingest_replay_dead_letter({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "messageId": "<messageId>",
})
```

```rust Rust
let spec = client.build_request("palette", "ingest_replay_dead_letter", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("messageId", "<messageId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "ingestReplayDeadLetter", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "messageId": "<messageId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
    {"messageId", "<messageId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "ingestReplayDeadLetter",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### ingestGetQueueStatus

`GET /v1/ingest/{tenantId}/{projectId}/queue`

Call GET /v1/ingest/\{tenant_id\}/\{project_id\}/queue.

- **Producer operationId:** `ingest.getQueueStatus`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.ingestGetQueueStatus()` · Python `client.palette.ingest_get_queue_status()` · Rust `build_request("palette", "ingest_get_queue_status", params)` · Go `BuildRequest("palette", "ingestGetQueueStatus", params)` · C `tempera_build_request(..., "palette", "ingestGetQueueStatus", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.ingestGetQueueStatus({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
});
```

```python Python
result = client.palette.ingest_get_queue_status({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
})
```

```rust Rust
let spec = client.build_request("palette", "ingest_get_queue_status", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "ingestGetQueueStatus", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "ingestGetQueueStatus",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### ingestDrainTraceIngested

`POST /v1/ingest/{tenantId}/{projectId}/trace-ingested/drain`

Call POST /v1/ingest/\{tenant_id\}/\{project_id\}/trace-ingested/drain.

- **Producer operationId:** `ingest.drainTraceIngested`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.ingestDrainTraceIngested()` · Python `client.palette.ingest_drain_trace_ingested()` · Rust `build_request("palette", "ingest_drain_trace_ingested", params)` · Go `BuildRequest("palette", "ingestDrainTraceIngested", params)` · C `tempera_build_request(..., "palette", "ingestDrainTraceIngested", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `limit` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.ingestDrainTraceIngested({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
});
```

```python Python
result = client.palette.ingest_drain_trace_ingested({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
})
```

```rust Rust
let spec = client.build_request("palette", "ingest_drain_trace_ingested", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "ingestDrainTraceIngested", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "ingestDrainTraceIngested",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### ingestDrainTraceWrites

`POST /v1/ingest/{tenantId}/{projectId}/trace-writes/drain`

Call POST /v1/ingest/\{tenant_id\}/\{project_id\}/trace-writes/drain.

- **Producer operationId:** `ingest.drainTraceWrites`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.ingestDrainTraceWrites()` · Python `client.palette.ingest_drain_trace_writes()` · Rust `build_request("palette", "ingest_drain_trace_writes", params)` · Go `BuildRequest("palette", "ingestDrainTraceWrites", params)` · C `tempera_build_request(..., "palette", "ingestDrainTraceWrites", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `limit` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.ingestDrainTraceWrites({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
});
```

```python Python
result = client.palette.ingest_drain_trace_writes({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
})
```

```rust Rust
let spec = client.build_request("palette", "ingest_drain_trace_writes", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "ingestDrainTraceWrites", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "ingestDrainTraceWrites",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### ingestReconcileTrace

`POST /v1/ingest/{tenantId}/{projectId}/traces/{traceId}/reconcile`

Call POST /v1/ingest/\{tenant_id\}/\{project_id\}/traces/\{trace_id\}/reconcile.

- **Producer operationId:** `ingest.reconcileTrace`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.ingestReconcileTrace()` · Python `client.palette.ingest_reconcile_trace()` · Rust `build_request("palette", "ingest_reconcile_trace", params)` · Go `BuildRequest("palette", "ingestReconcileTrace", params)` · C `tempera_build_request(..., "palette", "ingestReconcileTrace", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |
| `traceId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.ingestReconcileTrace({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  traceId: "<traceId>",
});
```

```python Python
result = client.palette.ingest_reconcile_trace({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "traceId": "<traceId>",
})
```

```rust Rust
let spec = client.build_request("palette", "ingest_reconcile_trace", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("traceId", "<traceId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "ingestReconcileTrace", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "traceId": "<traceId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
    {"traceId", "<traceId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "ingestReconcileTrace",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### judgeEvaluate

`POST /v1/judge/{tenantId}/{projectId}/evaluate`

Call POST /v1/judge/\{tenant_id\}/\{project_id\}/evaluate.

- **Producer operationId:** `judge.evaluate`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.judgeEvaluate()` · Python `client.palette.judge_evaluate()` · Rust `build_request("palette", "judge_evaluate", params)` · Go `BuildRequest("palette", "judgeEvaluate", params)` · C `tempera_build_request(..., "palette", "judgeEvaluate", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `cacheNamespace` | no | — |
| `case` | yes | — |
| `evaluator` | yes | — |
| `providerSecretId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.judgeEvaluate({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  case: "<case>",
  evaluator: "<evaluator>",
  providerSecretId: "<providerSecretId>",
});
```

```python Python
result = client.palette.judge_evaluate({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "case": "<case>",
    "evaluator": "<evaluator>",
    "providerSecretId": "<providerSecretId>",
})
```

```rust Rust
let spec = client.build_request("palette", "judge_evaluate", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("case", "<case>".into()),
    ("evaluator", "<evaluator>".into()),
    ("providerSecretId", "<providerSecretId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "judgeEvaluate", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "case": "<case>",
    "evaluator": "<evaluator>",
    "providerSecretId": "<providerSecretId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"case\":\"<case>\",\"evaluator\":\"<evaluator>\",\"providerSecretId\":\"<providerSecretId>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "judgeEvaluate",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### judgeListLedger

`GET /v1/judge/{tenantId}/{projectId}/ledger`

Call GET /v1/judge/\{tenant_id\}/\{project_id\}/ledger.

- **Producer operationId:** `judge.listLedger`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.judgeListLedger()` · Python `client.palette.judge_list_ledger()` · Rust `build_request("palette", "judge_list_ledger", params)` · Go `BuildRequest("palette", "judgeListLedger", params)` · C `tempera_build_request(..., "palette", "judgeListLedger", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.judgeListLedger({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.palette.judge_list_ledger({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("palette", "judge_list_ledger", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "judgeListLedger", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "pageSize=<pageSize>&pageToken=<pageToken>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "judgeListLedger",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### onlineDecideSampling

`POST /v1/online/{tenantId}/{projectId}/traces/{traceId}/sampling`

Call POST /v1/online/\{tenant_id\}/\{project_id\}/traces/\{trace_id\}/sampling.

- **Producer operationId:** `online.decideSampling`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.onlineDecideSampling()` · Python `client.palette.online_decide_sampling()` · Rust `build_request("palette", "online_decide_sampling", params)` · Go `BuildRequest("palette", "onlineDecideSampling", params)` · C `tempera_build_request(..., "palette", "onlineDecideSampling", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |
| `traceId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `highCostMicrosThreshold` | no | — |
| `keepErrors` | yes | — |
| `sampleRatePerMille` | yes | — |
| `slowMsThreshold` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.onlineDecideSampling({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  traceId: "<traceId>",
  keepErrors: "<keepErrors>",
  sampleRatePerMille: "<sampleRatePerMille>",
  highCostMicrosThreshold: "<highCostMicrosThreshold>",
});
```

```python Python
result = client.palette.online_decide_sampling({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "traceId": "<traceId>",
    "keepErrors": "<keepErrors>",
    "sampleRatePerMille": "<sampleRatePerMille>",
    "highCostMicrosThreshold": "<highCostMicrosThreshold>",
})
```

```rust Rust
let spec = client.build_request("palette", "online_decide_sampling", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("traceId", "<traceId>".into()),
    ("keepErrors", "<keepErrors>".into()),
    ("sampleRatePerMille", "<sampleRatePerMille>".into()),
    ("highCostMicrosThreshold", "<highCostMicrosThreshold>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "onlineDecideSampling", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "traceId": "<traceId>",
    "keepErrors": "<keepErrors>",
    "sampleRatePerMille": "<sampleRatePerMille>",
    "highCostMicrosThreshold": "<highCostMicrosThreshold>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
    {"traceId", "<traceId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"highCostMicrosThreshold\":\"<highCostMicrosThreshold>\",\"keepErrors\":\"<keepErrors>\",\"sampleRatePerMille\":\"<sampleRatePerMille>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "onlineDecideSampling",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### promptsList

`GET /v1/prompts/{tenantId}/{projectId}`

Call GET /v1/prompts/\{tenant_id\}/\{project_id\}.

- **Producer operationId:** `prompts.list`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.promptsList()` · Python `client.palette.prompts_list()` · Rust `build_request("palette", "prompts_list", params)` · Go `BuildRequest("palette", "promptsList", params)` · C `tempera_build_request(..., "palette", "promptsList", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.promptsList({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.palette.prompts_list({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("palette", "prompts_list", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "promptsList", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "pageSize=<pageSize>&pageToken=<pageToken>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "promptsList",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### promptsCreate

`POST /v1/prompts/{tenantId}/{projectId}`

Call POST /v1/prompts/\{tenant_id\}/\{project_id\}.

- **Producer operationId:** `prompts.create`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.promptsCreate()` · Python `client.palette.prompts_create()` · Rust `build_request("palette", "prompts_create", params)` · Go `BuildRequest("palette", "promptsCreate", params)` · C `tempera_build_request(..., "palette", "promptsCreate", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `createdBy` | no | — |
| `description` | no | — |
| `message` | no | — |
| `name` | yes | — |
| `template` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.promptsCreate({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  name: "<name>",
  template: "<template>",
  createdBy: "<createdBy>",
});
```

```python Python
result = client.palette.prompts_create({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "name": "<name>",
    "template": "<template>",
    "createdBy": "<createdBy>",
})
```

```rust Rust
let spec = client.build_request("palette", "prompts_create", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("name", "<name>".into()),
    ("template", "<template>".into()),
    ("createdBy", "<createdBy>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "promptsCreate", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "name": "<name>",
    "template": "<template>",
    "createdBy": "<createdBy>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"createdBy\":\"<createdBy>\",\"name\":\"<name>\",\"template\":\"<template>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "promptsCreate",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### promptsGet

`GET /v1/prompts/{tenantId}/{projectId}/{promptId}`

Call GET /v1/prompts/\{tenant_id\}/\{project_id\}/\{prompt_id\}.

- **Producer operationId:** `prompts.get`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.promptsGet()` · Python `client.palette.prompts_get()` · Rust `build_request("palette", "prompts_get", params)` · Go `BuildRequest("palette", "promptsGet", params)` · C `tempera_build_request(..., "palette", "promptsGet", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |
| `promptId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.promptsGet({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  promptId: "<promptId>",
});
```

```python Python
result = client.palette.prompts_get({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "promptId": "<promptId>",
})
```

```rust Rust
let spec = client.build_request("palette", "prompts_get", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("promptId", "<promptId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "promptsGet", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "promptId": "<promptId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
    {"promptId", "<promptId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "promptsGet",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### promptsDiffVersions

`GET /v1/prompts/{tenantId}/{projectId}/{promptId}/diff`

Call GET /v1/prompts/\{tenant_id\}/\{project_id\}/\{prompt_id\}/diff.

- **Producer operationId:** `prompts.diffVersions`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.promptsDiffVersions()` · Python `client.palette.prompts_diff_versions()` · Rust `build_request("palette", "prompts_diff_versions", params)` · Go `BuildRequest("palette", "promptsDiffVersions", params)` · C `tempera_build_request(..., "palette", "promptsDiffVersions", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |
| `promptId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `from` | yes | — |
| `to` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.promptsDiffVersions({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  promptId: "<promptId>",
  from: "<from>",
  to: "<to>",
});
```

```python Python
result = client.palette.prompts_diff_versions({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "promptId": "<promptId>",
    "from": "<from>",
    "to": "<to>",
})
```

```rust Rust
let spec = client.build_request("palette", "prompts_diff_versions", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("promptId", "<promptId>".into()),
    ("from", "<from>".into()),
    ("to", "<to>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "promptsDiffVersions", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "promptId": "<promptId>",
    "from": "<from>",
    "to": "<to>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
    {"promptId", "<promptId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "from=<from>&to=<to>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "promptsDiffVersions",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### promptsListVersions

`GET /v1/prompts/{tenantId}/{projectId}/{promptId}/versions`

Call GET /v1/prompts/\{tenant_id\}/\{project_id\}/\{prompt_id\}/versions.

- **Producer operationId:** `prompts.listVersions`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.promptsListVersions()` · Python `client.palette.prompts_list_versions()` · Rust `build_request("palette", "prompts_list_versions", params)` · Go `BuildRequest("palette", "promptsListVersions", params)` · C `tempera_build_request(..., "palette", "promptsListVersions", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |
| `promptId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.promptsListVersions({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  promptId: "<promptId>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.palette.prompts_list_versions({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "promptId": "<promptId>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("palette", "prompts_list_versions", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("promptId", "<promptId>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "promptsListVersions", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "promptId": "<promptId>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
    {"promptId", "<promptId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "pageSize=<pageSize>&pageToken=<pageToken>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "promptsListVersions",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### promptsAddVersion

`POST /v1/prompts/{tenantId}/{projectId}/{promptId}/versions`

Call POST /v1/prompts/\{tenant_id\}/\{project_id\}/\{prompt_id\}/versions.

- **Producer operationId:** `prompts.addVersion`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.promptsAddVersion()` · Python `client.palette.prompts_add_version()` · Rust `build_request("palette", "prompts_add_version", params)` · Go `BuildRequest("palette", "promptsAddVersion", params)` · C `tempera_build_request(..., "palette", "promptsAddVersion", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |
| `promptId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `createdBy` | no | — |
| `message` | no | — |
| `template` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.promptsAddVersion({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  promptId: "<promptId>",
  template: "<template>",
  createdBy: "<createdBy>",
  message: "<message>",
});
```

```python Python
result = client.palette.prompts_add_version({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "promptId": "<promptId>",
    "template": "<template>",
    "createdBy": "<createdBy>",
    "message": "<message>",
})
```

```rust Rust
let spec = client.build_request("palette", "prompts_add_version", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("promptId", "<promptId>".into()),
    ("template", "<template>".into()),
    ("createdBy", "<createdBy>".into()),
    ("message", "<message>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "promptsAddVersion", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "promptId": "<promptId>",
    "template": "<template>",
    "createdBy": "<createdBy>",
    "message": "<message>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
    {"promptId", "<promptId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"createdBy\":\"<createdBy>\",\"message\":\"<message>\",\"template\":\"<template>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "promptsAddVersion",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### providerSecretsList

`GET /v1/provider-secrets/{tenantId}/{projectId}`

Call GET /v1/provider-secrets/\{tenant_id\}/\{project_id\}.

- **Producer operationId:** `providerSecrets.list`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.providerSecretsList()` · Python `client.palette.provider_secrets_list()` · Rust `build_request("palette", "provider_secrets_list", params)` · Go `BuildRequest("palette", "providerSecretsList", params)` · C `tempera_build_request(..., "palette", "providerSecretsList", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.providerSecretsList({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.palette.provider_secrets_list({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("palette", "provider_secrets_list", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "providerSecretsList", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "pageSize=<pageSize>&pageToken=<pageToken>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "providerSecretsList",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### providerSecretsCreate

`POST /v1/provider-secrets/{tenantId}/{projectId}`

Call POST /v1/provider-secrets/\{tenant_id\}/\{project_id\}.

- **Producer operationId:** `providerSecrets.create`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.providerSecretsCreate()` · Python `client.palette.provider_secrets_create()` · Rust `build_request("palette", "provider_secrets_create", params)` · Go `BuildRequest("palette", "providerSecretsCreate", params)` · C `tempera_build_request(..., "palette", "providerSecretsCreate", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `displayName` | yes | — |
| `provider` | yes | — |
| `secretValue` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.providerSecretsCreate({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  displayName: "<displayName>",
  provider: "<provider>",
  secretValue: "<secretValue>",
});
```

```python Python
result = client.palette.provider_secrets_create({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "displayName": "<displayName>",
    "provider": "<provider>",
    "secretValue": "<secretValue>",
})
```

```rust Rust
let spec = client.build_request("palette", "provider_secrets_create", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("displayName", "<displayName>".into()),
    ("provider", "<provider>".into()),
    ("secretValue", "<secretValue>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "providerSecretsCreate", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "displayName": "<displayName>",
    "provider": "<provider>",
    "secretValue": "<secretValue>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"displayName\":\"<displayName>\",\"provider\":\"<provider>\",\"secretValue\":\"<secretValue>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "providerSecretsCreate",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### providerSecretsRevoke

`POST /v1/provider-secrets/{tenantId}/{projectId}/{providerSecretId}/revoke`

Call POST /v1/provider-secrets/\{tenant_id\}/\{project_id\}/\{provider_secret_id\}/revoke.

- **Producer operationId:** `providerSecrets.revoke`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.providerSecretsRevoke()` · Python `client.palette.provider_secrets_revoke()` · Rust `build_request("palette", "provider_secrets_revoke", params)` · Go `BuildRequest("palette", "providerSecretsRevoke", params)` · C `tempera_build_request(..., "palette", "providerSecretsRevoke", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |
| `providerSecretId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.providerSecretsRevoke({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  providerSecretId: "<providerSecretId>",
});
```

```python Python
result = client.palette.provider_secrets_revoke({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "providerSecretId": "<providerSecretId>",
})
```

```rust Rust
let spec = client.build_request("palette", "provider_secrets_revoke", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("providerSecretId", "<providerSecretId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "providerSecretsRevoke", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "providerSecretId": "<providerSecretId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
    {"providerSecretId", "<providerSecretId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "providerSecretsRevoke",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### reviewsCreateQueue

`POST /v1/review-queues/{tenantId}/{projectId}`

Call POST /v1/review-queues/\{tenant_id\}/\{project_id\}.

- **Producer operationId:** `reviews.createQueue`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.reviewsCreateQueue()` · Python `client.palette.reviews_create_queue()` · Rust `build_request("palette", "reviews_create_queue", params)` · Go `BuildRequest("palette", "reviewsCreateQueue", params)` · C `tempera_build_request(..., "palette", "reviewsCreateQueue", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `annotationSchema` | yes | — |
| `name` | yes | — |
| `queueId` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.reviewsCreateQueue({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  annotationSchema: "<annotationSchema>",
  name: "<name>",
  queueId: "<queueId>",
});
```

```python Python
result = client.palette.reviews_create_queue({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "annotationSchema": "<annotationSchema>",
    "name": "<name>",
    "queueId": "<queueId>",
})
```

```rust Rust
let spec = client.build_request("palette", "reviews_create_queue", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("annotationSchema", "<annotationSchema>".into()),
    ("name", "<name>".into()),
    ("queueId", "<queueId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "reviewsCreateQueue", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "annotationSchema": "<annotationSchema>",
    "name": "<name>",
    "queueId": "<queueId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"annotationSchema\":\"<annotationSchema>\",\"name\":\"<name>\",\"queueId\":\"<queueId>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "reviewsCreateQueue",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### reviewsListTasks

`GET /v1/review-queues/{tenantId}/{projectId}/{queueId}/tasks`

Call GET /v1/review-queues/\{tenant_id\}/\{project_id\}/\{queue_id\}/tasks.

- **Producer operationId:** `reviews.listTasks`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.reviewsListTasks()` · Python `client.palette.reviews_list_tasks()` · Rust `build_request("palette", "reviews_list_tasks", params)` · Go `BuildRequest("palette", "reviewsListTasks", params)` · C `tempera_build_request(..., "palette", "reviewsListTasks", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |
| `queueId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `state` | no | — |
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.reviewsListTasks({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  queueId: "<queueId>",
  state: "<state>",
  pageSize: "<pageSize>",
});
```

```python Python
result = client.palette.reviews_list_tasks({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "queueId": "<queueId>",
    "state": "<state>",
    "pageSize": "<pageSize>",
})
```

```rust Rust
let spec = client.build_request("palette", "reviews_list_tasks", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("queueId", "<queueId>".into()),
    ("state", "<state>".into()),
    ("pageSize", "<pageSize>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "reviewsListTasks", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "queueId": "<queueId>",
    "state": "<state>",
    "pageSize": "<pageSize>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
    {"queueId", "<queueId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "state=<state>&pageSize=<pageSize>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "reviewsListTasks",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### reviewsEnqueueTaskFromTrace

`POST /v1/review-queues/{tenantId}/{projectId}/{queueId}/tasks/from-trace`

Call POST /v1/review-queues/\{tenant_id\}/\{project_id\}/\{queue_id\}/tasks/from-trace.

- **Producer operationId:** `reviews.enqueueTaskFromTrace`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.reviewsEnqueueTaskFromTrace()` · Python `client.palette.reviews_enqueue_task_from_trace()` · Rust `build_request("palette", "reviews_enqueue_task_from_trace", params)` · Go `BuildRequest("palette", "reviewsEnqueueTaskFromTrace", params)` · C `tempera_build_request(..., "palette", "reviewsEnqueueTaskFromTrace", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |
| `queueId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `datasetCaseId` | no | — |
| `datasetId` | no | — |
| `priority` | no | — |
| `spanId` | no | — |
| `taskId` | no | — |
| `traceId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.reviewsEnqueueTaskFromTrace({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  queueId: "<queueId>",
  traceId: "<traceId>",
  datasetCaseId: "<datasetCaseId>",
  datasetId: "<datasetId>",
});
```

```python Python
result = client.palette.reviews_enqueue_task_from_trace({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "queueId": "<queueId>",
    "traceId": "<traceId>",
    "datasetCaseId": "<datasetCaseId>",
    "datasetId": "<datasetId>",
})
```

```rust Rust
let spec = client.build_request("palette", "reviews_enqueue_task_from_trace", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("queueId", "<queueId>".into()),
    ("traceId", "<traceId>".into()),
    ("datasetCaseId", "<datasetCaseId>".into()),
    ("datasetId", "<datasetId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "reviewsEnqueueTaskFromTrace", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "queueId": "<queueId>",
    "traceId": "<traceId>",
    "datasetCaseId": "<datasetCaseId>",
    "datasetId": "<datasetId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
    {"queueId", "<queueId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"datasetCaseId\":\"<datasetCaseId>\",\"datasetId\":\"<datasetId>\",\"traceId\":\"<traceId>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "reviewsEnqueueTaskFromTrace",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### reviewsSubmitAnnotation

`POST /v1/review-queues/{tenantId}/{projectId}/{queueId}/tasks/{taskId}/annotations`

Call POST /v1/review-queues/\{tenant_id\}/\{project_id\}/\{queue_id\}/tasks/\{task_id\}/annotations.

- **Producer operationId:** `reviews.submitAnnotation`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.reviewsSubmitAnnotation()` · Python `client.palette.reviews_submit_annotation()` · Rust `build_request("palette", "reviews_submit_annotation", params)` · Go `BuildRequest("palette", "reviewsSubmitAnnotation", params)` · C `tempera_build_request(..., "palette", "reviewsSubmitAnnotation", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |
| `queueId` | yes | Substituted into the URL path (percent-encoded). |
| `taskId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `annotationId` | no | — |
| `payload` | yes | — |
| `reviewerId` | yes | — |
| `verdict` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.reviewsSubmitAnnotation({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  queueId: "<queueId>",
  taskId: "<taskId>",
  payload: "<payload>",
  reviewerId: "<reviewerId>",
  verdict: "<verdict>",
});
```

```python Python
result = client.palette.reviews_submit_annotation({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "queueId": "<queueId>",
    "taskId": "<taskId>",
    "payload": "<payload>",
    "reviewerId": "<reviewerId>",
    "verdict": "<verdict>",
})
```

```rust Rust
let spec = client.build_request("palette", "reviews_submit_annotation", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("queueId", "<queueId>".into()),
    ("taskId", "<taskId>".into()),
    ("payload", "<payload>".into()),
    ("reviewerId", "<reviewerId>".into()),
    ("verdict", "<verdict>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "reviewsSubmitAnnotation", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "queueId": "<queueId>",
    "taskId": "<taskId>",
    "payload": "<payload>",
    "reviewerId": "<reviewerId>",
    "verdict": "<verdict>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
    {"queueId", "<queueId>"},
    {"taskId", "<taskId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"payload\":\"<payload>\",\"reviewerId\":\"<reviewerId>\",\"verdict\":\"<verdict>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "reviewsSubmitAnnotation",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### reviewsPromoteAnnotation

`POST /v1/review-queues/{tenantId}/{projectId}/{queueId}/tasks/{taskId}/annotations/{annotationId}/promote`

Call POST /v1/review-queues/\{tenant_id\}/\{project_id\}/\{queue_id\}/tasks/\{task_id\}/annotations/\{annotation_id\}/promote.

- **Producer operationId:** `reviews.promoteAnnotation`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.reviewsPromoteAnnotation()` · Python `client.palette.reviews_promote_annotation()` · Rust `build_request("palette", "reviews_promote_annotation", params)` · Go `BuildRequest("palette", "reviewsPromoteAnnotation", params)` · C `tempera_build_request(..., "palette", "reviewsPromoteAnnotation", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |
| `queueId` | yes | Substituted into the URL path (percent-encoded). |
| `taskId` | yes | Substituted into the URL path (percent-encoded). |
| `annotationId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `datasetId` | yes | — |
| `reference` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.reviewsPromoteAnnotation({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  queueId: "<queueId>",
  taskId: "<taskId>",
  annotationId: "<annotationId>",
  datasetId: "<datasetId>",
  reference: "<reference>",
});
```

```python Python
result = client.palette.reviews_promote_annotation({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "queueId": "<queueId>",
    "taskId": "<taskId>",
    "annotationId": "<annotationId>",
    "datasetId": "<datasetId>",
    "reference": "<reference>",
})
```

```rust Rust
let spec = client.build_request("palette", "reviews_promote_annotation", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("queueId", "<queueId>".into()),
    ("taskId", "<taskId>".into()),
    ("annotationId", "<annotationId>".into()),
    ("datasetId", "<datasetId>".into()),
    ("reference", "<reference>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "reviewsPromoteAnnotation", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "queueId": "<queueId>",
    "taskId": "<taskId>",
    "annotationId": "<annotationId>",
    "datasetId": "<datasetId>",
    "reference": "<reference>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
    {"queueId", "<queueId>"},
    {"taskId", "<taskId>"},
    {"annotationId", "<annotationId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"datasetId\":\"<datasetId>\",\"reference\":\"<reference>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "reviewsPromoteAnnotation",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### scenariosList

`GET /v1/scenarios/{tenantId}/{projectId}`

Call GET /v1/scenarios/\{tenant_id\}/\{project_id\}.

- **Producer operationId:** `scenarios.list`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.scenariosList()` · Python `client.palette.scenarios_list()` · Rust `build_request("palette", "scenarios_list", params)` · Go `BuildRequest("palette", "scenariosList", params)` · C `tempera_build_request(..., "palette", "scenariosList", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.scenariosList({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.palette.scenarios_list({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("palette", "scenarios_list", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "scenariosList", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "pageSize=<pageSize>&pageToken=<pageToken>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "scenariosList",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### scenariosCreate

`POST /v1/scenarios/{tenantId}/{projectId}`

Call POST /v1/scenarios/\{tenant_id\}/\{project_id\}.

- **Producer operationId:** `scenarios.create`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.scenariosCreate()` · Python `client.palette.scenarios_create()` · Rust `build_request("palette", "scenarios_create", params)` · Go `BuildRequest("palette", "scenariosCreate", params)` · C `tempera_build_request(..., "palette", "scenariosCreate", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `exemplarTraceId` | no | — |
| `expectedOutcome` | no | — |
| `failureMode` | no | — |
| `sourceTraceIds` | yes | — |
| `title` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.scenariosCreate({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  sourceTraceIds: "<sourceTraceIds>",
  title: "<title>",
  exemplarTraceId: "<exemplarTraceId>",
});
```

```python Python
result = client.palette.scenarios_create({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "sourceTraceIds": "<sourceTraceIds>",
    "title": "<title>",
    "exemplarTraceId": "<exemplarTraceId>",
})
```

```rust Rust
let spec = client.build_request("palette", "scenarios_create", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("sourceTraceIds", "<sourceTraceIds>".into()),
    ("title", "<title>".into()),
    ("exemplarTraceId", "<exemplarTraceId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "scenariosCreate", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "sourceTraceIds": "<sourceTraceIds>",
    "title": "<title>",
    "exemplarTraceId": "<exemplarTraceId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"exemplarTraceId\":\"<exemplarTraceId>\",\"sourceTraceIds\":\"<sourceTraceIds>\",\"title\":\"<title>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "scenariosCreate",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### scenariosMine

`POST /v1/scenarios/{tenantId}/{projectId}/mine`

Call POST /v1/scenarios/\{tenant_id\}/\{project_id\}/mine.

- **Producer operationId:** `scenarios.mine`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.scenariosMine()` · Python `client.palette.scenarios_mine()` · Rust `build_request("palette", "scenarios_mine", params)` · Go `BuildRequest("palette", "scenariosMine", params)` · C `tempera_build_request(..., "palette", "scenariosMine", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `jaccardThreshold` | no | — |
| `traceIds` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.scenariosMine({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  traceIds: "<traceIds>",
  jaccardThreshold: "<jaccardThreshold>",
});
```

```python Python
result = client.palette.scenarios_mine({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "traceIds": "<traceIds>",
    "jaccardThreshold": "<jaccardThreshold>",
})
```

```rust Rust
let spec = client.build_request("palette", "scenarios_mine", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("traceIds", "<traceIds>".into()),
    ("jaccardThreshold", "<jaccardThreshold>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "scenariosMine", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "traceIds": "<traceIds>",
    "jaccardThreshold": "<jaccardThreshold>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"jaccardThreshold\":\"<jaccardThreshold>\",\"traceIds\":\"<traceIds>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "scenariosMine",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### scenariosGet

`GET /v1/scenarios/{tenantId}/{projectId}/{scenarioId}`

Call GET /v1/scenarios/\{tenant_id\}/\{project_id\}/\{scenario_id\}.

- **Producer operationId:** `scenarios.get`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Call as:** TypeScript `client.palette.scenariosGet()` · Python `client.palette.scenarios_get()` · Rust `build_request("palette", "scenarios_get", params)` · Go `BuildRequest("palette", "scenariosGet", params)` · C `tempera_build_request(..., "palette", "scenariosGet", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |
| `scenarioId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.scenariosGet({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  scenarioId: "<scenarioId>",
});
```

```python Python
result = client.palette.scenarios_get({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "scenarioId": "<scenarioId>",
})
```

```rust Rust
let spec = client.build_request("palette", "scenarios_get", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("scenarioId", "<scenarioId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "scenariosGet", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "scenarioId": "<scenarioId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
    {"scenarioId", "<scenarioId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "scenariosGet",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### searchSpans

`GET /v1/search/{tenantId}/spans`

Search spans by text query and facet filters.

- **Producer operationId:** `search.spans`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Scope:** `trace:read`
- **Call as:** TypeScript `client.palette.searchSpans()` · Python `client.palette.search_spans()` · Rust `build_request("palette", "search_spans", params)` · Go `BuildRequest("palette", "searchSpans", params)` · C `tempera_build_request(..., "palette", "searchSpans", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `q` | no | — |
| `projectId` | no | — |
| `environmentId` | no | — |
| `traceId` | no | — |
| `spanId` | no | — |
| `kind` | no | — |
| `status` | no | — |
| `model` | no | — |
| `tool` | no | — |
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.searchSpans({
  tenantId: "<tenantId>",
  q: "<q>",
  projectId: "<projectId>",
});
```

```python Python
result = client.palette.search_spans({
    "tenantId": "<tenantId>",
    "q": "<q>",
    "projectId": "<projectId>",
})
```

```rust Rust
let spec = client.build_request("palette", "search_spans", &[
    ("tenantId", "<tenantId>".into()),
    ("q", "<q>".into()),
    ("projectId", "<projectId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "searchSpans", map[string]any{
    "tenantId": "<tenantId>",
    "q": "<q>",
    "projectId": "<projectId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "q=<q>&projectId=<projectId>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "searchSpans",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### getSpan

`GET /v1/spans/{tenantId}/{traceId}/{spanId}`

Fetch one canonical span by trace and span id.

- **Producer operationId:** `spans.get`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Scope:** `trace:read`
- **Call as:** TypeScript `client.palette.getSpan()` · Python `client.palette.get_span()` · Rust `build_request("palette", "get_span", params)` · Go `BuildRequest("palette", "getSpan", params)` · C `tempera_build_request(..., "palette", "getSpan", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `traceId` | yes | Substituted into the URL path (percent-encoded). |
| `spanId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `unmask` | no | — |
| `reason` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.getSpan({
  tenantId: "<tenantId>",
  traceId: "<traceId>",
  spanId: "<spanId>",
  unmask: "<unmask>",
  reason: "<reason>",
});
```

```python Python
result = client.palette.get_span({
    "tenantId": "<tenantId>",
    "traceId": "<traceId>",
    "spanId": "<spanId>",
    "unmask": "<unmask>",
    "reason": "<reason>",
})
```

```rust Rust
let spec = client.build_request("palette", "get_span", &[
    ("tenantId", "<tenantId>".into()),
    ("traceId", "<traceId>".into()),
    ("spanId", "<spanId>".into()),
    ("unmask", "<unmask>".into()),
    ("reason", "<reason>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "getSpan", map[string]any{
    "tenantId": "<tenantId>",
    "traceId": "<traceId>",
    "spanId": "<spanId>",
    "unmask": "<unmask>",
    "reason": "<reason>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"traceId", "<traceId>"},
    {"spanId", "<spanId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "unmask=<unmask>&reason=<reason>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "getSpan",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### getSpanIo

`GET /v1/spans/{tenantId}/{traceId}/{spanId}/io`

Fetch a span's recorded input and output values.

- **Producer operationId:** `spans.getIo`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Scope:** `trace:read`
- **Call as:** TypeScript `client.palette.getSpanIo()` · Python `client.palette.get_span_io()` · Rust `build_request("palette", "get_span_io", params)` · Go `BuildRequest("palette", "getSpanIo", params)` · C `tempera_build_request(..., "palette", "getSpanIo", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `traceId` | yes | Substituted into the URL path (percent-encoded). |
| `spanId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `unmask` | no | — |
| `reason` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.getSpanIo({
  tenantId: "<tenantId>",
  traceId: "<traceId>",
  spanId: "<spanId>",
  unmask: "<unmask>",
  reason: "<reason>",
});
```

```python Python
result = client.palette.get_span_io({
    "tenantId": "<tenantId>",
    "traceId": "<traceId>",
    "spanId": "<spanId>",
    "unmask": "<unmask>",
    "reason": "<reason>",
})
```

```rust Rust
let spec = client.build_request("palette", "get_span_io", &[
    ("tenantId", "<tenantId>".into()),
    ("traceId", "<traceId>".into()),
    ("spanId", "<spanId>".into()),
    ("unmask", "<unmask>".into()),
    ("reason", "<reason>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "getSpanIo", map[string]any{
    "tenantId": "<tenantId>",
    "traceId": "<traceId>",
    "spanId": "<spanId>",
    "unmask": "<unmask>",
    "reason": "<reason>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"traceId", "<traceId>"},
    {"spanId", "<spanId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "unmask=<unmask>&reason=<reason>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "getSpanIo",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### ingestSpan

`POST /v1/traces/native`

Ingest one native span; idempotent when an idempotency key is supplied.

- **Producer operationId:** `ingest.native`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Scope:** `trace:write`
- **Call as:** TypeScript `client.palette.ingestSpan()` · Python `client.palette.ingest_span()` · Rust `build_request("palette", "ingest_span", params)` · Go `BuildRequest("palette", "ingestSpan", params)` · C `tempera_build_request(..., "palette", "ingestSpan", ...)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `durability` | no | — |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `attributes` | yes | — |
| `authContext` | no | — |
| `cost` | no | — |
| `endTime` | no | — |
| `idempotencyKey` | no | — |
| `input` | no | — |
| `kind` | yes | — |
| `model` | no | — |
| `name` | yes | — |
| `output` | no | — |
| `parentSpanId` | no | — |
| `redactionClass` | yes | — |
| `scope` | yes | — |
| `seq` | yes | — |
| `spanId` | yes | — |
| `startTime` | no | — |
| `status` | yes | — |
| `tokens` | no | — |
| `traceId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.ingestSpan({
  attributes: "<attributes>",
  kind: "<kind>",
  name: "<name>",
  redactionClass: "<redactionClass>",
  scope: "<scope>",
  seq: "<seq>",
  spanId: "<spanId>",
  status: "<status>",
  traceId: "<traceId>",
});
```

```python Python
result = client.palette.ingest_span({
    "attributes": "<attributes>",
    "kind": "<kind>",
    "name": "<name>",
    "redactionClass": "<redactionClass>",
    "scope": "<scope>",
    "seq": "<seq>",
    "spanId": "<spanId>",
    "status": "<status>",
    "traceId": "<traceId>",
})
```

```rust Rust
let spec = client.build_request("palette", "ingest_span", &[
    ("attributes", "<attributes>".into()),
    ("kind", "<kind>".into()),
    ("name", "<name>".into()),
    ("redactionClass", "<redactionClass>".into()),
    ("scope", "<scope>".into()),
    ("seq", "<seq>".into()),
    ("spanId", "<spanId>".into()),
    ("status", "<status>".into()),
    ("traceId", "<traceId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "ingestSpan", map[string]any{
    "attributes": "<attributes>",
    "kind": "<kind>",
    "name": "<name>",
    "redactionClass": "<redactionClass>",
    "scope": "<scope>",
    "seq": "<seq>",
    "spanId": "<spanId>",
    "status": "<status>",
    "traceId": "<traceId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = "{\"attributes\":\"<attributes>\",\"kind\":\"<kind>\",\"name\":\"<name>\",\"redactionClass\":\"<redactionClass>\",\"scope\":\"<scope>\",\"seq\":\"<seq>\",\"spanId\":\"<spanId>\",\"status\":\"<status>\",\"traceId\":\"<traceId>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "ingestSpan",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### listTraces

`GET /v1/traces/{tenantId}`

List trace summaries for a tenant with filters and cursor pagination.

- **Producer operationId:** `traces.list`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Scope:** `trace:read`
- **Call as:** TypeScript `client.palette.listTraces()` · Python `client.palette.list_traces()` · Rust `build_request("palette", "list_traces", params)` · Go `BuildRequest("palette", "listTraces", params)` · C `tempera_build_request(..., "palette", "listTraces", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `projectId` | no | — |
| `environmentId` | no | — |
| `traceId` | no | — |
| `kind` | no | — |
| `status` | no | — |
| `startedAfter` | no | — |
| `startedBefore` | no | — |
| `model` | no | — |
| `release` | no | — |
| `minCostMicros` | no | — |
| `maxCostMicros` | no | — |
| `minLatencyMs` | no | — |
| `maxLatencyMs` | no | — |
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.listTraces({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
  environmentId: "<environmentId>",
});
```

```python Python
result = client.palette.list_traces({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "environmentId": "<environmentId>",
})
```

```rust Rust
let spec = client.build_request("palette", "list_traces", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
    ("environmentId", "<environmentId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "listTraces", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
    "environmentId": "<environmentId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "projectId=<projectId>&environmentId=<environmentId>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "listTraces",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### getTrace

`GET /v1/traces/{tenantId}/{traceId}`

Fetch one full trace with all canonical spans; unmasking PII requires the pii:unmask scope and a reason.

- **Producer operationId:** `traces.get`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Scope:** `trace:read`
- **Call as:** TypeScript `client.palette.getTrace()` · Python `client.palette.get_trace()` · Rust `build_request("palette", "get_trace", params)` · Go `BuildRequest("palette", "getTrace", params)` · C `tempera_build_request(..., "palette", "getTrace", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `traceId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `unmask` | no | — |
| `reason` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.getTrace({
  tenantId: "<tenantId>",
  traceId: "<traceId>",
  unmask: "<unmask>",
  reason: "<reason>",
});
```

```python Python
result = client.palette.get_trace({
    "tenantId": "<tenantId>",
    "traceId": "<traceId>",
    "unmask": "<unmask>",
    "reason": "<reason>",
})
```

```rust Rust
let spec = client.build_request("palette", "get_trace", &[
    ("tenantId", "<tenantId>".into()),
    ("traceId", "<traceId>".into()),
    ("unmask", "<unmask>".into()),
    ("reason", "<reason>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "getTrace", map[string]any{
    "tenantId": "<tenantId>",
    "traceId": "<traceId>",
    "unmask": "<unmask>",
    "reason": "<reason>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"traceId", "<traceId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "unmask=<unmask>&reason=<reason>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "getTrace",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### getUsageSummary

`GET /v1/usage/{tenantId}/{projectId}`

Fetch usage totals for a tenant project.

- **Producer operationId:** `usage.getSummary`
- **Auth:** Product bearer — an access token minted for audience `palette`, or a central `tp_` API key.
- **Scope:** `admin`
- **Call as:** TypeScript `client.palette.getUsageSummary()` · Python `client.palette.get_usage_summary()` · Rust `build_request("palette", "get_usage_summary", params)` · Go `BuildRequest("palette", "getUsageSummary", params)` · C `tempera_build_request(..., "palette", "getUsageSummary", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | Substituted into the URL path (percent-encoded). |
| `projectId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.palette.getUsageSummary({
  tenantId: "<tenantId>",
  projectId: "<projectId>",
});
```

```python Python
result = client.palette.get_usage_summary({
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
})
```

```rust Rust
let spec = client.build_request("palette", "get_usage_summary", &[
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("palette", "getUsageSummary", map[string]any{
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"tenantId", "<tenantId>"},
    {"projectId", "<projectId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "palette", "getUsageSummary",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>
