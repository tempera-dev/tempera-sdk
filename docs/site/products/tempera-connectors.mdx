---
title: "tempera-connectors"
description: "Tenant-scoped external API connection control plane and bounded invocation runtime."
---

{/* Generated by scripts/gen-sdk-docs.py from surface.json -- do not hand-edit. */}

> **Private access.** Hosted Tempera services are available through design-partner onboarding.
> Use the issuer, credentials, environment, and product URLs provisioned for your workspace;
> start in staging unless Tempera explicitly approves another target.

> **Contract inventory.** This typed reference is retained for SDK compatibility.
> It is not part of the primary public workflow or evidence of general service availability.

Tenant-scoped external API connection control plane and bounded invocation runtime.

| | |
|---|---|
| Repository | [tempera-connectors-runtime](https://github.com/tempera-dev/tempera-connectors-runtime) |
| Base URL env var | `TEMPERA_CONNECTORS_URL` (see [Environments](/environments)) |
| Audience | `tempera-connectors` |
| Auth | bearer minted for audience tempera-connectors; the service additionally accepts a tempera-workflows bearer and rewrites those workflow scopes down to connection scopes, which the SDK deliberately does not model |
| Client | TypeScript `client.temperaConnectors` · Python `client.tempera_connectors` · Rust `build_request("tempera_connectors", ...)` |
| Typed operations | 8 |

Anything this page does not cover is reachable through the raw passthrough:
`client.temperaConnectors.request(path, options)` (TypeScript) /
`client.tempera_connectors.request(path, ...)` (Python).

## Operations

### healthCheck

`GET /healthz`

Read-only service liveness endpoint.

- **Producer operationId:** `health.check`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.temperaConnectors.healthCheck()` · Python `client.tempera_connectors.health_check()` · Rust `build_request("tempera_connectors", "health_check", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.temperaConnectors.healthCheck();
```

```python Python
result = client.tempera_connectors.health_check()
```

```rust Rust
let spec = client.build_request("tempera_connectors", "health_check", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### connectionsList

`GET /v1/connections`

List the authenticated workspace's redacted connector connections.

- **Producer operationId:** `connections.list`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-connectors`.
- **Scope:** `connection:read`
- **Call as:** TypeScript `client.temperaConnectors.connectionsList()` · Python `client.tempera_connectors.connections_list()` · Rust `build_request("tempera_connectors", "connections_list", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `state` | no | — |
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaConnectors.connectionsList({
  state: "<state>",
  pageSize: "<pageSize>",
});
```

```python Python
result = client.tempera_connectors.connections_list({
    "state": "<state>",
    "pageSize": "<pageSize>",
})
```

```rust Rust
let spec = client.build_request("tempera_connectors", "connections_list", &[
    ("state", "<state>".into()),
    ("pageSize", "<pageSize>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### connectionsCreate

`POST /v1/connections`

Create a draft tenant-scoped connection from the catalog or a manual manifest.

- **Producer operationId:** `connections.create`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-connectors`.
- **Scope:** `connection:write`
- **Call as:** TypeScript `client.temperaConnectors.connectionsCreate()` · Python `client.tempera_connectors.connections_create()` · Rust `build_request("tempera_connectors", "connections_create", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `authScheme` | no | — |
| `baseUrl` | no | — |
| `connectorId` | yes | — |
| `connectorRevision` | no | — |
| `credentialName` | no | — |
| `displayName` | yes | — |
| `operations` | no | — |
| `specificationArtifactRef` | no | — |
| `specificationDigest` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaConnectors.connectionsCreate({
  connectorId: "<connectorId>",
  displayName: "<displayName>",
  authScheme: "<authScheme>",
});
```

```python Python
result = client.tempera_connectors.connections_create({
    "connectorId": "<connectorId>",
    "displayName": "<displayName>",
    "authScheme": "<authScheme>",
})
```

```rust Rust
let spec = client.build_request("tempera_connectors", "connections_create", &[
    ("connectorId", "<connectorId>".into()),
    ("displayName", "<displayName>".into()),
    ("authScheme", "<authScheme>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### connectionsGet

`GET /v1/connections/{id}`

Get one tenant-scoped connection without exposing secret material.

- **Producer operationId:** `connections.get`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-connectors`.
- **Scope:** `connection:read`
- **Call as:** TypeScript `client.temperaConnectors.connectionsGet()` · Python `client.tempera_connectors.connections_get()` · Rust `build_request("tempera_connectors", "connections_get", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `id` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaConnectors.connectionsGet({
  id: "<id>",
});
```

```python Python
result = client.tempera_connectors.connections_get({
    "id": "<id>",
})
```

```rust Rust
let spec = client.build_request("tempera_connectors", "connections_get", &[
    ("id", "<id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### connectionsInvoke

`POST /v1/connections/{id}:invoke`

Execute an active declared operation with a revision-pinned receipt.

- **Producer operationId:** `connections.invoke`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-connectors`.
- **Physical action:** Yes. This operation may cause an external real-world action.
- **Scope:** `connection:invoke`
- **Call as:** TypeScript `client.temperaConnectors.connectionsInvoke()` · Python `client.tempera_connectors.connections_invoke()` · Rust `build_request("tempera_connectors", "connections_invoke", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `id` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `idempotencyKey` | no | — |
| `input` | no | — |
| `operationId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaConnectors.connectionsInvoke({
  id: "<id>",
  operationId: "<operationId>",
  idempotencyKey: "<idempotencyKey>",
  input: "<input>",
});
```

```python Python
result = client.tempera_connectors.connections_invoke({
    "id": "<id>",
    "operationId": "<operationId>",
    "idempotencyKey": "<idempotencyKey>",
    "input": "<input>",
})
```

```rust Rust
let spec = client.build_request("tempera_connectors", "connections_invoke", &[
    ("id", "<id>".into()),
    ("operationId", "<operationId>".into()),
    ("idempotencyKey", "<idempotencyKey>".into()),
    ("input", "<input>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### connectionsTest

`POST /v1/connections/{id}:test`

Safely test one declared read-only operation and activate/degrade the connection.

- **Producer operationId:** `connections.test`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-connectors`.
- **Physical action:** Yes. This operation may cause an external real-world action.
- **Scope:** `connection:write`
- **Call as:** TypeScript `client.temperaConnectors.connectionsTest()` · Python `client.tempera_connectors.connections_test()` · Rust `build_request("tempera_connectors", "connections_test", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `id` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `operationId` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaConnectors.connectionsTest({
  id: "<id>",
  operationId: "<operationId>",
});
```

```python Python
result = client.tempera_connectors.connections_test({
    "id": "<id>",
    "operationId": "<operationId>",
})
```

```rust Rust
let spec = client.build_request("tempera_connectors", "connections_test", &[
    ("id", "<id>".into()),
    ("operationId", "<operationId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### connectionsImportOpenApi

`POST /v1/connections:importOpenApi`

Normalize a caller-supplied OpenAPI 3 document into a draft connection.

- **Producer operationId:** `connections.importOpenApi`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-connectors`.
- **Scope:** `connection:write`
- **Call as:** TypeScript `client.temperaConnectors.connectionsImportOpenApi()` · Python `client.tempera_connectors.connections_import_open_api()` · Rust `build_request("tempera_connectors", "connections_import_open_api", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `authScheme` | yes | — |
| `baseUrl` | no | — |
| `connectorId` | no | — |
| `credentialName` | no | — |
| `displayName` | yes | — |
| `document` | no | — |
| `sourceUrl` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaConnectors.connectionsImportOpenApi({
  authScheme: "<authScheme>",
  displayName: "<displayName>",
  baseUrl: "<baseUrl>",
});
```

```python Python
result = client.tempera_connectors.connections_import_open_api({
    "authScheme": "<authScheme>",
    "displayName": "<displayName>",
    "baseUrl": "<baseUrl>",
})
```

```rust Rust
let spec = client.build_request("tempera_connectors", "connections_import_open_api", &[
    ("authScheme", "<authScheme>".into()),
    ("displayName", "<displayName>".into()),
    ("baseUrl", "<baseUrl>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### connectorsList

`GET /v1/connectors`

List the reviewed, read-only connector catalog.

- **Producer operationId:** `connectors.list`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-connectors`.
- **Scope:** `connection:read`
- **Call as:** TypeScript `client.temperaConnectors.connectorsList()` · Python `client.tempera_connectors.connectors_list()` · Rust `build_request("tempera_connectors", "connectors_list", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaConnectors.connectorsList({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.tempera_connectors.connectors_list({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("tempera_connectors", "connectors_list", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>
