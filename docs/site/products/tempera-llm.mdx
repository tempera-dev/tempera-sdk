---
title: "tempera-llm"
description: "OpenAI-compatible LLM gateway every Tempera product calls instead of hitting providers directly; reports LLM cost as model_cost usage events per the billing-credits contract."
---

{/* Generated by scripts/gen-sdk-docs.py from surface.json -- do not hand-edit. */}

> **Private access.** Hosted Tempera services are available through design-partner onboarding.
> Use the issuer, credentials, environment, and product URLs provisioned for your workspace;
> start in staging unless Tempera explicitly approves another target.

> **Contract inventory.** This typed reference is retained for SDK compatibility.
> It is not part of the primary public workflow or evidence of general service availability.

OpenAI-compatible LLM gateway every Tempera product calls instead of hitting providers directly; reports LLM cost as model_cost usage events per the billing-credits contract.

| | |
|---|---|
| Repository | [tempera-llm](https://github.com/tempera-dev/tempera-llm) |
| Base URL env var | `TEMPERA_LLM_URL` (see [Environments](/environments)) |
| Audience | `tempera-llm` |
| Auth | bearer minted for audience tempera-llm with model:read/model:invoke, or a central tp_ API key |
| Client | TypeScript `client.temperaLlm` · Python `client.tempera_llm` · Rust `build_request("tempera_llm", ...)` |
| Typed operations | 5 |

Anything this page does not cover is reachable through the raw passthrough:
`client.temperaLlm.request(path, options)` (TypeScript) /
`client.tempera_llm.request(path, ...)` (Python).

## Operations

### health

`GET /healthz`

Check tempera-llm gateway liveness; returns \{ok: true\}.

- **Producer operationId:** `health.check`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.temperaLlm.health()` · Python `client.tempera_llm.health()` · Rust `build_request("tempera_llm", "health", params)` · Go `BuildRequest("temperaLlm", "health", params)` · C `tempera_build_request(..., "temperaLlm", "health", ...)`

<CodeGroup>

```typescript TypeScript
const result = await client.temperaLlm.health();
```

```python Python
result = client.tempera_llm.health()
```

```rust Rust
let spec = client.build_request("tempera_llm", "health", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaLlm", "health", map[string]any{})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaLlm", "health",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### readinessCheck

`GET /readyz`

Call GET /readyz.

- **Producer operationId:** `readiness.check`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.temperaLlm.readinessCheck()` · Python `client.tempera_llm.readiness_check()` · Rust `build_request("tempera_llm", "readiness_check", params)` · Go `BuildRequest("temperaLlm", "readinessCheck", params)` · C `tempera_build_request(..., "temperaLlm", "readinessCheck", ...)`

<CodeGroup>

```typescript TypeScript
const result = await client.temperaLlm.readinessCheck();
```

```python Python
result = client.tempera_llm.readiness_check()
```

```rust Rust
let spec = client.build_request("tempera_llm", "readiness_check", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaLlm", "readinessCheck", map[string]any{})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaLlm", "readinessCheck",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### createChatCompletion

`POST /v1/chat/completions`

Create a non-streaming OpenAI-compatible chat completion through the tempera-llm gateway.

- **Producer operationId:** `chatCompletions.create`
- **Auth:** Product bearer — an access token minted for audience `tempera-llm`, or a central `tp_` API key.
- **Scope:** `model:invoke`
- **Call as:** TypeScript `client.temperaLlm.createChatCompletion()` · Python `client.tempera_llm.create_chat_completion()` · Rust `build_request("tempera_llm", "create_chat_completion", params)` · Go `BuildRequest("temperaLlm", "createChatCompletion", params)` · C `tempera_build_request(..., "temperaLlm", "createChatCompletion", ...)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `byok` | no | — |
| `max_tokens` | yes | — |
| `messages` | yes | — |
| `model` | yes | — |
| `response_format` | no | — |
| `stream` | no | — |
| `temperature` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaLlm.createChatCompletion({
  max_tokens: "<max_tokens>",
  messages: "<messages>",
  model: "<model>",
});
```

```python Python
result = client.tempera_llm.create_chat_completion({
    "max_tokens": "<max_tokens>",
    "messages": "<messages>",
    "model": "<model>",
})
```

```rust Rust
let spec = client.build_request("tempera_llm", "create_chat_completion", &[
    ("max_tokens", "<max_tokens>".into()),
    ("messages", "<messages>".into()),
    ("model", "<model>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaLlm", "createChatCompletion", map[string]any{
    "max_tokens": "<max_tokens>",
    "messages": "<messages>",
    "model": "<model>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = "{\"max_tokens\":\"<max_tokens>\",\"messages\":\"<messages>\",\"model\":\"<model>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaLlm", "createChatCompletion",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### listModels

`GET /v1/models`

List the configured model catalog the gateway can route to.

- **Producer operationId:** `models.list`
- **Auth:** Product bearer — an access token minted for audience `tempera-llm`, or a central `tp_` API key.
- **Scope:** `model:read`
- **Call as:** TypeScript `client.temperaLlm.listModels()` · Python `client.tempera_llm.list_models()` · Rust `build_request("tempera_llm", "list_models", params)` · Go `BuildRequest("temperaLlm", "listModels", params)` · C `tempera_build_request(..., "temperaLlm", "listModels", ...)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaLlm.listModels({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.tempera_llm.list_models({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("tempera_llm", "list_models", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaLlm", "listModels", map[string]any{
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = "pageSize=<pageSize>&pageToken=<pageToken>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaLlm", "listModels",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### createResponse

`POST /v1/responses`

Create a non-streaming OpenAI Responses-style inference request through the tempera-llm gateway.

- **Producer operationId:** `responses.create`
- **Auth:** Product bearer — an access token minted for audience `tempera-llm`, or a central `tp_` API key.
- **Scope:** `model:invoke`
- **Call as:** TypeScript `client.temperaLlm.createResponse()` · Python `client.tempera_llm.create_response()` · Rust `build_request("tempera_llm", "create_response", params)` · Go `BuildRequest("temperaLlm", "createResponse", params)` · C `tempera_build_request(..., "temperaLlm", "createResponse", ...)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `byok` | no | — |
| `input` | yes | — |
| `max_output_tokens` | yes | — |
| `model` | yes | — |
| `response_format` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaLlm.createResponse({
  input: "<input>",
  max_output_tokens: "<max_output_tokens>",
  model: "<model>",
});
```

```python Python
result = client.tempera_llm.create_response({
    "input": "<input>",
    "max_output_tokens": "<max_output_tokens>",
    "model": "<model>",
})
```

```rust Rust
let spec = client.build_request("tempera_llm", "create_response", &[
    ("input", "<input>".into()),
    ("max_output_tokens", "<max_output_tokens>".into()),
    ("model", "<model>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaLlm", "createResponse", map[string]any{
    "input": "<input>",
    "max_output_tokens": "<max_output_tokens>",
    "model": "<model>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = "{\"input\":\"<input>\",\"max_output_tokens\":\"<max_output_tokens>\",\"model\":\"<model>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaLlm", "createResponse",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>
