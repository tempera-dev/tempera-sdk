---
title: "Tempera Gym"
description: "Seeded RL environment service: registered gym environments, episode rollout with stepwise actions, and trajectory verification."
---

{/* Generated by scripts/gen-sdk-docs.py from surface.json -- do not hand-edit. */}

> **Private access.** Hosted Tempera services are available through design-partner onboarding.
> Use the issuer, credentials, environment, and product URLs provisioned for your workspace;
> start in staging unless Tempera explicitly approves another target.

> **Contract inventory.** This typed reference is retained for SDK compatibility.
> It is not part of the primary public workflow or evidence of general service availability.

Seeded RL environment service: registered gym environments, episode rollout with stepwise actions, and trajectory verification.

| | |
|---|---|
| Repository | [tempera-gym](https://github.com/tempera-dev/tempera-gym) |
| Base URL env var | `TEMPERA_GYM_URL` (see [Environments](/environments)) |
| Audience | `tempera-gym` |
| Auth | bearer minted for audience tempera-gym, or a central tp_ API key |
| Client | TypeScript `client.temperaGym` · Python `client.tempera_gym` · Rust `build_request("tempera_gym", ...)` |
| Typed operations | 8 |

Anything this page does not cover is reachable through the raw passthrough:
`client.temperaGym.request(path, options)` (TypeScript) /
`client.tempera_gym.request(path, ...)` (Python).

## Operations

### health

`GET /v1/health`

Check tempera-gym liveness; returns the service status.

- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.temperaGym.health()` · Python `client.tempera_gym.health()` · Rust `build_request("tempera_gym", "health", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.health();
```

```python Python
result = client.tempera_gym.health()
```

```rust Rust
let spec = client.build_request("tempera_gym", "health", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listEnvironments

`GET /v1/environments`

List the registered gym environments.

- **Auth:** Product bearer — an access token minted for audience `tempera-gym`, or a central `tp_` API key.
- **Call as:** TypeScript `client.temperaGym.listEnvironments()` · Python `client.tempera_gym.list_environments()` · Rust `build_request("tempera_gym", "list_environments", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.listEnvironments();
```

```python Python
result = client.tempera_gym.list_environments()
```

```rust Rust
let spec = client.build_request("tempera_gym", "list_environments", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getEnvironment

`GET /v1/environments/{environment_id}`

Fetch one registered gym environment definition.

- **Auth:** Product bearer — an access token minted for audience `tempera-gym`, or a central `tp_` API key.
- **Call as:** TypeScript `client.temperaGym.getEnvironment()` · Python `client.tempera_gym.get_environment()` · Rust `build_request("tempera_gym", "get_environment", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `environment_id` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.getEnvironment({
  environment_id: "<environment_id>",
});
```

```python Python
result = client.tempera_gym.get_environment({
    "environment_id": "<environment_id>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "get_environment", &[
    ("environment_id", "<environment_id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createEpisode

`POST /v1/episodes`

Create a seeded episode in a gym environment and receive its initial observation.

- **Auth:** Product bearer — an access token minted for audience `tempera-gym`, or a central `tp_` API key.
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.temperaGym.createEpisode()` · Python `client.tempera_gym.create_episode()` · Rust `build_request("tempera_gym", "create_episode", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `environment_id` | yes | — |
| `seed` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.createEpisode({
  environment_id: "<environment_id>",
  seed: "<seed>",
});
```

```python Python
result = client.tempera_gym.create_episode({
    "environment_id": "<environment_id>",
    "seed": "<seed>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "create_episode", &[
    ("environment_id", "<environment_id>".into()),
    ("seed", "<seed>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getEpisode

`GET /v1/episodes/{episode_id}`

Fetch one episode with its current state.

- **Auth:** Product bearer — an access token minted for audience `tempera-gym`, or a central `tp_` API key.
- **Call as:** TypeScript `client.temperaGym.getEpisode()` · Python `client.tempera_gym.get_episode()` · Rust `build_request("tempera_gym", "get_episode", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `episode_id` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.getEpisode({
  episode_id: "<episode_id>",
});
```

```python Python
result = client.tempera_gym.get_episode({
    "episode_id": "<episode_id>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "get_episode", &[
    ("episode_id", "<episode_id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### stepEpisode

`POST /v1/episodes/{episode_id}/step`

Apply one action to an episode and receive the resulting observation, reward, and termination state.

- **Auth:** Product bearer — an access token minted for audience `tempera-gym`, or a central `tp_` API key.
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.temperaGym.stepEpisode()` · Python `client.tempera_gym.step_episode()` · Rust `build_request("tempera_gym", "step_episode", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `episode_id` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `action` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.stepEpisode({
  episode_id: "<episode_id>",
  action: "<action>",
});
```

```python Python
result = client.tempera_gym.step_episode({
    "episode_id": "<episode_id>",
    "action": "<action>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "step_episode", &[
    ("episode_id", "<episode_id>".into()),
    ("action", "<action>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### closeEpisode

`DELETE /v1/episodes/{episode_id}`

Close an episode and release its environment resources (idempotent).

- **Auth:** Product bearer — an access token minted for audience `tempera-gym`, or a central `tp_` API key.
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.temperaGym.closeEpisode()` · Python `client.tempera_gym.close_episode()` · Rust `build_request("tempera_gym", "close_episode", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `episode_id` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.closeEpisode({
  episode_id: "<episode_id>",
});
```

```python Python
result = client.tempera_gym.close_episode({
    "episode_id": "<episode_id>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "close_episode", &[
    ("episode_id", "<episode_id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### verifyTrajectory

`POST /v1/trajectories/verify`

Verify a recorded trajectory against its environment's transition and reward rules.

- **Auth:** Product bearer — an access token minted for audience `tempera-gym`, or a central `tp_` API key.
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.temperaGym.verifyTrajectory()` · Python `client.tempera_gym.verify_trajectory()` · Rust `build_request("tempera_gym", "verify_trajectory", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `trajectory` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.verifyTrajectory({
  trajectory: "<trajectory>",
});
```

```python Python
result = client.tempera_gym.verify_trajectory({
    "trajectory": "<trajectory>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "verify_trajectory", &[
    ("trajectory", "<trajectory>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>
