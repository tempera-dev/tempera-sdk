---
title: "tempera-gym"
description: "RL and evaluation service with persisted trajectories, sealed evaluators, and deterministic outcome-blind Bio batch proposals."
---

{/* Generated by scripts/gen-sdk-docs.py from surface.json -- do not hand-edit. */}

> **Private access.** Hosted Tempera services are available through design-partner onboarding.
> Use the issuer, credentials, environment, and product URLs provisioned for your workspace;
> start in staging unless Tempera explicitly approves another target.

> **Contract inventory.** This typed reference is retained for SDK compatibility.
> It is not part of the primary public workflow or evidence of general service availability.

RL and evaluation service with persisted trajectories, sealed evaluators, and deterministic outcome-blind Bio batch proposals.

| | |
|---|---|
| Repository | [tempera-gym](https://github.com/tempera-dev/tempera-gym) |
| Base URL env var | `TEMPERA_GYM_URL` (see [Environments](/environments)) |
| Audience | `tempera-gym` |
| Auth | bearer minted for audience tempera-gym with dataset:read/eval:run, central tp_ API key, or the gym static token |
| Client | TypeScript `client.temperaGym` · Python `client.tempera_gym` · Rust `build_request("tempera_gym", ...)` |
| Typed operations | 22 |

Anything this page does not cover is reachable through the raw passthrough:
`client.temperaGym.request(path, options)` (TypeScript) /
`client.tempera_gym.request(path, ...)` (Python).

## Operations

### health

`GET /healthz`

Check tempera-gym service liveness.

- **Producer operationId:** `health.get`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.temperaGym.health()` · Python `client.tempera_gym.health()` · Rust `build_request("tempera_gym", "health", params)` · Go `BuildRequest("temperaGym", "health", params)` · C `tempera_build_request(..., "temperaGym", "health", ...)`

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.health();
```

```python Python
result = client.tempera_gym.health()
```

```rust Rust
let spec = client.build_request("tempera_gym", "health", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaGym", "health", map[string]any{})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaGym", "health",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### listEnvironments

`GET /v1/environments`

List the gym pack's environment catalog, including implementation status and per-environment manifests.

- **Producer operationId:** `environments.list`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-gym`.
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.temperaGym.listEnvironments()` · Python `client.tempera_gym.list_environments()` · Rust `build_request("tempera_gym", "list_environments", params)` · Go `BuildRequest("temperaGym", "listEnvironments", params)` · C `tempera_build_request(..., "temperaGym", "listEnvironments", ...)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.listEnvironments({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.tempera_gym.list_environments({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "list_environments", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaGym", "listEnvironments", map[string]any{
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = "pageSize=<pageSize>&pageToken=<pageToken>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaGym", "listEnvironments",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### listDomains

`GET /v1/domains`

Domain capabilities represented in the versioned task catalog.

- **Producer operationId:** `domains.list`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-gym`.
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.temperaGym.listDomains()` · Python `client.tempera_gym.list_domains()` · Rust `build_request("tempera_gym", "list_domains", params)` · Go `BuildRequest("temperaGym", "listDomains", params)` · C `tempera_build_request(..., "temperaGym", "listDomains", ...)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.listDomains({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.tempera_gym.list_domains({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "list_domains", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaGym", "listDomains", map[string]any{
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = "pageSize=<pageSize>&pageToken=<pageToken>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaGym", "listDomains",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### listTasks

`GET /v1/tasks`

List versioned task definitions without agent inputs.

- **Producer operationId:** `tasks.list`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-gym`.
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.temperaGym.listTasks()` · Python `client.tempera_gym.list_tasks()` · Rust `build_request("tempera_gym", "list_tasks", params)` · Go `BuildRequest("temperaGym", "listTasks", params)` · C `tempera_build_request(..., "temperaGym", "listTasks", ...)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `domain` | no | — |
| `family` | no | — |
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.listTasks({
  domain: "<domain>",
  family: "<family>",
});
```

```python Python
result = client.tempera_gym.list_tasks({
    "domain": "<domain>",
    "family": "<family>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "list_tasks", &[
    ("domain", "<domain>".into()),
    ("family", "<family>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaGym", "listTasks", map[string]any{
    "domain": "<domain>",
    "family": "<family>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = "domain=<domain>&family=<family>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaGym", "listTasks",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### getTask

`GET /v1/tasks/{task}`

Get one immutable task definition including its agent-visible input.

- **Producer operationId:** `tasks.get`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-gym`.
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.temperaGym.getTask()` · Python `client.tempera_gym.get_task()` · Rust `build_request("tempera_gym", "get_task", params)` · Go `BuildRequest("temperaGym", "getTask", params)` · C `tempera_build_request(..., "temperaGym", "getTask", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `task` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `version` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.getTask({
  task: "<task>",
  version: "<version>",
});
```

```python Python
result = client.tempera_gym.get_task({
    "task": "<task>",
    "version": "<version>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "get_task", &[
    ("task", "<task>".into()),
    ("version", "<version>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaGym", "getTask", map[string]any{
    "task": "<task>",
    "version": "<version>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"task", "<task>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "version=<version>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaGym", "getTask",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### evaluateTask

`POST /v1/tasks/{task}:evaluate`

Deterministically verify one candidate without creating an episode.

- **Producer operationId:** `tasks.evaluate`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-gym`.
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.temperaGym.evaluateTask()` · Python `client.tempera_gym.evaluate_task()` · Rust `build_request("tempera_gym", "evaluate_task", params)` · Go `BuildRequest("temperaGym", "evaluateTask", params)` · C `tempera_build_request(..., "temperaGym", "evaluateTask", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `task` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `version` | no | — |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `candidate` | yes | — |
| `seed` | no | — |
| `receipts` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.evaluateTask({
  task: "<task>",
  candidate: "<candidate>",
  seed: "<seed>",
  receipts: "<receipts>",
});
```

```python Python
result = client.tempera_gym.evaluate_task({
    "task": "<task>",
    "candidate": "<candidate>",
    "seed": "<seed>",
    "receipts": "<receipts>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "evaluate_task", &[
    ("task", "<task>".into()),
    ("candidate", "<candidate>".into()),
    ("seed", "<seed>".into()),
    ("receipts", "<receipts>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaGym", "evaluateTask", map[string]any{
    "task": "<task>",
    "candidate": "<candidate>",
    "seed": "<seed>",
    "receipts": "<receipts>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"task", "<task>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"candidate\":\"<candidate>\",\"seed\":\"<seed>\",\"receipts\":\"<receipts>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaGym", "evaluateTask",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### listVerifiers

`GET /v1/verifiers`

List verifier identities and bound tasks without grader content.

- **Producer operationId:** `verifiers.list`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-gym`.
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.temperaGym.listVerifiers()` · Python `client.tempera_gym.list_verifiers()` · Rust `build_request("tempera_gym", "list_verifiers", params)` · Go `BuildRequest("temperaGym", "listVerifiers", params)` · C `tempera_build_request(..., "temperaGym", "listVerifiers", ...)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.listVerifiers({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.tempera_gym.list_verifiers({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "list_verifiers", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaGym", "listVerifiers", map[string]any{
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = "pageSize=<pageSize>&pageToken=<pageToken>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaGym", "listVerifiers",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### listEpisodes

`GET /v1/episodes`

List durable episode snapshots, newest first.

- **Producer operationId:** `episodes.list`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-gym`.
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.temperaGym.listEpisodes()` · Python `client.tempera_gym.list_episodes()` · Rust `build_request("tempera_gym", "list_episodes", params)` · Go `BuildRequest("temperaGym", "listEpisodes", params)` · C `tempera_build_request(..., "temperaGym", "listEpisodes", ...)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `domain` | no | — |
| `status` | no | — |
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.listEpisodes({
  domain: "<domain>",
  status: "<status>",
});
```

```python Python
result = client.tempera_gym.list_episodes({
    "domain": "<domain>",
    "status": "<status>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "list_episodes", &[
    ("domain", "<domain>".into()),
    ("status", "<status>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaGym", "listEpisodes", map[string]any{
    "domain": "<domain>",
    "status": "<status>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = "domain=<domain>&status=<status>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaGym", "listEpisodes",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### createEpisode

`POST /v1/episodes`

Reset a versioned task into a durable episode.

- **Producer operationId:** `episodes.create`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-gym`.
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.temperaGym.createEpisode()` · Python `client.tempera_gym.create_episode()` · Rust `build_request("tempera_gym", "create_episode", params)` · Go `BuildRequest("temperaGym", "createEpisode", params)` · C `tempera_build_request(..., "temperaGym", "createEpisode", ...)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `task` | yes | — |
| `seed` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.createEpisode({
  task: "<task>",
  seed: "<seed>",
});
```

```python Python
result = client.tempera_gym.create_episode({
    "task": "<task>",
    "seed": "<seed>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "create_episode", &[
    ("task", "<task>".into()),
    ("seed", "<seed>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaGym", "createEpisode", map[string]any{
    "task": "<task>",
    "seed": "<seed>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = "{\"task\":\"<task>\",\"seed\":\"<seed>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaGym", "createEpisode",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### getEpisode

`GET /v1/episodes/{episode}`

Read and content-verify one durable episode snapshot.

- **Producer operationId:** `episodes.get`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-gym`.
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.temperaGym.getEpisode()` · Python `client.tempera_gym.get_episode()` · Rust `build_request("tempera_gym", "get_episode", params)` · Go `BuildRequest("temperaGym", "getEpisode", params)` · C `tempera_build_request(..., "temperaGym", "getEpisode", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `episode` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.getEpisode({
  episode: "<episode>",
});
```

```python Python
result = client.tempera_gym.get_episode({
    "episode": "<episode>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "get_episode", &[
    ("episode", "<episode>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaGym", "getEpisode", map[string]any{
    "episode": "<episode>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"episode", "<episode>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaGym", "getEpisode",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### stepEpisode

`POST /v1/episodes/{episode}:step`

Apply one schema-validated action and persist the transition.

- **Producer operationId:** `episodes.step`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-gym`.
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.temperaGym.stepEpisode()` · Python `client.tempera_gym.step_episode()` · Rust `build_request("tempera_gym", "step_episode", params)` · Go `BuildRequest("temperaGym", "stepEpisode", params)` · C `tempera_build_request(..., "temperaGym", "stepEpisode", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `episode` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `action` | yes | — |
| `receipts` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.stepEpisode({
  episode: "<episode>",
  action: "<action>",
  receipts: "<receipts>",
});
```

```python Python
result = client.tempera_gym.step_episode({
    "episode": "<episode>",
    "action": "<action>",
    "receipts": "<receipts>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "step_episode", &[
    ("episode", "<episode>".into()),
    ("action", "<action>".into()),
    ("receipts", "<receipts>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaGym", "stepEpisode", map[string]any{
    "episode": "<episode>",
    "action": "<action>",
    "receipts": "<receipts>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"episode", "<episode>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"action\":\"<action>\",\"receipts\":\"<receipts>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaGym", "stepEpisode",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### exportEpisode

`POST /v1/episodes/{episode}:export`

Retain a completed Gym episode and trajectory in Data Engine.

- **Producer operationId:** `episodes.export`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-gym`.
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.temperaGym.exportEpisode()` · Python `client.tempera_gym.export_episode()` · Rust `build_request("tempera_gym", "export_episode", params)` · Go `BuildRequest("temperaGym", "exportEpisode", params)` · C `tempera_build_request(..., "temperaGym", "exportEpisode", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `episode` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `contextEvidenceRef` | yes | — |
| `verifierReceiptArtifactRef` | no | — |
| `requestId` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.exportEpisode({
  episode: "<episode>",
  contextEvidenceRef: "<contextEvidenceRef>",
  verifierReceiptArtifactRef: "<verifierReceiptArtifactRef>",
  requestId: "<requestId>",
});
```

```python Python
result = client.tempera_gym.export_episode({
    "episode": "<episode>",
    "contextEvidenceRef": "<contextEvidenceRef>",
    "verifierReceiptArtifactRef": "<verifierReceiptArtifactRef>",
    "requestId": "<requestId>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "export_episode", &[
    ("episode", "<episode>".into()),
    ("contextEvidenceRef", "<contextEvidenceRef>".into()),
    ("verifierReceiptArtifactRef", "<verifierReceiptArtifactRef>".into()),
    ("requestId", "<requestId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaGym", "exportEpisode", map[string]any{
    "episode": "<episode>",
    "contextEvidenceRef": "<contextEvidenceRef>",
    "verifierReceiptArtifactRef": "<verifierReceiptArtifactRef>",
    "requestId": "<requestId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"episode", "<episode>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"contextEvidenceRef\":\"<contextEvidenceRef>\",\"verifierReceiptArtifactRef\":\"<verifierReceiptArtifactRef>\",\"requestId\":\"<requestId>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaGym", "exportEpisode",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### listRuns

`GET /v1/runs`

List persisted rollout run index records, newest first.

- **Producer operationId:** `runs.list`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-gym`.
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.temperaGym.listRuns()` · Python `client.tempera_gym.list_runs()` · Rust `build_request("tempera_gym", "list_runs", params)` · Go `BuildRequest("temperaGym", "listRuns", params)` · C `tempera_build_request(..., "temperaGym", "listRuns", ...)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `environmentId` | no | — |
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.listRuns({
  environmentId: "<environmentId>",
  pageSize: "<pageSize>",
});
```

```python Python
result = client.tempera_gym.list_runs({
    "environmentId": "<environmentId>",
    "pageSize": "<pageSize>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "list_runs", &[
    ("environmentId", "<environmentId>".into()),
    ("pageSize", "<pageSize>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaGym", "listRuns", map[string]any{
    "environmentId": "<environmentId>",
    "pageSize": "<pageSize>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = "environmentId=<environmentId>&pageSize=<pageSize>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaGym", "listRuns",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### getRun

`GET /v1/runs/{run}`

Fetch one persisted run's index record and verified trajectory-v1 envelope by run id or trajectory content hash.

- **Producer operationId:** `runs.get`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-gym`.
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.temperaGym.getRun()` · Python `client.tempera_gym.get_run()` · Rust `build_request("tempera_gym", "get_run", params)` · Go `BuildRequest("temperaGym", "getRun", params)` · C `tempera_build_request(..., "temperaGym", "getRun", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `run` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.getRun({
  run: "<run>",
});
```

```python Python
result = client.tempera_gym.get_run({
    "run": "<run>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "get_run", &[
    ("run", "<run>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaGym", "getRun", map[string]any{
    "run": "<run>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"run", "<run>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaGym", "getRun",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### createRollout

`POST /v1/rollouts`

Execute one rollout synchronously, persist the trajectory, and return the completed operation envelope.

- **Producer operationId:** `rollouts.create`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-gym`.
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.temperaGym.createRollout()` · Python `client.tempera_gym.create_rollout()` · Rust `build_request("tempera_gym", "create_rollout", params)` · Go `BuildRequest("temperaGym", "createRollout", params)` · C `tempera_build_request(..., "temperaGym", "createRollout", ...)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `environmentId` | yes | — |
| `policy` | no | — |
| `seed` | yes | — |
| `maxSteps` | no | — |
| `model` | no | — |
| `dataEngineProductId` | no | — |
| `dataEngineMaxRecords` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.createRollout({
  environmentId: "<environmentId>",
  seed: "<seed>",
  policy: "<policy>",
});
```

```python Python
result = client.tempera_gym.create_rollout({
    "environmentId": "<environmentId>",
    "seed": "<seed>",
    "policy": "<policy>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "create_rollout", &[
    ("environmentId", "<environmentId>".into()),
    ("seed", "<seed>".into()),
    ("policy", "<policy>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaGym", "createRollout", map[string]any{
    "environmentId": "<environmentId>",
    "seed": "<seed>",
    "policy": "<policy>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = "{\"environmentId\":\"<environmentId>\",\"policy\":\"<policy>\",\"seed\":\"<seed>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaGym", "createRollout",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### bioProposalPoliciesList

`GET /v1/bio-proposal-policies`

Discover the four frozen outcome-blind Bio proposal policies.

- **Producer operationId:** `bioProposalPolicies.list`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-gym`.
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.temperaGym.bioProposalPoliciesList()` · Python `client.tempera_gym.bio_proposal_policies_list()` · Rust `build_request("tempera_gym", "bio_proposal_policies_list", params)` · Go `BuildRequest("temperaGym", "bioProposalPoliciesList", params)` · C `tempera_build_request(..., "temperaGym", "bioProposalPoliciesList", ...)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.bioProposalPoliciesList({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.tempera_gym.bio_proposal_policies_list({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "bio_proposal_policies_list", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaGym", "bioProposalPoliciesList", map[string]any{
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = "pageSize=<pageSize>&pageToken=<pageToken>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaGym", "bioProposalPoliciesList",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### bioProposalsProposeBatch

`POST /v1/bio-proposals:proposeBatch`

Propose one constrained Bio candidate batch without outcome access.

- **Producer operationId:** `bioProposals.proposeBatch`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-gym`.
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.temperaGym.bioProposalsProposeBatch()` · Python `client.tempera_gym.bio_proposals_propose_batch()` · Rust `build_request("tempera_gym", "bio_proposals_propose_batch", params)` · Go `BuildRequest("temperaGym", "bioProposalsProposeBatch", params)` · C `tempera_build_request(..., "temperaGym", "bioProposalsProposeBatch", ...)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `schemaVersion` | yes | — |
| `campaignDigest` | yes | — |
| `datasetDigest` | yes | — |
| `candidateSetDigest` | yes | — |
| `model` | yes | — |
| `policy` | yes | — |
| `shadowPolicies` | yes | — |
| `mode` | yes | — |
| `constraints` | yes | — |
| `candidates` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.bioProposalsProposeBatch({
  schemaVersion: "<schemaVersion>",
  campaignDigest: "<campaignDigest>",
  datasetDigest: "<datasetDigest>",
  candidateSetDigest: "<candidateSetDigest>",
  model: "<model>",
  policy: "<policy>",
  shadowPolicies: "<shadowPolicies>",
  mode: "<mode>",
  constraints: "<constraints>",
  candidates: "<candidates>",
});
```

```python Python
result = client.tempera_gym.bio_proposals_propose_batch({
    "schemaVersion": "<schemaVersion>",
    "campaignDigest": "<campaignDigest>",
    "datasetDigest": "<datasetDigest>",
    "candidateSetDigest": "<candidateSetDigest>",
    "model": "<model>",
    "policy": "<policy>",
    "shadowPolicies": "<shadowPolicies>",
    "mode": "<mode>",
    "constraints": "<constraints>",
    "candidates": "<candidates>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "bio_proposals_propose_batch", &[
    ("schemaVersion", "<schemaVersion>".into()),
    ("campaignDigest", "<campaignDigest>".into()),
    ("datasetDigest", "<datasetDigest>".into()),
    ("candidateSetDigest", "<candidateSetDigest>".into()),
    ("model", "<model>".into()),
    ("policy", "<policy>".into()),
    ("shadowPolicies", "<shadowPolicies>".into()),
    ("mode", "<mode>".into()),
    ("constraints", "<constraints>".into()),
    ("candidates", "<candidates>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaGym", "bioProposalsProposeBatch", map[string]any{
    "schemaVersion": "<schemaVersion>",
    "campaignDigest": "<campaignDigest>",
    "datasetDigest": "<datasetDigest>",
    "candidateSetDigest": "<candidateSetDigest>",
    "model": "<model>",
    "policy": "<policy>",
    "shadowPolicies": "<shadowPolicies>",
    "mode": "<mode>",
    "constraints": "<constraints>",
    "candidates": "<candidates>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = "{\"schemaVersion\":\"<schemaVersion>\",\"campaignDigest\":\"<campaignDigest>\",\"datasetDigest\":\"<datasetDigest>\",\"candidateSetDigest\":\"<candidateSetDigest>\",\"model\":\"<model>\",\"policy\":\"<policy>\",\"shadowPolicies\":\"<shadowPolicies>\",\"mode\":\"<mode>\",\"constraints\":\"<constraints>\",\"candidates\":\"<candidates>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaGym", "bioProposalsProposeBatch",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### listSealedEvaluators

`GET /v1/sealed-evaluators`

Discover boot-trusted exact sealed-evaluator identities.

- **Producer operationId:** `sealedEvaluators.list`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-gym`.
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.temperaGym.listSealedEvaluators()` · Python `client.tempera_gym.list_sealed_evaluators()` · Rust `build_request("tempera_gym", "list_sealed_evaluators", params)` · Go `BuildRequest("temperaGym", "listSealedEvaluators", params)` · C `tempera_build_request(..., "temperaGym", "listSealedEvaluators", ...)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.listSealedEvaluators({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.tempera_gym.list_sealed_evaluators({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "list_sealed_evaluators", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaGym", "listSealedEvaluators", map[string]any{
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = "pageSize=<pageSize>&pageToken=<pageToken>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaGym", "listSealedEvaluators",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### listSealedEvaluations

`GET /v1/sealed-evaluations`

List persisted sealed-evaluation precommits and results.

- **Producer operationId:** `sealedEvaluations.list`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-gym`.
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.temperaGym.listSealedEvaluations()` · Python `client.tempera_gym.list_sealed_evaluations()` · Rust `build_request("tempera_gym", "list_sealed_evaluations", params)` · Go `BuildRequest("temperaGym", "listSealedEvaluations", params)` · C `tempera_build_request(..., "temperaGym", "listSealedEvaluations", ...)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.listSealedEvaluations({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.tempera_gym.list_sealed_evaluations({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "list_sealed_evaluations", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaGym", "listSealedEvaluations", map[string]any{
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = "pageSize=<pageSize>&pageToken=<pageToken>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaGym", "listSealedEvaluations",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### precommitSealedEvaluation

`POST /v1/sealed-evaluations:precommit`

Persist an opaque sealed-suite commitment before policy freeze.

- **Producer operationId:** `sealedEvaluations.precommit`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-gym`.
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.temperaGym.precommitSealedEvaluation()` · Python `client.tempera_gym.precommit_sealed_evaluation()` · Rust `build_request("tempera_gym", "precommit_sealed_evaluation", params)` · Go `BuildRequest("temperaGym", "precommitSealedEvaluation", params)` · C `tempera_build_request(..., "temperaGym", "precommitSealedEvaluation", ...)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `evaluator` | yes | — |
| `qualificationPolicy` | yes | — |
| `suite` | yes | — |
| `trainer` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.precommitSealedEvaluation({
  evaluator: "<evaluator>",
  qualificationPolicy: "<qualificationPolicy>",
  suite: "<suite>",
  trainer: "<trainer>",
});
```

```python Python
result = client.tempera_gym.precommit_sealed_evaluation({
    "evaluator": "<evaluator>",
    "qualificationPolicy": "<qualificationPolicy>",
    "suite": "<suite>",
    "trainer": "<trainer>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "precommit_sealed_evaluation", &[
    ("evaluator", "<evaluator>".into()),
    ("qualificationPolicy", "<qualificationPolicy>".into()),
    ("suite", "<suite>".into()),
    ("trainer", "<trainer>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaGym", "precommitSealedEvaluation", map[string]any{
    "evaluator": "<evaluator>",
    "qualificationPolicy": "<qualificationPolicy>",
    "suite": "<suite>",
    "trainer": "<trainer>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = "{\"evaluator\":\"<evaluator>\",\"qualificationPolicy\":\"<qualificationPolicy>\",\"suite\":\"<suite>\",\"trainer\":\"<trainer>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaGym", "precommitSealedEvaluation",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### getSealedEvaluation

`GET /v1/sealed-evaluations/{evaluation}`

Read one verified precommit and aggregate sealed result.

- **Producer operationId:** `sealedEvaluations.get`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-gym`.
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.temperaGym.getSealedEvaluation()` · Python `client.tempera_gym.get_sealed_evaluation()` · Rust `build_request("tempera_gym", "get_sealed_evaluation", params)` · Go `BuildRequest("temperaGym", "getSealedEvaluation", params)` · C `tempera_build_request(..., "temperaGym", "getSealedEvaluation", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `evaluation` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.getSealedEvaluation({
  evaluation: "<evaluation>",
});
```

```python Python
result = client.tempera_gym.get_sealed_evaluation({
    "evaluation": "<evaluation>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "get_sealed_evaluation", &[
    ("evaluation", "<evaluation>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaGym", "getSealedEvaluation", map[string]any{
    "evaluation": "<evaluation>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"evaluation", "<evaluation>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaGym", "getSealedEvaluation",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### runSealedEvaluation

`POST /v1/sealed-evaluations/{evaluation}:run`

Evaluate one frozen policy through its exact sealed adapter.

- **Producer operationId:** `sealedEvaluations.run`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-gym`.
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.temperaGym.runSealedEvaluation()` · Python `client.tempera_gym.run_sealed_evaluation()` · Rust `build_request("tempera_gym", "run_sealed_evaluation", params)` · Go `BuildRequest("temperaGym", "runSealedEvaluation", params)` · C `tempera_build_request(..., "temperaGym", "runSealedEvaluation", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `evaluation` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `policySubmission` | yes | — |
| `trainingReport` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.runSealedEvaluation({
  evaluation: "<evaluation>",
  policySubmission: "<policySubmission>",
  trainingReport: "<trainingReport>",
});
```

```python Python
result = client.tempera_gym.run_sealed_evaluation({
    "evaluation": "<evaluation>",
    "policySubmission": "<policySubmission>",
    "trainingReport": "<trainingReport>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "run_sealed_evaluation", &[
    ("evaluation", "<evaluation>".into()),
    ("policySubmission", "<policySubmission>".into()),
    ("trainingReport", "<trainingReport>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaGym", "runSealedEvaluation", map[string]any{
    "evaluation": "<evaluation>",
    "policySubmission": "<policySubmission>",
    "trainingReport": "<trainingReport>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"evaluation", "<evaluation>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"policySubmission\":\"<policySubmission>\",\"trainingReport\":\"<trainingReport>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaGym", "runSealedEvaluation",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>
