---
title: "tempera-gym"
description: "RL environment pack service: environment catalog with implementation status, synchronous rollout execution, and persisted content-addressed trajectory-v1 runs."
---

{/* Generated by scripts/gen-sdk-docs.py from surface.json -- do not hand-edit. */}

> **Private access.** Hosted Tempera services are available through design-partner onboarding.
> Use the issuer, credentials, environment, and product URLs provisioned for your workspace;
> start in staging unless Tempera explicitly approves another target.

> **Contract inventory.** This typed reference is retained for SDK compatibility.
> It is not part of the primary public workflow or evidence of general service availability.

RL environment pack service: environment catalog with implementation status, synchronous rollout execution, and persisted content-addressed trajectory-v1 runs.

| | |
|---|---|
| Repository | [tempera-gym](https://github.com/tempera-dev/tempera-gym) |
| Base URL env var | `TEMPERA_GYM_URL` (see [Environments](/environments)) |
| Audience | `tempera-gym` |
| Auth | bearer minted for audience tempera-gym with dataset:read/eval:run, central tp_ API key, or the gym static token |
| Client | TypeScript `client.temperaGym` · Python `client.tempera_gym` · Rust `build_request("tempera_gym", ...)` |
| Typed operations | 20 |

Anything this page does not cover is reachable through the raw passthrough:
`client.temperaGym.request(path, options)` (TypeScript) /
`client.tempera_gym.request(path, ...)` (Python).

## Operations

### health

`GET /healthz`

Check tempera-gym service liveness.

- **Producer operationId:** `health.get`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.temperaGym.health()` · Python `client.tempera_gym.health()` · Rust `build_request("tempera_gym", "health", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.health();
```

```python Python
result = client.tempera_gym.health()
```

```rust Rust
let spec = client.build_request("tempera_gym", "health", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listEnvironments

`GET /v1/environments`

List the gym pack's environment catalog, including implementation status and per-environment manifests.

- **Producer operationId:** `environments.list`
- **Auth:** Product bearer — an access token minted for audience `tempera-gym`, or a central `tp_` API key.
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.temperaGym.listEnvironments()` · Python `client.tempera_gym.list_environments()` · Rust `build_request("tempera_gym", "list_environments", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.listEnvironments({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.tempera_gym.list_environments({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "list_environments", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### domainsList

`GET /v1/domains`

Domain capabilities represented in the versioned task catalog.

- **Producer operationId:** `domains.list`
- **Auth:** Product bearer — an access token minted for audience `tempera-gym`, or a central `tp_` API key.
- **Call as:** TypeScript `client.temperaGym.domainsList()` · Python `client.tempera_gym.domains_list()` · Rust `build_request("tempera_gym", "domains_list", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.domainsList({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.tempera_gym.domains_list({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "domains_list", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### tasksList

`GET /v1/tasks`

List versioned task definitions without agent inputs.

- **Producer operationId:** `tasks.list`
- **Auth:** Product bearer — an access token minted for audience `tempera-gym`, or a central `tp_` API key.
- **Call as:** TypeScript `client.temperaGym.tasksList()` · Python `client.tempera_gym.tasks_list()` · Rust `build_request("tempera_gym", "tasks_list", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `domain` | no | — |
| `family` | no | — |
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.tasksList({
  domain: "<domain>",
  family: "<family>",
});
```

```python Python
result = client.tempera_gym.tasks_list({
    "domain": "<domain>",
    "family": "<family>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "tasks_list", &[
    ("domain", "<domain>".into()),
    ("family", "<family>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### tasksGet

`GET /v1/tasks/{task}`

Get one immutable task definition including its agent-visible input.

- **Producer operationId:** `tasks.get`
- **Auth:** Product bearer — an access token minted for audience `tempera-gym`, or a central `tp_` API key.
- **Call as:** TypeScript `client.temperaGym.tasksGet()` · Python `client.tempera_gym.tasks_get()` · Rust `build_request("tempera_gym", "tasks_get", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `task` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `version` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.tasksGet({
  task: "<task>",
  version: "<version>",
});
```

```python Python
result = client.tempera_gym.tasks_get({
    "task": "<task>",
    "version": "<version>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "tasks_get", &[
    ("task", "<task>".into()),
    ("version", "<version>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### tasksEvaluate

`POST /v1/tasks/{task}:evaluate`

Deterministically verify one candidate without creating an episode.

- **Producer operationId:** `tasks.evaluate`
- **Auth:** Product bearer — an access token minted for audience `tempera-gym`, or a central `tp_` API key.
- **Call as:** TypeScript `client.temperaGym.tasksEvaluate()` · Python `client.tempera_gym.tasks_evaluate()` · Rust `build_request("tempera_gym", "tasks_evaluate", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `task` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `version` | no | — |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `candidate` | yes | — |
| `seed` | no | — |
| `receipts` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.tasksEvaluate({
  task: "<task>",
  candidate: "<candidate>",
  seed: "<seed>",
  receipts: "<receipts>",
});
```

```python Python
result = client.tempera_gym.tasks_evaluate({
    "task": "<task>",
    "candidate": "<candidate>",
    "seed": "<seed>",
    "receipts": "<receipts>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "tasks_evaluate", &[
    ("task", "<task>".into()),
    ("candidate", "<candidate>".into()),
    ("seed", "<seed>".into()),
    ("receipts", "<receipts>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### verifiersList

`GET /v1/verifiers`

List verifier identities and bound tasks without grader content.

- **Producer operationId:** `verifiers.list`
- **Auth:** Product bearer — an access token minted for audience `tempera-gym`, or a central `tp_` API key.
- **Call as:** TypeScript `client.temperaGym.verifiersList()` · Python `client.tempera_gym.verifiers_list()` · Rust `build_request("tempera_gym", "verifiers_list", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.verifiersList({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.tempera_gym.verifiers_list({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "verifiers_list", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### episodesList

`GET /v1/episodes`

List durable episode snapshots, newest first.

- **Producer operationId:** `episodes.list`
- **Auth:** Product bearer — an access token minted for audience `tempera-gym`, or a central `tp_` API key.
- **Call as:** TypeScript `client.temperaGym.episodesList()` · Python `client.tempera_gym.episodes_list()` · Rust `build_request("tempera_gym", "episodes_list", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `domain` | no | — |
| `status` | no | — |
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.episodesList({
  domain: "<domain>",
  status: "<status>",
});
```

```python Python
result = client.tempera_gym.episodes_list({
    "domain": "<domain>",
    "status": "<status>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "episodes_list", &[
    ("domain", "<domain>".into()),
    ("status", "<status>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### episodesCreate

`POST /v1/episodes`

Reset a versioned task into a durable episode.

- **Producer operationId:** `episodes.create`
- **Auth:** Product bearer — an access token minted for audience `tempera-gym`, or a central `tp_` API key.
- **Call as:** TypeScript `client.temperaGym.episodesCreate()` · Python `client.tempera_gym.episodes_create()` · Rust `build_request("tempera_gym", "episodes_create", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `task` | yes | — |
| `seed` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.episodesCreate({
  task: "<task>",
  seed: "<seed>",
});
```

```python Python
result = client.tempera_gym.episodes_create({
    "task": "<task>",
    "seed": "<seed>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "episodes_create", &[
    ("task", "<task>".into()),
    ("seed", "<seed>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### episodesGet

`GET /v1/episodes/{episode}`

Read and content-verify one durable episode snapshot.

- **Producer operationId:** `episodes.get`
- **Auth:** Product bearer — an access token minted for audience `tempera-gym`, or a central `tp_` API key.
- **Call as:** TypeScript `client.temperaGym.episodesGet()` · Python `client.tempera_gym.episodes_get()` · Rust `build_request("tempera_gym", "episodes_get", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `episode` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.episodesGet({
  episode: "<episode>",
});
```

```python Python
result = client.tempera_gym.episodes_get({
    "episode": "<episode>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "episodes_get", &[
    ("episode", "<episode>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### episodesStep

`POST /v1/episodes/{episode}:step`

Apply one schema-validated action and persist the transition.

- **Producer operationId:** `episodes.step`
- **Auth:** Product bearer — an access token minted for audience `tempera-gym`, or a central `tp_` API key.
- **Call as:** TypeScript `client.temperaGym.episodesStep()` · Python `client.tempera_gym.episodes_step()` · Rust `build_request("tempera_gym", "episodes_step", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `episode` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `action` | yes | — |
| `receipts` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.episodesStep({
  episode: "<episode>",
  action: "<action>",
  receipts: "<receipts>",
});
```

```python Python
result = client.tempera_gym.episodes_step({
    "episode": "<episode>",
    "action": "<action>",
    "receipts": "<receipts>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "episodes_step", &[
    ("episode", "<episode>".into()),
    ("action", "<action>".into()),
    ("receipts", "<receipts>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### episodesExport

`POST /v1/episodes/{episode}:export`

Retain a completed Gym episode and trajectory in Data Engine.

- **Producer operationId:** `episodes.export`
- **Auth:** Product bearer — an access token minted for audience `tempera-gym`, or a central `tp_` API key.
- **Call as:** TypeScript `client.temperaGym.episodesExport()` · Python `client.tempera_gym.episodes_export()` · Rust `build_request("tempera_gym", "episodes_export", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `episode` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `context_evidence_ref` | yes | — |
| `verifier_receipt_artifact_ref` | no | — |
| `request_id` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.episodesExport({
  episode: "<episode>",
  context_evidence_ref: "<context_evidence_ref>",
  verifier_receipt_artifact_ref: "<verifier_receipt_artifact_ref>",
  request_id: "<request_id>",
});
```

```python Python
result = client.tempera_gym.episodes_export({
    "episode": "<episode>",
    "context_evidence_ref": "<context_evidence_ref>",
    "verifier_receipt_artifact_ref": "<verifier_receipt_artifact_ref>",
    "request_id": "<request_id>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "episodes_export", &[
    ("episode", "<episode>".into()),
    ("context_evidence_ref", "<context_evidence_ref>".into()),
    ("verifier_receipt_artifact_ref", "<verifier_receipt_artifact_ref>".into()),
    ("request_id", "<request_id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listRuns

`GET /v1/runs`

List persisted rollout run index records, newest first.

- **Producer operationId:** `runs.list`
- **Auth:** Product bearer — an access token minted for audience `tempera-gym`, or a central `tp_` API key.
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.temperaGym.listRuns()` · Python `client.tempera_gym.list_runs()` · Rust `build_request("tempera_gym", "list_runs", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `environmentId` | no | — |
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.listRuns({
  environmentId: "<environmentId>",
  pageSize: "<pageSize>",
});
```

```python Python
result = client.tempera_gym.list_runs({
    "environmentId": "<environmentId>",
    "pageSize": "<pageSize>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "list_runs", &[
    ("environmentId", "<environmentId>".into()),
    ("pageSize", "<pageSize>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getRun

`GET /v1/runs/{run}`

Fetch one persisted run's index record and verified trajectory-v1 envelope by run id or trajectory content hash.

- **Producer operationId:** `runs.get`
- **Auth:** Product bearer — an access token minted for audience `tempera-gym`, or a central `tp_` API key.
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.temperaGym.getRun()` · Python `client.tempera_gym.get_run()` · Rust `build_request("tempera_gym", "get_run", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `run` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.getRun({
  run: "<run>",
});
```

```python Python
result = client.tempera_gym.get_run({
    "run": "<run>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "get_run", &[
    ("run", "<run>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createRollout

`POST /v1/rollouts`

Execute one rollout synchronously, persist the trajectory, and return the completed operation envelope.

- **Producer operationId:** `rollouts.create`
- **Auth:** Product bearer — an access token minted for audience `tempera-gym`, or a central `tp_` API key.
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.temperaGym.createRollout()` · Python `client.tempera_gym.create_rollout()` · Rust `build_request("tempera_gym", "create_rollout", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `environmentId` | yes | — |
| `policy` | no | — |
| `seed` | yes | — |
| `maxSteps` | no | — |
| `model` | no | — |
| `dataEngineProductId` | no | — |
| `dataEngineMaxRecords` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.createRollout({
  environmentId: "<environmentId>",
  policy: "<policy>",
  seed: "<seed>",
});
```

```python Python
result = client.tempera_gym.create_rollout({
    "environmentId": "<environmentId>",
    "policy": "<policy>",
    "seed": "<seed>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "create_rollout", &[
    ("environmentId", "<environmentId>".into()),
    ("policy", "<policy>".into()),
    ("seed", "<seed>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### sealedEvaluatorsList

`GET /v1/sealed-evaluators`

Discover boot-trusted exact sealed-evaluator identities.

- **Producer operationId:** `sealedEvaluators.list`
- **Auth:** Product bearer — an access token minted for audience `tempera-gym`, or a central `tp_` API key.
- **Call as:** TypeScript `client.temperaGym.sealedEvaluatorsList()` · Python `client.tempera_gym.sealed_evaluators_list()` · Rust `build_request("tempera_gym", "sealed_evaluators_list", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.sealedEvaluatorsList({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.tempera_gym.sealed_evaluators_list({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "sealed_evaluators_list", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### sealedEvaluationsList

`GET /v1/sealed-evaluations`

List persisted sealed-evaluation precommits and results.

- **Producer operationId:** `sealedEvaluations.list`
- **Auth:** Product bearer — an access token minted for audience `tempera-gym`, or a central `tp_` API key.
- **Call as:** TypeScript `client.temperaGym.sealedEvaluationsList()` · Python `client.tempera_gym.sealed_evaluations_list()` · Rust `build_request("tempera_gym", "sealed_evaluations_list", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.sealedEvaluationsList({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.tempera_gym.sealed_evaluations_list({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "sealed_evaluations_list", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### sealedEvaluationsPrecommit

`POST /v1/sealed-evaluations:precommit`

Persist an opaque sealed-suite commitment before policy freeze.

- **Producer operationId:** `sealedEvaluations.precommit`
- **Auth:** Product bearer — an access token minted for audience `tempera-gym`, or a central `tp_` API key.
- **Call as:** TypeScript `client.temperaGym.sealedEvaluationsPrecommit()` · Python `client.tempera_gym.sealed_evaluations_precommit()` · Rust `build_request("tempera_gym", "sealed_evaluations_precommit", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `evaluator` | yes | — |
| `qualification_policy` | yes | — |
| `suite` | yes | — |
| `trainer` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.sealedEvaluationsPrecommit({
  evaluator: "<evaluator>",
  qualification_policy: "<qualification_policy>",
  suite: "<suite>",
});
```

```python Python
result = client.tempera_gym.sealed_evaluations_precommit({
    "evaluator": "<evaluator>",
    "qualification_policy": "<qualification_policy>",
    "suite": "<suite>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "sealed_evaluations_precommit", &[
    ("evaluator", "<evaluator>".into()),
    ("qualification_policy", "<qualification_policy>".into()),
    ("suite", "<suite>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### sealedEvaluationsGet

`GET /v1/sealed-evaluations/{evaluation}`

Read one verified precommit and aggregate sealed result.

- **Producer operationId:** `sealedEvaluations.get`
- **Auth:** Product bearer — an access token minted for audience `tempera-gym`, or a central `tp_` API key.
- **Call as:** TypeScript `client.temperaGym.sealedEvaluationsGet()` · Python `client.tempera_gym.sealed_evaluations_get()` · Rust `build_request("tempera_gym", "sealed_evaluations_get", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `evaluation` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.sealedEvaluationsGet({
  evaluation: "<evaluation>",
});
```

```python Python
result = client.tempera_gym.sealed_evaluations_get({
    "evaluation": "<evaluation>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "sealed_evaluations_get", &[
    ("evaluation", "<evaluation>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### sealedEvaluationsRun

`POST /v1/sealed-evaluations/{evaluation}:run`

Evaluate one frozen policy through its exact sealed adapter.

- **Producer operationId:** `sealedEvaluations.run`
- **Auth:** Product bearer — an access token minted for audience `tempera-gym`, or a central `tp_` API key.
- **Call as:** TypeScript `client.temperaGym.sealedEvaluationsRun()` · Python `client.tempera_gym.sealed_evaluations_run()` · Rust `build_request("tempera_gym", "sealed_evaluations_run", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `evaluation` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `policy_submission` | yes | — |
| `training_report` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaGym.sealedEvaluationsRun({
  evaluation: "<evaluation>",
  policy_submission: "<policy_submission>",
  training_report: "<training_report>",
});
```

```python Python
result = client.tempera_gym.sealed_evaluations_run({
    "evaluation": "<evaluation>",
    "policy_submission": "<policy_submission>",
    "training_report": "<training_report>",
})
```

```rust Rust
let spec = client.build_request("tempera_gym", "sealed_evaluations_run", &[
    ("evaluation", "<evaluation>".into()),
    ("policy_submission", "<policy_submission>".into()),
    ("training_report", "<training_report>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>
