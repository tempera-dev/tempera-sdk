---
title: "tempera-risk"
description: "Governed, non-decisional cross-domain investigation service. The trusted SDK surface manages cases, grants, research jobs, sources, and reviewed dossiers; the smaller MCP surface is separately curated."
---

{/* Generated by scripts/gen-sdk-docs.py from surface.json -- do not hand-edit. */}

> **Private access.** Hosted Tempera services are available through design-partner onboarding.
> Use the issuer, credentials, environment, and product URLs provisioned for your workspace;
> start in staging unless Tempera explicitly approves another target.

> **Contract inventory.** This typed reference is retained for SDK compatibility.
> It is not part of the primary public workflow or evidence of general service availability.

Governed, non-decisional cross-domain investigation service. The trusted SDK surface manages cases, grants, research jobs, sources, and reviewed dossiers; the smaller MCP surface is separately curated.

| | |
|---|---|
| Repository | [tempera-risk](https://github.com/tempera-dev/tempera-risk) |
| Base URL env var | `TEMPERA_RISK_URL` (see [Environments](/environments)) |
| Audience | `tempera-risk` |
| Auth | bearer minted for audience tempera-risk; Risk additionally enforces tenant, project, actor, case, access-grant, permissible-purpose, source-rights, and human-review boundaries. |
| Client | TypeScript `client.temperaRisk` · Python `client.tempera_risk` · Rust `build_request("tempera_risk", ...)` |
| Typed operations | 44 |

Anything this page does not cover is reachable through the raw passthrough:
`client.temperaRisk.request(path, options)` (TypeScript) /
`client.tempera_risk.request(path, ...)` (Python).

## Operations

### health

`GET /healthz`

Call GET /healthz.

- **Producer operationId:** `health`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.temperaRisk.health()` · Python `client.tempera_risk.health()` · Rust `build_request("tempera_risk", "health", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.health();
```

```python Python
result = client.tempera_risk.health()
```

```rust Rust
let spec = client.build_request("tempera_risk", "health", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### executeGraphRead

`POST /v1/projects/{project}/graphReads/execute`

Execute one principal-bound, signed, bounded graph read.

- **Producer operationId:** `executeGraphRead`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:write`
- **Call as:** TypeScript `client.temperaRisk.executeGraphRead()` · Python `client.tempera_risk.execute_graph_read()` · Rust `build_request("tempera_risk", "execute_graph_read", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `request` | yes | — |
| `peopleBinding` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.executeGraphRead({
  project: "<project>",
  request: "<request>",
  peopleBinding: "<peopleBinding>",
});
```

```python Python
result = client.tempera_risk.execute_graph_read({
    "project": "<project>",
    "request": "<request>",
    "peopleBinding": "<peopleBinding>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "execute_graph_read", &[
    ("project", "<project>".into()),
    ("request", "<request>".into()),
    ("peopleBinding", "<peopleBinding>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createSubject

`POST /v1/projects/{project}/subjects`

Call POST /v1/projects/\{project\}/subjects.

- **Producer operationId:** `createSubject`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:write`
- **Call as:** TypeScript `client.temperaRisk.createSubject()` · Python `client.tempera_risk.create_subject()` · Rust `build_request("tempera_risk", "create_subject", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `schema` | yes | — |
| `subjectId` | yes | — |
| `subjectType` | yes | — |
| `state` | no | — |
| `attributes` | no | — |
| `externalRefs` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.createSubject({
  project: "<project>",
  schema: "<schema>",
  subjectId: "<subjectId>",
  subjectType: "<subjectType>",
});
```

```python Python
result = client.tempera_risk.create_subject({
    "project": "<project>",
    "schema": "<schema>",
    "subjectId": "<subjectId>",
    "subjectType": "<subjectType>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "create_subject", &[
    ("project", "<project>".into()),
    ("schema", "<schema>".into()),
    ("subjectId", "<subjectId>".into()),
    ("subjectType", "<subjectType>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listSubjects

`GET /v1/projects/{project}/subjects`

Call GET /v1/projects/\{project\}/subjects.

- **Producer operationId:** `listSubjects`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:read`
- **Call as:** TypeScript `client.temperaRisk.listSubjects()` · Python `client.tempera_risk.list_subjects()` · Rust `build_request("tempera_risk", "list_subjects", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.listSubjects({
  project: "<project>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.tempera_risk.list_subjects({
    "project": "<project>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "list_subjects", &[
    ("project", "<project>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getSubject

`GET /v1/projects/{project}/subjects/{subjectId}`

Call GET /v1/projects/\{project\}/subjects/\{subjectId\}.

- **Producer operationId:** `getSubject`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:read`
- **Call as:** TypeScript `client.temperaRisk.getSubject()` · Python `client.tempera_risk.get_subject()` · Rust `build_request("tempera_risk", "get_subject", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `subjectId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.getSubject({
  project: "<project>",
  subjectId: "<subjectId>",
});
```

```python Python
result = client.tempera_risk.get_subject({
    "project": "<project>",
    "subjectId": "<subjectId>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "get_subject", &[
    ("project", "<project>".into()),
    ("subjectId", "<subjectId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### ingestEvent

`POST /v1/projects/{project}/events:ingest`

Call POST /v1/projects/\{project\}/events:ingest.

- **Producer operationId:** `ingestEvent`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:write`
- **Call as:** TypeScript `client.temperaRisk.ingestEvent()` · Python `client.tempera_risk.ingest_event()` · Rust `build_request("tempera_risk", "ingest_event", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `schema` | yes | — |
| `eventId` | yes | — |
| `eventType` | yes | — |
| `subjectId` | yes | — |
| `counterpartyId` | no | — |
| `deviceId` | no | — |
| `amount` | no | — |
| `eventTime` | yes | — |
| `receivedAt` | yes | — |
| `attributes` | no | — |
| `evidenceRefs` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.ingestEvent({
  project: "<project>",
  schema: "<schema>",
  eventId: "<eventId>",
  eventType: "<eventType>",
  subjectId: "<subjectId>",
  eventTime: "<eventTime>",
  receivedAt: "<receivedAt>",
});
```

```python Python
result = client.tempera_risk.ingest_event({
    "project": "<project>",
    "schema": "<schema>",
    "eventId": "<eventId>",
    "eventType": "<eventType>",
    "subjectId": "<subjectId>",
    "eventTime": "<eventTime>",
    "receivedAt": "<receivedAt>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "ingest_event", &[
    ("project", "<project>".into()),
    ("schema", "<schema>".into()),
    ("eventId", "<eventId>".into()),
    ("eventType", "<eventType>".into()),
    ("subjectId", "<subjectId>".into()),
    ("eventTime", "<eventTime>".into()),
    ("receivedAt", "<receivedAt>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### registerPolicy

`POST /v1/projects/{project}/policies`

Call POST /v1/projects/\{project\}/policies.

- **Producer operationId:** `registerPolicy`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:write`
- **Call as:** TypeScript `client.temperaRisk.registerPolicy()` · Python `client.tempera_risk.register_policy()` · Rust `build_request("tempera_risk", "register_policy", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `schema` | yes | — |
| `policyId` | yes | — |
| `version` | yes | — |
| `useCase` | yes | — |
| `segment` | no | — |
| `description` | no | — |
| `defaultAction` | yes | — |
| `defaultReasonCode` | yes | — |
| `missingDataAction` | yes | — |
| `missingDataReasonCode` | yes | — |
| `conflictStrategy` | yes | — |
| `rules` | yes | — |
| `modelDependencies` | no | — |
| `changeTicket` | yes | — |
| `createdBy` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.registerPolicy({
  project: "<project>",
  schema: "<schema>",
  policyId: "<policyId>",
  version: "<version>",
  useCase: "<useCase>",
  defaultAction: "<defaultAction>",
  defaultReasonCode: "<defaultReasonCode>",
  missingDataAction: "<missingDataAction>",
  missingDataReasonCode: "<missingDataReasonCode>",
  conflictStrategy: "<conflictStrategy>",
  rules: "<rules>",
  changeTicket: "<changeTicket>",
  createdBy: "<createdBy>",
});
```

```python Python
result = client.tempera_risk.register_policy({
    "project": "<project>",
    "schema": "<schema>",
    "policyId": "<policyId>",
    "version": "<version>",
    "useCase": "<useCase>",
    "defaultAction": "<defaultAction>",
    "defaultReasonCode": "<defaultReasonCode>",
    "missingDataAction": "<missingDataAction>",
    "missingDataReasonCode": "<missingDataReasonCode>",
    "conflictStrategy": "<conflictStrategy>",
    "rules": "<rules>",
    "changeTicket": "<changeTicket>",
    "createdBy": "<createdBy>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "register_policy", &[
    ("project", "<project>".into()),
    ("schema", "<schema>".into()),
    ("policyId", "<policyId>".into()),
    ("version", "<version>".into()),
    ("useCase", "<useCase>".into()),
    ("defaultAction", "<defaultAction>".into()),
    ("defaultReasonCode", "<defaultReasonCode>".into()),
    ("missingDataAction", "<missingDataAction>".into()),
    ("missingDataReasonCode", "<missingDataReasonCode>".into()),
    ("conflictStrategy", "<conflictStrategy>".into()),
    ("rules", "<rules>".into()),
    ("changeTicket", "<changeTicket>".into()),
    ("createdBy", "<createdBy>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### registerModel

`POST /v1/projects/{project}/models`

Register an immutable model artifact after dual human approval.

- **Producer operationId:** `registerModel`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:write`
- **Call as:** TypeScript `client.temperaRisk.registerModel()` · Python `client.tempera_risk.register_model()` · Rust `build_request("tempera_risk", "register_model", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `schema` | yes | — |
| `modelId` | yes | — |
| `version` | yes | — |
| `state` | yes | — |
| `artifactSha256` | yes | — |
| `runtime` | yes | — |
| `inputFeatures` | yes | — |
| `outputFeature` | yes | — |
| `trainingDataRefs` | yes | — |
| `validationReportRef` | yes | — |
| `calibration` | yes | — |
| `limitations` | yes | — |
| `createdAt` | yes | — |
| `approvalIds` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.registerModel({
  project: "<project>",
  schema: "<schema>",
  modelId: "<modelId>",
  version: "<version>",
  state: "<state>",
  artifactSha256: "<artifactSha256>",
  runtime: "<runtime>",
  inputFeatures: "<inputFeatures>",
  outputFeature: "<outputFeature>",
  trainingDataRefs: "<trainingDataRefs>",
  validationReportRef: "<validationReportRef>",
  calibration: "<calibration>",
  limitations: "<limitations>",
  createdAt: "<createdAt>",
  approvalIds: "<approvalIds>",
});
```

```python Python
result = client.tempera_risk.register_model({
    "project": "<project>",
    "schema": "<schema>",
    "modelId": "<modelId>",
    "version": "<version>",
    "state": "<state>",
    "artifactSha256": "<artifactSha256>",
    "runtime": "<runtime>",
    "inputFeatures": "<inputFeatures>",
    "outputFeature": "<outputFeature>",
    "trainingDataRefs": "<trainingDataRefs>",
    "validationReportRef": "<validationReportRef>",
    "calibration": "<calibration>",
    "limitations": "<limitations>",
    "createdAt": "<createdAt>",
    "approvalIds": "<approvalIds>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "register_model", &[
    ("project", "<project>".into()),
    ("schema", "<schema>".into()),
    ("modelId", "<modelId>".into()),
    ("version", "<version>".into()),
    ("state", "<state>".into()),
    ("artifactSha256", "<artifactSha256>".into()),
    ("runtime", "<runtime>".into()),
    ("inputFeatures", "<inputFeatures>".into()),
    ("outputFeature", "<outputFeature>".into()),
    ("trainingDataRefs", "<trainingDataRefs>".into()),
    ("validationReportRef", "<validationReportRef>".into()),
    ("calibration", "<calibration>".into()),
    ("limitations", "<limitations>".into()),
    ("createdAt", "<createdAt>".into()),
    ("approvalIds", "<approvalIds>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### ingestModelScore

`POST /v1/projects/{project}/modelScores:ingest`

Ingest a signed non-decisional score from an authenticated scorer workload.

- **Producer operationId:** `ingestModelScore`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:write`
- **Call as:** TypeScript `client.temperaRisk.ingestModelScore()` · Python `client.tempera_risk.ingest_model_score()` · Rust `build_request("tempera_risk", "ingest_model_score", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `schema` | yes | — |
| `algorithm` | yes | — |
| `keyId` | yes | — |
| `score` | yes | — |
| `signature` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.ingestModelScore({
  project: "<project>",
  schema: "<schema>",
  algorithm: "<algorithm>",
  keyId: "<keyId>",
  score: "<score>",
  signature: "<signature>",
});
```

```python Python
result = client.tempera_risk.ingest_model_score({
    "project": "<project>",
    "schema": "<schema>",
    "algorithm": "<algorithm>",
    "keyId": "<keyId>",
    "score": "<score>",
    "signature": "<signature>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "ingest_model_score", &[
    ("project", "<project>".into()),
    ("schema", "<schema>".into()),
    ("algorithm", "<algorithm>".into()),
    ("keyId", "<keyId>".into()),
    ("score", "<score>".into()),
    ("signature", "<signature>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createApproval

`POST /v1/projects/{project}/approvals`

Call POST /v1/projects/\{project\}/approvals.

- **Producer operationId:** `createApproval`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:review`
- **Call as:** TypeScript `client.temperaRisk.createApproval()` · Python `client.tempera_risk.create_approval()` · Rust `build_request("tempera_risk", "create_approval", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `action` | yes | — |
| `target` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.createApproval({
  project: "<project>",
  action: "<action>",
  target: "<target>",
});
```

```python Python
result = client.tempera_risk.create_approval({
    "project": "<project>",
    "action": "<action>",
    "target": "<target>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "create_approval", &[
    ("project", "<project>".into()),
    ("action", "<action>".into()),
    ("target", "<target>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### deployPolicy

`POST /v1/projects/{project}/policyDeployments`

Call POST /v1/projects/\{project\}/policyDeployments.

- **Producer operationId:** `deployPolicy`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:write`
- **Call as:** TypeScript `client.temperaRisk.deployPolicy()` · Python `client.tempera_risk.deploy_policy()` · Rust `build_request("tempera_risk", "deploy_policy", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `deploymentId` | yes | — |
| `policyId` | yes | — |
| `policyVersion` | yes | — |
| `mode` | yes | — |
| `trafficPercent` | yes | — |
| `effectiveAt` | yes | — |
| `approvalIds` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.deployPolicy({
  project: "<project>",
  deploymentId: "<deploymentId>",
  policyId: "<policyId>",
  policyVersion: "<policyVersion>",
  mode: "<mode>",
  trafficPercent: "<trafficPercent>",
  effectiveAt: "<effectiveAt>",
  approvalIds: "<approvalIds>",
});
```

```python Python
result = client.tempera_risk.deploy_policy({
    "project": "<project>",
    "deploymentId": "<deploymentId>",
    "policyId": "<policyId>",
    "policyVersion": "<policyVersion>",
    "mode": "<mode>",
    "trafficPercent": "<trafficPercent>",
    "effectiveAt": "<effectiveAt>",
    "approvalIds": "<approvalIds>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "deploy_policy", &[
    ("project", "<project>".into()),
    ("deploymentId", "<deploymentId>".into()),
    ("policyId", "<policyId>".into()),
    ("policyVersion", "<policyVersion>".into()),
    ("mode", "<mode>".into()),
    ("trafficPercent", "<trafficPercent>".into()),
    ("effectiveAt", "<effectiveAt>".into()),
    ("approvalIds", "<approvalIds>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createDecision

`POST /v1/projects/{project}/decisions`

Call POST /v1/projects/\{project\}/decisions.

- **Producer operationId:** `createDecision`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:write`
- **Call as:** TypeScript `client.temperaRisk.createDecision()` · Python `client.tempera_risk.create_decision()` · Rust `build_request("tempera_risk", "create_decision", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `schema` | yes | — |
| `requestId` | yes | — |
| `subjectId` | yes | — |
| `eventId` | no | — |
| `useCase` | yes | — |
| `segment` | no | — |
| `features` | no | — |
| `modelVersions` | no | — |
| `evidenceRefs` | no | — |
| `asOf` | yes | — |
| `availableAsOf` | no | — |
| `executionMode` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.createDecision({
  project: "<project>",
  schema: "<schema>",
  requestId: "<requestId>",
  subjectId: "<subjectId>",
  useCase: "<useCase>",
  asOf: "<asOf>",
});
```

```python Python
result = client.tempera_risk.create_decision({
    "project": "<project>",
    "schema": "<schema>",
    "requestId": "<requestId>",
    "subjectId": "<subjectId>",
    "useCase": "<useCase>",
    "asOf": "<asOf>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "create_decision", &[
    ("project", "<project>".into()),
    ("schema", "<schema>".into()),
    ("requestId", "<requestId>".into()),
    ("subjectId", "<subjectId>".into()),
    ("useCase", "<useCase>".into()),
    ("asOf", "<asOf>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getDecision

`GET /v1/projects/{project}/decisions/{decisionId}`

Call GET /v1/projects/\{project\}/decisions/\{decisionId\}.

- **Producer operationId:** `getDecision`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:read`
- **Call as:** TypeScript `client.temperaRisk.getDecision()` · Python `client.tempera_risk.get_decision()` · Rust `build_request("tempera_risk", "get_decision", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `decisionId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.getDecision({
  project: "<project>",
  decisionId: "<decisionId>",
});
```

```python Python
result = client.tempera_risk.get_decision({
    "project": "<project>",
    "decisionId": "<decisionId>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "get_decision", &[
    ("project", "<project>".into()),
    ("decisionId", "<decisionId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### recordOutcome

`POST /v1/projects/{project}/outcomes`

Call POST /v1/projects/\{project\}/outcomes.

- **Producer operationId:** `recordOutcome`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:write`
- **Call as:** TypeScript `client.temperaRisk.recordOutcome()` · Python `client.tempera_risk.record_outcome()` · Rust `build_request("tempera_risk", "record_outcome", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.recordOutcome({
  project: "<project>",
});
```

```python Python
result = client.tempera_risk.record_outcome({
    "project": "<project>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "record_outcome", &[
    ("project", "<project>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createScreening

`POST /v1/projects/{project}/screenings`

Call POST /v1/projects/\{project\}/screenings.

- **Producer operationId:** `createScreening`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:write`
- **Call as:** TypeScript `client.temperaRisk.createScreening()` · Python `client.tempera_risk.create_screening()` · Rust `build_request("tempera_risk", "create_screening", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `indexName` | yes | — |
| `subject` | yes | — |
| `permissiblePurpose` | yes | — |
| `caseId` | yes | — |
| `threshold` | no | — |
| `maxCandidates` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.createScreening({
  project: "<project>",
  indexName: "<indexName>",
  subject: "<subject>",
  permissiblePurpose: "<permissiblePurpose>",
  caseId: "<caseId>",
});
```

```python Python
result = client.tempera_risk.create_screening({
    "project": "<project>",
    "indexName": "<indexName>",
    "subject": "<subject>",
    "permissiblePurpose": "<permissiblePurpose>",
    "caseId": "<caseId>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "create_screening", &[
    ("project", "<project>".into()),
    ("indexName", "<indexName>".into()),
    ("subject", "<subject>".into()),
    ("permissiblePurpose", "<permissiblePurpose>".into()),
    ("caseId", "<caseId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listCases

`GET /v1/projects/{project}/cases`

Call GET /v1/projects/\{project\}/cases.

- **Producer operationId:** `listCases`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:read`
- **Call as:** TypeScript `client.temperaRisk.listCases()` · Python `client.tempera_risk.list_cases()` · Rust `build_request("tempera_risk", "list_cases", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.listCases({
  project: "<project>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.tempera_risk.list_cases({
    "project": "<project>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "list_cases", &[
    ("project", "<project>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createCase

`POST /v1/projects/{project}/cases`

Open a governed multi-domain investigation case.

- **Producer operationId:** `createCase`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:write`
- **Call as:** TypeScript `client.temperaRisk.createCase()` · Python `client.tempera_risk.create_case()` · Rust `build_request("tempera_risk", "create_case", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `schema` | yes | — |
| `caseId` | yes | — |
| `caseType` | yes | — |
| `state` | yes | — |
| `subjectIds` | yes | — |
| `alertIds` | yes | — |
| `assignedTo` | yes | — |
| `disposition` | yes | — |
| `evidenceRefs` | yes | — |
| `attributes` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.createCase({
  project: "<project>",
  schema: "<schema>",
  caseId: "<caseId>",
  caseType: "<caseType>",
  state: "<state>",
  subjectIds: "<subjectIds>",
  alertIds: "<alertIds>",
  assignedTo: "<assignedTo>",
  disposition: "<disposition>",
  evidenceRefs: "<evidenceRefs>",
  attributes: "<attributes>",
});
```

```python Python
result = client.tempera_risk.create_case({
    "project": "<project>",
    "schema": "<schema>",
    "caseId": "<caseId>",
    "caseType": "<caseType>",
    "state": "<state>",
    "subjectIds": "<subjectIds>",
    "alertIds": "<alertIds>",
    "assignedTo": "<assignedTo>",
    "disposition": "<disposition>",
    "evidenceRefs": "<evidenceRefs>",
    "attributes": "<attributes>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "create_case", &[
    ("project", "<project>".into()),
    ("schema", "<schema>".into()),
    ("caseId", "<caseId>".into()),
    ("caseType", "<caseType>".into()),
    ("state", "<state>".into()),
    ("subjectIds", "<subjectIds>".into()),
    ("alertIds", "<alertIds>".into()),
    ("assignedTo", "<assignedTo>".into()),
    ("disposition", "<disposition>".into()),
    ("evidenceRefs", "<evidenceRefs>".into()),
    ("attributes", "<attributes>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getCase

`GET /v1/projects/{project}/cases/{caseId}`

Call GET /v1/projects/\{project\}/cases/\{caseId\}.

- **Producer operationId:** `getCase`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:read`
- **Call as:** TypeScript `client.temperaRisk.getCase()` · Python `client.tempera_risk.get_case()` · Rust `build_request("tempera_risk", "get_case", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `caseId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.getCase({
  project: "<project>",
  caseId: "<caseId>",
});
```

```python Python
result = client.tempera_risk.get_case({
    "project": "<project>",
    "caseId": "<caseId>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "get_case", &[
    ("project", "<project>".into()),
    ("caseId", "<caseId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getCaseDossier

`GET /v1/projects/{project}/cases/{caseId}/dossier`

Read all completed non-decisional investigation dossiers for a case.

- **Producer operationId:** `getCaseDossier`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:read`
- **Call as:** TypeScript `client.temperaRisk.getCaseDossier()` · Python `client.tempera_risk.get_case_dossier()` · Rust `build_request("tempera_risk", "get_case_dossier", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `caseId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.getCaseDossier({
  project: "<project>",
  caseId: "<caseId>",
});
```

```python Python
result = client.tempera_risk.get_case_dossier({
    "project": "<project>",
    "caseId": "<caseId>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "get_case_dossier", &[
    ("project", "<project>".into()),
    ("caseId", "<caseId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createAccessGrant

`POST /v1/projects/{project}/accessGrants`

Grant purpose-, case-, subject-, actor-, field-, provider-, and time-bounded access.

- **Producer operationId:** `createAccessGrant`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:write`
- **Call as:** TypeScript `client.temperaRisk.createAccessGrant()` · Python `client.tempera_risk.create_access_grant()` · Rust `build_request("tempera_risk", "create_access_grant", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `schema` | yes | — |
| `grantId` | yes | — |
| `caseId` | yes | — |
| `subjectIds` | yes | — |
| `actorIds` | yes | — |
| `permissiblePurpose` | yes | — |
| `allowedFields` | yes | — |
| `allowedProviders` | yes | — |
| `thirdPartyDisclosureAllowed` | yes | — |
| `validFrom` | yes | — |
| `expiresAt` | yes | — |
| `createdBy` | yes | — |
| `approvalIds` | yes | — |
| `changeTicket` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.createAccessGrant({
  project: "<project>",
  schema: "<schema>",
  grantId: "<grantId>",
  caseId: "<caseId>",
  subjectIds: "<subjectIds>",
  actorIds: "<actorIds>",
  permissiblePurpose: "<permissiblePurpose>",
  allowedFields: "<allowedFields>",
  allowedProviders: "<allowedProviders>",
  thirdPartyDisclosureAllowed: "<thirdPartyDisclosureAllowed>",
  validFrom: "<validFrom>",
  expiresAt: "<expiresAt>",
  createdBy: "<createdBy>",
  approvalIds: "<approvalIds>",
  changeTicket: "<changeTicket>",
});
```

```python Python
result = client.tempera_risk.create_access_grant({
    "project": "<project>",
    "schema": "<schema>",
    "grantId": "<grantId>",
    "caseId": "<caseId>",
    "subjectIds": "<subjectIds>",
    "actorIds": "<actorIds>",
    "permissiblePurpose": "<permissiblePurpose>",
    "allowedFields": "<allowedFields>",
    "allowedProviders": "<allowedProviders>",
    "thirdPartyDisclosureAllowed": "<thirdPartyDisclosureAllowed>",
    "validFrom": "<validFrom>",
    "expiresAt": "<expiresAt>",
    "createdBy": "<createdBy>",
    "approvalIds": "<approvalIds>",
    "changeTicket": "<changeTicket>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "create_access_grant", &[
    ("project", "<project>".into()),
    ("schema", "<schema>".into()),
    ("grantId", "<grantId>".into()),
    ("caseId", "<caseId>".into()),
    ("subjectIds", "<subjectIds>".into()),
    ("actorIds", "<actorIds>".into()),
    ("permissiblePurpose", "<permissiblePurpose>".into()),
    ("allowedFields", "<allowedFields>".into()),
    ("allowedProviders", "<allowedProviders>".into()),
    ("thirdPartyDisclosureAllowed", "<thirdPartyDisclosureAllowed>".into()),
    ("validFrom", "<validFrom>".into()),
    ("expiresAt", "<expiresAt>".into()),
    ("createdBy", "<createdBy>".into()),
    ("approvalIds", "<approvalIds>".into()),
    ("changeTicket", "<changeTicket>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### revokeAccessGrant

`POST /v1/projects/{project}/accessGrants/{grantId}/revoke`

Revoke a sensitive investigation-data access grant.

- **Producer operationId:** `revokeAccessGrant`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:write`
- **Call as:** TypeScript `client.temperaRisk.revokeAccessGrant()` · Python `client.tempera_risk.revoke_access_grant()` · Rust `build_request("tempera_risk", "revoke_access_grant", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `grantId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `reason` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.revokeAccessGrant({
  project: "<project>",
  grantId: "<grantId>",
  reason: "<reason>",
});
```

```python Python
result = client.tempera_risk.revoke_access_grant({
    "project": "<project>",
    "grantId": "<grantId>",
    "reason": "<reason>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "revoke_access_grant", &[
    ("project", "<project>".into()),
    ("grantId", "<grantId>".into()),
    ("reason", "<reason>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createPeopleDirectoryProfile

`POST /v1/projects/{project}/peopleDirectoryProfiles`

Ingest an evidence-backed bitemporal person or company profile.

- **Producer operationId:** `createPeopleDirectoryProfile`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:write`
- **Call as:** TypeScript `client.temperaRisk.createPeopleDirectoryProfile()` · Python `client.tempera_risk.create_people_directory_profile()` · Rust `build_request("tempera_risk", "create_people_directory_profile", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `schema` | yes | — |
| `profileId` | yes | — |
| `subjectId` | yes | — |
| `entityKind` | yes | — |
| `anchors` | yes | — |
| `attributes` | yes | — |
| `sourceIds` | yes | — |
| `evidenceRefs` | yes | — |
| `validFrom` | yes | — |
| `validTo` | yes | — |
| `recordedAt` | yes | — |
| `retentionExpiresAt` | yes | — |
| `nonDecisional` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.createPeopleDirectoryProfile({
  project: "<project>",
  schema: "<schema>",
  profileId: "<profileId>",
  subjectId: "<subjectId>",
  entityKind: "<entityKind>",
  anchors: "<anchors>",
  attributes: "<attributes>",
  sourceIds: "<sourceIds>",
  evidenceRefs: "<evidenceRefs>",
  validFrom: "<validFrom>",
  validTo: "<validTo>",
  recordedAt: "<recordedAt>",
  retentionExpiresAt: "<retentionExpiresAt>",
});
```

```python Python
result = client.tempera_risk.create_people_directory_profile({
    "project": "<project>",
    "schema": "<schema>",
    "profileId": "<profileId>",
    "subjectId": "<subjectId>",
    "entityKind": "<entityKind>",
    "anchors": "<anchors>",
    "attributes": "<attributes>",
    "sourceIds": "<sourceIds>",
    "evidenceRefs": "<evidenceRefs>",
    "validFrom": "<validFrom>",
    "validTo": "<validTo>",
    "recordedAt": "<recordedAt>",
    "retentionExpiresAt": "<retentionExpiresAt>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "create_people_directory_profile", &[
    ("project", "<project>".into()),
    ("schema", "<schema>".into()),
    ("profileId", "<profileId>".into()),
    ("subjectId", "<subjectId>".into()),
    ("entityKind", "<entityKind>".into()),
    ("anchors", "<anchors>".into()),
    ("attributes", "<attributes>".into()),
    ("sourceIds", "<sourceIds>".into()),
    ("evidenceRefs", "<evidenceRefs>".into()),
    ("validFrom", "<validFrom>".into()),
    ("validTo", "<validTo>".into()),
    ("recordedAt", "<recordedAt>".into()),
    ("retentionExpiresAt", "<retentionExpiresAt>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### searchPeople

`POST /v1/projects/{project}/people/search`

Find non-decisional identity-resolution candidates under an active case grant.

- **Producer operationId:** `searchPeople`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:write`
- **Call as:** TypeScript `client.temperaRisk.searchPeople()` · Python `client.tempera_risk.search_people()` · Rust `build_request("tempera_risk", "search_people", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `schema` | yes | — |
| `queryId` | yes | — |
| `subjectId` | yes | — |
| `accessGrantId` | yes | — |
| `caseId` | yes | — |
| `permissiblePurpose` | yes | — |
| `entityKind` | yes | — |
| `anchors` | yes | — |
| `attributes` | yes | — |
| `maxCandidates` | yes | — |
| `asOf` | yes | — |
| `knownAt` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.searchPeople({
  project: "<project>",
  schema: "<schema>",
  queryId: "<queryId>",
  subjectId: "<subjectId>",
  accessGrantId: "<accessGrantId>",
  caseId: "<caseId>",
  permissiblePurpose: "<permissiblePurpose>",
  entityKind: "<entityKind>",
  anchors: "<anchors>",
  attributes: "<attributes>",
  maxCandidates: "<maxCandidates>",
  asOf: "<asOf>",
  knownAt: "<knownAt>",
});
```

```python Python
result = client.tempera_risk.search_people({
    "project": "<project>",
    "schema": "<schema>",
    "queryId": "<queryId>",
    "subjectId": "<subjectId>",
    "accessGrantId": "<accessGrantId>",
    "caseId": "<caseId>",
    "permissiblePurpose": "<permissiblePurpose>",
    "entityKind": "<entityKind>",
    "anchors": "<anchors>",
    "attributes": "<attributes>",
    "maxCandidates": "<maxCandidates>",
    "asOf": "<asOf>",
    "knownAt": "<knownAt>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "search_people", &[
    ("project", "<project>".into()),
    ("schema", "<schema>".into()),
    ("queryId", "<queryId>".into()),
    ("subjectId", "<subjectId>".into()),
    ("accessGrantId", "<accessGrantId>".into()),
    ("caseId", "<caseId>".into()),
    ("permissiblePurpose", "<permissiblePurpose>".into()),
    ("entityKind", "<entityKind>".into()),
    ("anchors", "<anchors>".into()),
    ("attributes", "<attributes>".into()),
    ("maxCandidates", "<maxCandidates>".into()),
    ("asOf", "<asOf>".into()),
    ("knownAt", "<knownAt>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listSourcePacks

`GET /v1/projects/{project}/sourcePacks`

List declared non-runnable investigation source packs.

- **Producer operationId:** `listSourcePacks`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:read`
- **Call as:** TypeScript `client.temperaRisk.listSourcePacks()` · Python `client.tempera_risk.list_source_packs()` · Rust `build_request("tempera_risk", "list_source_packs", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.listSourcePacks({
  project: "<project>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.tempera_risk.list_source_packs({
    "project": "<project>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "list_source_packs", &[
    ("project", "<project>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getSourcePack

`GET /v1/projects/{project}/sourcePacks/{sourcePackId}`

Read one declared non-runnable investigation source pack.

- **Producer operationId:** `getSourcePack`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:read`
- **Call as:** TypeScript `client.temperaRisk.getSourcePack()` · Python `client.tempera_risk.get_source_pack()` · Rust `build_request("tempera_risk", "get_source_pack", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `sourcePackId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.getSourcePack({
  project: "<project>",
  sourcePackId: "<sourcePackId>",
});
```

```python Python
result = client.tempera_risk.get_source_pack({
    "project": "<project>",
    "sourcePackId": "<sourcePackId>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "get_source_pack", &[
    ("project", "<project>".into()),
    ("sourcePackId", "<sourcePackId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getSourceCoverage

`GET /v1/projects/{project}/sourceCoverage/{sourcePackId}`

Read truthful admitted coverage for one source pack.

- **Producer operationId:** `getSourceCoverage`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:read`
- **Call as:** TypeScript `client.temperaRisk.getSourceCoverage()` · Python `client.tempera_risk.get_source_coverage()` · Rust `build_request("tempera_risk", "get_source_coverage", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `sourcePackId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.getSourceCoverage({
  project: "<project>",
  sourcePackId: "<sourcePackId>",
});
```

```python Python
result = client.tempera_risk.get_source_coverage({
    "project": "<project>",
    "sourcePackId": "<sourcePackId>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "get_source_coverage", &[
    ("project", "<project>".into()),
    ("sourcePackId", "<sourcePackId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createResearchJob

`POST /v1/projects/{project}/researchJobs`

Call POST /v1/projects/\{project\}/researchJobs.

- **Producer operationId:** `createResearchJob`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:write`
- **Call as:** TypeScript `client.temperaRisk.createResearchJob()` · Python `client.tempera_risk.create_research_job()` · Rust `build_request("tempera_risk", "create_research_job", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `subject` | yes | — |
| `question` | yes | — |
| `purpose` | yes | — |
| `permissiblePurpose` | yes | — |
| `outputSchema` | yes | — |
| `budgets` | yes | — |
| `sourcePolicy` | yes | — |
| `caseId` | yes | — |
| `accessGrantId` | no | — |
| `actorId` | yes | — |
| `retentionPolicyId` | yes | — |
| `humanReviewRequired` | yes | — |
| `candidateBinding` | no | — |
| `providerPlan` | no | — |
| `investigationProfile` | no | — |
| `sourcePackIds` | no | — |
| `continuationOfJobId` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.createResearchJob({
  project: "<project>",
  subject: "<subject>",
  question: "<question>",
  purpose: "<purpose>",
  permissiblePurpose: "<permissiblePurpose>",
  outputSchema: "<outputSchema>",
  budgets: "<budgets>",
  sourcePolicy: "<sourcePolicy>",
  caseId: "<caseId>",
  actorId: "<actorId>",
  retentionPolicyId: "<retentionPolicyId>",
  humanReviewRequired: "<humanReviewRequired>",
});
```

```python Python
result = client.tempera_risk.create_research_job({
    "project": "<project>",
    "subject": "<subject>",
    "question": "<question>",
    "purpose": "<purpose>",
    "permissiblePurpose": "<permissiblePurpose>",
    "outputSchema": "<outputSchema>",
    "budgets": "<budgets>",
    "sourcePolicy": "<sourcePolicy>",
    "caseId": "<caseId>",
    "actorId": "<actorId>",
    "retentionPolicyId": "<retentionPolicyId>",
    "humanReviewRequired": "<humanReviewRequired>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "create_research_job", &[
    ("project", "<project>".into()),
    ("subject", "<subject>".into()),
    ("question", "<question>".into()),
    ("purpose", "<purpose>".into()),
    ("permissiblePurpose", "<permissiblePurpose>".into()),
    ("outputSchema", "<outputSchema>".into()),
    ("budgets", "<budgets>".into()),
    ("sourcePolicy", "<sourcePolicy>".into()),
    ("caseId", "<caseId>".into()),
    ("actorId", "<actorId>".into()),
    ("retentionPolicyId", "<retentionPolicyId>".into()),
    ("humanReviewRequired", "<humanReviewRequired>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getResearchJob

`GET /v1/projects/{project}/researchJobs/{jobId}`

Call GET /v1/projects/\{project\}/researchJobs/\{jobId\}.

- **Producer operationId:** `getResearchJob`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:read`
- **Call as:** TypeScript `client.temperaRisk.getResearchJob()` · Python `client.tempera_risk.get_research_job()` · Rust `build_request("tempera_risk", "get_research_job", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `jobId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.getResearchJob({
  project: "<project>",
  jobId: "<jobId>",
});
```

```python Python
result = client.tempera_risk.get_research_job({
    "project": "<project>",
    "jobId": "<jobId>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "get_research_job", &[
    ("project", "<project>".into()),
    ("jobId", "<jobId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### streamResearchJobEvents

`GET /v1/projects/{project}/researchJobs/{jobId}/events:stream`

Replay bounded authorized job events as SSE; reconnect with afterSequence.

- **Producer operationId:** `streamResearchJobEvents`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:read`
- **Call as:** TypeScript `client.temperaRisk.streamResearchJobEvents()` · Python `client.tempera_risk.stream_research_job_events()` · Rust `build_request("tempera_risk", "stream_research_job_events", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `jobId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `afterSequence` | no | — |
| `pageSize` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.streamResearchJobEvents({
  project: "<project>",
  jobId: "<jobId>",
  afterSequence: "<afterSequence>",
  pageSize: "<pageSize>",
});
```

```python Python
result = client.tempera_risk.stream_research_job_events({
    "project": "<project>",
    "jobId": "<jobId>",
    "afterSequence": "<afterSequence>",
    "pageSize": "<pageSize>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "stream_research_job_events", &[
    ("project", "<project>".into()),
    ("jobId", "<jobId>".into()),
    ("afterSequence", "<afterSequence>".into()),
    ("pageSize", "<pageSize>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getResearchJobResult

`GET /v1/projects/{project}/researchJobs/{jobId}/result`

Read the cited, non-decisional result under the job's original access grant.

- **Producer operationId:** `getResearchJobResult`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:read`
- **Call as:** TypeScript `client.temperaRisk.getResearchJobResult()` · Python `client.tempera_risk.get_research_job_result()` · Rust `build_request("tempera_risk", "get_research_job_result", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `jobId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.getResearchJobResult({
  project: "<project>",
  jobId: "<jobId>",
});
```

```python Python
result = client.tempera_risk.get_research_job_result({
    "project": "<project>",
    "jobId": "<jobId>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "get_research_job_result", &[
    ("project", "<project>".into()),
    ("jobId", "<jobId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listResearchJobEvents

`GET /v1/projects/{project}/researchJobs/{jobId}/events`

Read ordered non-decisional job events under the job's original access grant.

- **Producer operationId:** `listResearchJobEvents`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:read`
- **Call as:** TypeScript `client.temperaRisk.listResearchJobEvents()` · Python `client.tempera_risk.list_research_job_events()` · Rust `build_request("tempera_risk", "list_research_job_events", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `jobId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `afterSequence` | no | — |
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.listResearchJobEvents({
  project: "<project>",
  jobId: "<jobId>",
  afterSequence: "<afterSequence>",
  pageSize: "<pageSize>",
});
```

```python Python
result = client.tempera_risk.list_research_job_events({
    "project": "<project>",
    "jobId": "<jobId>",
    "afterSequence": "<afterSequence>",
    "pageSize": "<pageSize>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "list_research_job_events", &[
    ("project", "<project>".into()),
    ("jobId", "<jobId>".into()),
    ("afterSequence", "<afterSequence>".into()),
    ("pageSize", "<pageSize>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### cancelResearchJob

`POST /v1/projects/{project}/researchJobs/{jobId}:cancel`

Call POST /v1/projects/\{project\}/researchJobs/\{jobId\}:cancel.

- **Producer operationId:** `cancelResearchJob`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:write`
- **Call as:** TypeScript `client.temperaRisk.cancelResearchJob()` · Python `client.tempera_risk.cancel_research_job()` · Rust `build_request("tempera_risk", "cancel_research_job", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `jobId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `reason` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.cancelResearchJob({
  project: "<project>",
  jobId: "<jobId>",
  reason: "<reason>",
});
```

```python Python
result = client.tempera_risk.cancel_research_job({
    "project": "<project>",
    "jobId": "<jobId>",
    "reason": "<reason>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "cancel_research_job", &[
    ("project", "<project>".into()),
    ("jobId", "<jobId>".into()),
    ("reason", "<reason>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### reviewResearchJob

`POST /v1/projects/{project}/researchJobs/{jobId}:review`

Call POST /v1/projects/\{project\}/researchJobs/\{jobId\}:review.

- **Producer operationId:** `reviewResearchJob`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:review`
- **Call as:** TypeScript `client.temperaRisk.reviewResearchJob()` · Python `client.tempera_risk.review_research_job()` · Rust `build_request("tempera_risk", "review_research_job", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `jobId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `approved` | yes | — |
| `reason` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.reviewResearchJob({
  project: "<project>",
  jobId: "<jobId>",
  approved: "<approved>",
  reason: "<reason>",
});
```

```python Python
result = client.tempera_risk.review_research_job({
    "project": "<project>",
    "jobId": "<jobId>",
    "approved": "<approved>",
    "reason": "<reason>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "review_research_job", &[
    ("project", "<project>".into()),
    ("jobId", "<jobId>".into()),
    ("approved", "<approved>".into()),
    ("reason", "<reason>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### verifyRiskClearingEvidenceLive

`POST /v1/projects/{project}/riskClearingEvidence:verifyLive`

Verify one signed Risk Clearing evidence envelope against live Risk state.

- **Producer operationId:** `verifyRiskClearingEvidenceLive`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:write`
- **Call as:** TypeScript `client.temperaRisk.verifyRiskClearingEvidenceLive()` · Python `client.tempera_risk.verify_risk_clearing_evidence_live()` · Rust `build_request("tempera_risk", "verify_risk_clearing_evidence_live", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `envelope` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.verifyRiskClearingEvidenceLive({
  project: "<project>",
  envelope: "<envelope>",
});
```

```python Python
result = client.tempera_risk.verify_risk_clearing_evidence_live({
    "project": "<project>",
    "envelope": "<envelope>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "verify_risk_clearing_evidence_live", &[
    ("project", "<project>".into()),
    ("envelope", "<envelope>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### abortRiskClearingReservation

`POST /v1/projects/{project}/riskClearingReservations/{reservationId}:abort`

Release a reservation only when Clearing proved no external dispatch occurred.

- **Producer operationId:** `abortRiskClearingReservation`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:write`
- **Call as:** TypeScript `client.temperaRisk.abortRiskClearingReservation()` · Python `client.tempera_risk.abort_risk_clearing_reservation()` · Rust `build_request("tempera_risk", "abort_risk_clearing_reservation", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `reservationId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `envelope` | yes | — |
| `abortReceiptDigest` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.abortRiskClearingReservation({
  project: "<project>",
  reservationId: "<reservationId>",
  envelope: "<envelope>",
  abortReceiptDigest: "<abortReceiptDigest>",
});
```

```python Python
result = client.tempera_risk.abort_risk_clearing_reservation({
    "project": "<project>",
    "reservationId": "<reservationId>",
    "envelope": "<envelope>",
    "abortReceiptDigest": "<abortReceiptDigest>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "abort_risk_clearing_reservation", &[
    ("project", "<project>".into()),
    ("reservationId", "<reservationId>".into()),
    ("envelope", "<envelope>".into()),
    ("abortReceiptDigest", "<abortReceiptDigest>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### markRiskClearingOutcomeUnknown

`POST /v1/projects/{project}/riskClearingReservations/{reservationId}:markOutcomeUnknown`

Hold budget capacity after an ambiguous provider dispatch.

- **Producer operationId:** `markRiskClearingOutcomeUnknown`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:write`
- **Call as:** TypeScript `client.temperaRisk.markRiskClearingOutcomeUnknown()` · Python `client.tempera_risk.mark_risk_clearing_outcome_unknown()` · Rust `build_request("tempera_risk", "mark_risk_clearing_outcome_unknown", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `reservationId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `envelope` | yes | — |
| `dispatchAttemptDigest` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.markRiskClearingOutcomeUnknown({
  project: "<project>",
  reservationId: "<reservationId>",
  envelope: "<envelope>",
  dispatchAttemptDigest: "<dispatchAttemptDigest>",
});
```

```python Python
result = client.tempera_risk.mark_risk_clearing_outcome_unknown({
    "project": "<project>",
    "reservationId": "<reservationId>",
    "envelope": "<envelope>",
    "dispatchAttemptDigest": "<dispatchAttemptDigest>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "mark_risk_clearing_outcome_unknown", &[
    ("project", "<project>".into()),
    ("reservationId", "<reservationId>".into()),
    ("envelope", "<envelope>".into()),
    ("dispatchAttemptDigest", "<dispatchAttemptDigest>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### openRiskClearingExposure

`POST /v1/projects/{project}/riskClearingReservations/{reservationId}:open`

Record an independently observed provider effect while retaining capacity.

- **Producer operationId:** `openRiskClearingExposure`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:write`
- **Call as:** TypeScript `client.temperaRisk.openRiskClearingExposure()` · Python `client.tempera_risk.open_risk_clearing_exposure()` · Rust `build_request("tempera_risk", "open_risk_clearing_exposure", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `reservationId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `envelope` | yes | — |
| `executionReceiptDigest` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.openRiskClearingExposure({
  project: "<project>",
  reservationId: "<reservationId>",
  envelope: "<envelope>",
  executionReceiptDigest: "<executionReceiptDigest>",
});
```

```python Python
result = client.tempera_risk.open_risk_clearing_exposure({
    "project": "<project>",
    "reservationId": "<reservationId>",
    "envelope": "<envelope>",
    "executionReceiptDigest": "<executionReceiptDigest>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "open_risk_clearing_exposure", &[
    ("project", "<project>".into()),
    ("reservationId", "<reservationId>".into()),
    ("envelope", "<envelope>".into()),
    ("executionReceiptDigest", "<executionReceiptDigest>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### recordRiskClearingProvisionalOutcome

`POST /v1/projects/{project}/riskClearingReservations/{reservationId}:recordProvisionalOutcome`

Record a non-final observed outcome without releasing capacity.

- **Producer operationId:** `recordRiskClearingProvisionalOutcome`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:write`
- **Call as:** TypeScript `client.temperaRisk.recordRiskClearingProvisionalOutcome()` · Python `client.tempera_risk.record_risk_clearing_provisional_outcome()` · Rust `build_request("tempera_risk", "record_risk_clearing_provisional_outcome", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `reservationId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `envelope` | yes | — |
| `outcomeDigest` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.recordRiskClearingProvisionalOutcome({
  project: "<project>",
  reservationId: "<reservationId>",
  envelope: "<envelope>",
  outcomeDigest: "<outcomeDigest>",
});
```

```python Python
result = client.tempera_risk.record_risk_clearing_provisional_outcome({
    "project": "<project>",
    "reservationId": "<reservationId>",
    "envelope": "<envelope>",
    "outcomeDigest": "<outcomeDigest>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "record_risk_clearing_provisional_outcome", &[
    ("project", "<project>".into()),
    ("reservationId", "<reservationId>".into()),
    ("envelope", "<envelope>".into()),
    ("outcomeDigest", "<outcomeDigest>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### recordRiskClearingCompensation

`POST /v1/projects/{project}/riskClearingReservations/{reservationId}:recordCompensation`

Record compensation as a forward action without releasing capacity.

- **Producer operationId:** `recordRiskClearingCompensation`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:write`
- **Call as:** TypeScript `client.temperaRisk.recordRiskClearingCompensation()` · Python `client.tempera_risk.record_risk_clearing_compensation()` · Rust `build_request("tempera_risk", "record_risk_clearing_compensation", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `reservationId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `envelope` | yes | — |
| `compensationReceiptDigest` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.recordRiskClearingCompensation({
  project: "<project>",
  reservationId: "<reservationId>",
  envelope: "<envelope>",
  compensationReceiptDigest: "<compensationReceiptDigest>",
});
```

```python Python
result = client.tempera_risk.record_risk_clearing_compensation({
    "project": "<project>",
    "reservationId": "<reservationId>",
    "envelope": "<envelope>",
    "compensationReceiptDigest": "<compensationReceiptDigest>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "record_risk_clearing_compensation", &[
    ("project", "<project>".into()),
    ("reservationId", "<reservationId>".into()),
    ("envelope", "<envelope>".into()),
    ("compensationReceiptDigest", "<compensationReceiptDigest>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### disputeRiskClearingExposure

`POST /v1/projects/{project}/riskClearingReservations/{reservationId}:dispute`

Keep capacity held when authoritative evidence conflicts.

- **Producer operationId:** `disputeRiskClearingExposure`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:write`
- **Call as:** TypeScript `client.temperaRisk.disputeRiskClearingExposure()` · Python `client.tempera_risk.dispute_risk_clearing_exposure()` · Rust `build_request("tempera_risk", "dispute_risk_clearing_exposure", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `reservationId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `envelope` | yes | — |
| `disputeEvidenceDigest` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.disputeRiskClearingExposure({
  project: "<project>",
  reservationId: "<reservationId>",
  envelope: "<envelope>",
  disputeEvidenceDigest: "<disputeEvidenceDigest>",
});
```

```python Python
result = client.tempera_risk.dispute_risk_clearing_exposure({
    "project": "<project>",
    "reservationId": "<reservationId>",
    "envelope": "<envelope>",
    "disputeEvidenceDigest": "<disputeEvidenceDigest>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "dispute_risk_clearing_exposure", &[
    ("project", "<project>".into()),
    ("reservationId", "<reservationId>".into()),
    ("envelope", "<envelope>".into()),
    ("disputeEvidenceDigest", "<disputeEvidenceDigest>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### settleRiskClearingExposure

`POST /v1/projects/{project}/riskClearingReservations/{reservationId}:settle`

Settle a mature reconciled outcome and release held capacity.

- **Producer operationId:** `settleRiskClearingExposure`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:write`
- **Call as:** TypeScript `client.temperaRisk.settleRiskClearingExposure()` · Python `client.tempera_risk.settle_risk_clearing_exposure()` · Rust `build_request("tempera_risk", "settle_risk_clearing_exposure", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `reservationId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `envelope` | yes | — |
| `outcomeDigest` | yes | — |
| `realizedLoss` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.settleRiskClearingExposure({
  project: "<project>",
  reservationId: "<reservationId>",
  envelope: "<envelope>",
  outcomeDigest: "<outcomeDigest>",
  realizedLoss: "<realizedLoss>",
});
```

```python Python
result = client.tempera_risk.settle_risk_clearing_exposure({
    "project": "<project>",
    "reservationId": "<reservationId>",
    "envelope": "<envelope>",
    "outcomeDigest": "<outcomeDigest>",
    "realizedLoss": "<realizedLoss>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "settle_risk_clearing_exposure", &[
    ("project", "<project>".into()),
    ("reservationId", "<reservationId>".into()),
    ("envelope", "<envelope>".into()),
    ("outcomeDigest", "<outcomeDigest>".into()),
    ("realizedLoss", "<realizedLoss>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### exportAudit

`GET /v1/projects/{project}/audit:export`

Call GET /v1/projects/\{project\}/audit:export.

- **Producer operationId:** `exportAudit`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:read`
- **Call as:** TypeScript `client.temperaRisk.exportAudit()` · Python `client.tempera_risk.export_audit()` · Rust `build_request("tempera_risk", "export_audit", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.exportAudit({
  project: "<project>",
});
```

```python Python
result = client.tempera_risk.export_audit({
    "project": "<project>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "export_audit", &[
    ("project", "<project>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getOpenApi

`GET /openapi.json`

Call GET /openapi.json.

- **Producer operationId:** `getOpenApi`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.temperaRisk.getOpenApi()` · Python `client.tempera_risk.get_open_api()` · Rust `build_request("tempera_risk", "get_open_api", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.getOpenApi();
```

```python Python
result = client.tempera_risk.get_open_api()
```

```rust Rust
let spec = client.build_request("tempera_risk", "get_open_api", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### ingestPaymentObservation

`POST /v1/projects/{project}/paymentObservations:ingest`

Ingest one provider-owned payment observation as a trusted event.

- **Producer operationId:** `ingestPaymentObservation`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-risk`.
- **Scope:** `risk:write`
- **Call as:** TypeScript `client.temperaRisk.ingestPaymentObservation()` · Python `client.tempera_risk.ingest_payment_observation()` · Rust `build_request("tempera_risk", "ingest_payment_observation", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaRisk.ingestPaymentObservation({
  project: "<project>",
});
```

```python Python
result = client.tempera_risk.ingest_payment_observation({
    "project": "<project>",
})
```

```rust Rust
let spec = client.build_request("tempera_risk", "ingest_payment_observation", &[
    ("project", "<project>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>
