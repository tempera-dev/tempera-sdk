---
title: "Tempera Code"
description: "Durable local workflow orchestration and hosted Responses-compatible inference through the Tempera Code gateway."
---

{/* Generated by scripts/gen-sdk-docs.py from surface.json -- do not hand-edit. */}

> **Private access.** Hosted Tempera services are available through design-partner onboarding.
> Use the issuer, credentials, environment, and product URLs provisioned for your workspace;
> start in staging unless Tempera explicitly approves another target.

> **Contract inventory.** This typed reference is retained for SDK compatibility.
> It is not part of the primary public workflow or evidence of general service availability.

Durable local workflow orchestration and hosted Responses-compatible inference through the Tempera Code gateway.

| | |
|---|---|
| Repository | [tempera-code](https://github.com/tempera-dev/tempera-code) |
| Base URL env var | `TEMPERA_CODE_GATEWAY_URL` (see [Environments](/environments)) |
| Audience | `tempera-code` |
| Auth | bearer minted for audience tempera-code with model:read/model:invoke, or a central tp_ API key |
| Client | TypeScript `client.temperaCode` · Python `client.tempera_code` · Rust `build_request("tempera_code", ...)` |
| Typed operations | 3 |

Anything this page does not cover is reachable through the raw passthrough:
`client.temperaCode.request(path, options)` (TypeScript) /
`client.tempera_code.request(path, ...)` (Python).

## Operations

### health

`GET /healthz`

Check Tempera Code gateway liveness.

- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.temperaCode.health()` · Python `client.tempera_code.health()` · Rust `build_request("tempera_code", "health", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.temperaCode.health();
```

```python Python
result = client.tempera_code.health()
```

```rust Rust
let spec = client.build_request("tempera_code", "health", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listModels

`GET /v1/models`

List the entitled Tempera Code hosted model catalog.

- **Auth:** Product bearer — an access token minted for audience `tempera-code`, or a central `tp_` API key.
- **Scope:** `model:read`
- **Call as:** TypeScript `client.temperaCode.listModels()` · Python `client.tempera_code.list_models()` · Rust `build_request("tempera_code", "list_models", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.temperaCode.listModels();
```

```python Python
result = client.tempera_code.list_models()
```

```rust Rust
let spec = client.build_request("tempera_code", "list_models", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createResponse

`POST /v1/responses`

Create a non-streaming Responses-compatible inference request through the Tempera Code gateway.

- **Auth:** Product bearer — an access token minted for audience `tempera-code`, or a central `tp_` API key.
- **Scope:** `model:invoke`
- **Call as:** TypeScript `client.temperaCode.createResponse()` · Python `client.tempera_code.create_response()` · Rust `build_request("tempera_code", "create_response", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `model` | yes | — |
| `input` | yes | — |
| `instructions` | no | — |
| `tools` | no | — |
| `text` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaCode.createResponse({
  model: "<model>",
  input: "<input>",
  instructions: "<instructions>",
});
```

```python Python
result = client.tempera_code.create_response({
    "model": "<model>",
    "input": "<input>",
    "instructions": "<instructions>",
})
```

```rust Rust
let spec = client.build_request("tempera_code", "create_response", &[
    ("model", "<model>".into()),
    ("input", "<input>".into()),
    ("instructions", "<instructions>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>
