---
title: "tempera-voice"
description: "Provider-neutral realtime voice control plane with durable sessions, bounded provider adapters, MCP-mediated actions, Palette telemetry, and governed Tempera Evals evidence. A hosted URL must be supplied explicitly until a deployed environment is registered."
---

{/* Generated by scripts/gen-sdk-docs.py from surface.json -- do not hand-edit. */}

> **Private access.** Hosted Tempera services are available through design-partner onboarding.
> Use the issuer, credentials, environment, and product URLs provisioned for your workspace;
> start in staging unless Tempera explicitly approves another target.

> **Contract inventory.** This typed reference is retained for SDK compatibility.
> It is not part of the primary public workflow or evidence of general service availability.

Provider-neutral realtime voice control plane with durable sessions, bounded provider adapters, MCP-mediated actions, Palette telemetry, and governed Tempera Evals evidence. A hosted URL must be supplied explicitly until a deployed environment is registered.

| | |
|---|---|
| Repository | [tempera-voice](https://github.com/tempera-dev/tempera-voice) |
| Base URL env var | `TEMPERA_VOICE_URL` (see [Environments](/environments)) |
| Audience | `tempera-voice` |
| Auth | bearer minted for audience tempera-voice with voice:read/voice:write/voice:stream or shared eval:run, or a central tp_ API key |
| Client | TypeScript `client.temperaVoice` · Python `client.tempera_voice` · Rust `build_request("tempera_voice", ...)` |
| Typed operations | 24 |

Anything this page does not cover is reachable through the raw passthrough:
`client.temperaVoice.request(path, options)` (TypeScript) /
`client.tempera_voice.request(path, ...)` (Python).

## Operations

### voiceLiveness

`GET /livez`

Liveness.

- **Producer operationId:** `voiceLiveness`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.temperaVoice.voiceLiveness()` · Python `client.tempera_voice.voice_liveness()` · Rust `build_request("tempera_voice", "voice_liveness", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.temperaVoice.voiceLiveness();
```

```python Python
result = client.tempera_voice.voice_liveness()
```

```rust Rust
let spec = client.build_request("tempera_voice", "voice_liveness", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### voiceReadiness

`GET /readyz`

Readiness.

- **Producer operationId:** `voiceReadiness`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.temperaVoice.voiceReadiness()` · Python `client.tempera_voice.voice_readiness()` · Rust `build_request("tempera_voice", "voice_readiness", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.temperaVoice.voiceReadiness();
```

```python Python
result = client.tempera_voice.voice_readiness()
```

```rust Rust
let spec = client.build_request("tempera_voice", "voice_readiness", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getVoiceCapabilities

`GET /v1/capabilities`

Capabilities.

- **Producer operationId:** `getVoiceCapabilities`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-voice`.
- **Scope:** `voice:read`
- **Call as:** TypeScript `client.temperaVoice.getVoiceCapabilities()` · Python `client.tempera_voice.get_voice_capabilities()` · Rust `build_request("tempera_voice", "get_voice_capabilities", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.temperaVoice.getVoiceCapabilities();
```

```python Python
result = client.tempera_voice.get_voice_capabilities()
```

```rust Rust
let spec = client.build_request("tempera_voice", "get_voice_capabilities", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createVoiceAgent

`POST /v1/agents`

Create Agent.

- **Producer operationId:** `createVoiceAgent`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-voice`.
- **Scope:** `voice:write`
- **Call as:** TypeScript `client.temperaVoice.createVoiceAgent()` · Python `client.tempera_voice.create_voice_agent()` · Rust `build_request("tempera_voice", "create_voice_agent", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `id` | no | — |
| `name` | yes | — |
| `useCase` | no | — |
| `instructions` | yes | — |
| `provider` | no | — |
| `model` | no | — |
| `voice` | no | — |
| `language` | no | — |
| `outputModality` | no | — |
| `inputAudioFormat` | no | — |
| `outputAudioFormat` | no | — |
| `transcriptionModel` | no | — |
| `turnDetection` | no | — |
| `tools` | no | — |
| `retention` | no | — |
| `limits` | no | — |
| `metadata` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaVoice.createVoiceAgent({
  name: "<name>",
  instructions: "<instructions>",
  id: "<id>",
});
```

```python Python
result = client.tempera_voice.create_voice_agent({
    "name": "<name>",
    "instructions": "<instructions>",
    "id": "<id>",
})
```

```rust Rust
let spec = client.build_request("tempera_voice", "create_voice_agent", &[
    ("name", "<name>".into()),
    ("instructions", "<instructions>".into()),
    ("id", "<id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listVoiceAgents

`GET /v1/agents`

List Agents.

- **Producer operationId:** `listVoiceAgents`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-voice`.
- **Scope:** `voice:read`
- **Call as:** TypeScript `client.temperaVoice.listVoiceAgents()` · Python `client.tempera_voice.list_voice_agents()` · Rust `build_request("tempera_voice", "list_voice_agents", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaVoice.listVoiceAgents({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.tempera_voice.list_voice_agents({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("tempera_voice", "list_voice_agents", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### updateVoiceAgent

`PATCH /v1/agents/{agentId}`

Update Agent.

- **Producer operationId:** `updateVoiceAgent`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-voice`.
- **Scope:** `voice:write`
- **Call as:** TypeScript `client.temperaVoice.updateVoiceAgent()` · Python `client.tempera_voice.update_voice_agent()` · Rust `build_request("tempera_voice", "update_voice_agent", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `agentId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `updateMask` | no | — |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `id` | no | — |
| `name` | yes | — |
| `useCase` | no | — |
| `instructions` | yes | — |
| `provider` | no | — |
| `model` | no | — |
| `voice` | no | — |
| `language` | no | — |
| `outputModality` | no | — |
| `inputAudioFormat` | no | — |
| `outputAudioFormat` | no | — |
| `transcriptionModel` | no | — |
| `turnDetection` | no | — |
| `tools` | no | — |
| `retention` | no | — |
| `limits` | no | — |
| `metadata` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaVoice.updateVoiceAgent({
  agentId: "<agentId>",
  name: "<name>",
  instructions: "<instructions>",
  id: "<id>",
});
```

```python Python
result = client.tempera_voice.update_voice_agent({
    "agentId": "<agentId>",
    "name": "<name>",
    "instructions": "<instructions>",
    "id": "<id>",
})
```

```rust Rust
let spec = client.build_request("tempera_voice", "update_voice_agent", &[
    ("agentId", "<agentId>".into()),
    ("name", "<name>".into()),
    ("instructions", "<instructions>".into()),
    ("id", "<id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getVoiceAgent

`GET /v1/agents/{agentId}`

Get Agent.

- **Producer operationId:** `getVoiceAgent`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-voice`.
- **Scope:** `voice:read`
- **Call as:** TypeScript `client.temperaVoice.getVoiceAgent()` · Python `client.tempera_voice.get_voice_agent()` · Rust `build_request("tempera_voice", "get_voice_agent", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `agentId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaVoice.getVoiceAgent({
  agentId: "<agentId>",
});
```

```python Python
result = client.tempera_voice.get_voice_agent({
    "agentId": "<agentId>",
})
```

```rust Rust
let spec = client.build_request("tempera_voice", "get_voice_agent", &[
    ("agentId", "<agentId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createVoiceSession

`POST /v1/sessions`

Create Session.

- **Producer operationId:** `createVoiceSession`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-voice`.
- **Scope:** `voice:write`
- **Call as:** TypeScript `client.temperaVoice.createVoiceSession()` · Python `client.tempera_voice.create_voice_session()` · Rust `build_request("tempera_voice", "create_voice_session", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `agentId` | yes | — |
| `channel` | no | — |
| `externalId` | no | — |
| `safetyIdentifier` | no | — |
| `profileRef` | no | — |
| `goalRef` | no | — |
| `siteId` | no | — |
| `metadata` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaVoice.createVoiceSession({
  agentId: "<agentId>",
  channel: "<channel>",
  externalId: "<externalId>",
});
```

```python Python
result = client.tempera_voice.create_voice_session({
    "agentId": "<agentId>",
    "channel": "<channel>",
    "externalId": "<externalId>",
})
```

```rust Rust
let spec = client.build_request("tempera_voice", "create_voice_session", &[
    ("agentId", "<agentId>".into()),
    ("channel", "<channel>".into()),
    ("externalId", "<externalId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listVoiceSessions

`GET /v1/sessions`

List Sessions.

- **Producer operationId:** `listVoiceSessions`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-voice`.
- **Scope:** `voice:read`
- **Call as:** TypeScript `client.temperaVoice.listVoiceSessions()` · Python `client.tempera_voice.list_voice_sessions()` · Rust `build_request("tempera_voice", "list_voice_sessions", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `agentId` | no | — |
| `profileRef` | no | — |
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaVoice.listVoiceSessions({
  agentId: "<agentId>",
  profileRef: "<profileRef>",
});
```

```python Python
result = client.tempera_voice.list_voice_sessions({
    "agentId": "<agentId>",
    "profileRef": "<profileRef>",
})
```

```rust Rust
let spec = client.build_request("tempera_voice", "list_voice_sessions", &[
    ("agentId", "<agentId>".into()),
    ("profileRef", "<profileRef>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getDefaultVoiceAgent

`GET /v1/agents:default`

Get Default Agent.

- **Producer operationId:** `getDefaultVoiceAgent`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-voice`.
- **Scope:** `voice:read`
- **Call as:** TypeScript `client.temperaVoice.getDefaultVoiceAgent()` · Python `client.tempera_voice.get_default_voice_agent()` · Rust `build_request("tempera_voice", "get_default_voice_agent", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.temperaVoice.getDefaultVoiceAgent();
```

```python Python
result = client.tempera_voice.get_default_voice_agent()
```

```rust Rust
let spec = client.build_request("tempera_voice", "get_default_voice_agent", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getVoiceSession

`GET /v1/sessions/{sessionId}`

Get Session.

- **Producer operationId:** `getVoiceSession`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-voice`.
- **Scope:** `voice:read`
- **Call as:** TypeScript `client.temperaVoice.getVoiceSession()` · Python `client.tempera_voice.get_voice_session()` · Rust `build_request("tempera_voice", "get_voice_session", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `sessionId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaVoice.getVoiceSession({
  sessionId: "<sessionId>",
});
```

```python Python
result = client.tempera_voice.get_voice_session({
    "sessionId": "<sessionId>",
})
```

```rust Rust
let spec = client.build_request("tempera_voice", "get_voice_session", &[
    ("sessionId", "<sessionId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listVoiceSessionEvents

`GET /v1/sessions/{sessionId}/events`

List Events.

- **Producer operationId:** `listVoiceSessionEvents`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-voice`.
- **Scope:** `voice:read`
- **Call as:** TypeScript `client.temperaVoice.listVoiceSessionEvents()` · Python `client.tempera_voice.list_voice_session_events()` · Rust `build_request("tempera_voice", "list_voice_session_events", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `sessionId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `afterSequence` | no | — |
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaVoice.listVoiceSessionEvents({
  sessionId: "<sessionId>",
  afterSequence: "<afterSequence>",
  pageSize: "<pageSize>",
});
```

```python Python
result = client.tempera_voice.list_voice_session_events({
    "sessionId": "<sessionId>",
    "afterSequence": "<afterSequence>",
    "pageSize": "<pageSize>",
})
```

```rust Rust
let spec = client.build_request("tempera_voice", "list_voice_session_events", &[
    ("sessionId", "<sessionId>".into()),
    ("afterSequence", "<afterSequence>".into()),
    ("pageSize", "<pageSize>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listVoiceSessionActions

`GET /v1/sessions/{sessionId}/actions`

List Actions.

- **Producer operationId:** `listVoiceSessionActions`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-voice`.
- **Scope:** `voice:read`
- **Call as:** TypeScript `client.temperaVoice.listVoiceSessionActions()` · Python `client.tempera_voice.list_voice_session_actions()` · Rust `build_request("tempera_voice", "list_voice_session_actions", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `sessionId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `status` | no | — |
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaVoice.listVoiceSessionActions({
  sessionId: "<sessionId>",
  status: "<status>",
  pageSize: "<pageSize>",
});
```

```python Python
result = client.tempera_voice.list_voice_session_actions({
    "sessionId": "<sessionId>",
    "status": "<status>",
    "pageSize": "<pageSize>",
})
```

```rust Rust
let spec = client.build_request("tempera_voice", "list_voice_session_actions", &[
    ("sessionId", "<sessionId>".into()),
    ("status", "<status>".into()),
    ("pageSize", "<pageSize>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### resolveVoiceAction

`POST /v1/actions/{actionId}:resolve`

Resolve Action.

- **Producer operationId:** `resolveVoiceAction`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-voice`.
- **Scope:** `voice:write`
- **Call as:** TypeScript `client.temperaVoice.resolveVoiceAction()` · Python `client.tempera_voice.resolve_voice_action()` · Rust `build_request("tempera_voice", "resolve_voice_action", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `actionId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `approved` | yes | — |
| `note` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaVoice.resolveVoiceAction({
  actionId: "<actionId>",
  approved: "<approved>",
  note: "<note>",
});
```

```python Python
result = client.tempera_voice.resolve_voice_action({
    "actionId": "<actionId>",
    "approved": "<approved>",
    "note": "<note>",
})
```

```rust Rust
let spec = client.build_request("tempera_voice", "resolve_voice_action", &[
    ("actionId", "<actionId>".into()),
    ("approved", "<approved>".into()),
    ("note", "<note>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### endVoiceSession

`POST /v1/sessions/{sessionId}:end`

End Session.

- **Producer operationId:** `endVoiceSession`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-voice`.
- **Scope:** `voice:write`
- **Call as:** TypeScript `client.temperaVoice.endVoiceSession()` · Python `client.tempera_voice.end_voice_session()` · Rust `build_request("tempera_voice", "end_voice_session", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `sessionId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `reason` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaVoice.endVoiceSession({
  sessionId: "<sessionId>",
  reason: "<reason>",
});
```

```python Python
result = client.tempera_voice.end_voice_session({
    "sessionId": "<sessionId>",
    "reason": "<reason>",
})
```

```rust Rust
let spec = client.build_request("tempera_voice", "end_voice_session", &[
    ("sessionId", "<sessionId>".into()),
    ("reason", "<reason>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### exportVoiceSessions

`POST /v1/sessions:export`

Export Sessions.

- **Producer operationId:** `exportVoiceSessions`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-voice`.
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.temperaVoice.exportVoiceSessions()` · Python `client.tempera_voice.export_voice_sessions()` · Rust `build_request("tempera_voice", "export_voice_sessions", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `sessionIds` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaVoice.exportVoiceSessions({
  sessionIds: "<sessionIds>",
});
```

```python Python
result = client.tempera_voice.export_voice_sessions({
    "sessionIds": "<sessionIds>",
})
```

```rust Rust
let spec = client.build_request("tempera_voice", "export_voice_sessions", &[
    ("sessionIds", "<sessionIds>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### uploadVoiceArtifact

`POST /v1/artifacts:upload`

Upload Artifact.

- **Producer operationId:** `uploadVoiceArtifact`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-voice`.
- **Scope:** `voice:write`
- **Call as:** TypeScript `client.temperaVoice.uploadVoiceArtifact()` · Python `client.tempera_voice.upload_voice_artifact()` · Rust `build_request("tempera_voice", "upload_voice_artifact", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `name` | yes | — |
| `contentBase64` | yes | — |
| `mediaType` | no | — |
| `classification` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaVoice.uploadVoiceArtifact({
  name: "<name>",
  contentBase64: "<contentBase64>",
  mediaType: "<mediaType>",
});
```

```python Python
result = client.tempera_voice.upload_voice_artifact({
    "name": "<name>",
    "contentBase64": "<contentBase64>",
    "mediaType": "<mediaType>",
})
```

```rust Rust
let spec = client.build_request("tempera_voice", "upload_voice_artifact", &[
    ("name", "<name>".into()),
    ("contentBase64", "<contentBase64>".into()),
    ("mediaType", "<mediaType>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### downloadVoiceArtifact

`GET /v1/artifacts/{reference}`

Download Artifact.

- **Producer operationId:** `downloadVoiceArtifact`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-voice`.
- **Scope:** `voice:read`
- **Call as:** TypeScript `client.temperaVoice.downloadVoiceArtifact()` · Python `client.tempera_voice.download_voice_artifact()` · Rust `build_request("tempera_voice", "download_voice_artifact", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `reference` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaVoice.downloadVoiceArtifact({
  reference: "<reference>",
});
```

```python Python
result = client.tempera_voice.download_voice_artifact({
    "reference": "<reference>",
})
```

```rust Rust
let spec = client.build_request("tempera_voice", "download_voice_artifact", &[
    ("reference", "<reference>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listVoiceEvalProfiles

`GET /v1/eval-profiles`

List Eval Profiles.

- **Producer operationId:** `listVoiceEvalProfiles`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-voice`.
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.temperaVoice.listVoiceEvalProfiles()` · Python `client.tempera_voice.list_voice_eval_profiles()` · Rust `build_request("tempera_voice", "list_voice_eval_profiles", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaVoice.listVoiceEvalProfiles({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.tempera_voice.list_voice_eval_profiles({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("tempera_voice", "list_voice_eval_profiles", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### sealVoiceEvalResult

`POST /v1/evals/results:seal`

Seal Eval Result.

- **Producer operationId:** `sealVoiceEvalResult`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-voice`.
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.temperaVoice.sealVoiceEvalResult()` · Python `client.tempera_voice.seal_voice_eval_result()` · Rust `build_request("tempera_voice", "seal_voice_eval_result", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `profileId` | yes | — |
| `draft` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaVoice.sealVoiceEvalResult({
  profileId: "<profileId>",
  draft: "<draft>",
});
```

```python Python
result = client.tempera_voice.seal_voice_eval_result({
    "profileId": "<profileId>",
    "draft": "<draft>",
})
```

```rust Rust
let spec = client.build_request("tempera_voice", "seal_voice_eval_result", &[
    ("profileId", "<profileId>".into()),
    ("draft", "<draft>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### importVoiceEvalResult

`POST /v1/evals/results:import`

Import Eval Result.

- **Producer operationId:** `importVoiceEvalResult`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-voice`.
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.temperaVoice.importVoiceEvalResult()` · Python `client.tempera_voice.import_voice_eval_result()` · Rust `build_request("tempera_voice", "import_voice_eval_result", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `profileId` | yes | — |
| `sealedResultPath` | yes | — |
| `artifacts` | yes | — |
| `attestationPath` | no | — |
| `attestationSignaturePath` | no | — |
| `attestationPublicKeyPath` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaVoice.importVoiceEvalResult({
  profileId: "<profileId>",
  sealedResultPath: "<sealedResultPath>",
  artifacts: "<artifacts>",
});
```

```python Python
result = client.tempera_voice.import_voice_eval_result({
    "profileId": "<profileId>",
    "sealedResultPath": "<sealedResultPath>",
    "artifacts": "<artifacts>",
})
```

```rust Rust
let spec = client.build_request("tempera_voice", "import_voice_eval_result", &[
    ("profileId", "<profileId>".into()),
    ("sealedResultPath", "<sealedResultPath>".into()),
    ("artifacts", "<artifacts>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### buildVoiceEvalBundle

`POST /v1/evals/bundles:build`

Build Eval Bundle.

- **Producer operationId:** `buildVoiceEvalBundle`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-voice`.
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.temperaVoice.buildVoiceEvalBundle()` · Python `client.tempera_voice.build_voice_eval_bundle()` · Rust `build_request("tempera_voice", "build_voice_eval_bundle", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `profileId` | yes | — |
| `adapterImportPath` | yes | — |
| `adapterImportSignaturePath` | yes | — |
| `adapterImportPublicKeyPath` | yes | — |
| `releaseEvidencePath` | yes | — |
| `exposureLedgerPath` | yes | — |
| `redactionPolicyPath` | yes | — |
| `guardrailsPath` | yes | — |
| `preregistrationPath` | yes | — |
| `bundleId` | yes | — |
| `createdAt` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaVoice.buildVoiceEvalBundle({
  profileId: "<profileId>",
  adapterImportPath: "<adapterImportPath>",
  adapterImportSignaturePath: "<adapterImportSignaturePath>",
  adapterImportPublicKeyPath: "<adapterImportPublicKeyPath>",
  releaseEvidencePath: "<releaseEvidencePath>",
  exposureLedgerPath: "<exposureLedgerPath>",
  redactionPolicyPath: "<redactionPolicyPath>",
  guardrailsPath: "<guardrailsPath>",
  preregistrationPath: "<preregistrationPath>",
  bundleId: "<bundleId>",
});
```

```python Python
result = client.tempera_voice.build_voice_eval_bundle({
    "profileId": "<profileId>",
    "adapterImportPath": "<adapterImportPath>",
    "adapterImportSignaturePath": "<adapterImportSignaturePath>",
    "adapterImportPublicKeyPath": "<adapterImportPublicKeyPath>",
    "releaseEvidencePath": "<releaseEvidencePath>",
    "exposureLedgerPath": "<exposureLedgerPath>",
    "redactionPolicyPath": "<redactionPolicyPath>",
    "guardrailsPath": "<guardrailsPath>",
    "preregistrationPath": "<preregistrationPath>",
    "bundleId": "<bundleId>",
})
```

```rust Rust
let spec = client.build_request("tempera_voice", "build_voice_eval_bundle", &[
    ("profileId", "<profileId>".into()),
    ("adapterImportPath", "<adapterImportPath>".into()),
    ("adapterImportSignaturePath", "<adapterImportSignaturePath>".into()),
    ("adapterImportPublicKeyPath", "<adapterImportPublicKeyPath>".into()),
    ("releaseEvidencePath", "<releaseEvidencePath>".into()),
    ("exposureLedgerPath", "<exposureLedgerPath>".into()),
    ("redactionPolicyPath", "<redactionPolicyPath>".into()),
    ("guardrailsPath", "<guardrailsPath>".into()),
    ("preregistrationPath", "<preregistrationPath>".into()),
    ("bundleId", "<bundleId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createVoicePaletteHandoff

`POST /v1/evals/palette-handoffs:create`

Create Palette Handoff.

- **Producer operationId:** `createVoicePaletteHandoff`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-voice`.
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.temperaVoice.createVoicePaletteHandoff()` · Python `client.tempera_voice.create_voice_palette_handoff()` · Rust `build_request("tempera_voice", "create_voice_palette_handoff", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `profileId` | yes | — |
| `evidencePath` | yes | — |
| `kind` | yes | — |
| `signaturePath` | yes | — |
| `publicKeyPath` | yes | — |
| `tenantId` | yes | — |
| `projectId` | yes | — |
| `abPlanPath` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaVoice.createVoicePaletteHandoff({
  profileId: "<profileId>",
  evidencePath: "<evidencePath>",
  kind: "<kind>",
  signaturePath: "<signaturePath>",
  publicKeyPath: "<publicKeyPath>",
  tenantId: "<tenantId>",
  projectId: "<projectId>",
});
```

```python Python
result = client.tempera_voice.create_voice_palette_handoff({
    "profileId": "<profileId>",
    "evidencePath": "<evidencePath>",
    "kind": "<kind>",
    "signaturePath": "<signaturePath>",
    "publicKeyPath": "<publicKeyPath>",
    "tenantId": "<tenantId>",
    "projectId": "<projectId>",
})
```

```rust Rust
let spec = client.build_request("tempera_voice", "create_voice_palette_handoff", &[
    ("profileId", "<profileId>".into()),
    ("evidencePath", "<evidencePath>".into()),
    ("kind", "<kind>".into()),
    ("signaturePath", "<signaturePath>".into()),
    ("publicKeyPath", "<publicKeyPath>".into()),
    ("tenantId", "<tenantId>".into()),
    ("projectId", "<projectId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### publishVoicePaletteHandoff

`POST /v1/evals/palette-handoffs:publish`

Publish Palette Handoff.

- **Producer operationId:** `publishVoicePaletteHandoff`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-voice`.
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.temperaVoice.publishVoicePaletteHandoff()` · Python `client.tempera_voice.publish_voice_palette_handoff()` · Rust `build_request("tempera_voice", "publish_voice_palette_handoff", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `handoffPath` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaVoice.publishVoicePaletteHandoff({
  handoffPath: "<handoffPath>",
});
```

```python Python
result = client.tempera_voice.publish_voice_palette_handoff({
    "handoffPath": "<handoffPath>",
})
```

```rust Rust
let spec = client.build_request("tempera_voice", "publish_voice_palette_handoff", &[
    ("handoffPath", "<handoffPath>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>
