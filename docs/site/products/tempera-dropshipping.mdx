---
title: "tempera-dropshipping"
description: "Declared merchant order recovery: site-scoped orders, stores, proposals, and business workspaces. The producer prepares and reviews plans; it claims no provider execution authority."
---

{/* Generated by scripts/gen-sdk-docs.py from surface.json -- do not hand-edit. */}

> **Private access.** Hosted Tempera services are available through design-partner onboarding.
> Use the issuer, credentials, environment, and product URLs provisioned for your workspace;
> start in staging unless Tempera explicitly approves another target.

> **Contract inventory.** This typed reference is retained for SDK compatibility.
> It is not part of the primary public workflow or evidence of general service availability.

Declared merchant order recovery: site-scoped orders, stores, proposals, and business workspaces. The producer prepares and reviews plans; it claims no provider execution authority.

| | |
|---|---|
| Repository | [tempera-dropshipping](https://github.com/tempera-dev/tempera-dropshipping) |
| Base URL env var | `TEMPERA_DROPSHIPPING_URL` (see [Environments](/environments)) |
| Audience | `tempera-dropshipping` |
| Auth | OAuth bearer for audience tempera-dropshipping; orders:read for reads and orders:commerce:write for catalog-offer/sale-order creation. Legacy orders:write and orders:approve operations remain blocked on central scope registration. |
| Client | TypeScript `client.temperaDropshipping` · Python `client.tempera_dropshipping` · Rust `build_request("tempera_dropshipping", ...)` |
| Typed operations | 31 |

Anything this page does not cover is reachable through the raw passthrough:
`client.temperaDropshipping.request(path, options)` (TypeScript) /
`client.tempera_dropshipping.request(path, ...)` (Python).

## Operations

### createStore

`POST /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/stores`

Create Store.

- **Producer operationId:** `createStore`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:write` scope.
- **Scope:** `orders:write`
- **Call as:** TypeScript `client.temperaDropshipping.createStore()` · Python `client.tempera_dropshipping.create_store()` · Rust `build_request("tempera_dropshipping", "create_store", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `id` | yes | — |
| `displayName` | yes | — |
| `channel` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.createStore({
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
  id: "<id>",
  displayName: "<displayName>",
  channel: "<channel>",
});
```

```python Python
result = client.tempera_dropshipping.create_store({
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
    "id": "<id>",
    "displayName": "<displayName>",
    "channel": "<channel>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "create_store", &[
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
    ("id", "<id>".into()),
    ("displayName", "<displayName>".into()),
    ("channel", "<channel>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listStores

`GET /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/stores`

List Stores.

- **Producer operationId:** `listStores`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:read` scope.
- **Scope:** `orders:read`
- **Call as:** TypeScript `client.temperaDropshipping.listStores()` · Python `client.tempera_dropshipping.list_stores()` · Rust `build_request("tempera_dropshipping", "list_stores", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageToken` | no | — |
| `pageSize` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.listStores({
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
  pageToken: "<pageToken>",
  pageSize: "<pageSize>",
});
```

```python Python
result = client.tempera_dropshipping.list_stores({
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
    "pageToken": "<pageToken>",
    "pageSize": "<pageSize>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "list_stores", &[
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
    ("pageToken", "<pageToken>".into()),
    ("pageSize", "<pageSize>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getStore

`GET /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/stores/{storeId}`

Get Store.

- **Producer operationId:** `getStore`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:read` scope.
- **Scope:** `orders:read`
- **Call as:** TypeScript `client.temperaDropshipping.getStore()` · Python `client.tempera_dropshipping.get_store()` · Rust `build_request("tempera_dropshipping", "get_store", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `storeId` | yes | Substituted into the URL path (percent-encoded). |
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.getStore({
  storeId: "<storeId>",
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
});
```

```python Python
result = client.tempera_dropshipping.get_store({
    "storeId": "<storeId>",
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "get_store", &[
    ("storeId", "<storeId>".into()),
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createCatalogOffer

`POST /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/catalog/offers`

Create Catalog Offer.

- **Producer operationId:** `createCatalogOffer`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:commerce:write` scope.
- **Scope:** `orders:commerce:write`
- **Call as:** TypeScript `client.temperaDropshipping.createCatalogOffer()` · Python `client.tempera_dropshipping.create_catalog_offer()` · Rust `build_request("tempera_dropshipping", "create_catalog_offer", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `merchantId` | yes | — |
| `productClassification` | yes | — |
| `name` | yes | — |
| `description` | yes | — |
| `photoUrl` | no | — |
| `currency` | yes | — |
| `unitAmountMinor` | yes | — |
| `expiresAt` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.createCatalogOffer({
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
  merchantId: "<merchantId>",
  productClassification: "<productClassification>",
  name: "<name>",
  description: "<description>",
  currency: "<currency>",
  unitAmountMinor: "<unitAmountMinor>",
});
```

```python Python
result = client.tempera_dropshipping.create_catalog_offer({
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
    "merchantId": "<merchantId>",
    "productClassification": "<productClassification>",
    "name": "<name>",
    "description": "<description>",
    "currency": "<currency>",
    "unitAmountMinor": "<unitAmountMinor>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "create_catalog_offer", &[
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
    ("merchantId", "<merchantId>".into()),
    ("productClassification", "<productClassification>".into()),
    ("name", "<name>".into()),
    ("description", "<description>".into()),
    ("currency", "<currency>".into()),
    ("unitAmountMinor", "<unitAmountMinor>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listCatalogOffers

`GET /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/catalog/offers`

List Catalog Offers.

- **Producer operationId:** `listCatalogOffers`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:read` scope.
- **Scope:** `orders:read`
- **Call as:** TypeScript `client.temperaDropshipping.listCatalogOffers()` · Python `client.tempera_dropshipping.list_catalog_offers()` · Rust `build_request("tempera_dropshipping", "list_catalog_offers", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageToken` | no | — |
| `pageSize` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.listCatalogOffers({
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
  pageToken: "<pageToken>",
  pageSize: "<pageSize>",
});
```

```python Python
result = client.tempera_dropshipping.list_catalog_offers({
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
    "pageToken": "<pageToken>",
    "pageSize": "<pageSize>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "list_catalog_offers", &[
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
    ("pageToken", "<pageToken>".into()),
    ("pageSize", "<pageSize>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getCatalogOffer

`GET /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/catalog/offers/{offerId}`

Get Catalog Offer.

- **Producer operationId:** `getCatalogOffer`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:read` scope.
- **Scope:** `orders:read`
- **Call as:** TypeScript `client.temperaDropshipping.getCatalogOffer()` · Python `client.tempera_dropshipping.get_catalog_offer()` · Rust `build_request("tempera_dropshipping", "get_catalog_offer", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `offerId` | yes | Substituted into the URL path (percent-encoded). |
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.getCatalogOffer({
  offerId: "<offerId>",
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
});
```

```python Python
result = client.tempera_dropshipping.get_catalog_offer({
    "offerId": "<offerId>",
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "get_catalog_offer", &[
    ("offerId", "<offerId>".into()),
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createSaleOrder

`POST /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/sale-orders`

Create Sale Order.

- **Producer operationId:** `createSaleOrder`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:commerce:write` scope.
- **Scope:** `orders:commerce:write`
- **Call as:** TypeScript `client.temperaDropshipping.createSaleOrder()` · Python `client.tempera_dropshipping.create_sale_order()` · Rust `build_request("tempera_dropshipping", "create_sale_order", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `offerId` | yes | — |
| `offerRevision` | yes | — |
| `quantity` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.createSaleOrder({
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
  offerId: "<offerId>",
  offerRevision: "<offerRevision>",
  quantity: "<quantity>",
});
```

```python Python
result = client.tempera_dropshipping.create_sale_order({
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
    "offerId": "<offerId>",
    "offerRevision": "<offerRevision>",
    "quantity": "<quantity>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "create_sale_order", &[
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
    ("offerId", "<offerId>".into()),
    ("offerRevision", "<offerRevision>".into()),
    ("quantity", "<quantity>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listSaleOrders

`GET /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/sale-orders`

List Sale Orders.

- **Producer operationId:** `listSaleOrders`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:read` scope.
- **Scope:** `orders:read`
- **Call as:** TypeScript `client.temperaDropshipping.listSaleOrders()` · Python `client.tempera_dropshipping.list_sale_orders()` · Rust `build_request("tempera_dropshipping", "list_sale_orders", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageToken` | no | — |
| `pageSize` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.listSaleOrders({
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
  pageToken: "<pageToken>",
  pageSize: "<pageSize>",
});
```

```python Python
result = client.tempera_dropshipping.list_sale_orders({
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
    "pageToken": "<pageToken>",
    "pageSize": "<pageSize>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "list_sale_orders", &[
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
    ("pageToken", "<pageToken>".into()),
    ("pageSize", "<pageSize>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getSaleOrder

`GET /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/sale-orders/{orderId}`

Get Sale Order.

- **Producer operationId:** `getSaleOrder`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:read` scope.
- **Scope:** `orders:read`
- **Call as:** TypeScript `client.temperaDropshipping.getSaleOrder()` · Python `client.tempera_dropshipping.get_sale_order()` · Rust `build_request("tempera_dropshipping", "get_sale_order", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `orderId` | yes | Substituted into the URL path (percent-encoded). |
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.getSaleOrder({
  orderId: "<orderId>",
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
});
```

```python Python
result = client.tempera_dropshipping.get_sale_order({
    "orderId": "<orderId>",
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "get_sale_order", &[
    ("orderId", "<orderId>".into()),
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listEvents

`GET /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/events`

List Events.

- **Producer operationId:** `listEvents`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:read` scope.
- **Scope:** `orders:read`
- **Call as:** TypeScript `client.temperaDropshipping.listEvents()` · Python `client.tempera_dropshipping.list_events()` · Rust `build_request("tempera_dropshipping", "list_events", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageToken` | no | — |
| `pageSize` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.listEvents({
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
  pageToken: "<pageToken>",
  pageSize: "<pageSize>",
});
```

```python Python
result = client.tempera_dropshipping.list_events({
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
    "pageToken": "<pageToken>",
    "pageSize": "<pageSize>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "list_events", &[
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
    ("pageToken", "<pageToken>".into()),
    ("pageSize", "<pageSize>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listOrderEventProjections

`GET /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/event-projections`

List Event Projections.

- **Producer operationId:** `listOrderEventProjections`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:read` scope.
- **Scope:** `orders:read`
- **Call as:** TypeScript `client.temperaDropshipping.listOrderEventProjections()` · Python `client.tempera_dropshipping.list_order_event_projections()` · Rust `build_request("tempera_dropshipping", "list_order_event_projections", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageToken` | no | — |
| `pageSize` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.listOrderEventProjections({
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
  pageToken: "<pageToken>",
  pageSize: "<pageSize>",
});
```

```python Python
result = client.tempera_dropshipping.list_order_event_projections({
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
    "pageToken": "<pageToken>",
    "pageSize": "<pageSize>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "list_order_event_projections", &[
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
    ("pageToken", "<pageToken>".into()),
    ("pageSize", "<pageSize>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listOrders

`GET /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/orders`

List Orders.

- **Producer operationId:** `listOrders`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:read` scope.
- **Scope:** `orders:read`
- **Call as:** TypeScript `client.temperaDropshipping.listOrders()` · Python `client.tempera_dropshipping.list_orders()` · Rust `build_request("tempera_dropshipping", "list_orders", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageToken` | no | — |
| `pageSize` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.listOrders({
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
  pageToken: "<pageToken>",
  pageSize: "<pageSize>",
});
```

```python Python
result = client.tempera_dropshipping.list_orders({
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
    "pageToken": "<pageToken>",
    "pageSize": "<pageSize>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "list_orders", &[
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
    ("pageToken", "<pageToken>".into()),
    ("pageSize", "<pageSize>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createOrder

`POST /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/orders`

Create Order.

- **Producer operationId:** `createOrder`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:write` scope.
- **Scope:** `orders:write`
- **Call as:** TypeScript `client.temperaDropshipping.createOrder()` · Python `client.tempera_dropshipping.create_order()` · Rust `build_request("tempera_dropshipping", "create_order", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `expectedRevision` | no | — |
| `record` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.createOrder({
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
  record: "<record>",
  expectedRevision: "<expectedRevision>",
});
```

```python Python
result = client.tempera_dropshipping.create_order({
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
    "record": "<record>",
    "expectedRevision": "<expectedRevision>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "create_order", &[
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
    ("record", "<record>".into()),
    ("expectedRevision", "<expectedRevision>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listInbox

`GET /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/inbox`

Inbox.

- **Producer operationId:** `listInbox`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:read` scope.
- **Scope:** `orders:read`
- **Call as:** TypeScript `client.temperaDropshipping.listInbox()` · Python `client.tempera_dropshipping.list_inbox()` · Rust `build_request("tempera_dropshipping", "list_inbox", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageToken` | no | — |
| `pageSize` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.listInbox({
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
  pageToken: "<pageToken>",
  pageSize: "<pageSize>",
});
```

```python Python
result = client.tempera_dropshipping.list_inbox({
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
    "pageToken": "<pageToken>",
    "pageSize": "<pageSize>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "list_inbox", &[
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
    ("pageToken", "<pageToken>".into()),
    ("pageSize", "<pageSize>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getBusinessSummary

`GET /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/operating-summary`

Operating Summary.

- **Producer operationId:** `getBusinessSummary`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:read` scope.
- **Scope:** `orders:read`
- **Call as:** TypeScript `client.temperaDropshipping.getBusinessSummary()` · Python `client.tempera_dropshipping.get_business_summary()` · Rust `build_request("tempera_dropshipping", "get_business_summary", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.getBusinessSummary({
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
});
```

```python Python
result = client.tempera_dropshipping.get_business_summary({
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "get_business_summary", &[
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getOrder

`GET /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/orders/{orderId}`

Get Order.

- **Producer operationId:** `getOrder`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:read` scope.
- **Scope:** `orders:read`
- **Call as:** TypeScript `client.temperaDropshipping.getOrder()` · Python `client.tempera_dropshipping.get_order()` · Rust `build_request("tempera_dropshipping", "get_order", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `orderId` | yes | Substituted into the URL path (percent-encoded). |
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.getOrder({
  orderId: "<orderId>",
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
});
```

```python Python
result = client.tempera_dropshipping.get_order({
    "orderId": "<orderId>",
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "get_order", &[
    ("orderId", "<orderId>".into()),
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### updateOrder

`PATCH /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/orders/{orderId}`

Update Order.

- **Producer operationId:** `updateOrder`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:write` scope.
- **Scope:** `orders:write`
- **Call as:** TypeScript `client.temperaDropshipping.updateOrder()` · Python `client.tempera_dropshipping.update_order()` · Rust `build_request("tempera_dropshipping", "update_order", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `orderId` | yes | Substituted into the URL path (percent-encoded). |
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `updateMask` | no | — |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `expectedRevision` | yes | — |
| `record` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.updateOrder({
  orderId: "<orderId>",
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
  expectedRevision: "<expectedRevision>",
  record: "<record>",
});
```

```python Python
result = client.tempera_dropshipping.update_order({
    "orderId": "<orderId>",
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
    "expectedRevision": "<expectedRevision>",
    "record": "<record>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "update_order", &[
    ("orderId", "<orderId>".into()),
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
    ("expectedRevision", "<expectedRevision>".into()),
    ("record", "<record>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### evaluateOrder

`POST /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/orders/{orderId}:evaluate`

Evaluate Order.

- **Producer operationId:** `evaluateOrder`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:read` scope.
- **Scope:** `orders:read`
- **Call as:** TypeScript `client.temperaDropshipping.evaluateOrder()` · Python `client.tempera_dropshipping.evaluate_order()` · Rust `build_request("tempera_dropshipping", "evaluate_order", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `orderId` | yes | Substituted into the URL path (percent-encoded). |
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `expectedRevision` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.evaluateOrder({
  orderId: "<orderId>",
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
  expectedRevision: "<expectedRevision>",
});
```

```python Python
result = client.tempera_dropshipping.evaluate_order({
    "orderId": "<orderId>",
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
    "expectedRevision": "<expectedRevision>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "evaluate_order", &[
    ("orderId", "<orderId>".into()),
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
    ("expectedRevision", "<expectedRevision>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### prepareProposal

`POST /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/orders/{orderId}/proposals`

Prepare.

- **Producer operationId:** `prepareProposal`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:write` scope.
- **Scope:** `orders:write`
- **Call as:** TypeScript `client.temperaDropshipping.prepareProposal()` · Python `client.tempera_dropshipping.prepare_proposal()` · Rust `build_request("tempera_dropshipping", "prepare_proposal", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `orderId` | yes | Substituted into the URL path (percent-encoded). |
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `expectedRevision` | yes | — |
| `quoteId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.prepareProposal({
  orderId: "<orderId>",
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
  expectedRevision: "<expectedRevision>",
  quoteId: "<quoteId>",
});
```

```python Python
result = client.tempera_dropshipping.prepare_proposal({
    "orderId": "<orderId>",
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
    "expectedRevision": "<expectedRevision>",
    "quoteId": "<quoteId>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "prepare_proposal", &[
    ("orderId", "<orderId>".into()),
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
    ("expectedRevision", "<expectedRevision>".into()),
    ("quoteId", "<quoteId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getProposal

`GET /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/proposals/{proposalId}`

Get Proposal.

- **Producer operationId:** `getProposal`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:read` scope.
- **Scope:** `orders:read`
- **Call as:** TypeScript `client.temperaDropshipping.getProposal()` · Python `client.tempera_dropshipping.get_proposal()` · Rust `build_request("tempera_dropshipping", "get_proposal", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `proposalId` | yes | Substituted into the URL path (percent-encoded). |
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.getProposal({
  proposalId: "<proposalId>",
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
});
```

```python Python
result = client.tempera_dropshipping.get_proposal({
    "proposalId": "<proposalId>",
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "get_proposal", &[
    ("proposalId", "<proposalId>".into()),
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### approveProposal

`POST /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/proposals/{proposalId}:approve`

Approve.

- **Producer operationId:** `approveProposal`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:approve` scope.
- **Scope:** `orders:approve`
- **Call as:** TypeScript `client.temperaDropshipping.approveProposal()` · Python `client.tempera_dropshipping.approve_proposal()` · Rust `build_request("tempera_dropshipping", "approve_proposal", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `proposalId` | yes | Substituted into the URL path (percent-encoded). |
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `expectedRevision` | yes | — |
| `proposalDigest` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.approveProposal({
  proposalId: "<proposalId>",
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
  expectedRevision: "<expectedRevision>",
  proposalDigest: "<proposalDigest>",
});
```

```python Python
result = client.tempera_dropshipping.approve_proposal({
    "proposalId": "<proposalId>",
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
    "expectedRevision": "<expectedRevision>",
    "proposalDigest": "<proposalDigest>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "approve_proposal", &[
    ("proposalId", "<proposalId>".into()),
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
    ("expectedRevision", "<expectedRevision>".into()),
    ("proposalDigest", "<proposalDigest>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### executeProposal

`POST /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/proposals/{proposalId}:execute`

Execute.

- **Producer operationId:** `executeProposal`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:approve` scope.
- **Scope:** `orders:approve`
- **Call as:** TypeScript `client.temperaDropshipping.executeProposal()` · Python `client.tempera_dropshipping.execute_proposal()` · Rust `build_request("tempera_dropshipping", "execute_proposal", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `proposalId` | yes | Substituted into the URL path (percent-encoded). |
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `expectedRevision` | yes | — |
| `proposalDigest` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.executeProposal({
  proposalId: "<proposalId>",
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
  expectedRevision: "<expectedRevision>",
  proposalDigest: "<proposalDigest>",
});
```

```python Python
result = client.tempera_dropshipping.execute_proposal({
    "proposalId": "<proposalId>",
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
    "expectedRevision": "<expectedRevision>",
    "proposalDigest": "<proposalDigest>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "execute_proposal", &[
    ("proposalId", "<proposalId>".into()),
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
    ("expectedRevision", "<expectedRevision>".into()),
    ("proposalDigest", "<proposalDigest>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### recordManualOutcome

`POST /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/proposals/{proposalId}/manual-outcome`

Outcome.

- **Producer operationId:** `recordManualOutcome`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:write` scope.
- **Scope:** `orders:write`
- **Call as:** TypeScript `client.temperaDropshipping.recordManualOutcome()` · Python `client.tempera_dropshipping.record_manual_outcome()` · Rust `build_request("tempera_dropshipping", "record_manual_outcome", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `proposalId` | yes | Substituted into the URL path (percent-encoded). |
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `expectedRevision` | yes | — |
| `proposalDigest` | yes | — |
| `outcome` | yes | — |
| `evidenceRef` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.recordManualOutcome({
  proposalId: "<proposalId>",
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
  expectedRevision: "<expectedRevision>",
  proposalDigest: "<proposalDigest>",
  outcome: "<outcome>",
  evidenceRef: "<evidenceRef>",
});
```

```python Python
result = client.tempera_dropshipping.record_manual_outcome({
    "proposalId": "<proposalId>",
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
    "expectedRevision": "<expectedRevision>",
    "proposalDigest": "<proposalDigest>",
    "outcome": "<outcome>",
    "evidenceRef": "<evidenceRef>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "record_manual_outcome", &[
    ("proposalId", "<proposalId>".into()),
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
    ("expectedRevision", "<expectedRevision>".into()),
    ("proposalDigest", "<proposalDigest>".into()),
    ("outcome", "<outcome>".into()),
    ("evidenceRef", "<evidenceRef>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### reconcileOrder

`POST /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/orders/{orderId}:reconcile`

Reconcile.

- **Producer operationId:** `reconcileOrder`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:approve` scope.
- **Scope:** `orders:approve`
- **Call as:** TypeScript `client.temperaDropshipping.reconcileOrder()` · Python `client.tempera_dropshipping.reconcile_order()` · Rust `build_request("tempera_dropshipping", "reconcile_order", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `orderId` | yes | Substituted into the URL path (percent-encoded). |
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `expectedRevision` | yes | — |
| `purchaseState` | yes | — |
| `evidenceRef` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.reconcileOrder({
  orderId: "<orderId>",
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
  expectedRevision: "<expectedRevision>",
  purchaseState: "<purchaseState>",
  evidenceRef: "<evidenceRef>",
});
```

```python Python
result = client.tempera_dropshipping.reconcile_order({
    "orderId": "<orderId>",
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
    "expectedRevision": "<expectedRevision>",
    "purchaseState": "<purchaseState>",
    "evidenceRef": "<evidenceRef>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "reconcile_order", &[
    ("orderId", "<orderId>".into()),
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
    ("expectedRevision", "<expectedRevision>".into()),
    ("purchaseState", "<purchaseState>".into()),
    ("evidenceRef", "<evidenceRef>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listOrderAuditEvents

`GET /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/orders/{orderId}/audit`

Audit.

- **Producer operationId:** `listOrderAuditEvents`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:read` scope.
- **Scope:** `orders:read`
- **Call as:** TypeScript `client.temperaDropshipping.listOrderAuditEvents()` · Python `client.tempera_dropshipping.list_order_audit_events()` · Rust `build_request("tempera_dropshipping", "list_order_audit_events", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `orderId` | yes | Substituted into the URL path (percent-encoded). |
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageToken` | no | — |
| `pageSize` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.listOrderAuditEvents({
  orderId: "<orderId>",
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
  pageToken: "<pageToken>",
  pageSize: "<pageSize>",
});
```

```python Python
result = client.tempera_dropshipping.list_order_audit_events({
    "orderId": "<orderId>",
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
    "pageToken": "<pageToken>",
    "pageSize": "<pageSize>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "list_order_audit_events", &[
    ("orderId", "<orderId>".into()),
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
    ("pageToken", "<pageToken>".into()),
    ("pageSize", "<pageSize>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### updateBusinessWorkspace

`POST /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/business-workspaces`

Put Workspace.

- **Producer operationId:** `updateBusinessWorkspace`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:write` scope.
- **Scope:** `orders:write`
- **Call as:** TypeScript `client.temperaDropshipping.updateBusinessWorkspace()` · Python `client.tempera_dropshipping.update_business_workspace()` · Rust `build_request("tempera_dropshipping", "update_business_workspace", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `expectedRevision` | yes | — |
| `profileRef` | yes | — |
| `profileRevision` | yes | — |
| `idempotencyKey` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.updateBusinessWorkspace({
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
  expectedRevision: "<expectedRevision>",
  profileRef: "<profileRef>",
  profileRevision: "<profileRevision>",
});
```

```python Python
result = client.tempera_dropshipping.update_business_workspace({
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
    "expectedRevision": "<expectedRevision>",
    "profileRef": "<profileRef>",
    "profileRevision": "<profileRevision>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "update_business_workspace", &[
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
    ("expectedRevision", "<expectedRevision>".into()),
    ("profileRef", "<profileRef>".into()),
    ("profileRevision", "<profileRevision>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### prepareBusinessTask

`POST /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/business-workspaces/{workspaceId}/tasks/{taskId}:prepare`

Prepare Task.

- **Producer operationId:** `prepareBusinessTask`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:write` scope.
- **Scope:** `orders:write`
- **Call as:** TypeScript `client.temperaDropshipping.prepareBusinessTask()` · Python `client.tempera_dropshipping.prepare_business_task()` · Rust `build_request("tempera_dropshipping", "prepare_business_task", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `workspaceId` | yes | Substituted into the URL path (percent-encoded). |
| `taskId` | yes | Substituted into the URL path (percent-encoded). |
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `expectedRevision` | yes | — |
| `draft` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.prepareBusinessTask({
  workspaceId: "<workspaceId>",
  taskId: "<taskId>",
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
  expectedRevision: "<expectedRevision>",
  draft: "<draft>",
});
```

```python Python
result = client.tempera_dropshipping.prepare_business_task({
    "workspaceId": "<workspaceId>",
    "taskId": "<taskId>",
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
    "expectedRevision": "<expectedRevision>",
    "draft": "<draft>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "prepare_business_task", &[
    ("workspaceId", "<workspaceId>".into()),
    ("taskId", "<taskId>".into()),
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
    ("expectedRevision", "<expectedRevision>".into()),
    ("draft", "<draft>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### reviewBusinessTask

`POST /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/business-workspaces/{workspaceId}/tasks/{taskId}:review`

Review Task.

- **Producer operationId:** `reviewBusinessTask`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:approve` scope.
- **Scope:** `orders:approve`
- **Call as:** TypeScript `client.temperaDropshipping.reviewBusinessTask()` · Python `client.tempera_dropshipping.review_business_task()` · Rust `build_request("tempera_dropshipping", "review_business_task", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `workspaceId` | yes | Substituted into the URL path (percent-encoded). |
| `taskId` | yes | Substituted into the URL path (percent-encoded). |
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `expectedRevision` | yes | — |
| `review` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.reviewBusinessTask({
  workspaceId: "<workspaceId>",
  taskId: "<taskId>",
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
  expectedRevision: "<expectedRevision>",
  review: "<review>",
});
```

```python Python
result = client.tempera_dropshipping.review_business_task({
    "workspaceId": "<workspaceId>",
    "taskId": "<taskId>",
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
    "expectedRevision": "<expectedRevision>",
    "review": "<review>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "review_business_task", &[
    ("workspaceId", "<workspaceId>".into()),
    ("taskId", "<taskId>".into()),
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
    ("expectedRevision", "<expectedRevision>".into()),
    ("review", "<review>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### recordBusinessTaskReceipt

`POST /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/business-workspaces/{workspaceId}/tasks/{taskId}/declared-receipts`

Record Task Receipt.

- **Producer operationId:** `recordBusinessTaskReceipt`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:write` scope.
- **Scope:** `orders:write`
- **Call as:** TypeScript `client.temperaDropshipping.recordBusinessTaskReceipt()` · Python `client.tempera_dropshipping.record_business_task_receipt()` · Rust `build_request("tempera_dropshipping", "record_business_task_receipt", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `workspaceId` | yes | Substituted into the URL path (percent-encoded). |
| `taskId` | yes | Substituted into the URL path (percent-encoded). |
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `expectedRevision` | yes | — |
| `receipt` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.recordBusinessTaskReceipt({
  workspaceId: "<workspaceId>",
  taskId: "<taskId>",
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
  expectedRevision: "<expectedRevision>",
  receipt: "<receipt>",
});
```

```python Python
result = client.tempera_dropshipping.record_business_task_receipt({
    "workspaceId": "<workspaceId>",
    "taskId": "<taskId>",
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
    "expectedRevision": "<expectedRevision>",
    "receipt": "<receipt>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "record_business_task_receipt", &[
    ("workspaceId", "<workspaceId>".into()),
    ("taskId", "<taskId>".into()),
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
    ("expectedRevision", "<expectedRevision>".into()),
    ("receipt", "<receipt>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getBusinessWorkspace

`GET /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/business-workspaces/{workspaceId}`

Get Workspace.

- **Producer operationId:** `getBusinessWorkspace`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:read` scope.
- **Scope:** `orders:read`
- **Call as:** TypeScript `client.temperaDropshipping.getBusinessWorkspace()` · Python `client.tempera_dropshipping.get_business_workspace()` · Rust `build_request("tempera_dropshipping", "get_business_workspace", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `workspaceId` | yes | Substituted into the URL path (percent-encoded). |
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.getBusinessWorkspace({
  workspaceId: "<workspaceId>",
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
});
```

```python Python
result = client.tempera_dropshipping.get_business_workspace({
    "workspaceId": "<workspaceId>",
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "get_business_workspace", &[
    ("workspaceId", "<workspaceId>".into()),
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### prepareBusinessBrowserTask

`POST /v1/organizations/{organization}/projects/{project}/environments/{environment}/sites/{site}/business-workspaces/{workspaceId}/tasks/{taskId}:prepareBrowser`

Browser Task.

- **Producer operationId:** `prepareBusinessBrowserTask`
- **Auth:** OAuth access token for audience `tempera-dropshipping`. Central `tp_` API keys cannot carry the required `orders:write` scope.
- **Scope:** `orders:write`
- **Call as:** TypeScript `client.temperaDropshipping.prepareBusinessBrowserTask()` · Python `client.tempera_dropshipping.prepare_business_browser_task()` · Rust `build_request("tempera_dropshipping", "prepare_business_browser_task", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `workspaceId` | yes | Substituted into the URL path (percent-encoded). |
| `taskId` | yes | Substituted into the URL path (percent-encoded). |
| `organization` | yes | Substituted into the URL path (percent-encoded). |
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `environment` | yes | Substituted into the URL path (percent-encoded). |
| `site` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `expectedRevision` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDropshipping.prepareBusinessBrowserTask({
  workspaceId: "<workspaceId>",
  taskId: "<taskId>",
  organization: "<organization>",
  project: "<project>",
  environment: "<environment>",
  site: "<site>",
  expectedRevision: "<expectedRevision>",
});
```

```python Python
result = client.tempera_dropshipping.prepare_business_browser_task({
    "workspaceId": "<workspaceId>",
    "taskId": "<taskId>",
    "organization": "<organization>",
    "project": "<project>",
    "environment": "<environment>",
    "site": "<site>",
    "expectedRevision": "<expectedRevision>",
})
```

```rust Rust
let spec = client.build_request("tempera_dropshipping", "prepare_business_browser_task", &[
    ("workspaceId", "<workspaceId>".into()),
    ("taskId", "<taskId>".into()),
    ("organization", "<organization>".into()),
    ("project", "<project>".into()),
    ("environment", "<environment>".into()),
    ("site", "<site>".into()),
    ("expectedRevision", "<expectedRevision>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>
