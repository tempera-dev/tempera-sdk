---
title: "tempera-document"
description: "Document processing service with immutable uploads, globally anchored graph provenance, extraction, retrieval, and long-running operations. A hosted URL must be supplied explicitly until a deployed environment is registered."
---

{/* Generated by scripts/gen-sdk-docs.py from surface.json -- do not hand-edit. */}

> **Private access.** Hosted Tempera services are available through design-partner onboarding.
> Use the issuer, credentials, environment, and product URLs provisioned for your workspace;
> start in staging unless Tempera explicitly approves another target.

> **Contract inventory.** This typed reference is retained for SDK compatibility.
> It is not part of the primary public workflow or evidence of general service availability.

Document processing service with immutable uploads, globally anchored graph provenance, extraction, retrieval, and long-running operations. A hosted URL must be supplied explicitly until a deployed environment is registered.

| | |
|---|---|
| Repository | [tempera-document](https://github.com/tempera-dev/tempera-document) |
| Base URL env var | `TEMPERA_DOCUMENT_URL` (see [Environments](/environments)) |
| Audience | `tempera-document` |
| Auth | bearer minted for audience tempera-document with document:read/document:write, or a central tp_ API key |
| Client | TypeScript `client.temperaDocument` · Python `client.tempera_document` · Rust `build_request("tempera_document", ...)` |
| Typed operations | 14 |

Anything this page does not cover is reachable through the raw passthrough:
`client.temperaDocument.request(path, options)` (TypeScript) /
`client.tempera_document.request(path, ...)` (Python).

## Operations

### healthGet

`GET /v1/health`

Health check.

- **Producer operationId:** `health.get`
- **Auth:** Product bearer — an access token minted for audience `tempera-document`, or a central `tp_` API key.
- **Call as:** TypeScript `client.temperaDocument.healthGet()` · Python `client.tempera_document.health_get()` · Rust `build_request("tempera_document", "health_get", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDocument.healthGet();
```

```python Python
result = client.tempera_document.health_get()
```

```rust Rust
let spec = client.build_request("tempera_document", "health_get", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### documentsCreate

`POST /v1/projects/{project_id}/documents`

Create an immutable document version from a completed upload.

- **Producer operationId:** `documents.create`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-document`.
- **Scope:** `document:write`
- **Call as:** TypeScript `client.temperaDocument.documentsCreate()` · Python `client.tempera_document.documents_create()` · Rust `build_request("tempera_document", "documents_create", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project_id` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `upload` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDocument.documentsCreate({
  project_id: "<project_id>",
  upload: "<upload>",
});
```

```python Python
result = client.tempera_document.documents_create({
    "project_id": "<project_id>",
    "upload": "<upload>",
})
```

```rust Rust
let spec = client.build_request("tempera_document", "documents_create", &[
    ("project_id", "<project_id>".into()),
    ("upload", "<upload>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### documentsGet

`GET /v1/projects/{project_id}/documents/{document_id}`

Get a document.

- **Producer operationId:** `documents.get`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-document`.
- **Scope:** `document:read`
- **Call as:** TypeScript `client.temperaDocument.documentsGet()` · Python `client.tempera_document.documents_get()` · Rust `build_request("tempera_document", "documents_get", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project_id` | yes | Substituted into the URL path (percent-encoded). |
| `document_id` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDocument.documentsGet({
  project_id: "<project_id>",
  document_id: "<document_id>",
});
```

```python Python
result = client.tempera_document.documents_get({
    "project_id": "<project_id>",
    "document_id": "<document_id>",
})
```

```rust Rust
let spec = client.build_request("tempera_document", "documents_get", &[
    ("project_id", "<project_id>".into()),
    ("document_id", "<document_id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### documentsExtract

`POST /v1/projects/{project_id}/documents/{document_id}/extractions`

Extract one caller-schema object with exact source evidence.

- **Producer operationId:** `documents.extract`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-document`.
- **Scope:** `document:write`
- **Call as:** TypeScript `client.temperaDocument.documentsExtract()` · Python `client.tempera_document.documents_extract()` · Rust `build_request("tempera_document", "documents_extract", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project_id` | yes | Substituted into the URL path (percent-encoded). |
| `document_id` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `instructions` | yes | — |
| `max_output_tokens` | no | — |
| `schema` | yes | — |
| `schema_name` | yes | — |
| `unit_indexes` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDocument.documentsExtract({
  project_id: "<project_id>",
  document_id: "<document_id>",
  instructions: "<instructions>",
  schema: "<schema>",
  schema_name: "<schema_name>",
});
```

```python Python
result = client.tempera_document.documents_extract({
    "project_id": "<project_id>",
    "document_id": "<document_id>",
    "instructions": "<instructions>",
    "schema": "<schema>",
    "schema_name": "<schema_name>",
})
```

```rust Rust
let spec = client.build_request("tempera_document", "documents_extract", &[
    ("project_id", "<project_id>".into()),
    ("document_id", "<document_id>".into()),
    ("instructions", "<instructions>".into()),
    ("schema", "<schema>".into()),
    ("schema_name", "<schema_name>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### documentsGetGraph

`GET /v1/projects/{project_id}/documents/{document_id}/graph`

Return the latest successful canonical document graph.

- **Producer operationId:** `documents.getGraph`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-document`.
- **Scope:** `document:read`
- **Call as:** TypeScript `client.temperaDocument.documentsGetGraph()` · Python `client.tempera_document.documents_get_graph()` · Rust `build_request("tempera_document", "documents_get_graph", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project_id` | yes | Substituted into the URL path (percent-encoded). |
| `document_id` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDocument.documentsGetGraph({
  project_id: "<project_id>",
  document_id: "<document_id>",
});
```

```python Python
result = client.tempera_document.documents_get_graph({
    "project_id": "<project_id>",
    "document_id": "<document_id>",
})
```

```rust Rust
let spec = client.build_request("tempera_document", "documents_get_graph", &[
    ("project_id", "<project_id>".into()),
    ("document_id", "<document_id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### documentsPrepareRetrieval

`POST /v1/projects/{project_id}/documents/{document_id}/retrievalIndexes`

Build or reuse a qualified immutable hybrid-retrieval index.

- **Producer operationId:** `documents.prepareRetrieval`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-document`.
- **Scope:** `document:write`
- **Call as:** TypeScript `client.temperaDocument.documentsPrepareRetrieval()` · Python `client.tempera_document.documents_prepare_retrieval()` · Rust `build_request("tempera_document", "documents_prepare_retrieval", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project_id` | yes | Substituted into the URL path (percent-encoded). |
| `document_id` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `kind` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDocument.documentsPrepareRetrieval({
  project_id: "<project_id>",
  document_id: "<document_id>",
  kind: "<kind>",
});
```

```python Python
result = client.tempera_document.documents_prepare_retrieval({
    "project_id": "<project_id>",
    "document_id": "<document_id>",
    "kind": "<kind>",
})
```

```rust Rust
let spec = client.build_request("tempera_document", "documents_prepare_retrieval", &[
    ("project_id", "<project_id>".into()),
    ("document_id", "<document_id>".into()),
    ("kind", "<kind>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### documentsSearch

`POST /v1/projects/{project_id}/documents/{document_id}/searches`

Search the latest immutable graph and return grounded source excerpts.

- **Producer operationId:** `documents.search`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-document`.
- **Scope:** `document:read`
- **Call as:** TypeScript `client.temperaDocument.documentsSearch()` · Python `client.tempera_document.documents_search()` · Rust `build_request("tempera_document", "documents_search", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project_id` | yes | Substituted into the URL path (percent-encoded). |
| `document_id` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `query` | yes | — |
| `strategy` | no | — |
| `top_k` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDocument.documentsSearch({
  project_id: "<project_id>",
  document_id: "<document_id>",
  query: "<query>",
  strategy: "<strategy>",
  top_k: "<top_k>",
});
```

```python Python
result = client.tempera_document.documents_search({
    "project_id": "<project_id>",
    "document_id": "<document_id>",
    "query": "<query>",
    "strategy": "<strategy>",
    "top_k": "<top_k>",
})
```

```rust Rust
let spec = client.build_request("tempera_document", "documents_search", &[
    ("project_id", "<project_id>".into()),
    ("document_id", "<document_id>".into()),
    ("query", "<query>".into()),
    ("strategy", "<strategy>".into()),
    ("top_k", "<top_k>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### documentUnitsList

`GET /v1/projects/{project_id}/documents/{document_id}/units`

List logical units without materializing the complete document graph.

- **Producer operationId:** `documentUnits.list`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-document`.
- **Scope:** `document:read`
- **Call as:** TypeScript `client.temperaDocument.documentUnitsList()` · Python `client.tempera_document.document_units_list()` · Rust `build_request("tempera_document", "document_units_list", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project_id` | yes | Substituted into the URL path (percent-encoded). |
| `document_id` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `page_size` | no | — |
| `page_token` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDocument.documentUnitsList({
  project_id: "<project_id>",
  document_id: "<document_id>",
  page_size: "<page_size>",
  page_token: "<page_token>",
});
```

```python Python
result = client.tempera_document.document_units_list({
    "project_id": "<project_id>",
    "document_id": "<document_id>",
    "page_size": "<page_size>",
    "page_token": "<page_token>",
})
```

```rust Rust
let spec = client.build_request("tempera_document", "document_units_list", &[
    ("project_id", "<project_id>".into()),
    ("document_id", "<document_id>".into()),
    ("page_size", "<page_size>".into()),
    ("page_token", "<page_token>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### documentsProcess

`POST /v1/projects/{project_id}/documents/{document_id}:process`

Start an idempotent long-running processing operation.

- **Producer operationId:** `documents.process`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-document`.
- **Scope:** `document:write`
- **Call as:** TypeScript `client.temperaDocument.documentsProcess()` · Python `client.tempera_document.documents_process()` · Rust `build_request("tempera_document", "documents_process", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project_id` | yes | Substituted into the URL path (percent-encoded). |
| `document_id` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `profile` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDocument.documentsProcess({
  project_id: "<project_id>",
  document_id: "<document_id>",
  profile: "<profile>",
});
```

```python Python
result = client.tempera_document.documents_process({
    "project_id": "<project_id>",
    "document_id": "<document_id>",
    "profile": "<profile>",
})
```

```rust Rust
let spec = client.build_request("tempera_document", "documents_process", &[
    ("project_id", "<project_id>".into()),
    ("document_id", "<document_id>".into()),
    ("profile", "<profile>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### operationsGet

`GET /v1/projects/{project_id}/operations/{operation_id}`

Get a long-running operation.

- **Producer operationId:** `operations.get`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-document`.
- **Scope:** `document:read`
- **Call as:** TypeScript `client.temperaDocument.operationsGet()` · Python `client.tempera_document.operations_get()` · Rust `build_request("tempera_document", "operations_get", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project_id` | yes | Substituted into the URL path (percent-encoded). |
| `operation_id` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDocument.operationsGet({
  project_id: "<project_id>",
  operation_id: "<operation_id>",
});
```

```python Python
result = client.tempera_document.operations_get({
    "project_id": "<project_id>",
    "operation_id": "<operation_id>",
})
```

```rust Rust
let spec = client.build_request("tempera_document", "operations_get", &[
    ("project_id", "<project_id>".into()),
    ("operation_id", "<operation_id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### uploadsCreate

`POST /v1/projects/{project_id}/uploads`

Create a resumable upload resource.

- **Producer operationId:** `uploads.create`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-document`.
- **Scope:** `document:write`
- **Call as:** TypeScript `client.temperaDocument.uploadsCreate()` · Python `client.tempera_document.uploads_create()` · Rust `build_request("tempera_document", "uploads_create", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project_id` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `expected_byte_length` | no | — |
| `expected_sha256` | no | — |
| `media_type` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDocument.uploadsCreate({
  project_id: "<project_id>",
  media_type: "<media_type>",
  expected_byte_length: "<expected_byte_length>",
  expected_sha256: "<expected_sha256>",
});
```

```python Python
result = client.tempera_document.uploads_create({
    "project_id": "<project_id>",
    "media_type": "<media_type>",
    "expected_byte_length": "<expected_byte_length>",
    "expected_sha256": "<expected_sha256>",
})
```

```rust Rust
let spec = client.build_request("tempera_document", "uploads_create", &[
    ("project_id", "<project_id>".into()),
    ("media_type", "<media_type>".into()),
    ("expected_byte_length", "<expected_byte_length>".into()),
    ("expected_sha256", "<expected_sha256>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### uploadsGet

`GET /v1/projects/{project_id}/uploads/{upload_id}`

Get an upload.

- **Producer operationId:** `uploads.get`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-document`.
- **Scope:** `document:read`
- **Call as:** TypeScript `client.temperaDocument.uploadsGet()` · Python `client.tempera_document.uploads_get()` · Rust `build_request("tempera_document", "uploads_get", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project_id` | yes | Substituted into the URL path (percent-encoded). |
| `upload_id` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDocument.uploadsGet({
  project_id: "<project_id>",
  upload_id: "<upload_id>",
});
```

```python Python
result = client.tempera_document.uploads_get({
    "project_id": "<project_id>",
    "upload_id": "<upload_id>",
})
```

```rust Rust
let spec = client.build_request("tempera_document", "uploads_get", &[
    ("project_id", "<project_id>".into()),
    ("upload_id", "<upload_id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### uploadsWrite

`PUT /v1/projects/{project_id}/uploads/{upload_id}/content`

Stream source bytes to content-addressed storage.

- **Producer operationId:** `uploads.write`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-document`.
- **Scope:** `document:write`
- **Call as:** TypeScript `client.temperaDocument.uploadsWrite()` · Python `client.tempera_document.uploads_write()` · Rust `build_request("tempera_document", "uploads_write", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project_id` | yes | Substituted into the URL path (percent-encoded). |
| `upload_id` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDocument.uploadsWrite({
  project_id: "<project_id>",
  upload_id: "<upload_id>",
});
```

```python Python
result = client.tempera_document.uploads_write({
    "project_id": "<project_id>",
    "upload_id": "<upload_id>",
})
```

```rust Rust
let spec = client.build_request("tempera_document", "uploads_write", &[
    ("project_id", "<project_id>".into()),
    ("upload_id", "<upload_id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### uploadsComplete

`POST /v1/projects/{project_id}/uploads/{upload_id}:complete`

Complete a streamed upload after digest assertions.

- **Producer operationId:** `uploads.complete`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-document`.
- **Scope:** `document:write`
- **Call as:** TypeScript `client.temperaDocument.uploadsComplete()` · Python `client.tempera_document.uploads_complete()` · Rust `build_request("tempera_document", "uploads_complete", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project_id` | yes | Substituted into the URL path (percent-encoded). |
| `upload_id` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `byte_length` | no | — |
| `sha256` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaDocument.uploadsComplete({
  project_id: "<project_id>",
  upload_id: "<upload_id>",
  byte_length: "<byte_length>",
  sha256: "<sha256>",
});
```

```python Python
result = client.tempera_document.uploads_complete({
    "project_id": "<project_id>",
    "upload_id": "<upload_id>",
    "byte_length": "<byte_length>",
    "sha256": "<sha256>",
})
```

```rust Rust
let spec = client.build_request("tempera_document", "uploads_complete", &[
    ("project_id", "<project_id>".into()),
    ("upload_id", "<upload_id>".into()),
    ("byte_length", "<byte_length>".into()),
    ("sha256", "<sha256>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>
