---
title: "remi"
description: "Temporal memory server: remember, project, query, and maintain an agent memory graph."
---

{/* Generated by scripts/gen-sdk-docs.py from surface.json -- do not hand-edit. */}

> **Private access.** Hosted Tempera services are available through design-partner onboarding.
> Use the issuer, credentials, environment, and product URLs provisioned for your workspace;
> start in staging unless Tempera explicitly approves another target.

> **Contract inventory.** This typed reference is retained for SDK compatibility.
> It is not part of the primary public workflow or evidence of general service availability.

Temporal memory server: remember, project, query, and maintain an agent memory graph.

| | |
|---|---|
| Repository | [remi](https://github.com/tempera-dev/remi) |
| Base URL env var | `TEMPERA_REMI_URL` (see [Environments](/environments)) |
| Audience | `remi` |
| Auth | bearer minted for audience remi, central tp_ API key, or the memory-server static token |
| Client | TypeScript `client.remi` · Python `client.remi` · Rust `build_request("remi", ...)` |
| Typed operations | 11 |

Anything this page does not cover is reachable through the raw passthrough:
`client.remi.request(path, options)` (TypeScript) /
`client.remi.request(path, ...)` (Python).

## Operations

### livez

`GET /livez`

Check memory-server liveness.

- **Producer operationId:** `livez`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.remi.livez()` · Python `client.remi.livez()` · Rust `build_request("remi", "livez", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.remi.livez();
```

```python Python
result = client.remi.livez()
```

```rust Rust
let spec = client.build_request("remi", "livez", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### readyz

`GET /readyz`

Check memory-server readiness, including database health.

- **Producer operationId:** `readyz`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.remi.readyz()` · Python `client.remi.readyz()` · Rust `build_request("remi", "readyz", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.remi.readyz();
```

```python Python
result = client.remi.readyz()
```

```rust Rust
let spec = client.build_request("remi", "readyz", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### health

`GET /v1/health`

Fetch deep store health: schema version, integrity checks, and graph consistency.

- **Producer operationId:** `health`
- **Auth:** Product bearer — an access token minted for audience `remi`, or a central `tp_` API key.
- **Call as:** TypeScript `client.remi.health()` · Python `client.remi.health()` · Rust `build_request("remi", "health", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.remi.health();
```

```python Python
result = client.remi.health()
```

```rust Rust
let spec = client.build_request("remi", "health", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getStats

`GET /v1/stats`

Fetch memory-store statistics: ledger events, nodes, and token counts by kind.

- **Producer operationId:** `getStats`
- **Auth:** Product bearer — an access token minted for audience `remi`, or a central `tp_` API key.
- **Call as:** TypeScript `client.remi.getStats()` · Python `client.remi.get_stats()` · Rust `build_request("remi", "get_stats", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.remi.getStats();
```

```python Python
result = client.remi.get_stats()
```

```rust Rust
let spec = client.build_request("remi", "get_stats", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getMetrics

`GET /v1/metrics`

Fetch service metrics as JSON, including per-route counters and query-tier latencies.

- **Producer operationId:** `getMetrics`
- **Auth:** Product bearer — an access token minted for audience `remi`, or a central `tp_` API key.
- **Call as:** TypeScript `client.remi.getMetrics()` · Python `client.remi.get_metrics()` · Rust `build_request("remi", "get_metrics", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.remi.getMetrics();
```

```python Python
result = client.remi.get_metrics()
```

```rust Rust
let spec = client.build_request("remi", "get_metrics", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getPrometheusMetrics

`GET /v1/metrics/prometheus`

Fetch service metrics in Prometheus text exposition format for scrape-based monitoring.

- **Producer operationId:** `getPrometheusMetrics`
- **Auth:** Product bearer — an access token minted for audience `remi`, or a central `tp_` API key.
- **Call as:** TypeScript `client.remi.getPrometheusMetrics()` · Python `client.remi.get_prometheus_metrics()` · Rust `build_request("remi", "get_prometheus_metrics", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.remi.getPrometheusMetrics();
```

```python Python
result = client.remi.get_prometheus_metrics()
```

```rust Rust
let spec = client.build_request("remi", "get_prometheus_metrics", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listAudit

`GET /v1/audit`

List recent service audit events (default 100, maximum 500).

- **Producer operationId:** `listAudit`
- **Auth:** Product bearer — an access token minted for audience `remi`, or a central `tp_` API key.
- **Call as:** TypeScript `client.remi.listAudit()` · Python `client.remi.list_audit()` · Rust `build_request("remi", "list_audit", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.remi.listAudit({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.remi.list_audit({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("remi", "list_audit", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### remember

`POST /v1/remember`

Write one memory event into the tenant and project ledger.

- **Producer operationId:** `remember`
- **Auth:** Product bearer — an access token minted for audience `remi`, or a central `tp_` API key.
- **Call as:** TypeScript `client.remi.remember()` · Python `client.remi.remember()` · Rust `build_request("remi", "remember", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenant_id` | yes | — |
| `project_id` | yes | — |
| `environment_id` | no | — |
| `kind` | yes | — |
| `text` | yes | — |
| `idempotency_key` | no | — |
| `project` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.remi.remember({
  tenant_id: "<tenant_id>",
  project_id: "<project_id>",
  environment_id: "<environment_id>",
});
```

```python Python
result = client.remi.remember({
    "tenant_id": "<tenant_id>",
    "project_id": "<project_id>",
    "environment_id": "<environment_id>",
})
```

```rust Rust
let spec = client.build_request("remi", "remember", &[
    ("tenant_id", "<tenant_id>".into()),
    ("project_id", "<project_id>".into()),
    ("environment_id", "<environment_id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### project

`POST /v1/project`

Project pending ledger events into the memory graph and return the projection report.

- **Producer operationId:** `project`
- **Auth:** Product bearer — an access token minted for audience `remi`, or a central `tp_` API key.
- **Call as:** TypeScript `client.remi.project()` · Python `client.remi.project()` · Rust `build_request("remi", "project", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `limit` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.remi.project({
  limit: "<limit>",
});
```

```python Python
result = client.remi.project({
    "limit": "<limit>",
})
```

```rust Rust
let spec = client.build_request("remi", "project", &[
    ("limit", "<limit>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### query

`POST /v1/query`

Answer a scoped memory question with evidence and reconstruction metadata.

- **Producer operationId:** `query`
- **Auth:** Product bearer — an access token minted for audience `remi`, or a central `tp_` API key.
- **Call as:** TypeScript `client.remi.query()` · Python `client.remi.query()` · Rust `build_request("remi", "query", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `question` | yes | — |
| `scope` | yes | — |
| `max_tokens` | no | — |
| `require_fresh` | no | — |
| `modes` | no | — |
| `reconstruction_mode` | no | — |
| `max_reconstruction_steps` | no | — |
| `max_reconstruction_tokens` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.remi.query({
  question: "<question>",
  scope: "<scope>",
  max_tokens: "<max_tokens>",
});
```

```python Python
result = client.remi.query({
    "question": "<question>",
    "scope": "<scope>",
    "max_tokens": "<max_tokens>",
})
```

```rust Rust
let spec = client.build_request("remi", "query", &[
    ("question", "<question>".into()),
    ("scope", "<scope>".into()),
    ("max_tokens", "<max_tokens>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### maintenance

`POST /v1/maintenance`

Run store maintenance: optimize, checkpoint, and optionally vacuum, repair orphans, and prune audit history.

- **Producer operationId:** `maintenance`
- **Auth:** Product bearer — an access token minted for audience `remi`, or a central `tp_` API key.
- **Call as:** TypeScript `client.remi.maintenance()` · Python `client.remi.maintenance()` · Rust `build_request("remi", "maintenance", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `vacuum` | no | — |
| `repair_orphans` | no | — |
| `prune_audit_before_unix_ms` | no | — |
| `retain_latest_audit_events` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.remi.maintenance({
  vacuum: "<vacuum>",
  repair_orphans: "<repair_orphans>",
  prune_audit_before_unix_ms: "<prune_audit_before_unix_ms>",
});
```

```python Python
result = client.remi.maintenance({
    "vacuum": "<vacuum>",
    "repair_orphans": "<repair_orphans>",
    "prune_audit_before_unix_ms": "<prune_audit_before_unix_ms>",
})
```

```rust Rust
let spec = client.build_request("remi", "maintenance", &[
    ("vacuum", "<vacuum>".into()),
    ("repair_orphans", "<repair_orphans>".into()),
    ("prune_audit_before_unix_ms", "<prune_audit_before_unix_ms>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>
