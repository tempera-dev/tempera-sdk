---
title: "tempera-clearing"
description: "Two-phase clearing of proposed actions: a proposal is recorded, reviewed, then committed or abandoned, so an irreversible effect never rides on a single call."
---

{/* Generated by scripts/gen-sdk-docs.py from surface.json -- do not hand-edit. */}

> **Private access.** Hosted Tempera services are available through design-partner onboarding.
> Use the issuer, credentials, environment, and product URLs provisioned for your workspace;
> start in staging unless Tempera explicitly approves another target.

> **Contract inventory.** This typed reference is retained for SDK compatibility.
> It is not part of the primary public workflow or evidence of general service availability.

Two-phase clearing of proposed actions: a proposal is recorded, reviewed, then committed or abandoned, so an irreversible effect never rides on a single call.

| | |
|---|---|
| Repository | [tempera-clearing](https://github.com/tempera-dev/tempera-clearing) |
| Base URL env var | `TEMPERA_CLEARING_URL` (see [Environments](/environments)) |
| Audience | `tempera-clearing` |
| Auth | bearer minted for audience tempera-clearing with the operation's clearing:actions:read/clearing:actions:propose/clearing:actions:commit scope |
| Client | TypeScript `client.temperaClearing` · Python `client.tempera_clearing` · Rust `build_request("tempera_clearing", ...)` |
| Typed operations | 8 |

Anything this page does not cover is reachable through the raw passthrough:
`client.temperaClearing.request(path, options)` (TypeScript) /
`client.tempera_clearing.request(path, ...)` (Python).

## Operations

### getClearingHealth

`GET /healthz`

Liveness probe.

- **Producer operationId:** `getClearingHealth`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.temperaClearing.getClearingHealth()` · Python `client.tempera_clearing.get_clearing_health()` · Rust `build_request("tempera_clearing", "get_clearing_health", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.temperaClearing.getClearingHealth();
```

```python Python
result = client.tempera_clearing.get_clearing_health()
```

```rust Rust
let spec = client.build_request("tempera_clearing", "get_clearing_health", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getClearingReadiness

`GET /readyz`

Readiness probe over the contract source lock and every hard dependency.

- **Producer operationId:** `getClearingReadiness`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.temperaClearing.getClearingReadiness()` · Python `client.tempera_clearing.get_clearing_readiness()` · Rust `build_request("tempera_clearing", "get_clearing_readiness", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.temperaClearing.getClearingReadiness();
```

```python Python
result = client.tempera_clearing.get_clearing_readiness()
```

```rust Rust
let spec = client.build_request("tempera_clearing", "get_clearing_readiness", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### validateClearingAction

`POST /v1/actions:validate`

Validate a canonical action envelope without persisting or dispatching it.

- **Producer operationId:** `validateClearingAction`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-clearing`.
- **Scope:** `clearing:actions:propose`
- **Call as:** TypeScript `client.temperaClearing.validateClearingAction()` · Python `client.tempera_clearing.validate_clearing_action()` · Rust `build_request("tempera_clearing", "validate_clearing_action", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `actionId` | yes | — |
| `actor` | yes | — |
| `authorityAdmission` | yes | — |
| `causality` | yes | — |
| `evidence` | yes | — |
| `expiresAt` | yes | — |
| `idempotencyKey` | yes | — |
| `mandate` | yes | — |
| `preparedEffect` | yes | — |
| `principal` | yes | — |
| `purpose` | yes | — |
| `requestDigest` | yes | — |
| `risk` | yes | — |
| `schema` | yes | — |
| `workspace` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaClearing.validateClearingAction({
  actionId: "<actionId>",
  actor: "<actor>",
  authorityAdmission: "<authorityAdmission>",
  causality: "<causality>",
  evidence: "<evidence>",
  expiresAt: "<expiresAt>",
  idempotencyKey: "<idempotencyKey>",
  mandate: "<mandate>",
  preparedEffect: "<preparedEffect>",
  principal: "<principal>",
  purpose: "<purpose>",
  requestDigest: "<requestDigest>",
  risk: "<risk>",
  schema: "<schema>",
  workspace: "<workspace>",
});
```

```python Python
result = client.tempera_clearing.validate_clearing_action({
    "actionId": "<actionId>",
    "actor": "<actor>",
    "authorityAdmission": "<authorityAdmission>",
    "causality": "<causality>",
    "evidence": "<evidence>",
    "expiresAt": "<expiresAt>",
    "idempotencyKey": "<idempotencyKey>",
    "mandate": "<mandate>",
    "preparedEffect": "<preparedEffect>",
    "principal": "<principal>",
    "purpose": "<purpose>",
    "requestDigest": "<requestDigest>",
    "risk": "<risk>",
    "schema": "<schema>",
    "workspace": "<workspace>",
})
```

```rust Rust
let spec = client.build_request("tempera_clearing", "validate_clearing_action", &[
    ("actionId", "<actionId>".into()),
    ("actor", "<actor>".into()),
    ("authorityAdmission", "<authorityAdmission>".into()),
    ("causality", "<causality>".into()),
    ("evidence", "<evidence>".into()),
    ("expiresAt", "<expiresAt>".into()),
    ("idempotencyKey", "<idempotencyKey>".into()),
    ("mandate", "<mandate>".into()),
    ("preparedEffect", "<preparedEffect>".into()),
    ("principal", "<principal>".into()),
    ("purpose", "<purpose>".into()),
    ("requestDigest", "<requestDigest>".into()),
    ("risk", "<risk>".into()),
    ("schema", "<schema>".into()),
    ("workspace", "<workspace>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### prepareClearingAction

`POST /v1/preparedActions`

Persist a non-executable canonical effect before authority is issued.

- **Producer operationId:** `prepareClearingAction`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-clearing`.
- **Scope:** `clearing:actions:propose`
- **Call as:** TypeScript `client.temperaClearing.prepareClearingAction()` · Python `client.tempera_clearing.prepare_clearing_action()` · Rust `build_request("tempera_clearing", "prepare_clearing_action", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `schema` | yes | — |
| `actionId` | yes | — |
| `workspace` | yes | — |
| `idempotencyKey` | yes | — |
| `preparedEffect` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaClearing.prepareClearingAction({
  schema: "<schema>",
  actionId: "<actionId>",
  workspace: "<workspace>",
  idempotencyKey: "<idempotencyKey>",
  preparedEffect: "<preparedEffect>",
});
```

```python Python
result = client.tempera_clearing.prepare_clearing_action({
    "schema": "<schema>",
    "actionId": "<actionId>",
    "workspace": "<workspace>",
    "idempotencyKey": "<idempotencyKey>",
    "preparedEffect": "<preparedEffect>",
})
```

```rust Rust
let spec = client.build_request("tempera_clearing", "prepare_clearing_action", &[
    ("schema", "<schema>".into()),
    ("actionId", "<actionId>".into()),
    ("workspace", "<workspace>".into()),
    ("idempotencyKey", "<idempotencyKey>".into()),
    ("preparedEffect", "<preparedEffect>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### proposeClearingAction

`POST /v1/actions`

Bind exact authority and Risk evidence to a prior prepared effect.

- **Producer operationId:** `proposeClearingAction`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-clearing`.
- **Scope:** `clearing:actions:propose`
- **Call as:** TypeScript `client.temperaClearing.proposeClearingAction()` · Python `client.tempera_clearing.propose_clearing_action()` · Rust `build_request("tempera_clearing", "propose_clearing_action", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `actionId` | yes | — |
| `actor` | yes | — |
| `authorityAdmission` | yes | — |
| `causality` | yes | — |
| `evidence` | yes | — |
| `expiresAt` | yes | — |
| `idempotencyKey` | yes | — |
| `mandate` | yes | — |
| `preparedEffect` | yes | — |
| `principal` | yes | — |
| `purpose` | yes | — |
| `requestDigest` | yes | — |
| `risk` | yes | — |
| `schema` | yes | — |
| `workspace` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaClearing.proposeClearingAction({
  actionId: "<actionId>",
  actor: "<actor>",
  authorityAdmission: "<authorityAdmission>",
  causality: "<causality>",
  evidence: "<evidence>",
  expiresAt: "<expiresAt>",
  idempotencyKey: "<idempotencyKey>",
  mandate: "<mandate>",
  preparedEffect: "<preparedEffect>",
  principal: "<principal>",
  purpose: "<purpose>",
  requestDigest: "<requestDigest>",
  risk: "<risk>",
  schema: "<schema>",
  workspace: "<workspace>",
});
```

```python Python
result = client.tempera_clearing.propose_clearing_action({
    "actionId": "<actionId>",
    "actor": "<actor>",
    "authorityAdmission": "<authorityAdmission>",
    "causality": "<causality>",
    "evidence": "<evidence>",
    "expiresAt": "<expiresAt>",
    "idempotencyKey": "<idempotencyKey>",
    "mandate": "<mandate>",
    "preparedEffect": "<preparedEffect>",
    "principal": "<principal>",
    "purpose": "<purpose>",
    "requestDigest": "<requestDigest>",
    "risk": "<risk>",
    "schema": "<schema>",
    "workspace": "<workspace>",
})
```

```rust Rust
let spec = client.build_request("tempera_clearing", "propose_clearing_action", &[
    ("actionId", "<actionId>".into()),
    ("actor", "<actor>".into()),
    ("authorityAdmission", "<authorityAdmission>".into()),
    ("causality", "<causality>".into()),
    ("evidence", "<evidence>".into()),
    ("expiresAt", "<expiresAt>".into()),
    ("idempotencyKey", "<idempotencyKey>".into()),
    ("mandate", "<mandate>".into()),
    ("preparedEffect", "<preparedEffect>".into()),
    ("principal", "<principal>".into()),
    ("purpose", "<purpose>".into()),
    ("requestDigest", "<requestDigest>".into()),
    ("risk", "<risk>".into()),
    ("schema", "<schema>".into()),
    ("workspace", "<workspace>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getClearingAction

`GET /v1/actions/{id}`

Read one prepared or authority-bound action.

- **Producer operationId:** `getClearingAction`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-clearing`.
- **Scope:** `clearing:actions:read`
- **Call as:** TypeScript `client.temperaClearing.getClearingAction()` · Python `client.tempera_clearing.get_clearing_action()` · Rust `build_request("tempera_clearing", "get_clearing_action", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `id` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaClearing.getClearingAction({
  id: "<id>",
});
```

```python Python
result = client.tempera_clearing.get_clearing_action({
    "id": "<id>",
})
```

```rust Rust
let spec = client.build_request("tempera_clearing", "get_clearing_action", &[
    ("id", "<id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listClearingActionEvents

`GET /v1/actions/{id}/events`

List the hash-linked events of one action.

- **Producer operationId:** `listClearingActionEvents`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-clearing`.
- **Scope:** `clearing:actions:read`
- **Call as:** TypeScript `client.temperaClearing.listClearingActionEvents()` · Python `client.tempera_clearing.list_clearing_action_events()` · Rust `build_request("tempera_clearing", "list_clearing_action_events", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `id` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaClearing.listClearingActionEvents({
  id: "<id>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.tempera_clearing.list_clearing_action_events({
    "id": "<id>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("tempera_clearing", "list_clearing_action_events", &[
    ("id", "<id>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### commitClearingAction

`POST /v1/actions/{id}:commit`

Live-verify Risk evidence, then durably commit the exact previously prepared and authority-bound action.

- **Producer operationId:** `commitClearingAction`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-clearing`.
- **Physical action:** Yes. This operation may cause an external real-world action.
- **Prepare/commit:** Required. Obtain a valid prepare receipt and commit authorization before invoking it.
- **Scope:** `clearing:actions:commit`
- **Call as:** TypeScript `client.temperaClearing.commitClearingAction()` · Python `client.tempera_clearing.commit_clearing_action()` · Rust `build_request("tempera_clearing", "commit_clearing_action", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `id` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `action` | yes | — |
| `riskEvidenceEnvelope` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaClearing.commitClearingAction({
  id: "<id>",
  action: "<action>",
  riskEvidenceEnvelope: "<riskEvidenceEnvelope>",
});
```

```python Python
result = client.tempera_clearing.commit_clearing_action({
    "id": "<id>",
    "action": "<action>",
    "riskEvidenceEnvelope": "<riskEvidenceEnvelope>",
})
```

```rust Rust
let spec = client.build_request("tempera_clearing", "commit_clearing_action", &[
    ("id", "<id>".into()),
    ("action", "<action>".into()),
    ("riskEvidenceEnvelope", "<riskEvidenceEnvelope>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>
