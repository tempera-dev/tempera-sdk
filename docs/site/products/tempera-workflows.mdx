---
title: "tempera-workflows"
description: "Deterministic workflow engine: bounded-DAG workflows (tempera.workflow/v1) of typed nodes executed as replayable, event-streamed runs; the run event stream (GET /v1/runs/{run_id}/events, SSE) is reachable through the raw passthrough request only."
---

{/* Generated by scripts/gen-sdk-docs.py from surface.json -- do not hand-edit. */}

> **Private access.** Hosted Tempera services are available through design-partner onboarding.
> Use the issuer, credentials, environment, and product URLs provisioned for your workspace;
> start in staging unless Tempera explicitly approves another target.

> **Contract inventory.** This typed reference is retained for SDK compatibility.
> It is not part of the primary public workflow or evidence of general service availability.

Deterministic workflow engine: bounded-DAG workflows (tempera.workflow/v1) of typed nodes executed as replayable, event-streamed runs; the run event stream (GET /v1/runs/\{run_id\}/events, SSE) is reachable through the raw passthrough request only.

| | |
|---|---|
| Repository | [tempera-workflows](https://github.com/tempera-dev/tempera-workflows) |
| Base URL env var | `TEMPERA_WORKFLOWS_URL` (see [Environments](/environments)) |
| Audience | `tempera-workflows` |
| Auth | bearer minted for audience tempera-workflows with workflow:read/workflow:write/workflow:run, or a central tp_ API key |
| Client | TypeScript `client.temperaWorkflows` · Python `client.tempera_workflows` · Rust `build_request("tempera_workflows", ...)` |
| Typed operations | 13 |

Anything this page does not cover is reachable through the raw passthrough:
`client.temperaWorkflows.request(path, options)` (TypeScript) /
`client.tempera_workflows.request(path, ...)` (Python).

## Operations

### health

`GET /healthz`

Check tempera-workflows engine liveness.

- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.temperaWorkflows.health()` · Python `client.tempera_workflows.health()` · Rust `build_request("tempera_workflows", "health", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.temperaWorkflows.health();
```

```python Python
result = client.tempera_workflows.health()
```

```rust Rust
let spec = client.build_request("tempera_workflows", "health", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listNodeTypes

`GET /v1/node-types`

List the typed node catalog: native orchestration nodes plus the sdk.\<product>.\<operation> nodes generated from the SDK surface.

- **Auth:** Product bearer — an access token minted for audience `tempera-workflows`, or a central `tp_` API key.
- **Scope:** `workflow:read`
- **Call as:** TypeScript `client.temperaWorkflows.listNodeTypes()` · Python `client.tempera_workflows.list_node_types()` · Rust `build_request("tempera_workflows", "list_node_types", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.temperaWorkflows.listNodeTypes();
```

```python Python
result = client.tempera_workflows.list_node_types()
```

```rust Rust
let spec = client.build_request("tempera_workflows", "list_node_types", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listWorkflows

`GET /v1/workflows`

List stored workflow definitions, newest first.

- **Auth:** Product bearer — an access token minted for audience `tempera-workflows`, or a central `tp_` API key.
- **Scope:** `workflow:read`
- **Call as:** TypeScript `client.temperaWorkflows.listWorkflows()` · Python `client.tempera_workflows.list_workflows()` · Rust `build_request("tempera_workflows", "list_workflows", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `limit` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaWorkflows.listWorkflows({
  limit: "<limit>",
});
```

```python Python
result = client.tempera_workflows.list_workflows({
    "limit": "<limit>",
})
```

```rust Rust
let spec = client.build_request("tempera_workflows", "list_workflows", &[
    ("limit", "<limit>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createWorkflow

`POST /v1/workflows`

Create a workflow definition (tempera.workflow/v1 bounded DAG of typed nodes); the definition is validated before it is stored.

- **Auth:** Product bearer — an access token minted for audience `tempera-workflows`, or a central `tp_` API key.
- **Scope:** `workflow:write`
- **Call as:** TypeScript `client.temperaWorkflows.createWorkflow()` · Python `client.tempera_workflows.create_workflow()` · Rust `build_request("tempera_workflows", "create_workflow", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `contractVersion` | yes | — |
| `id` | yes | — |
| `name` | yes | — |
| `description` | no | — |
| `nodes` | yes | — |
| `edges` | yes | — |
| `settings` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaWorkflows.createWorkflow({
  contractVersion: "<contractVersion>",
  id: "<id>",
  name: "<name>",
});
```

```python Python
result = client.tempera_workflows.create_workflow({
    "contractVersion": "<contractVersion>",
    "id": "<id>",
    "name": "<name>",
})
```

```rust Rust
let spec = client.build_request("tempera_workflows", "create_workflow", &[
    ("contractVersion", "<contractVersion>".into()),
    ("id", "<id>".into()),
    ("name", "<name>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getWorkflow

`GET /v1/workflows/{workflow_id}`

Fetch one stored workflow definition.

- **Auth:** Product bearer — an access token minted for audience `tempera-workflows`, or a central `tp_` API key.
- **Scope:** `workflow:read`
- **Call as:** TypeScript `client.temperaWorkflows.getWorkflow()` · Python `client.tempera_workflows.get_workflow()` · Rust `build_request("tempera_workflows", "get_workflow", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `workflow_id` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaWorkflows.getWorkflow({
  workflow_id: "<workflow_id>",
});
```

```python Python
result = client.tempera_workflows.get_workflow({
    "workflow_id": "<workflow_id>",
})
```

```rust Rust
let spec = client.build_request("tempera_workflows", "get_workflow", &[
    ("workflow_id", "<workflow_id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### updateWorkflow

`PUT /v1/workflows/{workflow_id}`

Replace a stored workflow definition with a new validated revision.

- **Auth:** Product bearer — an access token minted for audience `tempera-workflows`, or a central `tp_` API key.
- **Scope:** `workflow:write`
- **Call as:** TypeScript `client.temperaWorkflows.updateWorkflow()` · Python `client.tempera_workflows.update_workflow()` · Rust `build_request("tempera_workflows", "update_workflow", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `workflow_id` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `contractVersion` | yes | — |
| `id` | yes | — |
| `name` | yes | — |
| `description` | no | — |
| `nodes` | yes | — |
| `edges` | yes | — |
| `settings` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaWorkflows.updateWorkflow({
  workflow_id: "<workflow_id>",
  contractVersion: "<contractVersion>",
  id: "<id>",
  name: "<name>",
});
```

```python Python
result = client.tempera_workflows.update_workflow({
    "workflow_id": "<workflow_id>",
    "contractVersion": "<contractVersion>",
    "id": "<id>",
    "name": "<name>",
})
```

```rust Rust
let spec = client.build_request("tempera_workflows", "update_workflow", &[
    ("workflow_id", "<workflow_id>".into()),
    ("contractVersion", "<contractVersion>".into()),
    ("id", "<id>".into()),
    ("name", "<name>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### deleteWorkflow

`DELETE /v1/workflows/{workflow_id}`

Delete a stored workflow definition.

- **Auth:** Product bearer — an access token minted for audience `tempera-workflows`, or a central `tp_` API key.
- **Scope:** `workflow:write`
- **Call as:** TypeScript `client.temperaWorkflows.deleteWorkflow()` · Python `client.tempera_workflows.delete_workflow()` · Rust `build_request("tempera_workflows", "delete_workflow", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `workflow_id` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaWorkflows.deleteWorkflow({
  workflow_id: "<workflow_id>",
});
```

```python Python
result = client.tempera_workflows.delete_workflow({
    "workflow_id": "<workflow_id>",
})
```

```rust Rust
let spec = client.build_request("tempera_workflows", "delete_workflow", &[
    ("workflow_id", "<workflow_id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### validateWorkflow

`POST /v1/workflows:validate`

Validate a workflow definition without storing it; returns the full diagnostic list.

- **Auth:** Product bearer — an access token minted for audience `tempera-workflows`, or a central `tp_` API key.
- **Scope:** `workflow:write`
- **Call as:** TypeScript `client.temperaWorkflows.validateWorkflow()` · Python `client.tempera_workflows.validate_workflow()` · Rust `build_request("tempera_workflows", "validate_workflow", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `contractVersion` | yes | — |
| `id` | yes | — |
| `name` | yes | — |
| `description` | no | — |
| `nodes` | yes | — |
| `edges` | yes | — |
| `settings` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaWorkflows.validateWorkflow({
  contractVersion: "<contractVersion>",
  id: "<id>",
  name: "<name>",
});
```

```python Python
result = client.tempera_workflows.validate_workflow({
    "contractVersion": "<contractVersion>",
    "id": "<id>",
    "name": "<name>",
})
```

```rust Rust
let spec = client.build_request("tempera_workflows", "validate_workflow", &[
    ("contractVersion", "<contractVersion>".into()),
    ("id", "<id>".into()),
    ("name", "<name>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createRun

`POST /v1/workflows/{workflow_id}/runs`

Start a run of a stored workflow with an optional input document and idempotency key.

- **Auth:** Product bearer — an access token minted for audience `tempera-workflows`, or a central `tp_` API key.
- **Scope:** `workflow:run`
- **Call as:** TypeScript `client.temperaWorkflows.createRun()` · Python `client.tempera_workflows.create_run()` · Rust `build_request("tempera_workflows", "create_run", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `workflow_id` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `input` | no | — |
| `idempotencyKey` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaWorkflows.createRun({
  workflow_id: "<workflow_id>",
  input: "<input>",
  idempotencyKey: "<idempotencyKey>",
});
```

```python Python
result = client.tempera_workflows.create_run({
    "workflow_id": "<workflow_id>",
    "input": "<input>",
    "idempotencyKey": "<idempotencyKey>",
})
```

```rust Rust
let spec = client.build_request("tempera_workflows", "create_run", &[
    ("workflow_id", "<workflow_id>".into()),
    ("input", "<input>".into()),
    ("idempotencyKey", "<idempotencyKey>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### callWorkflow

`POST /v1/workflows/{workflow_id}:call`

Run a workflow to completion and return its output in a single call.

- **Auth:** Product bearer — an access token minted for audience `tempera-workflows`, or a central `tp_` API key.
- **Scope:** `workflow:run`
- **Call as:** TypeScript `client.temperaWorkflows.callWorkflow()` · Python `client.tempera_workflows.call_workflow()` · Rust `build_request("tempera_workflows", "call_workflow", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `workflow_id` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `input` | no | — |
| `idempotencyKey` | no | — |
| `usePinned` | no | — |
| `startAt` | no | — |
| `only` | no | — |
| `seedOutputs` | no | — |
| `waitMs` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaWorkflows.callWorkflow({
  workflow_id: "<workflow_id>",
  input: "<input>",
  idempotencyKey: "<idempotencyKey>",
  usePinned: "<usePinned>",
});
```

```python Python
result = client.tempera_workflows.call_workflow({
    "workflow_id": "<workflow_id>",
    "input": "<input>",
    "idempotencyKey": "<idempotencyKey>",
    "usePinned": "<usePinned>",
})
```

```rust Rust
let spec = client.build_request("tempera_workflows", "call_workflow", &[
    ("workflow_id", "<workflow_id>".into()),
    ("input", "<input>".into()),
    ("idempotencyKey", "<idempotencyKey>".into()),
    ("usePinned", "<usePinned>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listRuns

`GET /v1/runs`

List workflow runs, optionally filtered to one workflow.

- **Auth:** Product bearer — an access token minted for audience `tempera-workflows`, or a central `tp_` API key.
- **Scope:** `workflow:read`
- **Call as:** TypeScript `client.temperaWorkflows.listRuns()` · Python `client.tempera_workflows.list_runs()` · Rust `build_request("tempera_workflows", "list_runs", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `workflowId` | no | — |
| `limit` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaWorkflows.listRuns({
  workflowId: "<workflowId>",
  limit: "<limit>",
});
```

```python Python
result = client.tempera_workflows.list_runs({
    "workflowId": "<workflowId>",
    "limit": "<limit>",
})
```

```rust Rust
let spec = client.build_request("tempera_workflows", "list_runs", &[
    ("workflowId", "<workflowId>".into()),
    ("limit", "<limit>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getRun

`GET /v1/runs/{run_id}`

Fetch one workflow run with its state, node results, and timings; the live SSE event stream at /v1/runs/\{run_id\}/events is passthrough-only.

- **Auth:** Product bearer — an access token minted for audience `tempera-workflows`, or a central `tp_` API key.
- **Scope:** `workflow:read`
- **Call as:** TypeScript `client.temperaWorkflows.getRun()` · Python `client.tempera_workflows.get_run()` · Rust `build_request("tempera_workflows", "get_run", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `run_id` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaWorkflows.getRun({
  run_id: "<run_id>",
});
```

```python Python
result = client.tempera_workflows.get_run({
    "run_id": "<run_id>",
})
```

```rust Rust
let spec = client.build_request("tempera_workflows", "get_run", &[
    ("run_id", "<run_id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### cancelRun

`POST /v1/runs/{run_id}:cancel`

Cancel a queued or running workflow run.

- **Auth:** Product bearer — an access token minted for audience `tempera-workflows`, or a central `tp_` API key.
- **Scope:** `workflow:run`
- **Call as:** TypeScript `client.temperaWorkflows.cancelRun()` · Python `client.tempera_workflows.cancel_run()` · Rust `build_request("tempera_workflows", "cancel_run", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `run_id` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaWorkflows.cancelRun({
  run_id: "<run_id>",
});
```

```python Python
result = client.tempera_workflows.cancel_run({
    "run_id": "<run_id>",
})
```

```rust Rust
let spec = client.build_request("tempera_workflows", "cancel_run", &[
    ("run_id", "<run_id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>
