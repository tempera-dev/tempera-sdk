---
title: "data-engine"
description: "Domain-portable label-emergence engine: deterministic ingestion, sandboxed verification in cradle, and RL/eval/SFT dataset emission."
---

{/* Generated by scripts/gen-sdk-docs.py from surface.json -- do not hand-edit. */}

> **Private access.** Hosted Tempera services are available through design-partner onboarding.
> Use the issuer, credentials, environment, and product URLs provisioned for your workspace;
> start in staging unless Tempera explicitly approves another target.

> **Contract inventory.** This typed reference is retained for SDK compatibility.
> It is not part of the primary public workflow or evidence of general service availability.

Domain-portable label-emergence engine: deterministic ingestion, sandboxed verification in cradle, and RL/eval/SFT dataset emission.

| | |
|---|---|
| Repository | [data-engine](https://github.com/tempera-dev/data-engine) |
| Base URL env var | `TEMPERA_DATA_ENGINE_URL` (see [Environments](/environments)) |
| Audience | `data-engine` |
| Auth | bearer minted for audience data-engine, central tp_ API key, or the data-engine static token |
| Client | TypeScript `client.dataEngine` · Python `client.data_engine` · Rust `build_request("data_engine", ...)` |
| Typed operations | 53 |

Anything this page does not cover is reachable through the raw passthrough:
`client.dataEngine.request(path, options)` (TypeScript) /
`client.data_engine.request(path, ...)` (Python).

## Operations

### health

`GET /v1/health`

Check data-engine liveness; returns the service status.

- **Producer operationId:** `health.get`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.dataEngine.health()` · Python `client.data_engine.health()` · Rust `build_request("data_engine", "health", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.health();
```

```python Python
result = client.data_engine.health()
```

```rust Rust
let spec = client.build_request("data_engine", "health", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listUseCases

`GET /v1/{parent}/use-cases`

List the MVP use-case templates (data products and pipeline templates) for a project.

- **Producer operationId:** `projects.useCases.list`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.listUseCases()` · Python `client.data_engine.list_use_cases()` · Rust `build_request("data_engine", "list_use_cases", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.listUseCases({
  parent: "<parent>",
});
```

```python Python
result = client.data_engine.list_use_cases({
    "parent": "<parent>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "list_use_cases", &[
    ("parent", "<parent>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getUseCase

`GET /v1/{parent}/use-cases/{useCaseId}`

Fetch one MVP use-case template with its rubric, modalities, skill tags, and target accuracy.

- **Producer operationId:** `projects.useCases.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.getUseCase()` · Python `client.data_engine.get_use_case()` · Rust `build_request("data_engine", "get_use_case", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `useCaseId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getUseCase({
  parent: "<parent>",
  useCaseId: "<useCaseId>",
});
```

```python Python
result = client.data_engine.get_use_case({
    "parent": "<parent>",
    "useCaseId": "<useCaseId>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_use_case", &[
    ("parent", "<parent>".into()),
    ("useCaseId", "<useCaseId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### ingestArtifact

`POST /v1/{parent}/artifacts:ingest`

Ingest one artifact deterministically into the project; returns an async operation handle.

- **Producer operationId:** `projects.artifacts.ingest`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:write`
- **Call as:** TypeScript `client.dataEngine.ingestArtifact()` · Python `client.data_engine.ingest_artifact()` · Rust `build_request("data_engine", "ingest_artifact", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `artifactType` | no | — |
| `source` | no | — |
| `external_id` | no | — |
| `raw_body` | no | — |
| `metadata` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.ingestArtifact({
  parent: "<parent>",
  artifactType: "<artifactType>",
  source: "<source>",
  external_id: "<external_id>",
});
```

```python Python
result = client.data_engine.ingest_artifact({
    "parent": "<parent>",
    "artifactType": "<artifactType>",
    "source": "<source>",
    "external_id": "<external_id>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "ingest_artifact", &[
    ("parent", "<parent>".into()),
    ("artifactType", "<artifactType>".into()),
    ("source", "<source>".into()),
    ("external_id", "<external_id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### ingestWeb

`POST /v1/{parent}/web:ingest`

Fetch, parse, and ingest one public HTTP(S) page as a web artifact; returns an async operation handle.

- **Producer operationId:** `projects.web.ingest`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:write`
- **Call as:** TypeScript `client.dataEngine.ingestWeb()` · Python `client.data_engine.ingest_web()` · Rust `build_request("data_engine", "ingest_web", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `url` | yes | — |
| `artifactType` | no | — |
| `timeoutSeconds` | no | — |
| `maxBytes` | no | — |
| `producer_version` | no | — |
| `metadata` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.ingestWeb({
  parent: "<parent>",
  url: "<url>",
  artifactType: "<artifactType>",
  timeoutSeconds: "<timeoutSeconds>",
});
```

```python Python
result = client.data_engine.ingest_web({
    "parent": "<parent>",
    "url": "<url>",
    "artifactType": "<artifactType>",
    "timeoutSeconds": "<timeoutSeconds>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "ingest_web", &[
    ("parent", "<parent>".into()),
    ("url", "<url>".into()),
    ("artifactType", "<artifactType>".into()),
    ("timeoutSeconds", "<timeoutSeconds>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createCampaign

`POST /v1/{parent}/campaigns`

Create a data campaign with a rubric, budget, target accuracy, and skill tags.

- **Producer operationId:** `projects.campaigns.create`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:write`
- **Call as:** TypeScript `client.dataEngine.createCampaign()` · Python `client.data_engine.create_campaign()` · Rust `build_request("data_engine", "create_campaign", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `task_family` | no | — |
| `target_accuracy` | no | — |
| `budget_cents` | no | — |
| `rubric` | no | — |
| `skill_tags` | no | — |
| `required_reviews` | no | — |
| `qualification_policy` | no | — |
| `routing_policy` | no | — |
| `annotation_schema` | no | — |
| `idempotency_key` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.createCampaign({
  parent: "<parent>",
  task_family: "<task_family>",
  target_accuracy: "<target_accuracy>",
  budget_cents: "<budget_cents>",
});
```

```python Python
result = client.data_engine.create_campaign({
    "parent": "<parent>",
    "task_family": "<task_family>",
    "target_accuracy": "<target_accuracy>",
    "budget_cents": "<budget_cents>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "create_campaign", &[
    ("parent", "<parent>".into()),
    ("task_family", "<task_family>".into()),
    ("target_accuracy", "<target_accuracy>".into()),
    ("budget_cents", "<budget_cents>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listCampaigns

`GET /v1/{parent}/campaigns`

List a project's data campaigns with pagination.

- **Producer operationId:** `projects.campaigns.list`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.listCampaigns()` · Python `client.data_engine.list_campaigns()` · Rust `build_request("data_engine", "list_campaigns", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `page_size` | no | — |
| `page_token` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.listCampaigns({
  parent: "<parent>",
  page_size: "<page_size>",
  page_token: "<page_token>",
});
```

```python Python
result = client.data_engine.list_campaigns({
    "parent": "<parent>",
    "page_size": "<page_size>",
    "page_token": "<page_token>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "list_campaigns", &[
    ("parent", "<parent>".into()),
    ("page_size", "<page_size>".into()),
    ("page_token", "<page_token>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### transitionCampaign

`POST /v1/{parent}/campaigns/{campaign_id}:transition`

Pause, resume, or permanently close campaign job admission; returns an immutable receipt for the committed lifecycle transition.

- **Producer operationId:** `projects.campaigns.transition`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:write`
- **Call as:** TypeScript `client.dataEngine.transitionCampaign()` · Python `client.data_engine.transition_campaign()` · Rust `build_request("data_engine", "transition_campaign", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `campaign_id` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `target_status` | yes | — |
| `idempotency_key` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.transitionCampaign({
  parent: "<parent>",
  campaign_id: "<campaign_id>",
  target_status: "<target_status>",
  idempotency_key: "<idempotency_key>",
});
```

```python Python
result = client.data_engine.transition_campaign({
    "parent": "<parent>",
    "campaign_id": "<campaign_id>",
    "target_status": "<target_status>",
    "idempotency_key": "<idempotency_key>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "transition_campaign", &[
    ("parent", "<parent>".into()),
    ("campaign_id", "<campaign_id>".into()),
    ("target_status", "<target_status>".into()),
    ("idempotency_key", "<idempotency_key>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getReviewerQualification

`GET /v1/{parent}/campaigns/{campaign_id}/reviewer-qualification`

Fetch the authenticated reviewer's project-scoped qualification and campaign eligibility without blind-probe outcomes.

- **Producer operationId:** `projects.reviewerQualifications.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `review:resolve`
- **Call as:** TypeScript `client.dataEngine.getReviewerQualification()` · Python `client.data_engine.get_reviewer_qualification()` · Rust `build_request("data_engine", "get_reviewer_qualification", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `campaign_id` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getReviewerQualification({
  parent: "<parent>",
  campaign_id: "<campaign_id>",
});
```

```python Python
result = client.data_engine.get_reviewer_qualification({
    "parent": "<parent>",
    "campaign_id": "<campaign_id>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_reviewer_qualification", &[
    ("parent", "<parent>".into()),
    ("campaign_id", "<campaign_id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### runUseCase

`POST /v1/{parent}/pipelines:run-use-case`

Run a complete MVP use-case pipeline end to end; verifier selects the configured verification backend.

- **Producer operationId:** `projects.pipelines.runUseCase`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.dataEngine.runUseCase()` · Python `client.data_engine.run_use_case()` · Rust `build_request("data_engine", "run_use_case", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `use_case` | yes | — |
| `verifier` | no | — |
| `budget_cents` | no | — |
| `target_accuracy` | no | — |
| `rubric` | no | — |
| `skill_tags` | no | — |
| `required_reviews` | no | — |
| `urls` | no | — |
| `artifacts` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.runUseCase({
  parent: "<parent>",
  use_case: "<use_case>",
  verifier: "<verifier>",
  budget_cents: "<budget_cents>",
});
```

```python Python
result = client.data_engine.run_use_case({
    "parent": "<parent>",
    "use_case": "<use_case>",
    "verifier": "<verifier>",
    "budget_cents": "<budget_cents>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "run_use_case", &[
    ("parent", "<parent>".into()),
    ("use_case", "<use_case>".into()),
    ("verifier", "<verifier>".into()),
    ("budget_cents", "<budget_cents>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listExpertTasks

`GET /v1/{parent}/expert-tasks`

List human residual review tasks, optionally filtered by status and campaign.

- **Producer operationId:** `projects.expertTasks.list`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.listExpertTasks()` · Python `client.data_engine.list_expert_tasks()` · Rust `build_request("data_engine", "list_expert_tasks", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `page_size` | no | — |
| `page_token` | no | — |
| `status` | no | — |
| `campaign_name` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.listExpertTasks({
  parent: "<parent>",
  page_size: "<page_size>",
  page_token: "<page_token>",
});
```

```python Python
result = client.data_engine.list_expert_tasks({
    "parent": "<parent>",
    "page_size": "<page_size>",
    "page_token": "<page_token>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "list_expert_tasks", &[
    ("parent", "<parent>".into()),
    ("page_size", "<page_size>".into()),
    ("page_token", "<page_token>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createReviewQualificationTask

`POST /v1/{parent}/review-qualification-tasks`

Clone a review task into an isolated, HMAC-scored qualification task without returning the expected label.

- **Producer operationId:** `projects.reviewQualificationTasks.create`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `review:gold:manage`
- **Call as:** TypeScript `client.dataEngine.createReviewQualificationTask()` · Python `client.data_engine.create_review_qualification_task()` · Rust `build_request("data_engine", "create_review_qualification_task", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `source_expert_task_name` | yes | — |
| `expected_label` | yes | — |
| `mode` | no | — |
| `feedback_policy` | no | — |
| `idempotency_key` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.createReviewQualificationTask({
  parent: "<parent>",
  source_expert_task_name: "<source_expert_task_name>",
  expected_label: "<expected_label>",
  mode: "<mode>",
});
```

```python Python
result = client.data_engine.create_review_qualification_task({
    "parent": "<parent>",
    "source_expert_task_name": "<source_expert_task_name>",
    "expected_label": "<expected_label>",
    "mode": "<mode>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "create_review_qualification_task", &[
    ("parent", "<parent>".into()),
    ("source_expert_task_name", "<source_expert_task_name>".into()),
    ("expected_label", "<expected_label>".into()),
    ("mode", "<mode>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### resolveExpertTask

`POST /v1/{parent}/expert-tasks/{expert_task_id}:resolve`

Resolve, abstain, flag, or adjudicate one human residual with an idempotent normalized decision.

- **Producer operationId:** `projects.expertTasks.resolve`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `review:resolve`
- **Call as:** TypeScript `client.dataEngine.resolveExpertTask()` · Python `client.data_engine.resolve_expert_task()` · Rust `build_request("data_engine", "resolve_expert_task", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `expert_task_id` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `label` | yes | — |
| `outcome` | no | — |
| `confidence` | no | — |
| `rationale` | no | — |
| `evidence` | no | — |
| `annotation` | no | — |
| `annotator_id` | no | — |
| `idempotency_key` | yes | — |
| `lease_token` | no | — |
| `review_context` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.resolveExpertTask({
  parent: "<parent>",
  expert_task_id: "<expert_task_id>",
  label: "<label>",
  outcome: "<outcome>",
  confidence: "<confidence>",
});
```

```python Python
result = client.data_engine.resolve_expert_task({
    "parent": "<parent>",
    "expert_task_id": "<expert_task_id>",
    "label": "<label>",
    "outcome": "<outcome>",
    "confidence": "<confidence>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "resolve_expert_task", &[
    ("parent", "<parent>".into()),
    ("expert_task_id", "<expert_task_id>".into()),
    ("label", "<label>".into()),
    ("outcome", "<outcome>".into()),
    ("confidence", "<confidence>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### claimExpertTask

`POST /v1/{parent}/expert-tasks/{expert_task_id}:claim`

Atomically claim one open expert task with an exclusive renewable lease.

- **Producer operationId:** `projects.expertTaskAssignments.claim`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `review:resolve`
- **Call as:** TypeScript `client.dataEngine.claimExpertTask()` · Python `client.data_engine.claim_expert_task()` · Rust `build_request("data_engine", "claim_expert_task", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `expert_task_id` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `idempotency_key` | yes | — |
| `lease_token` | yes | — |
| `lease_duration_seconds` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.claimExpertTask({
  parent: "<parent>",
  expert_task_id: "<expert_task_id>",
  idempotency_key: "<idempotency_key>",
  lease_token: "<lease_token>",
  lease_duration_seconds: "<lease_duration_seconds>",
});
```

```python Python
result = client.data_engine.claim_expert_task({
    "parent": "<parent>",
    "expert_task_id": "<expert_task_id>",
    "idempotency_key": "<idempotency_key>",
    "lease_token": "<lease_token>",
    "lease_duration_seconds": "<lease_duration_seconds>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "claim_expert_task", &[
    ("parent", "<parent>".into()),
    ("expert_task_id", "<expert_task_id>".into()),
    ("idempotency_key", "<idempotency_key>".into()),
    ("lease_token", "<lease_token>".into()),
    ("lease_duration_seconds", "<lease_duration_seconds>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### renewExpertTaskAssignment

`POST /v1/{parent}/expert-tasks/{expert_task_id}:renew`

Renew the authenticated reviewer's active expert-task lease.

- **Producer operationId:** `projects.expertTaskAssignments.renew`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `review:resolve`
- **Call as:** TypeScript `client.dataEngine.renewExpertTaskAssignment()` · Python `client.data_engine.renew_expert_task_assignment()` · Rust `build_request("data_engine", "renew_expert_task_assignment", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `expert_task_id` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `idempotency_key` | yes | — |
| `lease_token` | yes | — |
| `lease_duration_seconds` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.renewExpertTaskAssignment({
  parent: "<parent>",
  expert_task_id: "<expert_task_id>",
  idempotency_key: "<idempotency_key>",
  lease_token: "<lease_token>",
  lease_duration_seconds: "<lease_duration_seconds>",
});
```

```python Python
result = client.data_engine.renew_expert_task_assignment({
    "parent": "<parent>",
    "expert_task_id": "<expert_task_id>",
    "idempotency_key": "<idempotency_key>",
    "lease_token": "<lease_token>",
    "lease_duration_seconds": "<lease_duration_seconds>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "renew_expert_task_assignment", &[
    ("parent", "<parent>".into()),
    ("expert_task_id", "<expert_task_id>".into()),
    ("idempotency_key", "<idempotency_key>".into()),
    ("lease_token", "<lease_token>".into()),
    ("lease_duration_seconds", "<lease_duration_seconds>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### releaseExpertTaskAssignment

`POST /v1/{parent}/expert-tasks/{expert_task_id}:release`

Release the authenticated reviewer's active expert-task lease for reassignment.

- **Producer operationId:** `projects.expertTaskAssignments.release`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `review:resolve`
- **Call as:** TypeScript `client.dataEngine.releaseExpertTaskAssignment()` · Python `client.data_engine.release_expert_task_assignment()` · Rust `build_request("data_engine", "release_expert_task_assignment", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `expert_task_id` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `idempotency_key` | yes | — |
| `lease_token` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.releaseExpertTaskAssignment({
  parent: "<parent>",
  expert_task_id: "<expert_task_id>",
  idempotency_key: "<idempotency_key>",
  lease_token: "<lease_token>",
});
```

```python Python
result = client.data_engine.release_expert_task_assignment({
    "parent": "<parent>",
    "expert_task_id": "<expert_task_id>",
    "idempotency_key": "<idempotency_key>",
    "lease_token": "<lease_token>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "release_expert_task_assignment", &[
    ("parent", "<parent>".into()),
    ("expert_task_id", "<expert_task_id>".into()),
    ("idempotency_key", "<idempotency_key>".into()),
    ("lease_token", "<lease_token>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### saveExpertTaskDraft

`POST /v1/{parent}/expert-tasks/{expert_task_id}:save-draft`

Autosave a version-checked draft under the active reviewer lease.

- **Producer operationId:** `projects.expertTaskAssignments.saveDraft`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `review:resolve`
- **Call as:** TypeScript `client.dataEngine.saveExpertTaskDraft()` · Python `client.data_engine.save_expert_task_draft()` · Rust `build_request("data_engine", "save_expert_task_draft", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `expert_task_id` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `idempotency_key` | yes | — |
| `lease_token` | yes | — |
| `draft` | yes | — |
| `expected_version` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.saveExpertTaskDraft({
  parent: "<parent>",
  expert_task_id: "<expert_task_id>",
  idempotency_key: "<idempotency_key>",
  lease_token: "<lease_token>",
  draft: "<draft>",
});
```

```python Python
result = client.data_engine.save_expert_task_draft({
    "parent": "<parent>",
    "expert_task_id": "<expert_task_id>",
    "idempotency_key": "<idempotency_key>",
    "lease_token": "<lease_token>",
    "draft": "<draft>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "save_expert_task_draft", &[
    ("parent", "<parent>".into()),
    ("expert_task_id", "<expert_task_id>".into()),
    ("idempotency_key", "<idempotency_key>".into()),
    ("lease_token", "<lease_token>".into()),
    ("draft", "<draft>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getReviewOperations

`GET /v1/{parent}/review-operations`

Fetch bounded project review-operations, SLA, agreement, calibration, rubric-drift, and budget observations.

- **Producer operationId:** `projects.reviewOperations.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `review:resolve`
- **Call as:** TypeScript `client.dataEngine.getReviewOperations()` · Python `client.data_engine.get_review_operations()` · Rust `build_request("data_engine", "get_review_operations", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `window_seconds` | no | — |
| `sla_target_seconds` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getReviewOperations({
  parent: "<parent>",
  window_seconds: "<window_seconds>",
  sla_target_seconds: "<sla_target_seconds>",
});
```

```python Python
result = client.data_engine.get_review_operations({
    "parent": "<parent>",
    "window_seconds": "<window_seconds>",
    "sla_target_seconds": "<sla_target_seconds>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_review_operations", &[
    ("parent", "<parent>".into()),
    ("window_seconds", "<window_seconds>".into()),
    ("sla_target_seconds", "<sla_target_seconds>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getMetrics

`GET /v1/{parent}/metrics`

Fetch data-engine usage and quality metrics for a project.

- **Producer operationId:** `projects.metrics.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.getMetrics()` · Python `client.data_engine.get_metrics()` · Rust `build_request("data_engine", "get_metrics", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getMetrics({
  parent: "<parent>",
});
```

```python Python
result = client.data_engine.get_metrics({
    "parent": "<parent>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_metrics", &[
    ("parent", "<parent>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getLabelQuality

`GET /v1/{parent}/label-quality`

Fetch the project label-quality report and unresolved expert backlog.

- **Producer operationId:** `projects.labelQuality.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.getLabelQuality()` · Python `client.data_engine.get_label_quality()` · Rust `build_request("data_engine", "get_label_quality", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getLabelQuality({
  parent: "<parent>",
});
```

```python Python
result = client.data_engine.get_label_quality({
    "parent": "<parent>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_label_quality", &[
    ("parent", "<parent>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getEcosystemReadiness

`GET /v1/{parent}/ecosystem/readiness`

Fetch public-site and ecosystem readiness signals for a project.

- **Producer operationId:** `projects.ecosystem.readiness.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.getEcosystemReadiness()` · Python `client.data_engine.get_ecosystem_readiness()` · Rust `build_request("data_engine", "get_ecosystem_readiness", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getEcosystemReadiness({
  parent: "<parent>",
});
```

```python Python
result = client.data_engine.get_ecosystem_readiness({
    "parent": "<parent>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_ecosystem_readiness", &[
    ("parent", "<parent>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listArtifacts

`GET /v1/{parent}/artifacts`

List a project's artifacts with cursor pagination, expanded to the requested view (BASIC or FULL).

- **Producer operationId:** `projects.artifacts.list`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.listArtifacts()` · Python `client.data_engine.list_artifacts()` · Rust `build_request("data_engine", "list_artifacts", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `page_size` | no | — |
| `page_token` | no | — |
| `view` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.listArtifacts({
  parent: "<parent>",
  page_size: "<page_size>",
  page_token: "<page_token>",
});
```

```python Python
result = client.data_engine.list_artifacts({
    "parent": "<parent>",
    "page_size": "<page_size>",
    "page_token": "<page_token>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "list_artifacts", &[
    ("parent", "<parent>".into()),
    ("page_size", "<page_size>".into()),
    ("page_token", "<page_token>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getArtifact

`GET /v1/{parent}/artifacts/{artifactId}`

Fetch one artifact, expanded to the requested view (BASIC or FULL).

- **Producer operationId:** `projects.artifacts.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.getArtifact()` · Python `client.data_engine.get_artifact()` · Rust `build_request("data_engine", "get_artifact", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `artifactId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `view` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getArtifact({
  parent: "<parent>",
  artifactId: "<artifactId>",
  view: "<view>",
});
```

```python Python
result = client.data_engine.get_artifact({
    "parent": "<parent>",
    "artifactId": "<artifactId>",
    "view": "<view>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_artifact", &[
    ("parent", "<parent>".into()),
    ("artifactId", "<artifactId>".into()),
    ("view", "<view>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listArtifactLabels

`GET /v1/{parent}/artifacts/{artifactId}/labels`

List the labels attached to one artifact.

- **Producer operationId:** `projects.artifacts.labels.list`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.listArtifactLabels()` · Python `client.data_engine.list_artifact_labels()` · Rust `build_request("data_engine", "list_artifact_labels", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `artifactId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `page_size` | no | — |
| `page_token` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.listArtifactLabels({
  parent: "<parent>",
  artifactId: "<artifactId>",
  page_size: "<page_size>",
  page_token: "<page_token>",
});
```

```python Python
result = client.data_engine.list_artifact_labels({
    "parent": "<parent>",
    "artifactId": "<artifactId>",
    "page_size": "<page_size>",
    "page_token": "<page_token>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "list_artifact_labels", &[
    ("parent", "<parent>".into()),
    ("artifactId", "<artifactId>".into()),
    ("page_size", "<page_size>".into()),
    ("page_token", "<page_token>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### profileDataset

`POST /v1/{parent}/datasets:profile`

Profile dataset quality before export, including duplicates, label coverage, and distributions.

- **Producer operationId:** `projects.datasets.profile`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:write`
- **Call as:** TypeScript `client.dataEngine.profileDataset()` · Python `client.data_engine.profile_dataset()` · Rust `build_request("data_engine", "profile_dataset", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `artifact_ids` | no | — |
| `artifact_type` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.profileDataset({
  parent: "<parent>",
  artifact_ids: "<artifact_ids>",
  artifact_type: "<artifact_type>",
});
```

```python Python
result = client.data_engine.profile_dataset({
    "parent": "<parent>",
    "artifact_ids": "<artifact_ids>",
    "artifact_type": "<artifact_type>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "profile_dataset", &[
    ("parent", "<parent>".into()),
    ("artifact_ids", "<artifact_ids>".into()),
    ("artifact_type", "<artifact_type>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createJob

`POST /v1/{parent}/jobs`

Create an asynchronous labeling job over a set of artifacts; returns an operation handle to poll.

- **Producer operationId:** `projects.jobs.create`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.dataEngine.createJob()` · Python `client.data_engine.create_job()` · Rust `build_request("data_engine", "create_job", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `artifact_ids` | yes | — |
| `task_family` | yes | — |
| `campaign` | no | — |
| `target_accuracy` | no | — |
| `idempotency_key` | no | — |
| `verifier` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.createJob({
  parent: "<parent>",
  artifact_ids: "<artifact_ids>",
  task_family: "<task_family>",
  campaign: "<campaign>",
});
```

```python Python
result = client.data_engine.create_job({
    "parent": "<parent>",
    "artifact_ids": "<artifact_ids>",
    "task_family": "<task_family>",
    "campaign": "<campaign>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "create_job", &[
    ("parent", "<parent>".into()),
    ("artifact_ids", "<artifact_ids>".into()),
    ("task_family", "<task_family>".into()),
    ("campaign", "<campaign>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getJob

`GET /v1/{parent}/jobs/{jobId}`

Fetch one labeling job with its state and progress.

- **Producer operationId:** `projects.jobs.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.getJob()` · Python `client.data_engine.get_job()` · Rust `build_request("data_engine", "get_job", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `jobId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getJob({
  parent: "<parent>",
  jobId: "<jobId>",
});
```

```python Python
result = client.data_engine.get_job({
    "parent": "<parent>",
    "jobId": "<jobId>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_job", &[
    ("parent", "<parent>".into()),
    ("jobId", "<jobId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getJobResults

`GET /v1/{parent}/jobs/{jobId}/results`

List the deterministic label results a job produced.

- **Producer operationId:** `projects.jobs.results.list`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.getJobResults()` · Python `client.data_engine.get_job_results()` · Rust `build_request("data_engine", "get_job_results", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `jobId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getJobResults({
  parent: "<parent>",
  jobId: "<jobId>",
});
```

```python Python
result = client.data_engine.get_job_results({
    "parent": "<parent>",
    "jobId": "<jobId>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_job_results", &[
    ("parent", "<parent>".into()),
    ("jobId", "<jobId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getProduct

`GET /v1/{parent}/products/{productId}`

Fetch one emitted product bundle with its status and manifest URL.

- **Producer operationId:** `projects.products.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.getProduct()` · Python `client.data_engine.get_product()` · Rust `build_request("data_engine", "get_product", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `productId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getProduct({
  parent: "<parent>",
  productId: "<productId>",
});
```

```python Python
result = client.data_engine.get_product({
    "parent": "<parent>",
    "productId": "<productId>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_product", &[
    ("parent", "<parent>".into()),
    ("productId", "<productId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### validateProduct

`POST /v1/{parent}/products/{productId}:validate`

Validate an emitted product bundle's referential integrity and hygiene.

- **Producer operationId:** `projects.products.validate`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:write`
- **Call as:** TypeScript `client.dataEngine.validateProduct()` · Python `client.data_engine.validate_product()` · Rust `build_request("data_engine", "validate_product", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `productId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.validateProduct({
  parent: "<parent>",
  productId: "<productId>",
});
```

```python Python
result = client.data_engine.validate_product({
    "parent": "<parent>",
    "productId": "<productId>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "validate_product", &[
    ("parent", "<parent>".into()),
    ("productId", "<productId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### checkProductLeakage

`POST /v1/{parent}/products:check-leakage`

Check raw-hash leakage between exactly two product bundles.

- **Producer operationId:** `projects.products.checkLeakage`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:write`
- **Call as:** TypeScript `client.dataEngine.checkProductLeakage()` · Python `client.data_engine.check_product_leakage()` · Rust `build_request("data_engine", "check_product_leakage", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `product_ids` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.checkProductLeakage({
  parent: "<parent>",
  product_ids: "<product_ids>",
});
```

```python Python
result = client.data_engine.check_product_leakage({
    "parent": "<parent>",
    "product_ids": "<product_ids>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "check_product_leakage", &[
    ("parent", "<parent>".into()),
    ("product_ids", "<product_ids>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getProductManifest

`GET /v1/{parent}/products/{productId}/manifest`

Fetch an integrity-checked, bounded manifest for an emitted eval product.

- **Producer operationId:** `projects.products.manifest.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.getProductManifest()` · Python `client.data_engine.get_product_manifest()` · Rust `build_request("data_engine", "get_product_manifest", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `productId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getProductManifest({
  parent: "<parent>",
  productId: "<productId>",
});
```

```python Python
result = client.data_engine.get_product_manifest({
    "parent": "<parent>",
    "productId": "<productId>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_product_manifest", &[
    ("parent", "<parent>".into()),
    ("productId", "<productId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### admitTrainingRelease

`POST /v1/{parent}/training-releases:admit`

Admit exact training and heldout product generations after revalidating integrity, review consent, and leakage constraints.

- **Producer operationId:** `projects.trainingReleases.admit`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `training:publish`
- **Call as:** TypeScript `client.dataEngine.admitTrainingRelease()` · Python `client.data_engine.admit_training_release()` · Rust `build_request("data_engine", "admit_training_release", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `training_product_id` | yes | — |
| `heldout_product_id` | yes | — |
| `idempotency_key` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.admitTrainingRelease({
  parent: "<parent>",
  training_product_id: "<training_product_id>",
  heldout_product_id: "<heldout_product_id>",
  idempotency_key: "<idempotency_key>",
});
```

```python Python
result = client.data_engine.admit_training_release({
    "parent": "<parent>",
    "training_product_id": "<training_product_id>",
    "heldout_product_id": "<heldout_product_id>",
    "idempotency_key": "<idempotency_key>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "admit_training_release", &[
    ("parent", "<parent>".into()),
    ("training_product_id", "<training_product_id>".into()),
    ("heldout_product_id", "<heldout_product_id>".into()),
    ("idempotency_key", "<idempotency_key>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getTrainingRelease

`GET /v1/{parent}/training-releases/{releaseId}`

Revalidate and fetch one training release, including any durable stale state.

- **Producer operationId:** `projects.trainingReleases.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `training:publish`
- **Call as:** TypeScript `client.dataEngine.getTrainingRelease()` · Python `client.data_engine.get_training_release()` · Rust `build_request("data_engine", "get_training_release", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `releaseId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getTrainingRelease({
  parent: "<parent>",
  releaseId: "<releaseId>",
});
```

```python Python
result = client.data_engine.get_training_release({
    "parent": "<parent>",
    "releaseId": "<releaseId>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_training_release", &[
    ("parent", "<parent>".into()),
    ("releaseId", "<releaseId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### deriveBundle

`POST /v1/{parent}/products/{productId}:derive`

Derive a deterministic post-training bundle from a ready product.

- **Producer operationId:** `projects.products.derive`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.dataEngine.deriveBundle()` · Python `client.data_engine.derive_bundle()` · Rust `build_request("data_engine", "derive_bundle", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `productId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `format` | yes | — |
| `train_fraction` | no | — |
| `include_raw` | no | — |
| `page_size` | no | — |
| `page_token` | no | — |
| `pair_sources` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.deriveBundle({
  parent: "<parent>",
  productId: "<productId>",
  format: "<format>",
  train_fraction: "<train_fraction>",
  include_raw: "<include_raw>",
});
```

```python Python
result = client.data_engine.derive_bundle({
    "parent": "<parent>",
    "productId": "<productId>",
    "format": "<format>",
    "train_fraction": "<train_fraction>",
    "include_raw": "<include_raw>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "derive_bundle", &[
    ("parent", "<parent>".into()),
    ("productId", "<productId>".into()),
    ("format", "<format>".into()),
    ("train_fraction", "<train_fraction>".into()),
    ("include_raw", "<include_raw>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### emitEval

`POST /v1/{parent}/products:emit-eval`

Emit an eval dataset bundle from verified artifacts; returns an async operation handle.

- **Producer operationId:** `projects.products.emitEval`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.dataEngine.emitEval()` · Python `client.data_engine.emit_eval()` · Rust `build_request("data_engine", "emit_eval", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `artifact_ids` | yes | — |
| `job` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.emitEval({
  parent: "<parent>",
  artifact_ids: "<artifact_ids>",
  job: "<job>",
});
```

```python Python
result = client.data_engine.emit_eval({
    "parent": "<parent>",
    "artifact_ids": "<artifact_ids>",
    "job": "<job>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "emit_eval", &[
    ("parent", "<parent>".into()),
    ("artifact_ids", "<artifact_ids>".into()),
    ("job", "<job>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### extractSource

`POST /v1/{parent}/sources:extract`

Extract bounded objects or records from a configured source connector.

- **Producer operationId:** `projects.sources.extract`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:write`
- **Call as:** TypeScript `client.dataEngine.extractSource()` · Python `client.data_engine.extract_source()` · Rust `build_request("data_engine", "extract_source", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `connector` | yes | — |
| `bucket` | no | — |
| `prefix` | no | — |
| `key` | no | — |
| `max_objects` | no | — |
| `max_bytes` | no | — |
| `statement` | no | — |
| `limit` | no | — |
| `soql` | no | — |
| `max_pages` | no | — |
| `artifact_type` | no | — |
| `source` | no | — |
| `ingest` | no | — |
| `metadata` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.extractSource({
  parent: "<parent>",
  connector: "<connector>",
  bucket: "<bucket>",
  prefix: "<prefix>",
});
```

```python Python
result = client.data_engine.extract_source({
    "parent": "<parent>",
    "connector": "<connector>",
    "bucket": "<bucket>",
    "prefix": "<prefix>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "extract_source", &[
    ("parent", "<parent>".into()),
    ("connector", "<connector>".into()),
    ("bucket", "<bucket>".into()),
    ("prefix", "<prefix>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listConnectors

`GET /v1/{parent}/connectors`

List registered source connectors for a project.

- **Producer operationId:** `projects.connectors.list`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.listConnectors()` · Python `client.data_engine.list_connectors()` · Rust `build_request("data_engine", "list_connectors", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `page_size` | no | — |
| `page_token` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.listConnectors({
  parent: "<parent>",
  page_size: "<page_size>",
  page_token: "<page_token>",
});
```

```python Python
result = client.data_engine.list_connectors({
    "parent": "<parent>",
    "page_size": "<page_size>",
    "page_token": "<page_token>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "list_connectors", &[
    ("parent", "<parent>".into()),
    ("page_size", "<page_size>".into()),
    ("page_token", "<page_token>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createTool

`POST /v1/{parent}/tools`

Create or version-bump a stored custom tool.

- **Producer operationId:** `projects.tools.create`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:write`
- **Call as:** TypeScript `client.dataEngine.createTool()` · Python `client.data_engine.create_tool()` · Rust `build_request("data_engine", "create_tool", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `name` | yes | — |
| `description` | yes | — |
| `input_schema` | no | — |
| `kind` | yes | — |
| `implementation` | yes | — |
| `created_by` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.createTool({
  parent: "<parent>",
  name: "<name>",
  description: "<description>",
  input_schema: "<input_schema>",
});
```

```python Python
result = client.data_engine.create_tool({
    "parent": "<parent>",
    "name": "<name>",
    "description": "<description>",
    "input_schema": "<input_schema>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "create_tool", &[
    ("parent", "<parent>".into()),
    ("name", "<name>".into()),
    ("description", "<description>".into()),
    ("input_schema", "<input_schema>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listTools

`GET /v1/{parent}/tools`

List stored custom tools and their usage statistics.

- **Producer operationId:** `projects.tools.list`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.listTools()` · Python `client.data_engine.list_tools()` · Rust `build_request("data_engine", "list_tools", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.listTools({
  parent: "<parent>",
});
```

```python Python
result = client.data_engine.list_tools({
    "parent": "<parent>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "list_tools", &[
    ("parent", "<parent>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getTool

`GET /v1/{parent}/tools/{toolName}`

Fetch one stored custom tool and its usage statistics.

- **Producer operationId:** `projects.tools.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.getTool()` · Python `client.data_engine.get_tool()` · Rust `build_request("data_engine", "get_tool", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `toolName` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getTool({
  parent: "<parent>",
  toolName: "<toolName>",
});
```

```python Python
result = client.data_engine.get_tool({
    "parent": "<parent>",
    "toolName": "<toolName>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_tool", &[
    ("parent", "<parent>".into()),
    ("toolName", "<toolName>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### deleteTool

`DELETE /v1/{parent}/tools/{toolName}`

Delete a stored custom tool and every retained version.

- **Producer operationId:** `projects.tools.delete`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:write`
- **Call as:** TypeScript `client.dataEngine.deleteTool()` · Python `client.data_engine.delete_tool()` · Rust `build_request("data_engine", "delete_tool", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `toolName` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.deleteTool({
  parent: "<parent>",
  toolName: "<toolName>",
});
```

```python Python
result = client.data_engine.delete_tool({
    "parent": "<parent>",
    "toolName": "<toolName>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "delete_tool", &[
    ("parent", "<parent>".into()),
    ("toolName", "<toolName>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### invokeTool

`POST /v1/{parent}/tools/{toolName}:invoke`

Invoke a stored custom tool through its configured execution boundary.

- **Producer operationId:** `projects.tools.invoke`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:write`
- **Call as:** TypeScript `client.dataEngine.invokeTool()` · Python `client.data_engine.invoke_tool()` · Rust `build_request("data_engine", "invoke_tool", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `toolName` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `arguments` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.invokeTool({
  parent: "<parent>",
  toolName: "<toolName>",
  arguments: "<arguments>",
});
```

```python Python
result = client.data_engine.invoke_tool({
    "parent": "<parent>",
    "toolName": "<toolName>",
    "arguments": "<arguments>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "invoke_tool", &[
    ("parent", "<parent>".into()),
    ("toolName", "<toolName>".into()),
    ("arguments", "<arguments>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createEvidenceRecord

`POST /v1/{parent}/evidenceRecords`

Create an immutable shared evidence record by canonical content hash.

- **Producer operationId:** `projects.evidenceRecords.create`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.dataEngine.createEvidenceRecord()` · Python `client.data_engine.create_evidence_record()` · Rust `build_request("data_engine", "create_evidence_record", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `schema_version` | yes | — |
| `domain` | yes | — |
| `evidence_type` | yes | — |
| `payload_schema` | yes | — |
| `payload` | yes | — |
| `source_artifact_refs` | yes | — |
| `artifact_refs` | no | — |
| `verification_state` | yes | — |
| `verifier_receipt_artifact_refs` | no | — |
| `quality_flags` | no | — |
| `provenance` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.createEvidenceRecord({
  parent: "<parent>",
  schema_version: "<schema_version>",
  domain: "<domain>",
  evidence_type: "<evidence_type>",
});
```

```python Python
result = client.data_engine.create_evidence_record({
    "parent": "<parent>",
    "schema_version": "<schema_version>",
    "domain": "<domain>",
    "evidence_type": "<evidence_type>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "create_evidence_record", &[
    ("parent", "<parent>".into()),
    ("schema_version", "<schema_version>".into()),
    ("domain", "<domain>".into()),
    ("evidence_type", "<evidence_type>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listEvidenceRecords

`GET /v1/{parent}/evidenceRecords`

List immutable shared evidence records with bounded cursor pagination.

- **Producer operationId:** `projects.evidenceRecords.list`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.listEvidenceRecords()` · Python `client.data_engine.list_evidence_records()` · Rust `build_request("data_engine", "list_evidence_records", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `page_size` | no | — |
| `page_token` | no | — |
| `domain` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.listEvidenceRecords({
  parent: "<parent>",
  page_size: "<page_size>",
  page_token: "<page_token>",
});
```

```python Python
result = client.data_engine.list_evidence_records({
    "parent": "<parent>",
    "page_size": "<page_size>",
    "page_token": "<page_token>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "list_evidence_records", &[
    ("parent", "<parent>".into()),
    ("page_size", "<page_size>".into()),
    ("page_token", "<page_token>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getEvidenceRecord

`GET /v1/{parent}/evidenceRecords/{evidenceRecordId}`

Fetch one immutable shared evidence record by its platform digest.

- **Producer operationId:** `projects.evidenceRecords.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.getEvidenceRecord()` · Python `client.data_engine.get_evidence_record()` · Rust `build_request("data_engine", "get_evidence_record", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `evidenceRecordId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getEvidenceRecord({
  parent: "<parent>",
  evidenceRecordId: "<evidenceRecordId>",
});
```

```python Python
result = client.data_engine.get_evidence_record({
    "parent": "<parent>",
    "evidenceRecordId": "<evidenceRecordId>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_evidence_record", &[
    ("parent", "<parent>".into()),
    ("evidenceRecordId", "<evidenceRecordId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createEpisode

`POST /v1/{parent}/episodes`

Create an immutable shared episode by canonical content hash.

- **Producer operationId:** `projects.episodes.create`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.dataEngine.createEpisode()` · Python `client.data_engine.create_episode()` · Rust `build_request("data_engine", "create_episode", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `schema_version` | yes | — |
| `domain` | yes | — |
| `context_evidence_ref` | yes | — |
| `environment_ref` | yes | — |
| `seed` | yes | — |
| `observations` | yes | — |
| `tool_calls` | no | — |
| `measured_outcomes` | yes | — |
| `verifier_results` | yes | — |
| `reward_components` | yes | — |
| `terminal_reason` | yes | — |
| `uncertainty` | no | — |
| `provenance` | no | — |
| `safety_events` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.createEpisode({
  parent: "<parent>",
  schema_version: "<schema_version>",
  domain: "<domain>",
  context_evidence_ref: "<context_evidence_ref>",
});
```

```python Python
result = client.data_engine.create_episode({
    "parent": "<parent>",
    "schema_version": "<schema_version>",
    "domain": "<domain>",
    "context_evidence_ref": "<context_evidence_ref>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "create_episode", &[
    ("parent", "<parent>".into()),
    ("schema_version", "<schema_version>".into()),
    ("domain", "<domain>".into()),
    ("context_evidence_ref", "<context_evidence_ref>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listEpisodes

`GET /v1/{parent}/episodes`

List immutable shared episodes with bounded cursor pagination.

- **Producer operationId:** `projects.episodes.list`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.listEpisodes()` · Python `client.data_engine.list_episodes()` · Rust `build_request("data_engine", "list_episodes", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `page_size` | no | — |
| `page_token` | no | — |
| `domain` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.listEpisodes({
  parent: "<parent>",
  page_size: "<page_size>",
  page_token: "<page_token>",
});
```

```python Python
result = client.data_engine.list_episodes({
    "parent": "<parent>",
    "page_size": "<page_size>",
    "page_token": "<page_token>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "list_episodes", &[
    ("parent", "<parent>".into()),
    ("page_size", "<page_size>".into()),
    ("page_token", "<page_token>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getEpisode

`GET /v1/{parent}/episodes/{episodeId}`

Fetch one immutable shared episode by its platform digest.

- **Producer operationId:** `projects.episodes.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.getEpisode()` · Python `client.data_engine.get_episode()` · Rust `build_request("data_engine", "get_episode", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `episodeId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getEpisode({
  parent: "<parent>",
  episodeId: "<episodeId>",
});
```

```python Python
result = client.data_engine.get_episode({
    "parent": "<parent>",
    "episodeId": "<episodeId>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_episode", &[
    ("parent", "<parent>".into()),
    ("episodeId", "<episodeId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### queryResearchRetrieval

`POST /v1/{parent}/researchRetrieval:query`

Retrieve deterministic candidates for one exact canonical typed research obligation.

- **Producer operationId:** `query_research_retrieval`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.queryResearchRetrieval()` · Python `client.data_engine.query_research_retrieval()` · Rust `build_request("data_engine", "query_research_retrieval", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `contractVersion` | yes | — |
| `requestId` | yes | — |
| `obligation` | yes | — |
| `kinds` | no | — |
| `epistemicStatuses` | no | — |
| `checkerTrust` | no | — |
| `includeEquivalent` | no | — |
| `includeFailures` | no | — |
| `under` | no | — |
| `limit` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.queryResearchRetrieval({
  parent: "<parent>",
  contractVersion: "<contractVersion>",
  requestId: "<requestId>",
  obligation: "<obligation>",
});
```

```python Python
result = client.data_engine.query_research_retrieval({
    "parent": "<parent>",
    "contractVersion": "<contractVersion>",
    "requestId": "<requestId>",
    "obligation": "<obligation>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "query_research_retrieval", &[
    ("parent", "<parent>".into()),
    ("contractVersion", "<contractVersion>".into()),
    ("requestId", "<requestId>".into()),
    ("obligation", "<obligation>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createResearchCatalogEntry

`POST /v1/{parent}/researchCatalogEntries`

Create an immutable executable research catalog entry by canonical content hash.

- **Producer operationId:** `projects.researchCatalogEntries.create`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:write`
- **Call as:** TypeScript `client.dataEngine.createResearchCatalogEntry()` · Python `client.data_engine.create_research_catalog_entry()` · Rust `build_request("data_engine", "create_research_catalog_entry", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `kind` | yes | — |
| `version` | yes | — |
| `obligationHash` | yes | — |
| `capabilityRoute` | yes | — |
| `provenance` | yes | — |
| `tags` | no | — |
| `contentHash` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.createResearchCatalogEntry({
  parent: "<parent>",
  kind: "<kind>",
  version: "<version>",
  obligationHash: "<obligationHash>",
});
```

```python Python
result = client.data_engine.create_research_catalog_entry({
    "parent": "<parent>",
    "kind": "<kind>",
    "version": "<version>",
    "obligationHash": "<obligationHash>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "create_research_catalog_entry", &[
    ("parent", "<parent>".into()),
    ("kind", "<kind>".into()),
    ("version", "<version>".into()),
    ("obligationHash", "<obligationHash>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listResearchCatalogEntries

`GET /v1/{parent}/researchCatalogEntries`

List immutable executable research catalog entries with bounded pagination.

- **Producer operationId:** `projects.researchCatalogEntries.list`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.listResearchCatalogEntries()` · Python `client.data_engine.list_research_catalog_entries()` · Rust `build_request("data_engine", "list_research_catalog_entries", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `page_size` | no | — |
| `page_token` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.listResearchCatalogEntries({
  parent: "<parent>",
  page_size: "<page_size>",
  page_token: "<page_token>",
});
```

```python Python
result = client.data_engine.list_research_catalog_entries({
    "parent": "<parent>",
    "page_size": "<page_size>",
    "page_token": "<page_token>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "list_research_catalog_entries", &[
    ("parent", "<parent>".into()),
    ("page_size", "<page_size>".into()),
    ("page_token", "<page_token>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getResearchCatalogEntry

`GET /v1/{parent}/researchCatalogEntries/{entryId}`

Fetch one immutable executable research catalog entry by content hash.

- **Producer operationId:** `projects.researchCatalogEntries.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.getResearchCatalogEntry()` · Python `client.data_engine.get_research_catalog_entry()` · Rust `build_request("data_engine", "get_research_catalog_entry", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `entryId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getResearchCatalogEntry({
  parent: "<parent>",
  entryId: "<entryId>",
});
```

```python Python
result = client.data_engine.get_research_catalog_entry({
    "parent": "<parent>",
    "entryId": "<entryId>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_research_catalog_entry", &[
    ("parent", "<parent>".into()),
    ("entryId", "<entryId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>
