---
title: "data-engine"
description: "Domain-portable label-emergence engine: deterministic ingestion, sandboxed verification in cradle, and RL/eval/SFT dataset emission."
---

{/* Generated by scripts/gen-sdk-docs.py from surface.json -- do not hand-edit. */}

> **Private access.** Hosted Tempera services are available through design-partner onboarding.
> Use the issuer, credentials, environment, and product URLs provisioned for your workspace;
> start in staging unless Tempera explicitly approves another target.

> **Contract inventory.** This typed reference is retained for SDK compatibility.
> It is not part of the primary public workflow or evidence of general service availability.

Domain-portable label-emergence engine: deterministic ingestion, sandboxed verification in cradle, and RL/eval/SFT dataset emission.

| | |
|---|---|
| Repository | [data-engine](https://github.com/tempera-dev/data-engine) |
| Base URL env var | `TEMPERA_DATA_ENGINE_URL` (see [Environments](/environments)) |
| Audience | `data-engine` |
| Auth | bearer minted for audience data-engine, central tp_ API key, or the data-engine static token |
| Client | TypeScript `client.dataEngine` · Python `client.data_engine` · Rust `build_request("data_engine", ...)` |
| Typed operations | 58 |

Anything this page does not cover is reachable through the raw passthrough:
`client.dataEngine.request(path, options)` (TypeScript) /
`client.data_engine.request(path, ...)` (Python).

## Operations

### health

`GET /v1/health`

Check data-engine liveness; returns the service status.

- **Producer operationId:** `health.get`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.dataEngine.health()` · Python `client.data_engine.health()` · Rust `build_request("data_engine", "health", params)` · Go `BuildRequest("dataEngine", "health", params)` · C `tempera_build_request(..., "dataEngine", "health", ...)`

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.health();
```

```python Python
result = client.data_engine.health()
```

```rust Rust
let spec = client.build_request("data_engine", "health", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "health", map[string]any{})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "health",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### listUseCases

`GET /v1/{parent}/use-cases`

List the MVP use-case templates (data products and pipeline templates) for a project.

- **Producer operationId:** `projects.useCases.list`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.listUseCases()` · Python `client.data_engine.list_use_cases()` · Rust `build_request("data_engine", "list_use_cases", params)` · Go `BuildRequest("dataEngine", "listUseCases", params)` · C `tempera_build_request(..., "dataEngine", "listUseCases", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.listUseCases({
  parent: "<parent>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.data_engine.list_use_cases({
    "parent": "<parent>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "list_use_cases", &[
    ("parent", "<parent>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "listUseCases", map[string]any{
    "parent": "<parent>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "pageSize=<pageSize>&pageToken=<pageToken>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "listUseCases",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### getUseCase

`GET /v1/{parent}/use-cases/{useCaseId}`

Fetch one MVP use-case template with its rubric, modalities, skill tags, and target accuracy.

- **Producer operationId:** `projects.useCases.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.getUseCase()` · Python `client.data_engine.get_use_case()` · Rust `build_request("data_engine", "get_use_case", params)` · Go `BuildRequest("dataEngine", "getUseCase", params)` · C `tempera_build_request(..., "dataEngine", "getUseCase", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `useCaseId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getUseCase({
  parent: "<parent>",
  useCaseId: "<useCaseId>",
});
```

```python Python
result = client.data_engine.get_use_case({
    "parent": "<parent>",
    "useCaseId": "<useCaseId>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_use_case", &[
    ("parent", "<parent>".into()),
    ("useCaseId", "<useCaseId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "getUseCase", map[string]any{
    "parent": "<parent>",
    "useCaseId": "<useCaseId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
    {"useCaseId", "<useCaseId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "getUseCase",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### ingestArtifact

`POST /v1/{parent}/artifacts:ingest`

Ingest one artifact deterministically into the project; returns an async operation handle.

- **Producer operationId:** `projects.artifacts.ingest`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:write`
- **Call as:** TypeScript `client.dataEngine.ingestArtifact()` · Python `client.data_engine.ingest_artifact()` · Rust `build_request("data_engine", "ingest_artifact", params)` · Go `BuildRequest("dataEngine", "ingestArtifact", params)` · C `tempera_build_request(..., "dataEngine", "ingestArtifact", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `artifactType` | no | — |
| `source` | no | — |
| `externalId` | no | — |
| `rawBody` | no | — |
| `metadata` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.ingestArtifact({
  parent: "<parent>",
  artifactType: "<artifactType>",
  source: "<source>",
  externalId: "<externalId>",
});
```

```python Python
result = client.data_engine.ingest_artifact({
    "parent": "<parent>",
    "artifactType": "<artifactType>",
    "source": "<source>",
    "externalId": "<externalId>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "ingest_artifact", &[
    ("parent", "<parent>".into()),
    ("artifactType", "<artifactType>".into()),
    ("source", "<source>".into()),
    ("externalId", "<externalId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "ingestArtifact", map[string]any{
    "parent": "<parent>",
    "artifactType": "<artifactType>",
    "source": "<source>",
    "externalId": "<externalId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"artifactType\":\"<artifactType>\",\"source\":\"<source>\",\"externalId\":\"<externalId>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "ingestArtifact",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### ingestWeb

`POST /v1/{parent}/web:ingest`

Fetch, parse, and ingest one public HTTP(S) page as a web artifact; returns an async operation handle.

- **Producer operationId:** `projects.web.ingest`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:write`
- **Call as:** TypeScript `client.dataEngine.ingestWeb()` · Python `client.data_engine.ingest_web()` · Rust `build_request("data_engine", "ingest_web", params)` · Go `BuildRequest("dataEngine", "ingestWeb", params)` · C `tempera_build_request(..., "dataEngine", "ingestWeb", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `url` | yes | — |
| `artifactType` | no | — |
| `timeoutSeconds` | no | — |
| `maxBytes` | no | — |
| `producerVersion` | no | — |
| `metadata` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.ingestWeb({
  parent: "<parent>",
  url: "<url>",
  artifactType: "<artifactType>",
  timeoutSeconds: "<timeoutSeconds>",
});
```

```python Python
result = client.data_engine.ingest_web({
    "parent": "<parent>",
    "url": "<url>",
    "artifactType": "<artifactType>",
    "timeoutSeconds": "<timeoutSeconds>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "ingest_web", &[
    ("parent", "<parent>".into()),
    ("url", "<url>".into()),
    ("artifactType", "<artifactType>".into()),
    ("timeoutSeconds", "<timeoutSeconds>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "ingestWeb", map[string]any{
    "parent": "<parent>",
    "url": "<url>",
    "artifactType": "<artifactType>",
    "timeoutSeconds": "<timeoutSeconds>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"url\":\"<url>\",\"artifactType\":\"<artifactType>\",\"timeoutSeconds\":\"<timeoutSeconds>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "ingestWeb",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### createCampaign

`POST /v1/{parent}/campaigns`

Create a data campaign with a rubric, budget, target accuracy, and skill tags.

- **Producer operationId:** `projects.campaigns.create`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:write`
- **Call as:** TypeScript `client.dataEngine.createCampaign()` · Python `client.data_engine.create_campaign()` · Rust `build_request("data_engine", "create_campaign", params)` · Go `BuildRequest("dataEngine", "createCampaign", params)` · C `tempera_build_request(..., "dataEngine", "createCampaign", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `taskFamily` | no | — |
| `targetAccuracy` | no | — |
| `budgetCents` | no | — |
| `rubric` | no | — |
| `skillTags` | no | — |
| `requiredReviews` | no | — |
| `qualificationPolicy` | no | — |
| `routingPolicy` | no | — |
| `annotationSchema` | no | — |
| `idempotencyKey` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.createCampaign({
  parent: "<parent>",
  taskFamily: "<taskFamily>",
  targetAccuracy: "<targetAccuracy>",
  budgetCents: "<budgetCents>",
});
```

```python Python
result = client.data_engine.create_campaign({
    "parent": "<parent>",
    "taskFamily": "<taskFamily>",
    "targetAccuracy": "<targetAccuracy>",
    "budgetCents": "<budgetCents>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "create_campaign", &[
    ("parent", "<parent>".into()),
    ("taskFamily", "<taskFamily>".into()),
    ("targetAccuracy", "<targetAccuracy>".into()),
    ("budgetCents", "<budgetCents>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "createCampaign", map[string]any{
    "parent": "<parent>",
    "taskFamily": "<taskFamily>",
    "targetAccuracy": "<targetAccuracy>",
    "budgetCents": "<budgetCents>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"taskFamily\":\"<taskFamily>\",\"targetAccuracy\":\"<targetAccuracy>\",\"budgetCents\":\"<budgetCents>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "createCampaign",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### listCampaigns

`GET /v1/{parent}/campaigns`

List a project's data campaigns with pagination.

- **Producer operationId:** `projects.campaigns.list`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.listCampaigns()` · Python `client.data_engine.list_campaigns()` · Rust `build_request("data_engine", "list_campaigns", params)` · Go `BuildRequest("dataEngine", "listCampaigns", params)` · C `tempera_build_request(..., "dataEngine", "listCampaigns", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.listCampaigns({
  parent: "<parent>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.data_engine.list_campaigns({
    "parent": "<parent>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "list_campaigns", &[
    ("parent", "<parent>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "listCampaigns", map[string]any{
    "parent": "<parent>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "pageSize=<pageSize>&pageToken=<pageToken>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "listCampaigns",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### transitionCampaign

`POST /v1/{parent}/campaigns/{campaignId}:transition`

Pause, resume, or permanently close campaign job admission; returns an immutable receipt for the committed lifecycle transition.

- **Producer operationId:** `projects.campaigns.transition`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:write`
- **Call as:** TypeScript `client.dataEngine.transitionCampaign()` · Python `client.data_engine.transition_campaign()` · Rust `build_request("data_engine", "transition_campaign", params)` · Go `BuildRequest("dataEngine", "transitionCampaign", params)` · C `tempera_build_request(..., "dataEngine", "transitionCampaign", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `campaignId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `targetStatus` | yes | — |
| `idempotencyKey` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.transitionCampaign({
  parent: "<parent>",
  campaignId: "<campaignId>",
  targetStatus: "<targetStatus>",
  idempotencyKey: "<idempotencyKey>",
});
```

```python Python
result = client.data_engine.transition_campaign({
    "parent": "<parent>",
    "campaignId": "<campaignId>",
    "targetStatus": "<targetStatus>",
    "idempotencyKey": "<idempotencyKey>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "transition_campaign", &[
    ("parent", "<parent>".into()),
    ("campaignId", "<campaignId>".into()),
    ("targetStatus", "<targetStatus>".into()),
    ("idempotencyKey", "<idempotencyKey>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "transitionCampaign", map[string]any{
    "parent": "<parent>",
    "campaignId": "<campaignId>",
    "targetStatus": "<targetStatus>",
    "idempotencyKey": "<idempotencyKey>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
    {"campaignId", "<campaignId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"targetStatus\":\"<targetStatus>\",\"idempotencyKey\":\"<idempotencyKey>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "transitionCampaign",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### getReviewerQualification

`GET /v1/{parent}/campaigns/{campaignId}/reviewer-qualification`

Fetch the authenticated reviewer's project-scoped qualification and campaign eligibility without blind-probe outcomes.

- **Producer operationId:** `projects.reviewerQualifications.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `review:resolve`
- **Call as:** TypeScript `client.dataEngine.getReviewerQualification()` · Python `client.data_engine.get_reviewer_qualification()` · Rust `build_request("data_engine", "get_reviewer_qualification", params)` · Go `BuildRequest("dataEngine", "getReviewerQualification", params)` · C `tempera_build_request(..., "dataEngine", "getReviewerQualification", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `campaignId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getReviewerQualification({
  parent: "<parent>",
  campaignId: "<campaignId>",
});
```

```python Python
result = client.data_engine.get_reviewer_qualification({
    "parent": "<parent>",
    "campaignId": "<campaignId>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_reviewer_qualification", &[
    ("parent", "<parent>".into()),
    ("campaignId", "<campaignId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "getReviewerQualification", map[string]any{
    "parent": "<parent>",
    "campaignId": "<campaignId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
    {"campaignId", "<campaignId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "getReviewerQualification",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### runUseCase

`POST /v1/{parent}/pipelines:runUseCase`

Run a complete MVP use-case pipeline end to end; verifier selects the configured verification backend.

- **Producer operationId:** `projects.pipelines.runUseCase`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.dataEngine.runUseCase()` · Python `client.data_engine.run_use_case()` · Rust `build_request("data_engine", "run_use_case", params)` · Go `BuildRequest("dataEngine", "runUseCase", params)` · C `tempera_build_request(..., "dataEngine", "runUseCase", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `useCase` | yes | — |
| `verifier` | no | — |
| `budgetCents` | no | — |
| `targetAccuracy` | no | — |
| `rubric` | no | — |
| `skillTags` | no | — |
| `requiredReviews` | no | — |
| `urls` | no | — |
| `artifacts` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.runUseCase({
  parent: "<parent>",
  useCase: "<useCase>",
  verifier: "<verifier>",
  budgetCents: "<budgetCents>",
});
```

```python Python
result = client.data_engine.run_use_case({
    "parent": "<parent>",
    "useCase": "<useCase>",
    "verifier": "<verifier>",
    "budgetCents": "<budgetCents>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "run_use_case", &[
    ("parent", "<parent>".into()),
    ("useCase", "<useCase>".into()),
    ("verifier", "<verifier>".into()),
    ("budgetCents", "<budgetCents>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "runUseCase", map[string]any{
    "parent": "<parent>",
    "useCase": "<useCase>",
    "verifier": "<verifier>",
    "budgetCents": "<budgetCents>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"useCase\":\"<useCase>\",\"verifier\":\"<verifier>\",\"budgetCents\":\"<budgetCents>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "runUseCase",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### listExpertTasks

`GET /v1/{parent}/expert-tasks`

List human residual review tasks, optionally filtered by status and campaign.

- **Producer operationId:** `projects.expertTasks.list`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.listExpertTasks()` · Python `client.data_engine.list_expert_tasks()` · Rust `build_request("data_engine", "list_expert_tasks", params)` · Go `BuildRequest("dataEngine", "listExpertTasks", params)` · C `tempera_build_request(..., "dataEngine", "listExpertTasks", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |
| `status` | no | — |
| `campaignName` | no | — |
| `orderBy` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.listExpertTasks({
  parent: "<parent>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.data_engine.list_expert_tasks({
    "parent": "<parent>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "list_expert_tasks", &[
    ("parent", "<parent>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "listExpertTasks", map[string]any{
    "parent": "<parent>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "pageSize=<pageSize>&pageToken=<pageToken>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "listExpertTasks",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### createReviewQualificationTask

`POST /v1/{parent}/review-qualification-tasks`

Clone a review task into an isolated, HMAC-scored qualification task without returning the expected label.

- **Producer operationId:** `projects.reviewQualificationTasks.create`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `review:gold:manage`
- **Call as:** TypeScript `client.dataEngine.createReviewQualificationTask()` · Python `client.data_engine.create_review_qualification_task()` · Rust `build_request("data_engine", "create_review_qualification_task", params)` · Go `BuildRequest("dataEngine", "createReviewQualificationTask", params)` · C `tempera_build_request(..., "dataEngine", "createReviewQualificationTask", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `sourceExpertTaskName` | yes | — |
| `expectedLabel` | yes | — |
| `mode` | no | — |
| `feedbackPolicy` | no | — |
| `idempotencyKey` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.createReviewQualificationTask({
  parent: "<parent>",
  sourceExpertTaskName: "<sourceExpertTaskName>",
  expectedLabel: "<expectedLabel>",
  idempotencyKey: "<idempotencyKey>",
});
```

```python Python
result = client.data_engine.create_review_qualification_task({
    "parent": "<parent>",
    "sourceExpertTaskName": "<sourceExpertTaskName>",
    "expectedLabel": "<expectedLabel>",
    "idempotencyKey": "<idempotencyKey>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "create_review_qualification_task", &[
    ("parent", "<parent>".into()),
    ("sourceExpertTaskName", "<sourceExpertTaskName>".into()),
    ("expectedLabel", "<expectedLabel>".into()),
    ("idempotencyKey", "<idempotencyKey>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "createReviewQualificationTask", map[string]any{
    "parent": "<parent>",
    "sourceExpertTaskName": "<sourceExpertTaskName>",
    "expectedLabel": "<expectedLabel>",
    "idempotencyKey": "<idempotencyKey>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"sourceExpertTaskName\":\"<sourceExpertTaskName>\",\"expectedLabel\":\"<expectedLabel>\",\"idempotencyKey\":\"<idempotencyKey>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "createReviewQualificationTask",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### createReviewerRevocation

`POST /v1/{parent}/reviewer-revocations`

Record an immutable project-scoped reviewer revocation and terminate active assignments.

- **Producer operationId:** `projects.reviewerRevocations.create`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `review:gold:manage`
- **Call as:** TypeScript `client.dataEngine.createReviewerRevocation()` · Python `client.data_engine.create_reviewer_revocation()` · Rust `build_request("data_engine", "create_reviewer_revocation", params)` · Go `BuildRequest("dataEngine", "createReviewerRevocation", params)` · C `tempera_build_request(..., "dataEngine", "createReviewerRevocation", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `reviewerRef` | yes | — |
| `reason` | yes | — |
| `idempotencyKey` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.createReviewerRevocation({
  parent: "<parent>",
  reviewerRef: "<reviewerRef>",
  reason: "<reason>",
  idempotencyKey: "<idempotencyKey>",
});
```

```python Python
result = client.data_engine.create_reviewer_revocation({
    "parent": "<parent>",
    "reviewerRef": "<reviewerRef>",
    "reason": "<reason>",
    "idempotencyKey": "<idempotencyKey>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "create_reviewer_revocation", &[
    ("parent", "<parent>".into()),
    ("reviewerRef", "<reviewerRef>".into()),
    ("reason", "<reason>".into()),
    ("idempotencyKey", "<idempotencyKey>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "createReviewerRevocation", map[string]any{
    "parent": "<parent>",
    "reviewerRef": "<reviewerRef>",
    "reason": "<reason>",
    "idempotencyKey": "<idempotencyKey>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"reviewerRef\":\"<reviewerRef>\",\"reason\":\"<reason>\",\"idempotencyKey\":\"<idempotencyKey>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "createReviewerRevocation",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### resolveExpertTask

`POST /v1/{parent}/expert-tasks/{expertTaskId}:resolve`

Resolve, abstain, flag, or adjudicate one human residual with an idempotent normalized decision.

- **Producer operationId:** `projects.expertTasks.resolve`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `review:resolve`
- **Call as:** TypeScript `client.dataEngine.resolveExpertTask()` · Python `client.data_engine.resolve_expert_task()` · Rust `build_request("data_engine", "resolve_expert_task", params)` · Go `BuildRequest("dataEngine", "resolveExpertTask", params)` · C `tempera_build_request(..., "dataEngine", "resolveExpertTask", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `expertTaskId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `label` | yes | — |
| `outcome` | no | — |
| `confidence` | no | — |
| `rationale` | no | — |
| `evidence` | no | — |
| `annotation` | no | — |
| `annotatorId` | no | — |
| `idempotencyKey` | yes | — |
| `leaseToken` | no | — |
| `reviewContext` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.resolveExpertTask({
  parent: "<parent>",
  expertTaskId: "<expertTaskId>",
  label: "<label>",
  idempotencyKey: "<idempotencyKey>",
  reviewContext: "<reviewContext>",
});
```

```python Python
result = client.data_engine.resolve_expert_task({
    "parent": "<parent>",
    "expertTaskId": "<expertTaskId>",
    "label": "<label>",
    "idempotencyKey": "<idempotencyKey>",
    "reviewContext": "<reviewContext>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "resolve_expert_task", &[
    ("parent", "<parent>".into()),
    ("expertTaskId", "<expertTaskId>".into()),
    ("label", "<label>".into()),
    ("idempotencyKey", "<idempotencyKey>".into()),
    ("reviewContext", "<reviewContext>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "resolveExpertTask", map[string]any{
    "parent": "<parent>",
    "expertTaskId": "<expertTaskId>",
    "label": "<label>",
    "idempotencyKey": "<idempotencyKey>",
    "reviewContext": "<reviewContext>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
    {"expertTaskId", "<expertTaskId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"label\":\"<label>\",\"idempotencyKey\":\"<idempotencyKey>\",\"reviewContext\":\"<reviewContext>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "resolveExpertTask",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### createExpertTaskAppeal

`POST /v1/{parent}/expert-tasks/{expertTaskId}:appeal`

Record an immutable reviewer appeal and open independent adjudication without changing the appealed decision.

- **Producer operationId:** `projects.expertTasks.appeal`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `review:resolve`
- **Call as:** TypeScript `client.dataEngine.createExpertTaskAppeal()` · Python `client.data_engine.create_expert_task_appeal()` · Rust `build_request("data_engine", "create_expert_task_appeal", params)` · Go `BuildRequest("dataEngine", "createExpertTaskAppeal", params)` · C `tempera_build_request(..., "dataEngine", "createExpertTaskAppeal", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `expertTaskId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `rationale` | yes | — |
| `evidence` | no | — |
| `idempotencyKey` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.createExpertTaskAppeal({
  parent: "<parent>",
  expertTaskId: "<expertTaskId>",
  rationale: "<rationale>",
  idempotencyKey: "<idempotencyKey>",
  evidence: "<evidence>",
});
```

```python Python
result = client.data_engine.create_expert_task_appeal({
    "parent": "<parent>",
    "expertTaskId": "<expertTaskId>",
    "rationale": "<rationale>",
    "idempotencyKey": "<idempotencyKey>",
    "evidence": "<evidence>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "create_expert_task_appeal", &[
    ("parent", "<parent>".into()),
    ("expertTaskId", "<expertTaskId>".into()),
    ("rationale", "<rationale>".into()),
    ("idempotencyKey", "<idempotencyKey>".into()),
    ("evidence", "<evidence>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "createExpertTaskAppeal", map[string]any{
    "parent": "<parent>",
    "expertTaskId": "<expertTaskId>",
    "rationale": "<rationale>",
    "idempotencyKey": "<idempotencyKey>",
    "evidence": "<evidence>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
    {"expertTaskId", "<expertTaskId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"rationale\":\"<rationale>\",\"evidence\":\"<evidence>\",\"idempotencyKey\":\"<idempotencyKey>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "createExpertTaskAppeal",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### claimExpertTask

`POST /v1/{parent}/expert-tasks/{expertTaskId}:claim`

Atomically claim one open expert task with an exclusive renewable lease.

- **Producer operationId:** `projects.expertTaskAssignments.claim`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `review:resolve`
- **Call as:** TypeScript `client.dataEngine.claimExpertTask()` · Python `client.data_engine.claim_expert_task()` · Rust `build_request("data_engine", "claim_expert_task", params)` · Go `BuildRequest("dataEngine", "claimExpertTask", params)` · C `tempera_build_request(..., "dataEngine", "claimExpertTask", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `expertTaskId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `idempotencyKey` | yes | — |
| `leaseToken` | yes | — |
| `leaseDurationSeconds` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.claimExpertTask({
  parent: "<parent>",
  expertTaskId: "<expertTaskId>",
  idempotencyKey: "<idempotencyKey>",
  leaseToken: "<leaseToken>",
  leaseDurationSeconds: "<leaseDurationSeconds>",
});
```

```python Python
result = client.data_engine.claim_expert_task({
    "parent": "<parent>",
    "expertTaskId": "<expertTaskId>",
    "idempotencyKey": "<idempotencyKey>",
    "leaseToken": "<leaseToken>",
    "leaseDurationSeconds": "<leaseDurationSeconds>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "claim_expert_task", &[
    ("parent", "<parent>".into()),
    ("expertTaskId", "<expertTaskId>".into()),
    ("idempotencyKey", "<idempotencyKey>".into()),
    ("leaseToken", "<leaseToken>".into()),
    ("leaseDurationSeconds", "<leaseDurationSeconds>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "claimExpertTask", map[string]any{
    "parent": "<parent>",
    "expertTaskId": "<expertTaskId>",
    "idempotencyKey": "<idempotencyKey>",
    "leaseToken": "<leaseToken>",
    "leaseDurationSeconds": "<leaseDurationSeconds>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
    {"expertTaskId", "<expertTaskId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"idempotencyKey\":\"<idempotencyKey>\",\"leaseToken\":\"<leaseToken>\",\"leaseDurationSeconds\":\"<leaseDurationSeconds>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "claimExpertTask",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### renewExpertTaskAssignment

`POST /v1/{parent}/expert-tasks/{expertTaskId}:renew`

Renew the authenticated reviewer's active expert-task lease.

- **Producer operationId:** `projects.expertTaskAssignments.renew`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `review:resolve`
- **Call as:** TypeScript `client.dataEngine.renewExpertTaskAssignment()` · Python `client.data_engine.renew_expert_task_assignment()` · Rust `build_request("data_engine", "renew_expert_task_assignment", params)` · Go `BuildRequest("dataEngine", "renewExpertTaskAssignment", params)` · C `tempera_build_request(..., "dataEngine", "renewExpertTaskAssignment", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `expertTaskId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `idempotencyKey` | yes | — |
| `leaseToken` | yes | — |
| `leaseDurationSeconds` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.renewExpertTaskAssignment({
  parent: "<parent>",
  expertTaskId: "<expertTaskId>",
  idempotencyKey: "<idempotencyKey>",
  leaseToken: "<leaseToken>",
  leaseDurationSeconds: "<leaseDurationSeconds>",
});
```

```python Python
result = client.data_engine.renew_expert_task_assignment({
    "parent": "<parent>",
    "expertTaskId": "<expertTaskId>",
    "idempotencyKey": "<idempotencyKey>",
    "leaseToken": "<leaseToken>",
    "leaseDurationSeconds": "<leaseDurationSeconds>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "renew_expert_task_assignment", &[
    ("parent", "<parent>".into()),
    ("expertTaskId", "<expertTaskId>".into()),
    ("idempotencyKey", "<idempotencyKey>".into()),
    ("leaseToken", "<leaseToken>".into()),
    ("leaseDurationSeconds", "<leaseDurationSeconds>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "renewExpertTaskAssignment", map[string]any{
    "parent": "<parent>",
    "expertTaskId": "<expertTaskId>",
    "idempotencyKey": "<idempotencyKey>",
    "leaseToken": "<leaseToken>",
    "leaseDurationSeconds": "<leaseDurationSeconds>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
    {"expertTaskId", "<expertTaskId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"idempotencyKey\":\"<idempotencyKey>\",\"leaseToken\":\"<leaseToken>\",\"leaseDurationSeconds\":\"<leaseDurationSeconds>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "renewExpertTaskAssignment",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### releaseExpertTaskAssignment

`POST /v1/{parent}/expert-tasks/{expertTaskId}:release`

Release the authenticated reviewer's active expert-task lease for reassignment.

- **Producer operationId:** `projects.expertTaskAssignments.release`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `review:resolve`
- **Call as:** TypeScript `client.dataEngine.releaseExpertTaskAssignment()` · Python `client.data_engine.release_expert_task_assignment()` · Rust `build_request("data_engine", "release_expert_task_assignment", params)` · Go `BuildRequest("dataEngine", "releaseExpertTaskAssignment", params)` · C `tempera_build_request(..., "dataEngine", "releaseExpertTaskAssignment", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `expertTaskId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `idempotencyKey` | yes | — |
| `leaseToken` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.releaseExpertTaskAssignment({
  parent: "<parent>",
  expertTaskId: "<expertTaskId>",
  idempotencyKey: "<idempotencyKey>",
  leaseToken: "<leaseToken>",
});
```

```python Python
result = client.data_engine.release_expert_task_assignment({
    "parent": "<parent>",
    "expertTaskId": "<expertTaskId>",
    "idempotencyKey": "<idempotencyKey>",
    "leaseToken": "<leaseToken>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "release_expert_task_assignment", &[
    ("parent", "<parent>".into()),
    ("expertTaskId", "<expertTaskId>".into()),
    ("idempotencyKey", "<idempotencyKey>".into()),
    ("leaseToken", "<leaseToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "releaseExpertTaskAssignment", map[string]any{
    "parent": "<parent>",
    "expertTaskId": "<expertTaskId>",
    "idempotencyKey": "<idempotencyKey>",
    "leaseToken": "<leaseToken>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
    {"expertTaskId", "<expertTaskId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"idempotencyKey\":\"<idempotencyKey>\",\"leaseToken\":\"<leaseToken>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "releaseExpertTaskAssignment",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### saveExpertTaskDraft

`POST /v1/{parent}/expert-tasks/{expertTaskId}:saveDraft`

Autosave a version-checked draft under the active reviewer lease.

- **Producer operationId:** `projects.expertTaskAssignments.saveDraft`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `review:resolve`
- **Call as:** TypeScript `client.dataEngine.saveExpertTaskDraft()` · Python `client.data_engine.save_expert_task_draft()` · Rust `build_request("data_engine", "save_expert_task_draft", params)` · Go `BuildRequest("dataEngine", "saveExpertTaskDraft", params)` · C `tempera_build_request(..., "dataEngine", "saveExpertTaskDraft", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `expertTaskId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `idempotencyKey` | yes | — |
| `leaseToken` | yes | — |
| `draft` | yes | — |
| `expectedVersion` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.saveExpertTaskDraft({
  parent: "<parent>",
  expertTaskId: "<expertTaskId>",
  idempotencyKey: "<idempotencyKey>",
  leaseToken: "<leaseToken>",
  draft: "<draft>",
  expectedVersion: "<expectedVersion>",
});
```

```python Python
result = client.data_engine.save_expert_task_draft({
    "parent": "<parent>",
    "expertTaskId": "<expertTaskId>",
    "idempotencyKey": "<idempotencyKey>",
    "leaseToken": "<leaseToken>",
    "draft": "<draft>",
    "expectedVersion": "<expectedVersion>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "save_expert_task_draft", &[
    ("parent", "<parent>".into()),
    ("expertTaskId", "<expertTaskId>".into()),
    ("idempotencyKey", "<idempotencyKey>".into()),
    ("leaseToken", "<leaseToken>".into()),
    ("draft", "<draft>".into()),
    ("expectedVersion", "<expectedVersion>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "saveExpertTaskDraft", map[string]any{
    "parent": "<parent>",
    "expertTaskId": "<expertTaskId>",
    "idempotencyKey": "<idempotencyKey>",
    "leaseToken": "<leaseToken>",
    "draft": "<draft>",
    "expectedVersion": "<expectedVersion>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
    {"expertTaskId", "<expertTaskId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"idempotencyKey\":\"<idempotencyKey>\",\"leaseToken\":\"<leaseToken>\",\"draft\":\"<draft>\",\"expectedVersion\":\"<expectedVersion>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "saveExpertTaskDraft",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### recordReviewerSessionEvent

`POST /v1/{parent}/reviewer-sessions:record`

Record one same-origin reviewer-session lifecycle event using only an opaque browser-generated session identifier.

- **Producer operationId:** `projects.reviewerSessions.record`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `review:resolve`
- **Call as:** TypeScript `client.dataEngine.recordReviewerSessionEvent()` · Python `client.data_engine.record_reviewer_session_event()` · Rust `build_request("data_engine", "record_reviewer_session_event", params)` · Go `BuildRequest("dataEngine", "recordReviewerSessionEvent", params)` · C `tempera_build_request(..., "dataEngine", "recordReviewerSessionEvent", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `event` | yes | — |
| `sessionId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.recordReviewerSessionEvent({
  parent: "<parent>",
  event: "<event>",
  sessionId: "<sessionId>",
});
```

```python Python
result = client.data_engine.record_reviewer_session_event({
    "parent": "<parent>",
    "event": "<event>",
    "sessionId": "<sessionId>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "record_reviewer_session_event", &[
    ("parent", "<parent>".into()),
    ("event", "<event>".into()),
    ("sessionId", "<sessionId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "recordReviewerSessionEvent", map[string]any{
    "parent": "<parent>",
    "event": "<event>",
    "sessionId": "<sessionId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"event\":\"<event>\",\"sessionId\":\"<sessionId>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "recordReviewerSessionEvent",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### getReviewOperations

`GET /v1/{parent}/review-operations`

Fetch bounded project review-operations, SLA, agreement, calibration, rubric-drift, and budget observations.

- **Producer operationId:** `projects.reviewOperations.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `review:resolve`
- **Call as:** TypeScript `client.dataEngine.getReviewOperations()` · Python `client.data_engine.get_review_operations()` · Rust `build_request("data_engine", "get_review_operations", params)` · Go `BuildRequest("dataEngine", "getReviewOperations", params)` · C `tempera_build_request(..., "dataEngine", "getReviewOperations", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `windowSeconds` | no | — |
| `slaTargetSeconds` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getReviewOperations({
  parent: "<parent>",
  windowSeconds: "<windowSeconds>",
  slaTargetSeconds: "<slaTargetSeconds>",
});
```

```python Python
result = client.data_engine.get_review_operations({
    "parent": "<parent>",
    "windowSeconds": "<windowSeconds>",
    "slaTargetSeconds": "<slaTargetSeconds>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_review_operations", &[
    ("parent", "<parent>".into()),
    ("windowSeconds", "<windowSeconds>".into()),
    ("slaTargetSeconds", "<slaTargetSeconds>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "getReviewOperations", map[string]any{
    "parent": "<parent>",
    "windowSeconds": "<windowSeconds>",
    "slaTargetSeconds": "<slaTargetSeconds>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "windowSeconds=<windowSeconds>&slaTargetSeconds=<slaTargetSeconds>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "getReviewOperations",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### getMetrics

`GET /v1/{parent}/metrics`

Fetch data-engine usage and quality metrics for a project.

- **Producer operationId:** `projects.metrics.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.getMetrics()` · Python `client.data_engine.get_metrics()` · Rust `build_request("data_engine", "get_metrics", params)` · Go `BuildRequest("dataEngine", "getMetrics", params)` · C `tempera_build_request(..., "dataEngine", "getMetrics", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getMetrics({
  parent: "<parent>",
});
```

```python Python
result = client.data_engine.get_metrics({
    "parent": "<parent>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_metrics", &[
    ("parent", "<parent>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "getMetrics", map[string]any{
    "parent": "<parent>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "getMetrics",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### getLabelQuality

`GET /v1/{parent}/label-quality`

Fetch the project label-quality report and unresolved expert backlog.

- **Producer operationId:** `projects.labelQuality.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.getLabelQuality()` · Python `client.data_engine.get_label_quality()` · Rust `build_request("data_engine", "get_label_quality", params)` · Go `BuildRequest("dataEngine", "getLabelQuality", params)` · C `tempera_build_request(..., "dataEngine", "getLabelQuality", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getLabelQuality({
  parent: "<parent>",
});
```

```python Python
result = client.data_engine.get_label_quality({
    "parent": "<parent>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_label_quality", &[
    ("parent", "<parent>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "getLabelQuality", map[string]any{
    "parent": "<parent>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "getLabelQuality",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### getEcosystemReadiness

`GET /v1/{parent}/ecosystem/readiness`

Fetch public-site and ecosystem readiness signals for a project.

- **Producer operationId:** `projects.ecosystem.readiness.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.getEcosystemReadiness()` · Python `client.data_engine.get_ecosystem_readiness()` · Rust `build_request("data_engine", "get_ecosystem_readiness", params)` · Go `BuildRequest("dataEngine", "getEcosystemReadiness", params)` · C `tempera_build_request(..., "dataEngine", "getEcosystemReadiness", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getEcosystemReadiness({
  parent: "<parent>",
});
```

```python Python
result = client.data_engine.get_ecosystem_readiness({
    "parent": "<parent>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_ecosystem_readiness", &[
    ("parent", "<parent>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "getEcosystemReadiness", map[string]any{
    "parent": "<parent>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "getEcosystemReadiness",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### listArtifacts

`GET /v1/{parent}/artifacts`

List a project's artifacts with cursor pagination, expanded to the requested view (BASIC or FULL).

- **Producer operationId:** `projects.artifacts.list`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.listArtifacts()` · Python `client.data_engine.list_artifacts()` · Rust `build_request("data_engine", "list_artifacts", params)` · Go `BuildRequest("dataEngine", "listArtifacts", params)` · C `tempera_build_request(..., "dataEngine", "listArtifacts", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |
| `view` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.listArtifacts({
  parent: "<parent>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.data_engine.list_artifacts({
    "parent": "<parent>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "list_artifacts", &[
    ("parent", "<parent>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "listArtifacts", map[string]any{
    "parent": "<parent>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "pageSize=<pageSize>&pageToken=<pageToken>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "listArtifacts",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### getArtifact

`GET /v1/{parent}/artifacts/{artifactId}`

Fetch one artifact, expanded to the requested view (BASIC or FULL).

- **Producer operationId:** `projects.artifacts.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.getArtifact()` · Python `client.data_engine.get_artifact()` · Rust `build_request("data_engine", "get_artifact", params)` · Go `BuildRequest("dataEngine", "getArtifact", params)` · C `tempera_build_request(..., "dataEngine", "getArtifact", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `artifactId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `view` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getArtifact({
  parent: "<parent>",
  artifactId: "<artifactId>",
  view: "<view>",
});
```

```python Python
result = client.data_engine.get_artifact({
    "parent": "<parent>",
    "artifactId": "<artifactId>",
    "view": "<view>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_artifact", &[
    ("parent", "<parent>".into()),
    ("artifactId", "<artifactId>".into()),
    ("view", "<view>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "getArtifact", map[string]any{
    "parent": "<parent>",
    "artifactId": "<artifactId>",
    "view": "<view>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
    {"artifactId", "<artifactId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "view=<view>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "getArtifact",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### listArtifactLabels

`GET /v1/{parent}/artifacts/{artifactId}/labels`

List the labels attached to one artifact.

- **Producer operationId:** `projects.artifacts.labels.list`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.listArtifactLabels()` · Python `client.data_engine.list_artifact_labels()` · Rust `build_request("data_engine", "list_artifact_labels", params)` · Go `BuildRequest("dataEngine", "listArtifactLabels", params)` · C `tempera_build_request(..., "dataEngine", "listArtifactLabels", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `artifactId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.listArtifactLabels({
  parent: "<parent>",
  artifactId: "<artifactId>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.data_engine.list_artifact_labels({
    "parent": "<parent>",
    "artifactId": "<artifactId>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "list_artifact_labels", &[
    ("parent", "<parent>".into()),
    ("artifactId", "<artifactId>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "listArtifactLabels", map[string]any{
    "parent": "<parent>",
    "artifactId": "<artifactId>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
    {"artifactId", "<artifactId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "pageSize=<pageSize>&pageToken=<pageToken>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "listArtifactLabels",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### profileDataset

`POST /v1/{parent}/datasets:profile`

Profile dataset quality before export, including duplicates, label coverage, and distributions.

- **Producer operationId:** `projects.datasets.profile`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:write`
- **Call as:** TypeScript `client.dataEngine.profileDataset()` · Python `client.data_engine.profile_dataset()` · Rust `build_request("data_engine", "profile_dataset", params)` · Go `BuildRequest("dataEngine", "profileDataset", params)` · C `tempera_build_request(..., "dataEngine", "profileDataset", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `artifactIds` | no | — |
| `artifactType` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.profileDataset({
  parent: "<parent>",
  artifactIds: "<artifactIds>",
  artifactType: "<artifactType>",
});
```

```python Python
result = client.data_engine.profile_dataset({
    "parent": "<parent>",
    "artifactIds": "<artifactIds>",
    "artifactType": "<artifactType>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "profile_dataset", &[
    ("parent", "<parent>".into()),
    ("artifactIds", "<artifactIds>".into()),
    ("artifactType", "<artifactType>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "profileDataset", map[string]any{
    "parent": "<parent>",
    "artifactIds": "<artifactIds>",
    "artifactType": "<artifactType>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"artifactIds\":\"<artifactIds>\",\"artifactType\":\"<artifactType>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "profileDataset",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### createJob

`POST /v1/{parent}/jobs`

Create an asynchronous labeling job over a set of artifacts; returns an operation handle to poll.

- **Producer operationId:** `projects.jobs.create`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.dataEngine.createJob()` · Python `client.data_engine.create_job()` · Rust `build_request("data_engine", "create_job", params)` · Go `BuildRequest("dataEngine", "createJob", params)` · C `tempera_build_request(..., "dataEngine", "createJob", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `artifactIds` | yes | — |
| `taskFamily` | yes | — |
| `campaign` | no | — |
| `targetAccuracy` | no | — |
| `idempotencyKey` | no | — |
| `verifier` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.createJob({
  parent: "<parent>",
  artifactIds: "<artifactIds>",
  taskFamily: "<taskFamily>",
  campaign: "<campaign>",
});
```

```python Python
result = client.data_engine.create_job({
    "parent": "<parent>",
    "artifactIds": "<artifactIds>",
    "taskFamily": "<taskFamily>",
    "campaign": "<campaign>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "create_job", &[
    ("parent", "<parent>".into()),
    ("artifactIds", "<artifactIds>".into()),
    ("taskFamily", "<taskFamily>".into()),
    ("campaign", "<campaign>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "createJob", map[string]any{
    "parent": "<parent>",
    "artifactIds": "<artifactIds>",
    "taskFamily": "<taskFamily>",
    "campaign": "<campaign>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"artifactIds\":\"<artifactIds>\",\"taskFamily\":\"<taskFamily>\",\"campaign\":\"<campaign>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "createJob",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### getJob

`GET /v1/{parent}/jobs/{jobId}`

Fetch one labeling job with its state and progress.

- **Producer operationId:** `projects.jobs.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.getJob()` · Python `client.data_engine.get_job()` · Rust `build_request("data_engine", "get_job", params)` · Go `BuildRequest("dataEngine", "getJob", params)` · C `tempera_build_request(..., "dataEngine", "getJob", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `jobId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getJob({
  parent: "<parent>",
  jobId: "<jobId>",
});
```

```python Python
result = client.data_engine.get_job({
    "parent": "<parent>",
    "jobId": "<jobId>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_job", &[
    ("parent", "<parent>".into()),
    ("jobId", "<jobId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "getJob", map[string]any{
    "parent": "<parent>",
    "jobId": "<jobId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
    {"jobId", "<jobId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "getJob",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### getJobResults

`GET /v1/{parent}/jobs/{jobId}/results`

List the deterministic label results a job produced.

- **Producer operationId:** `projects.jobs.results.list`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.getJobResults()` · Python `client.data_engine.get_job_results()` · Rust `build_request("data_engine", "get_job_results", params)` · Go `BuildRequest("dataEngine", "getJobResults", params)` · C `tempera_build_request(..., "dataEngine", "getJobResults", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `jobId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getJobResults({
  parent: "<parent>",
  jobId: "<jobId>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.data_engine.get_job_results({
    "parent": "<parent>",
    "jobId": "<jobId>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_job_results", &[
    ("parent", "<parent>".into()),
    ("jobId", "<jobId>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "getJobResults", map[string]any{
    "parent": "<parent>",
    "jobId": "<jobId>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
    {"jobId", "<jobId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "pageSize=<pageSize>&pageToken=<pageToken>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "getJobResults",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### getProduct

`GET /v1/{parent}/products/{productId}`

Fetch one emitted product bundle with its status and manifest URL.

- **Producer operationId:** `projects.products.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.getProduct()` · Python `client.data_engine.get_product()` · Rust `build_request("data_engine", "get_product", params)` · Go `BuildRequest("dataEngine", "getProduct", params)` · C `tempera_build_request(..., "dataEngine", "getProduct", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `productId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getProduct({
  parent: "<parent>",
  productId: "<productId>",
});
```

```python Python
result = client.data_engine.get_product({
    "parent": "<parent>",
    "productId": "<productId>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_product", &[
    ("parent", "<parent>".into()),
    ("productId", "<productId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "getProduct", map[string]any{
    "parent": "<parent>",
    "productId": "<productId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
    {"productId", "<productId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "getProduct",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### validateProduct

`POST /v1/{parent}/products/{productId}:validate`

Validate an emitted product bundle's referential integrity and hygiene.

- **Producer operationId:** `projects.products.validate`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:write`
- **Call as:** TypeScript `client.dataEngine.validateProduct()` · Python `client.data_engine.validate_product()` · Rust `build_request("data_engine", "validate_product", params)` · Go `BuildRequest("dataEngine", "validateProduct", params)` · C `tempera_build_request(..., "dataEngine", "validateProduct", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `productId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.validateProduct({
  parent: "<parent>",
  productId: "<productId>",
});
```

```python Python
result = client.data_engine.validate_product({
    "parent": "<parent>",
    "productId": "<productId>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "validate_product", &[
    ("parent", "<parent>".into()),
    ("productId", "<productId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "validateProduct", map[string]any{
    "parent": "<parent>",
    "productId": "<productId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
    {"productId", "<productId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "validateProduct",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### checkProductLeakage

`POST /v1/{parent}/products:checkLeakage`

Check raw-hash leakage between exactly two product bundles.

- **Producer operationId:** `projects.products.checkLeakage`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:write`
- **Call as:** TypeScript `client.dataEngine.checkProductLeakage()` · Python `client.data_engine.check_product_leakage()` · Rust `build_request("data_engine", "check_product_leakage", params)` · Go `BuildRequest("dataEngine", "checkProductLeakage", params)` · C `tempera_build_request(..., "dataEngine", "checkProductLeakage", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `productIds` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.checkProductLeakage({
  parent: "<parent>",
  productIds: "<productIds>",
});
```

```python Python
result = client.data_engine.check_product_leakage({
    "parent": "<parent>",
    "productIds": "<productIds>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "check_product_leakage", &[
    ("parent", "<parent>".into()),
    ("productIds", "<productIds>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "checkProductLeakage", map[string]any{
    "parent": "<parent>",
    "productIds": "<productIds>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"productIds\":\"<productIds>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "checkProductLeakage",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### getProductManifest

`GET /v1/{parent}/products/{productId}/manifest`

Fetch an integrity-checked, bounded manifest for an emitted eval product.

- **Producer operationId:** `projects.products.manifest.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.getProductManifest()` · Python `client.data_engine.get_product_manifest()` · Rust `build_request("data_engine", "get_product_manifest", params)` · Go `BuildRequest("dataEngine", "getProductManifest", params)` · C `tempera_build_request(..., "dataEngine", "getProductManifest", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `productId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getProductManifest({
  parent: "<parent>",
  productId: "<productId>",
});
```

```python Python
result = client.data_engine.get_product_manifest({
    "parent": "<parent>",
    "productId": "<productId>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_product_manifest", &[
    ("parent", "<parent>".into()),
    ("productId", "<productId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "getProductManifest", map[string]any{
    "parent": "<parent>",
    "productId": "<productId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
    {"productId", "<productId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "getProductManifest",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### admitTrainingRelease

`POST /v1/{parent}/training-releases:admit`

Admit exact training and heldout product generations after revalidating integrity, review consent, and leakage constraints.

- **Producer operationId:** `projects.trainingReleases.admit`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `training:publish`
- **Call as:** TypeScript `client.dataEngine.admitTrainingRelease()` · Python `client.data_engine.admit_training_release()` · Rust `build_request("data_engine", "admit_training_release", params)` · Go `BuildRequest("dataEngine", "admitTrainingRelease", params)` · C `tempera_build_request(..., "dataEngine", "admitTrainingRelease", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `trainingProductId` | yes | — |
| `heldoutProductId` | yes | — |
| `idempotencyKey` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.admitTrainingRelease({
  parent: "<parent>",
  trainingProductId: "<trainingProductId>",
  heldoutProductId: "<heldoutProductId>",
  idempotencyKey: "<idempotencyKey>",
});
```

```python Python
result = client.data_engine.admit_training_release({
    "parent": "<parent>",
    "trainingProductId": "<trainingProductId>",
    "heldoutProductId": "<heldoutProductId>",
    "idempotencyKey": "<idempotencyKey>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "admit_training_release", &[
    ("parent", "<parent>".into()),
    ("trainingProductId", "<trainingProductId>".into()),
    ("heldoutProductId", "<heldoutProductId>".into()),
    ("idempotencyKey", "<idempotencyKey>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "admitTrainingRelease", map[string]any{
    "parent": "<parent>",
    "trainingProductId": "<trainingProductId>",
    "heldoutProductId": "<heldoutProductId>",
    "idempotencyKey": "<idempotencyKey>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"trainingProductId\":\"<trainingProductId>\",\"heldoutProductId\":\"<heldoutProductId>\",\"idempotencyKey\":\"<idempotencyKey>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "admitTrainingRelease",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### getTrainingRelease

`GET /v1/{parent}/training-releases/{releaseId}`

Revalidate and fetch one training release, including any durable stale state.

- **Producer operationId:** `projects.trainingReleases.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `training:publish`
- **Call as:** TypeScript `client.dataEngine.getTrainingRelease()` · Python `client.data_engine.get_training_release()` · Rust `build_request("data_engine", "get_training_release", params)` · Go `BuildRequest("dataEngine", "getTrainingRelease", params)` · C `tempera_build_request(..., "dataEngine", "getTrainingRelease", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `releaseId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getTrainingRelease({
  parent: "<parent>",
  releaseId: "<releaseId>",
});
```

```python Python
result = client.data_engine.get_training_release({
    "parent": "<parent>",
    "releaseId": "<releaseId>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_training_release", &[
    ("parent", "<parent>".into()),
    ("releaseId", "<releaseId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "getTrainingRelease", map[string]any{
    "parent": "<parent>",
    "releaseId": "<releaseId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
    {"releaseId", "<releaseId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "getTrainingRelease",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### deriveBundle

`POST /v1/{parent}/products/{productId}:derive`

Derive a deterministic post-training bundle from a ready product.

- **Producer operationId:** `projects.products.derive`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.dataEngine.deriveBundle()` · Python `client.data_engine.derive_bundle()` · Rust `build_request("data_engine", "derive_bundle", params)` · Go `BuildRequest("dataEngine", "deriveBundle", params)` · C `tempera_build_request(..., "dataEngine", "deriveBundle", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `productId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `format` | yes | — |
| `trainFraction` | no | — |
| `includeRaw` | no | — |
| `pageSize` | no | — |
| `pageToken` | no | — |
| `pairSources` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.deriveBundle({
  parent: "<parent>",
  productId: "<productId>",
  format: "<format>",
  trainFraction: "<trainFraction>",
  includeRaw: "<includeRaw>",
});
```

```python Python
result = client.data_engine.derive_bundle({
    "parent": "<parent>",
    "productId": "<productId>",
    "format": "<format>",
    "trainFraction": "<trainFraction>",
    "includeRaw": "<includeRaw>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "derive_bundle", &[
    ("parent", "<parent>".into()),
    ("productId", "<productId>".into()),
    ("format", "<format>".into()),
    ("trainFraction", "<trainFraction>".into()),
    ("includeRaw", "<includeRaw>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "deriveBundle", map[string]any{
    "parent": "<parent>",
    "productId": "<productId>",
    "format": "<format>",
    "trainFraction": "<trainFraction>",
    "includeRaw": "<includeRaw>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
    {"productId", "<productId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"format\":\"<format>\",\"trainFraction\":\"<trainFraction>\",\"includeRaw\":\"<includeRaw>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "deriveBundle",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### emitEval

`POST /v1/{parent}/products:emitEval`

Emit an eval dataset bundle from verified artifacts; returns an async operation handle.

- **Producer operationId:** `projects.products.emitEval`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.dataEngine.emitEval()` · Python `client.data_engine.emit_eval()` · Rust `build_request("data_engine", "emit_eval", params)` · Go `BuildRequest("dataEngine", "emitEval", params)` · C `tempera_build_request(..., "dataEngine", "emitEval", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `artifactIds` | yes | — |
| `job` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.emitEval({
  parent: "<parent>",
  artifactIds: "<artifactIds>",
  job: "<job>",
});
```

```python Python
result = client.data_engine.emit_eval({
    "parent": "<parent>",
    "artifactIds": "<artifactIds>",
    "job": "<job>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "emit_eval", &[
    ("parent", "<parent>".into()),
    ("artifactIds", "<artifactIds>".into()),
    ("job", "<job>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "emitEval", map[string]any{
    "parent": "<parent>",
    "artifactIds": "<artifactIds>",
    "job": "<job>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"artifactIds\":\"<artifactIds>\",\"job\":\"<job>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "emitEval",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### extractSource

`POST /v1/{parent}/sources:extract`

Extract bounded objects or records from a configured source connector.

- **Producer operationId:** `projects.sources.extract`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:write`
- **Call as:** TypeScript `client.dataEngine.extractSource()` · Python `client.data_engine.extract_source()` · Rust `build_request("data_engine", "extract_source", params)` · Go `BuildRequest("dataEngine", "extractSource", params)` · C `tempera_build_request(..., "dataEngine", "extractSource", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `connector` | yes | — |
| `bucket` | no | — |
| `prefix` | no | — |
| `key` | no | — |
| `maxObjects` | no | — |
| `maxBytes` | no | — |
| `statement` | no | — |
| `limit` | no | — |
| `soql` | no | — |
| `maxPages` | no | — |
| `artifactType` | no | — |
| `source` | no | — |
| `ingest` | no | — |
| `metadata` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.extractSource({
  parent: "<parent>",
  connector: "<connector>",
  bucket: "<bucket>",
  prefix: "<prefix>",
});
```

```python Python
result = client.data_engine.extract_source({
    "parent": "<parent>",
    "connector": "<connector>",
    "bucket": "<bucket>",
    "prefix": "<prefix>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "extract_source", &[
    ("parent", "<parent>".into()),
    ("connector", "<connector>".into()),
    ("bucket", "<bucket>".into()),
    ("prefix", "<prefix>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "extractSource", map[string]any{
    "parent": "<parent>",
    "connector": "<connector>",
    "bucket": "<bucket>",
    "prefix": "<prefix>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"connector\":\"<connector>\",\"bucket\":\"<bucket>\",\"prefix\":\"<prefix>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "extractSource",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### listConnectors

`GET /v1/{parent}/connectors`

List registered source connectors for a project.

- **Producer operationId:** `projects.connectors.list`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.listConnectors()` · Python `client.data_engine.list_connectors()` · Rust `build_request("data_engine", "list_connectors", params)` · Go `BuildRequest("dataEngine", "listConnectors", params)` · C `tempera_build_request(..., "dataEngine", "listConnectors", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.listConnectors({
  parent: "<parent>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.data_engine.list_connectors({
    "parent": "<parent>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "list_connectors", &[
    ("parent", "<parent>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "listConnectors", map[string]any{
    "parent": "<parent>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "pageSize=<pageSize>&pageToken=<pageToken>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "listConnectors",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### createTool

`POST /v1/{parent}/tools`

Create or version-bump a stored custom tool.

- **Producer operationId:** `projects.tools.create`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:write`
- **Call as:** TypeScript `client.dataEngine.createTool()` · Python `client.data_engine.create_tool()` · Rust `build_request("data_engine", "create_tool", params)` · Go `BuildRequest("dataEngine", "createTool", params)` · C `tempera_build_request(..., "dataEngine", "createTool", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `name` | yes | — |
| `description` | yes | — |
| `inputSchema` | no | — |
| `kind` | yes | — |
| `implementation` | yes | — |
| `createdBy` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.createTool({
  parent: "<parent>",
  name: "<name>",
  description: "<description>",
  kind: "<kind>",
  implementation: "<implementation>",
});
```

```python Python
result = client.data_engine.create_tool({
    "parent": "<parent>",
    "name": "<name>",
    "description": "<description>",
    "kind": "<kind>",
    "implementation": "<implementation>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "create_tool", &[
    ("parent", "<parent>".into()),
    ("name", "<name>".into()),
    ("description", "<description>".into()),
    ("kind", "<kind>".into()),
    ("implementation", "<implementation>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "createTool", map[string]any{
    "parent": "<parent>",
    "name": "<name>",
    "description": "<description>",
    "kind": "<kind>",
    "implementation": "<implementation>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"name\":\"<name>\",\"description\":\"<description>\",\"kind\":\"<kind>\",\"implementation\":\"<implementation>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "createTool",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### listTools

`GET /v1/{parent}/tools`

List stored custom tools and their usage statistics.

- **Producer operationId:** `projects.tools.list`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.listTools()` · Python `client.data_engine.list_tools()` · Rust `build_request("data_engine", "list_tools", params)` · Go `BuildRequest("dataEngine", "listTools", params)` · C `tempera_build_request(..., "dataEngine", "listTools", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.listTools({
  parent: "<parent>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.data_engine.list_tools({
    "parent": "<parent>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "list_tools", &[
    ("parent", "<parent>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "listTools", map[string]any{
    "parent": "<parent>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "pageSize=<pageSize>&pageToken=<pageToken>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "listTools",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### getTool

`GET /v1/{parent}/tools/{toolName}`

Fetch one stored custom tool and its usage statistics.

- **Producer operationId:** `projects.tools.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.getTool()` · Python `client.data_engine.get_tool()` · Rust `build_request("data_engine", "get_tool", params)` · Go `BuildRequest("dataEngine", "getTool", params)` · C `tempera_build_request(..., "dataEngine", "getTool", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `toolName` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getTool({
  parent: "<parent>",
  toolName: "<toolName>",
});
```

```python Python
result = client.data_engine.get_tool({
    "parent": "<parent>",
    "toolName": "<toolName>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_tool", &[
    ("parent", "<parent>".into()),
    ("toolName", "<toolName>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "getTool", map[string]any{
    "parent": "<parent>",
    "toolName": "<toolName>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
    {"toolName", "<toolName>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "getTool",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### deleteTool

`DELETE /v1/{parent}/tools/{toolName}`

Delete a stored custom tool and every retained version.

- **Producer operationId:** `projects.tools.delete`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:write`
- **Call as:** TypeScript `client.dataEngine.deleteTool()` · Python `client.data_engine.delete_tool()` · Rust `build_request("data_engine", "delete_tool", params)` · Go `BuildRequest("dataEngine", "deleteTool", params)` · C `tempera_build_request(..., "dataEngine", "deleteTool", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `toolName` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.deleteTool({
  parent: "<parent>",
  toolName: "<toolName>",
});
```

```python Python
result = client.data_engine.delete_tool({
    "parent": "<parent>",
    "toolName": "<toolName>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "delete_tool", &[
    ("parent", "<parent>".into()),
    ("toolName", "<toolName>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "deleteTool", map[string]any{
    "parent": "<parent>",
    "toolName": "<toolName>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
    {"toolName", "<toolName>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "deleteTool",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### invokeTool

`POST /v1/{parent}/tools/{toolName}:invoke`

Invoke a stored custom tool through its configured execution boundary.

- **Producer operationId:** `projects.tools.invoke`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:write`
- **Call as:** TypeScript `client.dataEngine.invokeTool()` · Python `client.data_engine.invoke_tool()` · Rust `build_request("data_engine", "invoke_tool", params)` · Go `BuildRequest("dataEngine", "invokeTool", params)` · C `tempera_build_request(..., "dataEngine", "invokeTool", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `toolName` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `arguments` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.invokeTool({
  parent: "<parent>",
  toolName: "<toolName>",
  arguments: "<arguments>",
});
```

```python Python
result = client.data_engine.invoke_tool({
    "parent": "<parent>",
    "toolName": "<toolName>",
    "arguments": "<arguments>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "invoke_tool", &[
    ("parent", "<parent>".into()),
    ("toolName", "<toolName>".into()),
    ("arguments", "<arguments>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "invokeTool", map[string]any{
    "parent": "<parent>",
    "toolName": "<toolName>",
    "arguments": "<arguments>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
    {"toolName", "<toolName>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"arguments\":\"<arguments>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "invokeTool",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### commitDiscoveryRelease

`POST /v1/{parent}/discoveryReleases:commit`

Atomically commit an immutable discovery release graph.

- **Producer operationId:** `projects.discoveryReleases.commit`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.dataEngine.commitDiscoveryRelease()` · Python `client.data_engine.commit_discovery_release()` · Rust `build_request("data_engine", "commit_discovery_release", params)` · Go `BuildRequest("dataEngine", "commitDiscoveryRelease", params)` · C `tempera_build_request(..., "dataEngine", "commitDiscoveryRelease", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `schemaVersion` | yes | — |
| `requestId` | yes | — |
| `releaseId` | yes | — |
| `releaseKind` | yes | — |
| `evidenceClass` | yes | — |
| `claimClass` | yes | — |
| `bindings` | yes | — |
| `lineageEdges` | yes | — |
| `budget` | yes | — |
| `eligibility` | yes | — |
| `prospectiveExecution` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.commitDiscoveryRelease({
  parent: "<parent>",
  schemaVersion: "<schemaVersion>",
  requestId: "<requestId>",
  releaseId: "<releaseId>",
  releaseKind: "<releaseKind>",
  evidenceClass: "<evidenceClass>",
  claimClass: "<claimClass>",
  bindings: "<bindings>",
  lineageEdges: "<lineageEdges>",
  budget: "<budget>",
  eligibility: "<eligibility>",
  prospectiveExecution: "<prospectiveExecution>",
});
```

```python Python
result = client.data_engine.commit_discovery_release({
    "parent": "<parent>",
    "schemaVersion": "<schemaVersion>",
    "requestId": "<requestId>",
    "releaseId": "<releaseId>",
    "releaseKind": "<releaseKind>",
    "evidenceClass": "<evidenceClass>",
    "claimClass": "<claimClass>",
    "bindings": "<bindings>",
    "lineageEdges": "<lineageEdges>",
    "budget": "<budget>",
    "eligibility": "<eligibility>",
    "prospectiveExecution": "<prospectiveExecution>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "commit_discovery_release", &[
    ("parent", "<parent>".into()),
    ("schemaVersion", "<schemaVersion>".into()),
    ("requestId", "<requestId>".into()),
    ("releaseId", "<releaseId>".into()),
    ("releaseKind", "<releaseKind>".into()),
    ("evidenceClass", "<evidenceClass>".into()),
    ("claimClass", "<claimClass>".into()),
    ("bindings", "<bindings>".into()),
    ("lineageEdges", "<lineageEdges>".into()),
    ("budget", "<budget>".into()),
    ("eligibility", "<eligibility>".into()),
    ("prospectiveExecution", "<prospectiveExecution>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "commitDiscoveryRelease", map[string]any{
    "parent": "<parent>",
    "schemaVersion": "<schemaVersion>",
    "requestId": "<requestId>",
    "releaseId": "<releaseId>",
    "releaseKind": "<releaseKind>",
    "evidenceClass": "<evidenceClass>",
    "claimClass": "<claimClass>",
    "bindings": "<bindings>",
    "lineageEdges": "<lineageEdges>",
    "budget": "<budget>",
    "eligibility": "<eligibility>",
    "prospectiveExecution": "<prospectiveExecution>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"schemaVersion\":\"<schemaVersion>\",\"requestId\":\"<requestId>\",\"releaseId\":\"<releaseId>\",\"releaseKind\":\"<releaseKind>\",\"evidenceClass\":\"<evidenceClass>\",\"claimClass\":\"<claimClass>\",\"bindings\":\"<bindings>\",\"lineageEdges\":\"<lineageEdges>\",\"budget\":\"<budget>\",\"eligibility\":\"<eligibility>\",\"prospectiveExecution\":\"<prospectiveExecution>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "commitDiscoveryRelease",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### getDiscoveryRelease

`GET /v1/{parent}/discoveryReleases/{discoveryReleaseId}`

Get one immutable discovery release.

- **Producer operationId:** `projects.discoveryReleases.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.getDiscoveryRelease()` · Python `client.data_engine.get_discovery_release()` · Rust `build_request("data_engine", "get_discovery_release", params)` · Go `BuildRequest("dataEngine", "getDiscoveryRelease", params)` · C `tempera_build_request(..., "dataEngine", "getDiscoveryRelease", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `discoveryReleaseId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getDiscoveryRelease({
  parent: "<parent>",
  discoveryReleaseId: "<discoveryReleaseId>",
});
```

```python Python
result = client.data_engine.get_discovery_release({
    "parent": "<parent>",
    "discoveryReleaseId": "<discoveryReleaseId>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_discovery_release", &[
    ("parent", "<parent>".into()),
    ("discoveryReleaseId", "<discoveryReleaseId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "getDiscoveryRelease", map[string]any{
    "parent": "<parent>",
    "discoveryReleaseId": "<discoveryReleaseId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
    {"discoveryReleaseId", "<discoveryReleaseId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "getDiscoveryRelease",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### createEvidenceRecord

`POST /v1/{parent}/evidenceRecords`

Create an immutable shared evidence record by canonical content hash.

- **Producer operationId:** `projects.evidenceRecords.create`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.dataEngine.createEvidenceRecord()` · Python `client.data_engine.create_evidence_record()` · Rust `build_request("data_engine", "create_evidence_record", params)` · Go `BuildRequest("dataEngine", "createEvidenceRecord", params)` · C `tempera_build_request(..., "dataEngine", "createEvidenceRecord", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `schemaVersion` | yes | — |
| `domain` | yes | — |
| `evidenceType` | yes | — |
| `payloadSchema` | yes | — |
| `payload` | yes | — |
| `sourceArtifactRefs` | yes | — |
| `artifactRefs` | no | — |
| `verificationState` | yes | — |
| `verifierReceiptArtifactRefs` | no | — |
| `qualityFlags` | no | — |
| `provenance` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.createEvidenceRecord({
  parent: "<parent>",
  schemaVersion: "<schemaVersion>",
  domain: "<domain>",
  evidenceType: "<evidenceType>",
  payloadSchema: "<payloadSchema>",
  payload: "<payload>",
  sourceArtifactRefs: "<sourceArtifactRefs>",
  verificationState: "<verificationState>",
});
```

```python Python
result = client.data_engine.create_evidence_record({
    "parent": "<parent>",
    "schemaVersion": "<schemaVersion>",
    "domain": "<domain>",
    "evidenceType": "<evidenceType>",
    "payloadSchema": "<payloadSchema>",
    "payload": "<payload>",
    "sourceArtifactRefs": "<sourceArtifactRefs>",
    "verificationState": "<verificationState>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "create_evidence_record", &[
    ("parent", "<parent>".into()),
    ("schemaVersion", "<schemaVersion>".into()),
    ("domain", "<domain>".into()),
    ("evidenceType", "<evidenceType>".into()),
    ("payloadSchema", "<payloadSchema>".into()),
    ("payload", "<payload>".into()),
    ("sourceArtifactRefs", "<sourceArtifactRefs>".into()),
    ("verificationState", "<verificationState>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "createEvidenceRecord", map[string]any{
    "parent": "<parent>",
    "schemaVersion": "<schemaVersion>",
    "domain": "<domain>",
    "evidenceType": "<evidenceType>",
    "payloadSchema": "<payloadSchema>",
    "payload": "<payload>",
    "sourceArtifactRefs": "<sourceArtifactRefs>",
    "verificationState": "<verificationState>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"schemaVersion\":\"<schemaVersion>\",\"domain\":\"<domain>\",\"evidenceType\":\"<evidenceType>\",\"payloadSchema\":\"<payloadSchema>\",\"payload\":\"<payload>\",\"sourceArtifactRefs\":\"<sourceArtifactRefs>\",\"verificationState\":\"<verificationState>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "createEvidenceRecord",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### listEvidenceRecords

`GET /v1/{parent}/evidenceRecords`

List immutable shared evidence records with bounded cursor pagination.

- **Producer operationId:** `projects.evidenceRecords.list`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.listEvidenceRecords()` · Python `client.data_engine.list_evidence_records()` · Rust `build_request("data_engine", "list_evidence_records", params)` · Go `BuildRequest("dataEngine", "listEvidenceRecords", params)` · C `tempera_build_request(..., "dataEngine", "listEvidenceRecords", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |
| `domain` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.listEvidenceRecords({
  parent: "<parent>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.data_engine.list_evidence_records({
    "parent": "<parent>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "list_evidence_records", &[
    ("parent", "<parent>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "listEvidenceRecords", map[string]any{
    "parent": "<parent>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "pageSize=<pageSize>&pageToken=<pageToken>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "listEvidenceRecords",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### getEvidenceRecord

`GET /v1/{parent}/evidenceRecords/{evidenceRecordId}`

Fetch one immutable shared evidence record by its platform digest.

- **Producer operationId:** `projects.evidenceRecords.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.getEvidenceRecord()` · Python `client.data_engine.get_evidence_record()` · Rust `build_request("data_engine", "get_evidence_record", params)` · Go `BuildRequest("dataEngine", "getEvidenceRecord", params)` · C `tempera_build_request(..., "dataEngine", "getEvidenceRecord", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `evidenceRecordId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getEvidenceRecord({
  parent: "<parent>",
  evidenceRecordId: "<evidenceRecordId>",
});
```

```python Python
result = client.data_engine.get_evidence_record({
    "parent": "<parent>",
    "evidenceRecordId": "<evidenceRecordId>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_evidence_record", &[
    ("parent", "<parent>".into()),
    ("evidenceRecordId", "<evidenceRecordId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "getEvidenceRecord", map[string]any{
    "parent": "<parent>",
    "evidenceRecordId": "<evidenceRecordId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
    {"evidenceRecordId", "<evidenceRecordId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "getEvidenceRecord",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### createEpisode

`POST /v1/{parent}/episodes`

Create an immutable shared episode by canonical content hash.

- **Producer operationId:** `projects.episodes.create`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `eval:run`
- **Call as:** TypeScript `client.dataEngine.createEpisode()` · Python `client.data_engine.create_episode()` · Rust `build_request("data_engine", "create_episode", params)` · Go `BuildRequest("dataEngine", "createEpisode", params)` · C `tempera_build_request(..., "dataEngine", "createEpisode", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `schemaVersion` | yes | — |
| `domain` | yes | — |
| `contextEvidenceRef` | yes | — |
| `environmentRef` | yes | — |
| `seed` | yes | — |
| `observations` | yes | — |
| `toolCalls` | no | — |
| `measuredOutcomes` | yes | — |
| `verifierResults` | yes | — |
| `rewardComponents` | yes | — |
| `terminalReason` | yes | — |
| `uncertainty` | no | — |
| `provenance` | no | — |
| `safetyEvents` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.createEpisode({
  parent: "<parent>",
  schemaVersion: "<schemaVersion>",
  domain: "<domain>",
  contextEvidenceRef: "<contextEvidenceRef>",
  environmentRef: "<environmentRef>",
  seed: "<seed>",
  observations: "<observations>",
  measuredOutcomes: "<measuredOutcomes>",
  verifierResults: "<verifierResults>",
  rewardComponents: "<rewardComponents>",
  terminalReason: "<terminalReason>",
});
```

```python Python
result = client.data_engine.create_episode({
    "parent": "<parent>",
    "schemaVersion": "<schemaVersion>",
    "domain": "<domain>",
    "contextEvidenceRef": "<contextEvidenceRef>",
    "environmentRef": "<environmentRef>",
    "seed": "<seed>",
    "observations": "<observations>",
    "measuredOutcomes": "<measuredOutcomes>",
    "verifierResults": "<verifierResults>",
    "rewardComponents": "<rewardComponents>",
    "terminalReason": "<terminalReason>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "create_episode", &[
    ("parent", "<parent>".into()),
    ("schemaVersion", "<schemaVersion>".into()),
    ("domain", "<domain>".into()),
    ("contextEvidenceRef", "<contextEvidenceRef>".into()),
    ("environmentRef", "<environmentRef>".into()),
    ("seed", "<seed>".into()),
    ("observations", "<observations>".into()),
    ("measuredOutcomes", "<measuredOutcomes>".into()),
    ("verifierResults", "<verifierResults>".into()),
    ("rewardComponents", "<rewardComponents>".into()),
    ("terminalReason", "<terminalReason>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "createEpisode", map[string]any{
    "parent": "<parent>",
    "schemaVersion": "<schemaVersion>",
    "domain": "<domain>",
    "contextEvidenceRef": "<contextEvidenceRef>",
    "environmentRef": "<environmentRef>",
    "seed": "<seed>",
    "observations": "<observations>",
    "measuredOutcomes": "<measuredOutcomes>",
    "verifierResults": "<verifierResults>",
    "rewardComponents": "<rewardComponents>",
    "terminalReason": "<terminalReason>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"schemaVersion\":\"<schemaVersion>\",\"domain\":\"<domain>\",\"contextEvidenceRef\":\"<contextEvidenceRef>\",\"environmentRef\":\"<environmentRef>\",\"seed\":\"<seed>\",\"observations\":\"<observations>\",\"measuredOutcomes\":\"<measuredOutcomes>\",\"verifierResults\":\"<verifierResults>\",\"rewardComponents\":\"<rewardComponents>\",\"terminalReason\":\"<terminalReason>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "createEpisode",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### listEpisodes

`GET /v1/{parent}/episodes`

List immutable shared episodes with bounded cursor pagination.

- **Producer operationId:** `projects.episodes.list`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.listEpisodes()` · Python `client.data_engine.list_episodes()` · Rust `build_request("data_engine", "list_episodes", params)` · Go `BuildRequest("dataEngine", "listEpisodes", params)` · C `tempera_build_request(..., "dataEngine", "listEpisodes", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |
| `domain` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.listEpisodes({
  parent: "<parent>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.data_engine.list_episodes({
    "parent": "<parent>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "list_episodes", &[
    ("parent", "<parent>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "listEpisodes", map[string]any{
    "parent": "<parent>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "pageSize=<pageSize>&pageToken=<pageToken>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "listEpisodes",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### getEpisode

`GET /v1/{parent}/episodes/{episodeId}`

Fetch one immutable shared episode by its platform digest.

- **Producer operationId:** `projects.episodes.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.getEpisode()` · Python `client.data_engine.get_episode()` · Rust `build_request("data_engine", "get_episode", params)` · Go `BuildRequest("dataEngine", "getEpisode", params)` · C `tempera_build_request(..., "dataEngine", "getEpisode", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `episodeId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getEpisode({
  parent: "<parent>",
  episodeId: "<episodeId>",
});
```

```python Python
result = client.data_engine.get_episode({
    "parent": "<parent>",
    "episodeId": "<episodeId>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_episode", &[
    ("parent", "<parent>".into()),
    ("episodeId", "<episodeId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "getEpisode", map[string]any{
    "parent": "<parent>",
    "episodeId": "<episodeId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
    {"episodeId", "<episodeId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "getEpisode",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### queryResearchRetrieval

`POST /v1/{parent}/researchRetrieval:query`

Retrieve deterministic candidates for one exact canonical typed research obligation.

- **Producer operationId:** `query_research_retrieval`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.queryResearchRetrieval()` · Python `client.data_engine.query_research_retrieval()` · Rust `build_request("data_engine", "query_research_retrieval", params)` · Go `BuildRequest("dataEngine", "queryResearchRetrieval", params)` · C `tempera_build_request(..., "dataEngine", "queryResearchRetrieval", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `contractVersion` | yes | — |
| `requestId` | yes | — |
| `obligation` | yes | — |
| `kinds` | no | — |
| `epistemicStatuses` | no | — |
| `checkerTrust` | no | — |
| `includeEquivalent` | no | — |
| `includeFailures` | no | — |
| `under` | no | — |
| `limit` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.queryResearchRetrieval({
  parent: "<parent>",
  contractVersion: "<contractVersion>",
  requestId: "<requestId>",
  obligation: "<obligation>",
  limit: "<limit>",
});
```

```python Python
result = client.data_engine.query_research_retrieval({
    "parent": "<parent>",
    "contractVersion": "<contractVersion>",
    "requestId": "<requestId>",
    "obligation": "<obligation>",
    "limit": "<limit>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "query_research_retrieval", &[
    ("parent", "<parent>".into()),
    ("contractVersion", "<contractVersion>".into()),
    ("requestId", "<requestId>".into()),
    ("obligation", "<obligation>".into()),
    ("limit", "<limit>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "queryResearchRetrieval", map[string]any{
    "parent": "<parent>",
    "contractVersion": "<contractVersion>",
    "requestId": "<requestId>",
    "obligation": "<obligation>",
    "limit": "<limit>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"contractVersion\":\"<contractVersion>\",\"requestId\":\"<requestId>\",\"obligation\":\"<obligation>\",\"limit\":\"<limit>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "queryResearchRetrieval",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### createResearchCatalogEntry

`POST /v1/{parent}/researchCatalogEntries`

Create an immutable executable research catalog entry by canonical content hash.

- **Producer operationId:** `projects.researchCatalogEntries.create`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:write`
- **Call as:** TypeScript `client.dataEngine.createResearchCatalogEntry()` · Python `client.data_engine.create_research_catalog_entry()` · Rust `build_request("data_engine", "create_research_catalog_entry", params)` · Go `BuildRequest("dataEngine", "createResearchCatalogEntry", params)` · C `tempera_build_request(..., "dataEngine", "createResearchCatalogEntry", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `kind` | yes | — |
| `version` | yes | — |
| `obligationHash` | yes | — |
| `capabilityRoute` | yes | — |
| `provenance` | yes | — |
| `tags` | no | — |
| `contentHash` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.createResearchCatalogEntry({
  parent: "<parent>",
  kind: "<kind>",
  version: "<version>",
  obligationHash: "<obligationHash>",
  capabilityRoute: "<capabilityRoute>",
  provenance: "<provenance>",
});
```

```python Python
result = client.data_engine.create_research_catalog_entry({
    "parent": "<parent>",
    "kind": "<kind>",
    "version": "<version>",
    "obligationHash": "<obligationHash>",
    "capabilityRoute": "<capabilityRoute>",
    "provenance": "<provenance>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "create_research_catalog_entry", &[
    ("parent", "<parent>".into()),
    ("kind", "<kind>".into()),
    ("version", "<version>".into()),
    ("obligationHash", "<obligationHash>".into()),
    ("capabilityRoute", "<capabilityRoute>".into()),
    ("provenance", "<provenance>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "createResearchCatalogEntry", map[string]any{
    "parent": "<parent>",
    "kind": "<kind>",
    "version": "<version>",
    "obligationHash": "<obligationHash>",
    "capabilityRoute": "<capabilityRoute>",
    "provenance": "<provenance>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"kind\":\"<kind>\",\"version\":\"<version>\",\"obligationHash\":\"<obligationHash>\",\"capabilityRoute\":\"<capabilityRoute>\",\"provenance\":\"<provenance>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "createResearchCatalogEntry",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### listResearchCatalogEntries

`GET /v1/{parent}/researchCatalogEntries`

List immutable executable research catalog entries with bounded pagination.

- **Producer operationId:** `projects.researchCatalogEntries.list`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.listResearchCatalogEntries()` · Python `client.data_engine.list_research_catalog_entries()` · Rust `build_request("data_engine", "list_research_catalog_entries", params)` · Go `BuildRequest("dataEngine", "listResearchCatalogEntries", params)` · C `tempera_build_request(..., "dataEngine", "listResearchCatalogEntries", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.listResearchCatalogEntries({
  parent: "<parent>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.data_engine.list_research_catalog_entries({
    "parent": "<parent>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "list_research_catalog_entries", &[
    ("parent", "<parent>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "listResearchCatalogEntries", map[string]any{
    "parent": "<parent>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "pageSize=<pageSize>&pageToken=<pageToken>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "listResearchCatalogEntries",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### getResearchCatalogEntry

`GET /v1/{parent}/researchCatalogEntries/{entryId}`

Fetch one immutable executable research catalog entry by content hash.

- **Producer operationId:** `projects.researchCatalogEntries.get`
- **Auth:** Product bearer — an access token minted for audience `data-engine`, or a central `tp_` API key.
- **AIP resource path templates:** `parent=projects/*`
- **Scope:** `dataset:read`
- **Call as:** TypeScript `client.dataEngine.getResearchCatalogEntry()` · Python `client.data_engine.get_research_catalog_entry()` · Rust `build_request("data_engine", "get_research_catalog_entry", params)` · Go `BuildRequest("dataEngine", "getResearchCatalogEntry", params)` · C `tempera_build_request(..., "dataEngine", "getResearchCatalogEntry", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `parent` | yes | Substituted into the URL path (percent-encoded). |
| `entryId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.dataEngine.getResearchCatalogEntry({
  parent: "<parent>",
  entryId: "<entryId>",
});
```

```python Python
result = client.data_engine.get_research_catalog_entry({
    "parent": "<parent>",
    "entryId": "<entryId>",
})
```

```rust Rust
let spec = client.build_request("data_engine", "get_research_catalog_entry", &[
    ("parent", "<parent>".into()),
    ("entryId", "<entryId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("dataEngine", "getResearchCatalogEntry", map[string]any{
    "parent": "<parent>",
    "entryId": "<entryId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"parent", "<parent>"},
    {"entryId", "<entryId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "dataEngine", "getResearchCatalogEntry",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>
