---
title: "auth-hub"
description: "Tempera control plane: unified accounts, OAuth issuance, workspaces, teams, API keys, billing, usage metering, and the unified MCP gateway."
---

{/* Generated by scripts/gen-sdk-docs.py from surface.json -- do not hand-edit. */}

> **Private access.** Hosted Tempera services are available through design-partner onboarding.
> Use the issuer, credentials, environment, and product URLs provisioned for your workspace;
> start in staging unless Tempera explicitly approves another target.

Tempera control plane: unified accounts, OAuth issuance, workspaces, teams, API keys, billing, usage metering, and the unified MCP gateway.

| | |
|---|---|
| Repository | [auth-hub](https://github.com/tempera-dev/auth-hub) |
| Base URL env var | `TEMPERA_CONTROL_PLANE_URL` (see [Environments](/environments)) |
| Audience | — (no OAuth audience) |
| Auth | account bearer token from createHostedSession (control-plane session); introspectToken requires the introspection secret |
| Client | TypeScript `client.controlPlane` · Python `client.control_plane` · Rust `build_request("control_plane", ...)` |
| Typed operations | 61 |

Anything this page does not cover is reachable through the raw passthrough:
`client.controlPlane.request(path, options)` (TypeScript) /
`client.control_plane.request(path, ...)` (Python).

## Operations

### health

`GET /healthz`

Check control-plane liveness; returns \{ok: true\}.

- **Producer operationId:** `getHealth`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.controlPlane.health()` · Python `client.control_plane.health()` · Rust `build_request("control_plane", "health", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.health();
```

```python Python
result = client.control_plane.health()
```

```rust Rust
let spec = client.build_request("control_plane", "health", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getReadiness

`GET /readyz`

Readiness probe for durable control-plane storage.

- **Producer operationId:** `getReadiness`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.controlPlane.getReadiness()` · Python `client.control_plane.get_readiness()` · Rust `build_request("control_plane", "get_readiness", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.getReadiness();
```

```python Python
result = client.control_plane.get_readiness()
```

```rust Rust
let spec = client.build_request("control_plane", "get_readiness", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### me

`GET /v1/me`

Fetch the authenticated user's identity, active workspace, and roles.

- **Producer operationId:** `getMe`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.me()` · Python `client.control_plane.me()` · Rust `build_request("control_plane", "me", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.me();
```

```python Python
result = client.control_plane.me()
```

```rust Rust
let spec = client.build_request("control_plane", "me", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listOrgs

`GET /v1/orgs`

List the organizations the authenticated user belongs to.

- **Producer operationId:** `listOrganizations`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.listOrgs()` · Python `client.control_plane.list_orgs()` · Rust `build_request("control_plane", "list_orgs", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.listOrgs({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.control_plane.list_orgs({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "list_orgs", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createOrg

`POST /v1/orgs`

Create an organization; the caller becomes its owner.

- **Producer operationId:** `createOrganization`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.createOrg()` · Python `client.control_plane.create_org()` · Rust `build_request("control_plane", "create_org", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `name` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.createOrg({
  name: "<name>",
});
```

```python Python
result = client.control_plane.create_org({
    "name": "<name>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "create_org", &[
    ("name", "<name>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listSessions

`GET /v1/sessions`

List the user's active account sessions.

- **Producer operationId:** `listAccountSessions`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.listSessions()` · Python `client.control_plane.list_sessions()` · Rust `build_request("control_plane", "list_sessions", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.listSessions({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.control_plane.list_sessions({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "list_sessions", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createHostedSession

`POST /v1/sessions`

Create a first-party hosted account session from email/password login or signup.

- **Producer operationId:** `createHostedSession`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.controlPlane.createHostedSession()` · Python `client.control_plane.create_hosted_session()` · Rust `build_request("control_plane", "create_hosted_session", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `mode` | no | — |
| `email` | yes | — |
| `password` | yes | — |
| `organization` | no | — |
| `inviteToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.createHostedSession({
  mode: "<mode>",
  email: "<email>",
  password: "<password>",
});
```

```python Python
result = client.control_plane.create_hosted_session({
    "mode": "<mode>",
    "email": "<email>",
    "password": "<password>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "create_hosted_session", &[
    ("mode", "<mode>".into()),
    ("email", "<email>".into()),
    ("password", "<password>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### revokeSession

`DELETE /v1/sessions/{id}`

Revoke an account session and its tokens immediately (idempotent).

- **Producer operationId:** `revokeAccountSession`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.revokeSession()` · Python `client.control_plane.revoke_session()` · Rust `build_request("control_plane", "revoke_session", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `id` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.revokeSession({
  id: "<id>",
});
```

```python Python
result = client.control_plane.revoke_session({
    "id": "<id>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "revoke_session", &[
    ("id", "<id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### selectWorkspace

`POST /v1/workspace/select`

Switch the active workspace and receive a token pair scoped to it.

- **Producer operationId:** `selectWorkspace`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.selectWorkspace()` · Python `client.control_plane.select_workspace()` · Rust `build_request("control_plane", "select_workspace", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `orgId` | yes | — |
| `projectId` | yes | — |
| `environmentId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.selectWorkspace({
  orgId: "<orgId>",
  projectId: "<projectId>",
  environmentId: "<environmentId>",
});
```

```python Python
result = client.control_plane.select_workspace({
    "orgId": "<orgId>",
    "projectId": "<projectId>",
    "environmentId": "<environmentId>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "select_workspace", &[
    ("orgId", "<orgId>".into()),
    ("projectId", "<projectId>".into()),
    ("environmentId", "<environmentId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listTeamMembers

`GET /v1/team/members`

List team members of the active organization.

- **Producer operationId:** `listTeamMembers`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.listTeamMembers()` · Python `client.control_plane.list_team_members()` · Rust `build_request("control_plane", "list_team_members", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.listTeamMembers({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.control_plane.list_team_members({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "list_team_members", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### updateTeamMember

`PATCH /v1/team/members/{id}`

Change a team member's role (requires an org admin role; at least one owner must remain).

- **Producer operationId:** `updateTeamMember`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.updateTeamMember()` · Python `client.control_plane.update_team_member()` · Rust `build_request("control_plane", "update_team_member", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `id` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `updateMask` | no | — |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `role` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.updateTeamMember({
  id: "<id>",
  role: "<role>",
});
```

```python Python
result = client.control_plane.update_team_member({
    "id": "<id>",
    "role": "<role>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "update_team_member", &[
    ("id", "<id>".into()),
    ("role", "<role>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### removeTeamMember

`DELETE /v1/team/members/{id}`

Remove a team member from the active organization (idempotent).

- **Producer operationId:** `removeTeamMember`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.removeTeamMember()` · Python `client.control_plane.remove_team_member()` · Rust `build_request("control_plane", "remove_team_member", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `id` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.removeTeamMember({
  id: "<id>",
});
```

```python Python
result = client.control_plane.remove_team_member({
    "id": "<id>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "remove_team_member", &[
    ("id", "<id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listInvites

`GET /v1/invites`

List invites for the active organization, newest first.

- **Producer operationId:** `listInvites`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.listInvites()` · Python `client.control_plane.list_invites()` · Rust `build_request("control_plane", "list_invites", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.listInvites({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.control_plane.list_invites({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "list_invites", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createInvite

`POST /v1/invites`

Invite a user to the active organization; the accept URL is returned once.

- **Producer operationId:** `createInvite`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.createInvite()` · Python `client.control_plane.create_invite()` · Rust `build_request("control_plane", "create_invite", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `email` | yes | — |
| `role` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.createInvite({
  email: "<email>",
  role: "<role>",
});
```

```python Python
result = client.control_plane.create_invite({
    "email": "<email>",
    "role": "<role>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "create_invite", &[
    ("email", "<email>".into()),
    ("role", "<role>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### cancelInvite

`DELETE /v1/invites/{id}`

Cancel a pending invite (idempotent).

- **Producer operationId:** `cancelInvite`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.cancelInvite()` · Python `client.control_plane.cancel_invite()` · Rust `build_request("control_plane", "cancel_invite", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `id` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.cancelInvite({
  id: "<id>",
});
```

```python Python
result = client.control_plane.cancel_invite({
    "id": "<id>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "cancel_invite", &[
    ("id", "<id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listProjects

`GET /v1/projects`

List projects across every organization the user belongs to.

- **Producer operationId:** `listProjects`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.listProjects()` · Python `client.control_plane.list_projects()` · Rust `build_request("control_plane", "list_projects", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.listProjects({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.control_plane.list_projects({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "list_projects", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createProject

`POST /v1/projects`

Create a project in an organization (requires an org admin role).

- **Producer operationId:** `createProject`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.createProject()` · Python `client.control_plane.create_project()` · Rust `build_request("control_plane", "create_project", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `orgId` | yes | — |
| `name` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.createProject({
  orgId: "<orgId>",
  name: "<name>",
});
```

```python Python
result = client.control_plane.create_project({
    "orgId": "<orgId>",
    "name": "<name>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "create_project", &[
    ("orgId", "<orgId>".into()),
    ("name", "<name>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listEnvironments

`GET /v1/environments`

List environments across every project the user can access.

- **Producer operationId:** `listEnvironments`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.listEnvironments()` · Python `client.control_plane.list_environments()` · Rust `build_request("control_plane", "list_environments", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.listEnvironments({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.control_plane.list_environments({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "list_environments", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createEnvironment

`POST /v1/environments`

Create an environment in a project (requires an org admin role).

- **Producer operationId:** `createEnvironment`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.createEnvironment()` · Python `client.control_plane.create_environment()` · Rust `build_request("control_plane", "create_environment", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `projectId` | yes | — |
| `name` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.createEnvironment({
  projectId: "<projectId>",
  name: "<name>",
});
```

```python Python
result = client.control_plane.create_environment({
    "projectId": "<projectId>",
    "name": "<name>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "create_environment", &[
    ("projectId", "<projectId>".into()),
    ("name", "<name>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listApiKeys

`GET /v1/api-keys`

List API keys in the active workspace; secrets are never returned.

- **Producer operationId:** `listApiKeys`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.listApiKeys()` · Python `client.control_plane.list_api_keys()` · Rust `build_request("control_plane", "list_api_keys", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.listApiKeys({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.control_plane.list_api_keys({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "list_api_keys", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createApiKey

`POST /v1/api-keys`

Mint a workspace API key (tp_...); the secret is returned exactly once. The workspace ids must match the token's workspace.

- **Producer operationId:** `createApiKey`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.createApiKey()` · Python `client.control_plane.create_api_key()` · Rust `build_request("control_plane", "create_api_key", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `orgId` | yes | — |
| `projectId` | yes | — |
| `environmentId` | yes | — |
| `name` | no | — |
| `scopes` | yes | — |
| `audience` | no | — |
| `expiresAt` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.createApiKey({
  orgId: "<orgId>",
  projectId: "<projectId>",
  environmentId: "<environmentId>",
});
```

```python Python
result = client.control_plane.create_api_key({
    "orgId": "<orgId>",
    "projectId": "<projectId>",
    "environmentId": "<environmentId>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "create_api_key", &[
    ("orgId", "<orgId>".into()),
    ("projectId", "<projectId>".into()),
    ("environmentId", "<environmentId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### revokeApiKey

`DELETE /v1/api-keys/{id}`

Revoke an API key (idempotent).

- **Producer operationId:** `revokeApiKey`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.revokeApiKey()` · Python `client.control_plane.revoke_api_key()` · Rust `build_request("control_plane", "revoke_api_key", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `id` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.revokeApiKey({
  id: "<id>",
});
```

```python Python
result = client.control_plane.revoke_api_key({
    "id": "<id>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "revoke_api_key", &[
    ("id", "<id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### rotateApiKey

`POST /v1/api-keys/{id}/rotate`

Rotate an API key's secret; the new secret is returned exactly once.

- **Producer operationId:** `rotateApiKey`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.rotateApiKey()` · Python `client.control_plane.rotate_api_key()` · Rust `build_request("control_plane", "rotate_api_key", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `id` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.rotateApiKey({
  id: "<id>",
});
```

```python Python
result = client.control_plane.rotate_api_key({
    "id": "<id>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "rotate_api_key", &[
    ("id", "<id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### providerConnectionsList

`GET /v1/provider-connections`

List provider connection metadata using a first-party account session. Secret references and values are never returned.

- **Producer operationId:** `providerConnections.list`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.providerConnectionsList()` · Python `client.control_plane.provider_connections_list()` · Rust `build_request("control_plane", "provider_connections_list", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.providerConnectionsList({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.control_plane.provider_connections_list({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "provider_connections_list", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### providerConnectionsCreate

`POST /v1/provider-connections`

Create a tenant-scoped provider connection using only an external secret reference.

- **Producer operationId:** `providerConnections.create`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.providerConnectionsCreate()` · Python `client.control_plane.provider_connections_create()` · Rust `build_request("control_plane", "provider_connections_create", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `orgId` | yes | — |
| `projectId` | yes | — |
| `environmentId` | yes | — |
| `provider` | yes | — |
| `name` | yes | — |
| `secretRef` | yes | — |
| `allowedModels` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.providerConnectionsCreate({
  orgId: "<orgId>",
  projectId: "<projectId>",
  environmentId: "<environmentId>",
});
```

```python Python
result = client.control_plane.provider_connections_create({
    "orgId": "<orgId>",
    "projectId": "<projectId>",
    "environmentId": "<environmentId>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "provider_connections_create", &[
    ("orgId", "<orgId>".into()),
    ("projectId", "<projectId>".into()),
    ("environmentId", "<environmentId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### providerConnectionsRevoke

`DELETE /v1/provider-connections/{id}`

Revoke a provider connection immediately. Revoking an unknown id is a no-op.

- **Producer operationId:** `providerConnections.revoke`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.providerConnectionsRevoke()` · Python `client.control_plane.provider_connections_revoke()` · Rust `build_request("control_plane", "provider_connections_revoke", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `id` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.providerConnectionsRevoke({
  id: "<id>",
});
```

```python Python
result = client.control_plane.provider_connections_revoke({
    "id": "<id>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "provider_connections_revoke", &[
    ("id", "<id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### providerConnectionsRotate

`POST /v1/provider-connections/{id}:rotate`

Replace a connection secret reference and increment its revision without exposing the reference.

- **Producer operationId:** `providerConnections.rotate`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.providerConnectionsRotate()` · Python `client.control_plane.provider_connections_rotate()` · Rust `build_request("control_plane", "provider_connections_rotate", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `id` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `secretRef` | yes | — |
| `allowedModels` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.providerConnectionsRotate({
  id: "<id>",
  secretRef: "<secretRef>",
  allowedModels: "<allowedModels>",
});
```

```python Python
result = client.control_plane.provider_connections_rotate({
    "id": "<id>",
    "secretRef": "<secretRef>",
    "allowedModels": "<allowedModels>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "provider_connections_rotate", &[
    ("id", "<id>".into()),
    ("secretRef", "<secretRef>".into()),
    ("allowedModels", "<allowedModels>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### providerConnectionsResolve

`POST /v1/provider-connections/{id}:resolve`

Resolve connection runtime metadata for a tenant-bound tempera-llm service credential.

- **Producer operationId:** `providerConnections.resolve`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.providerConnectionsResolve()` · Python `client.control_plane.provider_connections_resolve()` · Rust `build_request("control_plane", "provider_connections_resolve", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `id` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `orgId` | yes | — |
| `projectId` | yes | — |
| `environmentId` | yes | — |
| `usageDelegation` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.providerConnectionsResolve({
  id: "<id>",
  orgId: "<orgId>",
  projectId: "<projectId>",
  environmentId: "<environmentId>",
});
```

```python Python
result = client.control_plane.provider_connections_resolve({
    "id": "<id>",
    "orgId": "<orgId>",
    "projectId": "<projectId>",
    "environmentId": "<environmentId>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "provider_connections_resolve", &[
    ("id", "<id>".into()),
    ("orgId", "<orgId>".into()),
    ("projectId", "<projectId>".into()),
    ("environmentId", "<environmentId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listAuditLog

`GET /v1/audit-log`

List recent audit-log events for the user and active organization (up to 50, newest first).

- **Producer operationId:** `listAuditLog`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.listAuditLog()` · Python `client.control_plane.list_audit_log()` · Rust `build_request("control_plane", "list_audit_log", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.listAuditLog({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.control_plane.list_audit_log({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "list_audit_log", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listConnectors

`GET /v1/connectors`

List the connector catalog (MCP clients, editors, and API surfaces).

- **Producer operationId:** `listConnectors`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.listConnectors()` · Python `client.control_plane.list_connectors()` · Rust `build_request("control_plane", "list_connectors", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.listConnectors({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.control_plane.list_connectors({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "list_connectors", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getConnectorStatus

`GET /v1/connectors/{id}/status`

Fetch one connector's connection status for the active workspace.

- **Producer operationId:** `getConnectorStatus`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.getConnectorStatus()` · Python `client.control_plane.get_connector_status()` · Rust `build_request("control_plane", "get_connector_status", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `id` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.getConnectorStatus({
  id: "<id>",
});
```

```python Python
result = client.control_plane.get_connector_status({
    "id": "<id>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "get_connector_status", &[
    ("id", "<id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listProducts

`GET /v1/products`

List the product catalog with default scopes and setup paths.

- **Producer operationId:** `listProducts`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.listProducts()` · Python `client.control_plane.list_products()` · Rust `build_request("control_plane", "list_products", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.listProducts({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.control_plane.list_products({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "list_products", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getProductStatus

`GET /v1/products/{id}/status`

Fetch one product's activation status, entitlements, signals, and usage meters.

- **Producer operationId:** `getProductStatus`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.getProductStatus()` · Python `client.control_plane.get_product_status()` · Rust `build_request("control_plane", "get_product_status", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `id` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.getProductStatus({
  id: "<id>",
});
```

```python Python
result = client.control_plane.get_product_status({
    "id": "<id>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "get_product_status", &[
    ("id", "<id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getBillingStatus

`GET /v1/billing/status`

Fetch the organization's plan, subscription, usage meters, entitlements, invoices, and pricing (requires a billing role).

- **Producer operationId:** `getBillingStatus`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.getBillingStatus()` · Python `client.control_plane.get_billing_status()` · Rust `build_request("control_plane", "get_billing_status", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.getBillingStatus();
```

```python Python
result = client.control_plane.get_billing_status()
```

```rust Rust
let spec = client.build_request("control_plane", "get_billing_status", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createBillingCheckout

`GET /v1/billing/checkout`

Create a checkout handoff URL for a plan on the chosen payment rail (requires a billing role).

- **Producer operationId:** `createBillingCheckout`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.createBillingCheckout()` · Python `client.control_plane.create_billing_checkout()` · Rust `build_request("control_plane", "create_billing_checkout", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `rail` | no | — |
| `plan` | no | — |
| `planId` | no | — |
| `interval` | no | — |
| `currency` | no | — |
| `network` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.createBillingCheckout({
  rail: "<rail>",
  plan: "<plan>",
});
```

```python Python
result = client.control_plane.create_billing_checkout({
    "rail": "<rail>",
    "plan": "<plan>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "create_billing_checkout", &[
    ("rail", "<rail>".into()),
    ("plan", "<plan>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getBillingPortal

`GET /v1/billing/portal`

Fetch the billing-portal URL for the organization (requires a billing role).

- **Producer operationId:** `createBillingPortal`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.getBillingPortal()` · Python `client.control_plane.get_billing_portal()` · Rust `build_request("control_plane", "get_billing_portal", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.getBillingPortal();
```

```python Python
result = client.control_plane.get_billing_portal()
```

```rust Rust
let spec = client.build_request("control_plane", "get_billing_portal", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getBillingCredits

`GET /v1/billing/credits`

Return the org credit wallet balance, grant, overage, and recent ledger for owner, admin, or billing users. Internal cost/margin fields are redacted from the ledger for non-staff callers and returned in full only to platform staff.

- **Producer operationId:** `getBillingCredits`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.getBillingCredits()` · Python `client.control_plane.get_billing_credits()` · Rust `build_request("control_plane", "get_billing_credits", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.getBillingCredits();
```

```python Python
result = client.control_plane.get_billing_credits()
```

```rust Rust
let spec = client.build_request("control_plane", "get_billing_credits", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getModelCatalog

`GET /v1/model-catalog`

List the entitled Tempera Code model catalog; requires a tempera-code bearer with model:read and the model-gateway entitlement.

- **Producer operationId:** `getModelCatalog`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Scope:** `model:read`
- **Call as:** TypeScript `client.controlPlane.getModelCatalog()` · Python `client.control_plane.get_model_catalog()` · Rust `build_request("control_plane", "get_model_catalog", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.getModelCatalog();
```

```python Python
result = client.control_plane.get_model_catalog()
```

```rust Rust
let spec = client.build_request("control_plane", "get_model_catalog", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### recordUsage

`POST /v1/usage/events`

Record a usage event against a metered plan limit; requires a token carrying the meter's product scope and returns the updated meter.

- **Producer operationId:** `recordUsageEvent`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.recordUsage()` · Python `client.control_plane.record_usage()` · Rust `build_request("control_plane", "record_usage", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `orgId` | yes | — |
| `projectId` | yes | — |
| `environmentId` | yes | — |
| `metric` | yes | — |
| `quantity` | no | — |
| `idempotencyKey` | no | — |
| `cost` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.recordUsage({
  orgId: "<orgId>",
  projectId: "<projectId>",
  environmentId: "<environmentId>",
});
```

```python Python
result = client.control_plane.record_usage({
    "orgId": "<orgId>",
    "projectId": "<projectId>",
    "environmentId": "<environmentId>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "record_usage", &[
    ("orgId", "<orgId>".into()),
    ("projectId", "<projectId>".into()),
    ("environmentId", "<environmentId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### usageReservationsCreate

`POST /v1/usage/reservations`

Atomically reserve the maximum model cost before starting a provider request.

- **Producer operationId:** `usageReservations.create`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.usageReservationsCreate()` · Python `client.control_plane.usage_reservations_create()` · Rust `build_request("control_plane", "usage_reservations_create", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `orgId` | yes | — |
| `projectId` | yes | — |
| `environmentId` | yes | — |
| `subject` | no | — |
| `idempotencyKey` | yes | — |
| `provider` | yes | — |
| `model` | yes | — |
| `configId` | yes | — |
| `byok` | yes | — |
| `maximumProviderCostMicros` | yes | — |
| `ttlSeconds` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.usageReservationsCreate({
  orgId: "<orgId>",
  projectId: "<projectId>",
  environmentId: "<environmentId>",
});
```

```python Python
result = client.control_plane.usage_reservations_create({
    "orgId": "<orgId>",
    "projectId": "<projectId>",
    "environmentId": "<environmentId>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "usage_reservations_create", &[
    ("orgId", "<orgId>".into()),
    ("projectId", "<projectId>".into()),
    ("environmentId", "<environmentId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### usageReservationsCommit

`POST /v1/usage/reservations/{reservationId}:commit`

Commit exact provider usage against an admitted reservation and release unused capacity.

- **Producer operationId:** `usageReservations.commit`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.usageReservationsCommit()` · Python `client.control_plane.usage_reservations_commit()` · Rust `build_request("control_plane", "usage_reservations_commit", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `reservationId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `orgId` | yes | — |
| `projectId` | yes | — |
| `environmentId` | yes | — |
| `configId` | yes | — |
| `cost` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.usageReservationsCommit({
  reservationId: "<reservationId>",
  orgId: "<orgId>",
  projectId: "<projectId>",
  environmentId: "<environmentId>",
});
```

```python Python
result = client.control_plane.usage_reservations_commit({
    "reservationId": "<reservationId>",
    "orgId": "<orgId>",
    "projectId": "<projectId>",
    "environmentId": "<environmentId>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "usage_reservations_commit", &[
    ("reservationId", "<reservationId>".into()),
    ("orgId", "<orgId>".into()),
    ("projectId", "<projectId>".into()),
    ("environmentId", "<environmentId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### usageReservationsRelease

`POST /v1/usage/reservations/{reservationId}:release`

Release unused capacity after provider failure or cancellation.

- **Producer operationId:** `usageReservations.release`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.usageReservationsRelease()` · Python `client.control_plane.usage_reservations_release()` · Rust `build_request("control_plane", "usage_reservations_release", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `reservationId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `orgId` | yes | — |
| `projectId` | yes | — |
| `environmentId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.usageReservationsRelease({
  reservationId: "<reservationId>",
  orgId: "<orgId>",
  projectId: "<projectId>",
  environmentId: "<environmentId>",
});
```

```python Python
result = client.control_plane.usage_reservations_release({
    "reservationId": "<reservationId>",
    "orgId": "<orgId>",
    "projectId": "<projectId>",
    "environmentId": "<environmentId>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "usage_reservations_release", &[
    ("reservationId", "<reservationId>".into()),
    ("orgId", "<orgId>".into()),
    ("projectId", "<projectId>".into()),
    ("environmentId", "<environmentId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### usageReservationsReconcile

`POST /v1/usage/reservations/{reservationId}:reconcile`

Hold maximum capacity and record non-secret evidence when exact provider usage is unavailable.

- **Producer operationId:** `usageReservations.reconcile`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.usageReservationsReconcile()` · Python `client.control_plane.usage_reservations_reconcile()` · Rust `build_request("control_plane", "usage_reservations_reconcile", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `reservationId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `orgId` | yes | — |
| `projectId` | yes | — |
| `environmentId` | yes | — |
| `configId` | yes | — |
| `reason` | yes | — |
| `traceId` | no | — |
| `observedUsage` | yes | — |
| `expectedCost` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.usageReservationsReconcile({
  reservationId: "<reservationId>",
  orgId: "<orgId>",
  projectId: "<projectId>",
  environmentId: "<environmentId>",
});
```

```python Python
result = client.control_plane.usage_reservations_reconcile({
    "reservationId": "<reservationId>",
    "orgId": "<orgId>",
    "projectId": "<projectId>",
    "environmentId": "<environmentId>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "usage_reservations_reconcile", &[
    ("reservationId", "<reservationId>".into()),
    ("orgId", "<orgId>".into()),
    ("projectId", "<projectId>".into()),
    ("environmentId", "<environmentId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listGrants

`GET /v1/oauth/grants`

List the OAuth grants the user has approved in the active workspace.

- **Producer operationId:** `listOAuthGrants`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.listGrants()` · Python `client.control_plane.list_grants()` · Rust `build_request("control_plane", "list_grants", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.listGrants({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.control_plane.list_grants({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "list_grants", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### revokeGrant

`DELETE /v1/oauth/grants/{id}`

Revoke an OAuth grant and every refresh token issued under it.

- **Producer operationId:** `revokeOAuthGrant`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.revokeGrant()` · Python `client.control_plane.revoke_grant()` · Rust `build_request("control_plane", "revoke_grant", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `id` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.revokeGrant({
  id: "<id>",
});
```

```python Python
result = client.control_plane.revoke_grant({
    "id": "<id>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "revoke_grant", &[
    ("id", "<id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### introspectToken

`POST /v1/oauth/introspect`

Introspect a token or tp_ API key server-side; requires the introspection secret and returns \{active: false\} for anything invalid.

- **Producer operationId:** `introspectOAuthToken`
- **Auth:** Introspection secret bearer (server-side only).
- **Call as:** TypeScript `client.controlPlane.introspectToken()` · Python `client.control_plane.introspect_token()` · Rust `build_request("control_plane", "introspect_token", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `token` | yes | — |
| `token_type_hint` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.introspectToken({
  token: "<token>",
  token_type_hint: "<token_type_hint>",
});
```

```python Python
result = client.control_plane.introspect_token({
    "token": "<token>",
    "token_type_hint": "<token_type_hint>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "introspect_token", &[
    ("token", "<token>".into()),
    ("token_type_hint", "<token_type_hint>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### discovery

`GET /.well-known/oauth-authorization-server`

Fetch OAuth 2.1 authorization-server metadata for the issuer.

- **Producer operationId:** `getOAuthAuthorizationServerMetadata`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.controlPlane.discovery()` · Python `client.control_plane.discovery()` · Rust `build_request("control_plane", "discovery", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.discovery();
```

```python Python
result = client.control_plane.discovery()
```

```rust Rust
let spec = client.build_request("control_plane", "discovery", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getOAuthProtectedResourceMetadata

`GET /.well-known/oauth-protected-resource`

OAuth protected resource discovery metadata for MCP/resource clients.

- **Producer operationId:** `getOAuthProtectedResourceMetadata`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.getOAuthProtectedResourceMetadata()` · Python `client.control_plane.get_o_auth_protected_resource_metadata()` · Rust `build_request("control_plane", "get_o_auth_protected_resource_metadata", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.getOAuthProtectedResourceMetadata();
```

```python Python
result = client.control_plane.get_o_auth_protected_resource_metadata()
```

```rust Rust
let spec = client.build_request("control_plane", "get_o_auth_protected_resource_metadata", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### protectedResourceMetadata

`GET /.well-known/oauth-protected-resource/{resource}`

Fetch OAuth protected-resource metadata for one registered audience.

- **Producer operationId:** `getOAuthProtectedResourceMetadataForAudience`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.controlPlane.protectedResourceMetadata()` · Python `client.control_plane.protected_resource_metadata()` · Rust `build_request("control_plane", "protected_resource_metadata", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `resource` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.protectedResourceMetadata({
  resource: "<resource>",
});
```

```python Python
result = client.control_plane.protected_resource_metadata({
    "resource": "<resource>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "protected_resource_metadata", &[
    ("resource", "<resource>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### jwks

`GET /.well-known/jwks.json`

Fetch the JSON Web Key Set used to verify control-plane access tokens.

- **Producer operationId:** `getJsonWebKeySet`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.controlPlane.jwks()` · Python `client.control_plane.jwks()` · Rust `build_request("control_plane", "jwks", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.jwks();
```

```python Python
result = client.control_plane.jwks()
```

```rust Rust
let spec = client.build_request("control_plane", "jwks", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### adminOperationalProvenance

`GET /v1/admin/operations/provenance`

Return fail-closed source, machine, and image provenance for the serving runtime to a platform-staff account session.

- **Producer operationId:** `adminOperationalProvenance`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.adminOperationalProvenance()` · Python `client.control_plane.admin_operational_provenance()` · Rust `build_request("control_plane", "admin_operational_provenance", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.adminOperationalProvenance();
```

```python Python
result = client.control_plane.admin_operational_provenance()
```

```rust Rust
let spec = client.build_request("control_plane", "admin_operational_provenance", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### adminStepUp

`POST /v1/admin/step-up`

Re-authenticate a platform-staff account session to mint a short-lived step-up elevation required for sensitive admin mutations.

- **Producer operationId:** `adminStepUp`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.adminStepUp()` · Python `client.control_plane.admin_step_up()` · Rust `build_request("control_plane", "admin_step_up", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `password` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.adminStepUp({
  password: "<password>",
});
```

```python Python
result = client.control_plane.admin_step_up({
    "password": "<password>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "admin_step_up", &[
    ("password", "<password>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### adminAdjustCredits

`POST /v1/admin/billing/credits/adjust`

Platform-staff credit grant/adjustment to an org wallet. Requires a fresh step-up elevation; idempotent on the reference.

- **Producer operationId:** `adminAdjustCredits`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.adminAdjustCredits()` · Python `client.control_plane.admin_adjust_credits()` · Rust `build_request("control_plane", "admin_adjust_credits", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `orgId` | yes | — |
| `creditMicros` | yes | — |
| `reference` | no | — |
| `reason` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.adminAdjustCredits({
  orgId: "<orgId>",
  creditMicros: "<creditMicros>",
  reference: "<reference>",
});
```

```python Python
result = client.control_plane.admin_adjust_credits({
    "orgId": "<orgId>",
    "creditMicros": "<creditMicros>",
    "reference": "<reference>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "admin_adjust_credits", &[
    ("orgId", "<orgId>".into()),
    ("creditMicros", "<creditMicros>".into()),
    ("reference", "<reference>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### adminBillingOrgs

`GET /v1/admin/billing/orgs`

Platform-staff internal billing view: per-org wallet balances plus provider cost, customer charge, and margin economics.

- **Producer operationId:** `adminBillingOrgs`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.adminBillingOrgs()` · Python `client.control_plane.admin_billing_orgs()` · Rust `build_request("control_plane", "admin_billing_orgs", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.adminBillingOrgs();
```

```python Python
result = client.control_plane.admin_billing_orgs()
```

```rust Rust
let spec = client.build_request("control_plane", "admin_billing_orgs", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### githubSetupSessionsCreate

`POST /v1/github/setup-sessions`

Create a one-time CSRF-bound GitHub App installation session.

- **Producer operationId:** `githubSetupSessions.create`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.githubSetupSessionsCreate()` · Python `client.control_plane.github_setup_sessions_create()` · Rust `build_request("control_plane", "github_setup_sessions_create", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `projectId` | no | — |
| `environmentId` | no | — |
| `returnUrl` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.githubSetupSessionsCreate({
  projectId: "<projectId>",
  environmentId: "<environmentId>",
  returnUrl: "<returnUrl>",
});
```

```python Python
result = client.control_plane.github_setup_sessions_create({
    "projectId": "<projectId>",
    "environmentId": "<environmentId>",
    "returnUrl": "<returnUrl>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "github_setup_sessions_create", &[
    ("projectId", "<projectId>".into()),
    ("environmentId", "<environmentId>".into()),
    ("returnUrl", "<returnUrl>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### githubSetupSessionsComplete

`GET /github/callback`

Bind a GitHub installation callback to its authenticated workspace.

- **Producer operationId:** `githubSetupSessions.complete`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.controlPlane.githubSetupSessionsComplete()` · Python `client.control_plane.github_setup_sessions_complete()` · Rust `build_request("control_plane", "github_setup_sessions_complete", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `state` | no | — |
| `installation_id` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.githubSetupSessionsComplete({
  state: "<state>",
  installation_id: "<installation_id>",
});
```

```python Python
result = client.control_plane.github_setup_sessions_complete({
    "state": "<state>",
    "installation_id": "<installation_id>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "github_setup_sessions_complete", &[
    ("state", "<state>".into()),
    ("installation_id", "<installation_id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### githubInstallationsList

`GET /v1/github/installations`

List active GitHub App installations for the organization.

- **Producer operationId:** `githubInstallations.list`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.githubInstallationsList()` · Python `client.control_plane.github_installations_list()` · Rust `build_request("control_plane", "github_installations_list", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.githubInstallationsList({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.control_plane.github_installations_list({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "github_installations_list", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### githubInstallationsDisconnect

`DELETE /v1/github/installations/{installationId}`

Disconnect a GitHub App installation from the workspace.

- **Producer operationId:** `githubInstallations.disconnect`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.githubInstallationsDisconnect()` · Python `client.control_plane.github_installations_disconnect()` · Rust `build_request("control_plane", "github_installations_disconnect", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `installationId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.githubInstallationsDisconnect({
  installationId: "<installationId>",
});
```

```python Python
result = client.control_plane.github_installations_disconnect({
    "installationId": "<installationId>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "github_installations_disconnect", &[
    ("installationId", "<installationId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### githubInstallationRepositoriesList

`GET /v1/github/installations/{installationId}/repositories`

List repository metadata received for a GitHub installation.

- **Producer operationId:** `githubInstallationRepositories.list`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.githubInstallationRepositoriesList()` · Python `client.control_plane.github_installation_repositories_list()` · Rust `build_request("control_plane", "github_installation_repositories_list", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `installationId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.githubInstallationRepositoriesList({
  installationId: "<installationId>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.control_plane.github_installation_repositories_list({
    "installationId": "<installationId>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "github_installation_repositories_list", &[
    ("installationId", "<installationId>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### githubRepositorySnapshotsCapture

`POST /v1/github/installations/{installationId}/repositories/{repositoryId}:snapshot`

Capture an ephemeral, immutable GitHub repository snapshot for an authorized workspace.

- **Producer operationId:** `githubRepositorySnapshots.capture`
- **Auth:** Account bearer — the control-plane session token returned by `createHostedSession`.
- **Call as:** TypeScript `client.controlPlane.githubRepositorySnapshotsCapture()` · Python `client.control_plane.github_repository_snapshots_capture()` · Rust `build_request("control_plane", "github_repository_snapshots_capture", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `installationId` | yes | Substituted into the URL path (percent-encoded). |
| `repositoryId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `ref` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.githubRepositorySnapshotsCapture({
  installationId: "<installationId>",
  repositoryId: "<repositoryId>",
  ref: "<ref>",
});
```

```python Python
result = client.control_plane.github_repository_snapshots_capture({
    "installationId": "<installationId>",
    "repositoryId": "<repositoryId>",
    "ref": "<ref>",
})
```

```rust Rust
let spec = client.build_request("control_plane", "github_repository_snapshots_capture", &[
    ("installationId", "<installationId>".into()),
    ("repositoryId", "<repositoryId>".into()),
    ("ref", "<ref>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### githubWebhooksAccept

`POST /github/webhook`

Accept a signed, replay-deduplicated GitHub webhook.

- **Producer operationId:** `githubWebhooks.accept`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.controlPlane.githubWebhooksAccept()` · Python `client.control_plane.github_webhooks_accept()` · Rust `build_request("control_plane", "github_webhooks_accept", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.controlPlane.githubWebhooksAccept();
```

```python Python
result = client.control_plane.github_webhooks_accept()
```

```rust Rust
let spec = client.build_request("control_plane", "github_webhooks_accept", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>
