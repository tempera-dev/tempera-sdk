---
title: "cradle"
description: "Capability sandbox daemon (cradled): synchronous and job-based sandboxed execution plus browser admission control."
---

{/* Generated by scripts/gen-sdk-docs.py from surface.json -- do not hand-edit. */}

> **Private access.** Hosted Tempera services are available through design-partner onboarding.
> Use the issuer, credentials, environment, and product URLs provisioned for your workspace;
> start in staging unless Tempera explicitly approves another target.

> **Contract inventory.** This typed reference is retained for SDK compatibility.
> It is not part of the primary public workflow or evidence of general service availability.

Capability sandbox daemon (cradled): synchronous and job-based sandboxed execution plus browser admission control.

| | |
|---|---|
| Repository | [cradle](https://github.com/tempera-dev/cradle) |
| Base URL env var | `TEMPERA_CRADLE_URL` (see [Environments](/environments)) |
| Audience | `cradle` |
| Auth | bearer minted for audience cradle, central tp_ API key, or the cradled static key |
| Client | TypeScript `client.cradle` · Python `client.cradle` · Rust `build_request("cradle", ...)` |
| Typed operations | 18 |

Anything this page does not cover is reachable through the raw passthrough:
`client.cradle.request(path, options)` (TypeScript) /
`client.cradle.request(path, ...)` (Python).

## Operations

### projectsBrowserAdaptersIssueCapability

`POST /v1/browser/adapter/capability`

Call POST /v1/browser/adapter/capability.

- **Producer operationId:** `projects.browserAdapters.issueCapability`
- **Auth:** Product bearer — an access token minted for audience `cradle`, or a central `tp_` API key.
- **Call as:** TypeScript `client.cradle.projectsBrowserAdaptersIssueCapability()` · Python `client.cradle.projects_browser_adapters_issue_capability()` · Rust `build_request("cradle", "projects_browser_adapters_issue_capability", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `actor` | yes | — |
| `adapterId` | no | — |
| `sensitiveActivityMode` | no | — |
| `sensitivity` | yes | — |
| `ttlSeconds` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.cradle.projectsBrowserAdaptersIssueCapability({
  actor: "<actor>",
  sensitivity: "<sensitivity>",
  adapterId: "<adapterId>",
});
```

```python Python
result = client.cradle.projects_browser_adapters_issue_capability({
    "actor": "<actor>",
    "sensitivity": "<sensitivity>",
    "adapterId": "<adapterId>",
})
```

```rust Rust
let spec = client.build_request("cradle", "projects_browser_adapters_issue_capability", &[
    ("actor", "<actor>".into()),
    ("sensitivity", "<sensitivity>".into()),
    ("adapterId", "<adapterId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### projectsBrowserAdaptersValidateCompletion

`POST /v1/browser/adapter/completion/validate`

Call POST /v1/browser/adapter/completion/validate.

- **Producer operationId:** `projects.browserAdapters.validateCompletion`
- **Auth:** Product bearer — an access token minted for audience `cradle`, or a central `tp_` API key.
- **Call as:** TypeScript `client.cradle.projectsBrowserAdaptersValidateCompletion()` · Python `client.cradle.projects_browser_adapters_validate_completion()` · Rust `build_request("cradle", "projects_browser_adapters_validate_completion", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `adapterId` | yes | — |
| `contractVersion` | yes | — |
| `egressLogSealedOrDiscarded` | yes | — |
| `notes` | yes | — |
| `plaintextArtifactsRemoved` | yes | — |
| `processTerminated` | yes | — |
| `proofIds` | yes | — |
| `requestId` | yes | — |
| `sealedArtifactHandles` | yes | — |
| `temporaryProfileRemoved` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.cradle.projectsBrowserAdaptersValidateCompletion({
  adapterId: "<adapterId>",
  contractVersion: "<contractVersion>",
  egressLogSealedOrDiscarded: "<egressLogSealedOrDiscarded>",
  notes: "<notes>",
  plaintextArtifactsRemoved: "<plaintextArtifactsRemoved>",
  processTerminated: "<processTerminated>",
  proofIds: "<proofIds>",
  requestId: "<requestId>",
  sealedArtifactHandles: "<sealedArtifactHandles>",
  temporaryProfileRemoved: "<temporaryProfileRemoved>",
});
```

```python Python
result = client.cradle.projects_browser_adapters_validate_completion({
    "adapterId": "<adapterId>",
    "contractVersion": "<contractVersion>",
    "egressLogSealedOrDiscarded": "<egressLogSealedOrDiscarded>",
    "notes": "<notes>",
    "plaintextArtifactsRemoved": "<plaintextArtifactsRemoved>",
    "processTerminated": "<processTerminated>",
    "proofIds": "<proofIds>",
    "requestId": "<requestId>",
    "sealedArtifactHandles": "<sealedArtifactHandles>",
    "temporaryProfileRemoved": "<temporaryProfileRemoved>",
})
```

```rust Rust
let spec = client.build_request("cradle", "projects_browser_adapters_validate_completion", &[
    ("adapterId", "<adapterId>".into()),
    ("contractVersion", "<contractVersion>".into()),
    ("egressLogSealedOrDiscarded", "<egressLogSealedOrDiscarded>".into()),
    ("notes", "<notes>".into()),
    ("plaintextArtifactsRemoved", "<plaintextArtifactsRemoved>".into()),
    ("processTerminated", "<processTerminated>".into()),
    ("proofIds", "<proofIds>".into()),
    ("requestId", "<requestId>".into()),
    ("sealedArtifactHandles", "<sealedArtifactHandles>".into()),
    ("temporaryProfileRemoved", "<temporaryProfileRemoved>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getBrowserAdapterContract

`GET /v1/browser/adapter/contract`

Fetch the browser adapter contract, required controls, and conformance profile.

- **Producer operationId:** `projects.browserAdapters.getContract`
- **Auth:** Product bearer — an access token minted for audience `cradle`, or a central `tp_` API key.
- **Call as:** TypeScript `client.cradle.getBrowserAdapterContract()` · Python `client.cradle.get_browser_adapter_contract()` · Rust `build_request("cradle", "get_browser_adapter_contract", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.cradle.getBrowserAdapterContract();
```

```python Python
result = client.cradle.get_browser_adapter_contract()
```

```rust Rust
let spec = client.build_request("cradle", "get_browser_adapter_contract", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### projectsBrowserAdaptersClaimLaunch

`POST /v1/browser/adapter/launch/claim`

Call POST /v1/browser/adapter/launch/claim.

- **Producer operationId:** `projects.browserAdapters.claimLaunch`
- **Auth:** Product bearer — an access token minted for audience `cradle`, or a central `tp_` API key.
- **Call as:** TypeScript `client.cradle.projectsBrowserAdaptersClaimLaunch()` · Python `client.cradle.projects_browser_adapters_claim_launch()` · Rust `build_request("cradle", "projects_browser_adapters_claim_launch", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `launchRequest` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.cradle.projectsBrowserAdaptersClaimLaunch({
  launchRequest: "<launchRequest>",
});
```

```python Python
result = client.cradle.projects_browser_adapters_claim_launch({
    "launchRequest": "<launchRequest>",
})
```

```rust Rust
let spec = client.build_request("cradle", "projects_browser_adapters_claim_launch", &[
    ("launchRequest", "<launchRequest>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### projectsBrowserAdaptersPlanLaunch

`POST /v1/browser/adapter/launch/plan`

Call POST /v1/browser/adapter/launch/plan.

- **Producer operationId:** `projects.browserAdapters.planLaunch`
- **Auth:** Product bearer — an access token minted for audience `cradle`, or a central `tp_` API key.
- **Call as:** TypeScript `client.cradle.projectsBrowserAdaptersPlanLaunch()` · Python `client.cradle.projects_browser_adapters_plan_launch()` · Rust `build_request("cradle", "projects_browser_adapters_plan_launch", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `admission` | yes | — |
| `manifest` | yes | — |
| `sameUserCapability` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.cradle.projectsBrowserAdaptersPlanLaunch({
  admission: "<admission>",
  manifest: "<manifest>",
  sameUserCapability: "<sameUserCapability>",
});
```

```python Python
result = client.cradle.projects_browser_adapters_plan_launch({
    "admission": "<admission>",
    "manifest": "<manifest>",
    "sameUserCapability": "<sameUserCapability>",
})
```

```rust Rust
let spec = client.build_request("cradle", "projects_browser_adapters_plan_launch", &[
    ("admission", "<admission>".into()),
    ("manifest", "<manifest>".into()),
    ("sameUserCapability", "<sameUserCapability>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### projectsBrowserAdaptersRegister

`POST /v1/browser/adapter/register`

Call POST /v1/browser/adapter/register.

- **Producer operationId:** `projects.browserAdapters.register`
- **Auth:** Product bearer — an access token minted for audience `cradle`, or a central `tp_` API key.
- **Call as:** TypeScript `client.cradle.projectsBrowserAdaptersRegister()` · Python `client.cradle.projects_browser_adapters_register()` · Rust `build_request("cradle", "projects_browser_adapters_register", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `actor` | yes | — |
| `manifest` | yes | — |
| `sameUserCapability` | yes | — |
| `sensitivity` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.cradle.projectsBrowserAdaptersRegister({
  actor: "<actor>",
  manifest: "<manifest>",
  sameUserCapability: "<sameUserCapability>",
  sensitivity: "<sensitivity>",
});
```

```python Python
result = client.cradle.projects_browser_adapters_register({
    "actor": "<actor>",
    "manifest": "<manifest>",
    "sameUserCapability": "<sameUserCapability>",
    "sensitivity": "<sensitivity>",
})
```

```rust Rust
let spec = client.build_request("cradle", "projects_browser_adapters_register", &[
    ("actor", "<actor>".into()),
    ("manifest", "<manifest>".into()),
    ("sameUserCapability", "<sameUserCapability>".into()),
    ("sensitivity", "<sensitivity>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### projectsBrowserAdaptersValidate

`POST /v1/browser/adapter/validate`

Call POST /v1/browser/adapter/validate.

- **Producer operationId:** `projects.browserAdapters.validate`
- **Auth:** Product bearer — an access token minted for audience `cradle`, or a central `tp_` API key.
- **Call as:** TypeScript `client.cradle.projectsBrowserAdaptersValidate()` · Python `client.cradle.projects_browser_adapters_validate()` · Rust `build_request("cradle", "projects_browser_adapters_validate", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `adapterId` | yes | — |
| `completionProofs` | yes | — |
| `contractVersion` | yes | — |
| `guardFields` | yes | — |
| `launchEndpoint` | yes | — |
| `supportedControls` | yes | — |
| `supportedLevels` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.cradle.projectsBrowserAdaptersValidate({
  adapterId: "<adapterId>",
  completionProofs: "<completionProofs>",
  contractVersion: "<contractVersion>",
  guardFields: "<guardFields>",
  launchEndpoint: "<launchEndpoint>",
  supportedControls: "<supportedControls>",
  supportedLevels: "<supportedLevels>",
});
```

```python Python
result = client.cradle.projects_browser_adapters_validate({
    "adapterId": "<adapterId>",
    "completionProofs": "<completionProofs>",
    "contractVersion": "<contractVersion>",
    "guardFields": "<guardFields>",
    "launchEndpoint": "<launchEndpoint>",
    "supportedControls": "<supportedControls>",
    "supportedLevels": "<supportedLevels>",
})
```

```rust Rust
let spec = client.build_request("cradle", "projects_browser_adapters_validate", &[
    ("adapterId", "<adapterId>".into()),
    ("completionProofs", "<completionProofs>".into()),
    ("contractVersion", "<contractVersion>".into()),
    ("guardFields", "<guardFields>".into()),
    ("launchEndpoint", "<launchEndpoint>".into()),
    ("supportedControls", "<supportedControls>".into()),
    ("supportedLevels", "<supportedLevels>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### admitBrowserSession

`POST /v1/browser/admit`

Request admission for a browser session at a sandbox level and receive the guard plan.

- **Producer operationId:** `projects.browserSessions.admit`
- **Auth:** Product bearer — an access token minted for audience `cradle`, or a central `tp_` API key.
- **Call as:** TypeScript `client.cradle.admitBrowserSession()` · Python `client.cradle.admit_browser_session()` · Rust `build_request("cradle", "admit_browser_session", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `actor` | yes | — |
| `allowDowngrade` | no | — |
| `artifactMode` | no | — |
| `credentialMode` | no | — |
| `requestedLevel` | yes | — |
| `requiredControls` | no | — |
| `sensitiveActivityMode` | no | — |
| `sensitivity` | yes | — |
| `targetOrigins` | no | — |
| `taskLabel` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.cradle.admitBrowserSession({
  actor: "<actor>",
  requestedLevel: "<requestedLevel>",
  sensitivity: "<sensitivity>",
});
```

```python Python
result = client.cradle.admit_browser_session({
    "actor": "<actor>",
    "requestedLevel": "<requestedLevel>",
    "sensitivity": "<sensitivity>",
})
```

```rust Rust
let spec = client.build_request("cradle", "admit_browser_session", &[
    ("actor", "<actor>".into()),
    ("requestedLevel", "<requestedLevel>".into()),
    ("sensitivity", "<sensitivity>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getBrowserProfiles

`GET /v1/browser/profiles`

Fetch the browser sandbox profile levels and suppression modes this daemon offers.

- **Producer operationId:** `projects.browserProfiles.get`
- **Auth:** Product bearer — an access token minted for audience `cradle`, or a central `tp_` API key.
- **Call as:** TypeScript `client.cradle.getBrowserProfiles()` · Python `client.cradle.get_browser_profiles()` · Rust `build_request("cradle", "get_browser_profiles", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.cradle.getBrowserProfiles();
```

```python Python
result = client.cradle.get_browser_profiles()
```

```rust Rust
let spec = client.build_request("cradle", "get_browser_profiles", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getCapabilities

`GET /v1/capabilities`

Fetch the sandbox capability matrix: lanes, engines, limits, and integrations.

- **Producer operationId:** `projects.capabilities.get`
- **Auth:** Product bearer — an access token minted for audience `cradle`, or a central `tp_` API key.
- **Call as:** TypeScript `client.cradle.getCapabilities()` · Python `client.cradle.get_capabilities()` · Rust `build_request("cradle", "get_capabilities", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.cradle.getCapabilities();
```

```python Python
result = client.cradle.get_capabilities()
```

```rust Rust
let spec = client.build_request("cradle", "get_capabilities", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### execute

`POST /v1/execute`

Execute source synchronously in a sandbox lane and return the result with metrics.

- **Producer operationId:** `projects.executions.execute`
- **Auth:** Product bearer — an access token minted for audience `cradle`, or a central `tp_` API key.
- **Call as:** TypeScript `client.cradle.execute()` · Python `client.cradle.execute()` · Rust `build_request("cradle", "execute", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `entrypoint` | no | — |
| `idempotencyKey` | no | — |
| `input` | no | — |
| `lane` | yes | — |
| `policy` | no | — |
| `source` | yes | — |
| `stdin` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.cradle.execute({
  lane: "<lane>",
  source: "<source>",
  entrypoint: "<entrypoint>",
});
```

```python Python
result = client.cradle.execute({
    "lane": "<lane>",
    "source": "<source>",
    "entrypoint": "<entrypoint>",
})
```

```rust Rust
let spec = client.build_request("cradle", "execute", &[
    ("lane", "<lane>".into()),
    ("source", "<source>".into()),
    ("entrypoint", "<entrypoint>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### health

`GET /v1/health`

Check sandbox-daemon liveness; returns status, version, and uptime.

- **Producer operationId:** `health`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.cradle.health()` · Python `client.cradle.health()` · Rust `build_request("cradle", "health", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.cradle.health();
```

```python Python
result = client.cradle.health()
```

```rust Rust
let spec = client.build_request("cradle", "health", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getIntegrationContract

`GET /v1/integration`

Fetch the ecosystem integration contract this daemon implements.

- **Producer operationId:** `projects.integration.get`
- **Auth:** Product bearer — an access token minted for audience `cradle`, or a central `tp_` API key.
- **Call as:** TypeScript `client.cradle.getIntegrationContract()` · Python `client.cradle.get_integration_contract()` · Rust `build_request("cradle", "get_integration_contract", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.cradle.getIntegrationContract();
```

```python Python
result = client.cradle.get_integration_contract()
```

```rust Rust
let spec = client.build_request("cradle", "get_integration_contract", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createJob

`POST /v1/jobs`

Submit an asynchronous sandbox job; returns an operation handle to poll.

- **Producer operationId:** `projects.jobs.create`
- **Auth:** Product bearer — an access token minted for audience `cradle`, or a central `tp_` API key.
- **Call as:** TypeScript `client.cradle.createJob()` · Python `client.cradle.create_job()` · Rust `build_request("cradle", "create_job", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `entrypoint` | no | — |
| `idempotencyKey` | no | — |
| `input` | no | — |
| `lane` | yes | — |
| `policy` | no | — |
| `source` | yes | — |
| `stdin` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.cradle.createJob({
  lane: "<lane>",
  source: "<source>",
  entrypoint: "<entrypoint>",
});
```

```python Python
result = client.cradle.create_job({
    "lane": "<lane>",
    "source": "<source>",
    "entrypoint": "<entrypoint>",
})
```

```rust Rust
let spec = client.build_request("cradle", "create_job", &[
    ("lane", "<lane>".into()),
    ("source", "<source>".into()),
    ("entrypoint", "<entrypoint>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### cancelJob

`DELETE /v1/jobs/{id}`

Cancel a queued or running sandbox job (idempotent for already-cancelled jobs).

- **Producer operationId:** `projects.jobs.cancel`
- **Auth:** Product bearer — an access token minted for audience `cradle`, or a central `tp_` API key.
- **Call as:** TypeScript `client.cradle.cancelJob()` · Python `client.cradle.cancel_job()` · Rust `build_request("cradle", "cancel_job", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `id` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.cradle.cancelJob({
  id: "<id>",
});
```

```python Python
result = client.cradle.cancel_job({
    "id": "<id>",
})
```

```rust Rust
let spec = client.build_request("cradle", "cancel_job", &[
    ("id", "<id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getJob

`GET /v1/jobs/{id}`

Fetch a sandbox job's status and result.

- **Producer operationId:** `projects.jobs.get`
- **Auth:** Product bearer — an access token minted for audience `cradle`, or a central `tp_` API key.
- **Call as:** TypeScript `client.cradle.getJob()` · Python `client.cradle.get_job()` · Rust `build_request("cradle", "get_job", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `id` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.cradle.getJob({
  id: "<id>",
});
```

```python Python
result = client.cradle.get_job({
    "id": "<id>",
})
```

```rust Rust
let spec = client.build_request("cradle", "get_job", &[
    ("id", "<id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### projectsModulesCreate

`POST /v1/projects/{project}/modules`

Call POST /v1/projects/\{project\}/modules.

- **Producer operationId:** `projects.modules.create`
- **Auth:** Product bearer — an access token minted for audience `cradle`, or a central `tp_` API key.
- **Call as:** TypeScript `client.cradle.projectsModulesCreate()` · Python `client.cradle.projects_modules_create()` · Rust `build_request("cradle", "projects_modules_create", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `bytesBase64` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.cradle.projectsModulesCreate({
  project: "<project>",
  bytesBase64: "<bytesBase64>",
});
```

```python Python
result = client.cradle.projects_modules_create({
    "project": "<project>",
    "bytesBase64": "<bytesBase64>",
})
```

```rust Rust
let spec = client.build_request("cradle", "projects_modules_create", &[
    ("project", "<project>".into()),
    ("bytesBase64", "<bytesBase64>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### projectsModulesGet

`GET /v1/projects/{project}/modules/{sha256}`

Call GET /v1/projects/\{project\}/modules/\{sha256\}.

- **Producer operationId:** `projects.modules.get`
- **Auth:** Product bearer — an access token minted for audience `cradle`, or a central `tp_` API key.
- **Call as:** TypeScript `client.cradle.projectsModulesGet()` · Python `client.cradle.projects_modules_get()` · Rust `build_request("cradle", "projects_modules_get", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `sha256` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.cradle.projectsModulesGet({
  project: "<project>",
  sha256: "<sha256>",
});
```

```python Python
result = client.cradle.projects_modules_get({
    "project": "<project>",
    "sha256": "<sha256>",
})
```

```rust Rust
let spec = client.build_request("cradle", "projects_modules_get", &[
    ("project", "<project>".into()),
    ("sha256", "<sha256>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>
