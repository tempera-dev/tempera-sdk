---
title: "cradle"
description: "Capability sandbox daemon (cradled): synchronous and job-based sandboxed execution plus browser admission control."
---

{/* Generated by scripts/gen-sdk-docs.py from surface.json -- do not hand-edit. */}

> **Private access.** Hosted Tempera services are available through design-partner onboarding.
> Use the issuer, credentials, environment, and product URLs provisioned for your workspace;
> start in staging unless Tempera explicitly approves another target.

Capability sandbox daemon (cradled): synchronous and job-based sandboxed execution plus browser admission control.

| | |
|---|---|
| Repository | [cradle](https://github.com/tempera-dev/cradle) |
| Base URL env var | `TEMPERA_CRADLE_URL` (see [Environments](/environments)) |
| Audience | `cradle` |
| Auth | bearer minted for audience cradle, central tp_ API key, or the cradled static key |
| Client | TypeScript `client.cradle` · Python `client.cradle` · Rust `build_request("cradle", ...)` |
| Typed operations | 10 |

Anything this page does not cover is reachable through the raw passthrough:
`client.cradle.request(path, options)` (TypeScript) /
`client.cradle.request(path, ...)` (Python).

## Operations

### health

`GET /v1/health`

Check sandbox-daemon liveness; returns status, version, and uptime.

- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.cradle.health()` · Python `client.cradle.health()` · Rust `build_request("cradle", "health", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.cradle.health();
```

```python Python
result = client.cradle.health()
```

```rust Rust
let spec = client.build_request("cradle", "health", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getCapabilities

`GET /v1/capabilities`

Fetch the sandbox capability matrix: lanes, engines, limits, and integrations.

- **Auth:** Product bearer — an access token minted for audience `cradle`, or a central `tp_` API key.
- **Call as:** TypeScript `client.cradle.getCapabilities()` · Python `client.cradle.get_capabilities()` · Rust `build_request("cradle", "get_capabilities", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.cradle.getCapabilities();
```

```python Python
result = client.cradle.get_capabilities()
```

```rust Rust
let spec = client.build_request("cradle", "get_capabilities", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getIntegrationContract

`GET /v1/integration`

Fetch the ecosystem integration contract this daemon implements.

- **Auth:** Product bearer — an access token minted for audience `cradle`, or a central `tp_` API key.
- **Call as:** TypeScript `client.cradle.getIntegrationContract()` · Python `client.cradle.get_integration_contract()` · Rust `build_request("cradle", "get_integration_contract", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.cradle.getIntegrationContract();
```

```python Python
result = client.cradle.get_integration_contract()
```

```rust Rust
let spec = client.build_request("cradle", "get_integration_contract", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### execute

`POST /v1/execute`

Execute source synchronously in a sandbox lane and return the result with metrics.

- **Auth:** Product bearer — an access token minted for audience `cradle`, or a central `tp_` API key.
- **Call as:** TypeScript `client.cradle.execute()` · Python `client.cradle.execute()` · Rust `build_request("cradle", "execute", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `lane` | no | — |
| `source` | no | — |
| `entrypoint` | no | — |
| `input` | no | — |
| `stdin` | no | — |
| `policy` | no | — |
| `idempotency_key` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.cradle.execute({
  lane: "<lane>",
  source: "<source>",
  entrypoint: "<entrypoint>",
});
```

```python Python
result = client.cradle.execute({
    "lane": "<lane>",
    "source": "<source>",
    "entrypoint": "<entrypoint>",
})
```

```rust Rust
let spec = client.build_request("cradle", "execute", &[
    ("lane", "<lane>".into()),
    ("source", "<source>".into()),
    ("entrypoint", "<entrypoint>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createJob

`POST /v1/jobs`

Submit an asynchronous sandbox job; returns an operation handle to poll.

- **Auth:** Product bearer — an access token minted for audience `cradle`, or a central `tp_` API key.
- **Call as:** TypeScript `client.cradle.createJob()` · Python `client.cradle.create_job()` · Rust `build_request("cradle", "create_job", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `lane` | no | — |
| `source` | no | — |
| `entrypoint` | no | — |
| `input` | no | — |
| `stdin` | no | — |
| `policy` | no | — |
| `idempotency_key` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.cradle.createJob({
  lane: "<lane>",
  source: "<source>",
  entrypoint: "<entrypoint>",
});
```

```python Python
result = client.cradle.create_job({
    "lane": "<lane>",
    "source": "<source>",
    "entrypoint": "<entrypoint>",
})
```

```rust Rust
let spec = client.build_request("cradle", "create_job", &[
    ("lane", "<lane>".into()),
    ("source", "<source>".into()),
    ("entrypoint", "<entrypoint>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getJob

`GET /v1/jobs/{job_id}`

Fetch a sandbox job's status and result.

- **Auth:** Product bearer — an access token minted for audience `cradle`, or a central `tp_` API key.
- **Call as:** TypeScript `client.cradle.getJob()` · Python `client.cradle.get_job()` · Rust `build_request("cradle", "get_job", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `job_id` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.cradle.getJob({
  job_id: "<job_id>",
});
```

```python Python
result = client.cradle.get_job({
    "job_id": "<job_id>",
})
```

```rust Rust
let spec = client.build_request("cradle", "get_job", &[
    ("job_id", "<job_id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### cancelJob

`DELETE /v1/jobs/{job_id}`

Cancel a queued or running sandbox job (idempotent for already-cancelled jobs).

- **Auth:** Product bearer — an access token minted for audience `cradle`, or a central `tp_` API key.
- **Call as:** TypeScript `client.cradle.cancelJob()` · Python `client.cradle.cancel_job()` · Rust `build_request("cradle", "cancel_job", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `job_id` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.cradle.cancelJob({
  job_id: "<job_id>",
});
```

```python Python
result = client.cradle.cancel_job({
    "job_id": "<job_id>",
})
```

```rust Rust
let spec = client.build_request("cradle", "cancel_job", &[
    ("job_id", "<job_id>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getBrowserProfiles

`GET /v1/browser/profiles`

Fetch the browser sandbox profile levels and suppression modes this daemon offers.

- **Auth:** Product bearer — an access token minted for audience `cradle`, or a central `tp_` API key.
- **Call as:** TypeScript `client.cradle.getBrowserProfiles()` · Python `client.cradle.get_browser_profiles()` · Rust `build_request("cradle", "get_browser_profiles", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.cradle.getBrowserProfiles();
```

```python Python
result = client.cradle.get_browser_profiles()
```

```rust Rust
let spec = client.build_request("cradle", "get_browser_profiles", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### admitBrowserSession

`POST /v1/browser/admit`

Request admission for a browser session at a sandbox level and receive the guard plan.

- **Auth:** Product bearer — an access token minted for audience `cradle`, or a central `tp_` API key.
- **Call as:** TypeScript `client.cradle.admitBrowserSession()` · Python `client.cradle.admit_browser_session()` · Rust `build_request("cradle", "admit_browser_session", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `actor` | no | — |
| `requested_level` | no | — |
| `sensitivity` | no | — |
| `allow_downgrade` | no | — |
| `artifact_mode` | no | — |
| `credential_mode` | no | — |
| `required_controls` | no | — |
| `sensitive_activity_mode` | no | — |
| `target_origins` | no | — |
| `task_label` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.cradle.admitBrowserSession({
  actor: "<actor>",
  requested_level: "<requested_level>",
  sensitivity: "<sensitivity>",
});
```

```python Python
result = client.cradle.admit_browser_session({
    "actor": "<actor>",
    "requested_level": "<requested_level>",
    "sensitivity": "<sensitivity>",
})
```

```rust Rust
let spec = client.build_request("cradle", "admit_browser_session", &[
    ("actor", "<actor>".into()),
    ("requested_level", "<requested_level>".into()),
    ("sensitivity", "<sensitivity>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getBrowserAdapterContract

`GET /v1/browser/adapter/contract`

Fetch the browser adapter contract, required controls, and conformance profile.

- **Auth:** Product bearer — an access token minted for audience `cradle`, or a central `tp_` API key.
- **Call as:** TypeScript `client.cradle.getBrowserAdapterContract()` · Python `client.cradle.get_browser_adapter_contract()` · Rust `build_request("cradle", "get_browser_adapter_contract", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.cradle.getBrowserAdapterContract();
```

```python Python
result = client.cradle.get_browser_adapter_contract()
```

```rust Rust
let spec = client.build_request("cradle", "get_browser_adapter_contract", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>
