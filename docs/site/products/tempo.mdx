---
title: "tempo"
description: "Agent-native browser daemon (tempod): structured observation, batched actions, sessions, runs, and human handoff."
---

{/* Generated by scripts/gen-sdk-docs.py from surface.json -- do not hand-edit. */}

> **Private access.** Hosted Tempera services are available through design-partner onboarding.
> Use the issuer, credentials, environment, and product URLs provisioned for your workspace;
> start in staging unless Tempera explicitly approves another target.

Agent-native browser daemon (tempod): structured observation, batched actions, sessions, runs, and human handoff.

| | |
|---|---|
| Repository | [tempo](https://github.com/tempera-dev/tempo) |
| Base URL env var | `TEMPERA_TEMPO_URL` (see [Environments](/environments)) |
| Audience | `tempo` |
| Auth | bearer minted for audience tempo, central tp_ API key, or the tempod static token |
| Client | TypeScript `client.tempo` · Python `client.tempo` · Rust `build_request("tempo", ...)` |
| Typed operations | 27 |

Anything this page does not cover is reachable through the raw passthrough:
`client.tempo.request(path, options)` (TypeScript) /
`client.tempo.request(path, ...)` (Python).

## Operations

### agentCard

`GET /.well-known/agent-card.json`

Call GET /.well-known/agent-card.json.

- **Producer operationId:** `agentCard`
- **Auth:** Product bearer — an access token minted for audience `tempo`, or a central `tp_` API key.
- **Call as:** TypeScript `client.tempo.agentCard()` · Python `client.tempo.agent_card()` · Rust `build_request("tempo", "agent_card", params)` · Go `BuildRequest("tempo", "agentCard", params)` · C `tempera_build_request(..., "tempo", "agentCard", ...)`

<CodeGroup>

```typescript TypeScript
const result = await client.tempo.agentCard();
```

```python Python
result = client.tempo.agent_card()
```

```rust Rust
let spec = client.build_request("tempo", "agent_card", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("tempo", "agentCard", map[string]any{})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "tempo", "agentCard",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### agentJson

`GET /.well-known/agent.json`

Call GET /.well-known/agent.json.

- **Producer operationId:** `agentJson`
- **Auth:** Product bearer — an access token minted for audience `tempo`, or a central `tp_` API key.
- **Call as:** TypeScript `client.tempo.agentJson()` · Python `client.tempo.agent_json()` · Rust `build_request("tempo", "agent_json", params)` · Go `BuildRequest("tempo", "agentJson", params)` · C `tempera_build_request(..., "tempo", "agentJson", ...)`

<CodeGroup>

```typescript TypeScript
const result = await client.tempo.agentJson();
```

```python Python
result = client.tempo.agent_json()
```

```rust Rust
let spec = client.build_request("tempo", "agent_json", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("tempo", "agentJson", map[string]any{})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "tempo", "agentJson",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### health

`GET /health`

Check tempod liveness; returns \{ok: true\}.

- **Producer operationId:** `health`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.tempo.health()` · Python `client.tempo.health()` · Rust `build_request("tempo", "health", params)` · Go `BuildRequest("tempo", "health", params)` · C `tempera_build_request(..., "tempo", "health", ...)`

<CodeGroup>

```typescript TypeScript
const result = await client.tempo.health();
```

```python Python
result = client.tempo.health()
```

```rust Rust
let spec = client.build_request("tempo", "health", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("tempo", "health", map[string]any{})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "tempo", "health",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### metrics

`GET /metrics`

Call GET /metrics.

- **Producer operationId:** `metrics`
- **Auth:** Product bearer — an access token minted for audience `tempo`, or a central `tp_` API key.
- **Call as:** TypeScript `client.tempo.metrics()` · Python `client.tempo.metrics()` · Rust `build_request("tempo", "metrics", params)` · Go `BuildRequest("tempo", "metrics", params)` · C `tempera_build_request(..., "tempo", "metrics", ...)`

<CodeGroup>

```typescript TypeScript
const result = await client.tempo.metrics();
```

```python Python
result = client.tempo.metrics()
```

```rust Rust
let spec = client.build_request("tempo", "metrics", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("tempo", "metrics", map[string]any{})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "tempo", "metrics",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### openapi

`GET /openapi.json`

Fetch tempod's OpenAPI document, generated at runtime for this host.

- **Producer operationId:** `openapi`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.tempo.openapi()` · Python `client.tempo.openapi()` · Rust `build_request("tempo", "openapi", params)` · Go `BuildRequest("tempo", "openapi", params)` · C `tempera_build_request(..., "tempo", "openapi", ...)`

<CodeGroup>

```typescript TypeScript
const result = await client.tempo.openapi();
```

```python Python
result = client.tempo.openapi()
```

```rust Rust
let spec = client.build_request("tempo", "openapi", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("tempo", "openapi", map[string]any{})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "tempo", "openapi",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### ready

`GET /ready`

Check tempod readiness, including engine attachment, drain state, and session capacity.

- **Producer operationId:** `ready`
- **Auth:** Product bearer — an access token minted for audience `tempo`, or a central `tp_` API key.
- **Call as:** TypeScript `client.tempo.ready()` · Python `client.tempo.ready()` · Rust `build_request("tempo", "ready", params)` · Go `BuildRequest("tempo", "ready", params)` · C `tempera_build_request(..., "tempo", "ready", ...)`

<CodeGroup>

```typescript TypeScript
const result = await client.tempo.ready();
```

```python Python
result = client.tempo.ready()
```

```rust Rust
let spec = client.build_request("tempo", "ready", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("tempo", "ready", map[string]any{})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "tempo", "ready",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### drain

`POST /v1/drain`

Call POST /drain.

- **Producer operationId:** `drain`
- **Auth:** Product bearer — an access token minted for audience `tempo`, or a central `tp_` API key.
- **Call as:** TypeScript `client.tempo.drain()` · Python `client.tempo.drain()` · Rust `build_request("tempo", "drain", params)` · Go `BuildRequest("tempo", "drain", params)` · C `tempera_build_request(..., "tempo", "drain", ...)`

<CodeGroup>

```typescript TypeScript
const result = await client.tempo.drain();
```

```python Python
result = client.tempo.drain()
```

```rust Rust
let spec = client.build_request("tempo", "drain", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("tempo", "drain", map[string]any{})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "tempo", "drain",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### listRuns

`GET /v1/runs`

List agent runs, optionally filtered to one session.

- **Producer operationId:** `listRuns`
- **Auth:** Product bearer — an access token minted for audience `tempo`, or a central `tp_` API key.
- **Call as:** TypeScript `client.tempo.listRuns()` · Python `client.tempo.list_runs()` · Rust `build_request("tempo", "list_runs", params)` · Go `BuildRequest("tempo", "listRuns", params)` · C `tempera_build_request(..., "tempo", "listRuns", ...)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `sessionId` | no | — |
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.tempo.listRuns({
  sessionId: "<sessionId>",
  pageSize: "<pageSize>",
});
```

```python Python
result = client.tempo.list_runs({
    "sessionId": "<sessionId>",
    "pageSize": "<pageSize>",
})
```

```rust Rust
let spec = client.build_request("tempo", "list_runs", &[
    ("sessionId", "<sessionId>".into()),
    ("pageSize", "<pageSize>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("tempo", "listRuns", map[string]any{
    "sessionId": "<sessionId>",
    "pageSize": "<pageSize>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = "sessionId=<sessionId>&pageSize=<pageSize>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "tempo", "listRuns",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### getRun

`GET /v1/runs/{runId}`

Fetch one agent run with its state.

- **Producer operationId:** `getRun`
- **Auth:** Product bearer — an access token minted for audience `tempo`, or a central `tp_` API key.
- **Call as:** TypeScript `client.tempo.getRun()` · Python `client.tempo.get_run()` · Rust `build_request("tempo", "get_run", params)` · Go `BuildRequest("tempo", "getRun", params)` · C `tempera_build_request(..., "tempo", "getRun", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `runId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.tempo.getRun({
  runId: "<runId>",
});
```

```python Python
result = client.tempo.get_run({
    "runId": "<runId>",
})
```

```rust Rust
let spec = client.build_request("tempo", "get_run", &[
    ("runId", "<runId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("tempo", "getRun", map[string]any{
    "runId": "<runId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"runId", "<runId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "tempo", "getRun",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### runEvents

`GET /v1/runs/{runId}/events`

Call GET /runs/\{run_id\}/events.

- **Producer operationId:** `runEvents`
- **Auth:** Product bearer — an access token minted for audience `tempo`, or a central `tp_` API key.
- **Call as:** TypeScript `client.tempo.runEvents()` · Python `client.tempo.run_events()` · Rust `build_request("tempo", "run_events", params)` · Go `BuildRequest("tempo", "runEvents", params)` · C `tempera_build_request(..., "tempo", "runEvents", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `runId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `afterSeq` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.tempo.runEvents({
  runId: "<runId>",
  afterSeq: "<afterSeq>",
});
```

```python Python
result = client.tempo.run_events({
    "runId": "<runId>",
    "afterSeq": "<afterSeq>",
})
```

```rust Rust
let spec = client.build_request("tempo", "run_events", &[
    ("runId", "<runId>".into()),
    ("afterSeq", "<afterSeq>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("tempo", "runEvents", map[string]any{
    "runId": "<runId>",
    "afterSeq": "<afterSeq>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"runId", "<runId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "afterSeq=<afterSeq>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "tempo", "runEvents",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### cancelRun

`POST /v1/runs/{runId}:cancel`

Cancel an agent run.

- **Producer operationId:** `cancelRun`
- **Auth:** Product bearer — an access token minted for audience `tempo`, or a central `tp_` API key.
- **Call as:** TypeScript `client.tempo.cancelRun()` · Python `client.tempo.cancel_run()` · Rust `build_request("tempo", "cancel_run", params)` · Go `BuildRequest("tempo", "cancelRun", params)` · C `tempera_build_request(..., "tempo", "cancelRun", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `runId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.tempo.cancelRun({
  runId: "<runId>",
});
```

```python Python
result = client.tempo.cancel_run({
    "runId": "<runId>",
})
```

```rust Rust
let spec = client.build_request("tempo", "cancel_run", &[
    ("runId", "<runId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("tempo", "cancelRun", map[string]any{
    "runId": "<runId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"runId", "<runId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "tempo", "cancelRun",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### resumeRun

`POST /v1/runs/{runId}:resume`

Resume an agent run after a human handoff completes.

- **Producer operationId:** `resumeRun`
- **Auth:** Product bearer — an access token minted for audience `tempo`, or a central `tp_` API key.
- **Call as:** TypeScript `client.tempo.resumeRun()` · Python `client.tempo.resume_run()` · Rust `build_request("tempo", "resume_run", params)` · Go `BuildRequest("tempo", "resumeRun", params)` · C `tempera_build_request(..., "tempo", "resumeRun", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `runId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.tempo.resumeRun({
  runId: "<runId>",
});
```

```python Python
result = client.tempo.resume_run({
    "runId": "<runId>",
})
```

```rust Rust
let spec = client.build_request("tempo", "resume_run", &[
    ("runId", "<runId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("tempo", "resumeRun", map[string]any{
    "runId": "<runId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"runId", "<runId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "tempo", "resumeRun",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### listSessions

`GET /v1/sessions`

List browser sessions with their state and creation time.

- **Producer operationId:** `listSessions`
- **Auth:** Product bearer — an access token minted for audience `tempo`, or a central `tp_` API key.
- **Call as:** TypeScript `client.tempo.listSessions()` · Python `client.tempo.list_sessions()` · Rust `build_request("tempo", "list_sessions", params)` · Go `BuildRequest("tempo", "listSessions", params)` · C `tempera_build_request(..., "tempo", "listSessions", ...)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.tempo.listSessions({
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.tempo.list_sessions({
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("tempo", "list_sessions", &[
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("tempo", "listSessions", map[string]any{
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = "pageSize=<pageSize>&pageToken=<pageToken>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "tempo", "listSessions",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### createSession

`POST /v1/sessions`

Open a browser session at a URL; driverless sessions skip engine attachment.

- **Producer operationId:** `createSession`
- **Auth:** Product bearer — an access token minted for audience `tempo`, or a central `tp_` API key.
- **Call as:** TypeScript `client.tempo.createSession()` · Python `client.tempo.create_session()` · Rust `build_request("tempo", "create_session", params)` · Go `BuildRequest("tempo", "createSession", params)` · C `tempera_build_request(..., "tempo", "createSession", ...)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `driverless` | no | — |
| `url` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.tempo.createSession({
  url: "<url>",
  driverless: "<driverless>",
});
```

```python Python
result = client.tempo.create_session({
    "url": "<url>",
    "driverless": "<driverless>",
})
```

```rust Rust
let spec = client.build_request("tempo", "create_session", &[
    ("url", "<url>".into()),
    ("driverless", "<driverless>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("tempo", "createSession", map[string]any{
    "url": "<url>",
    "driverless": "<driverless>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = "{\"driverless\":\"<driverless>\",\"url\":\"<url>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "tempo", "createSession",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### closeSession

`DELETE /v1/sessions/{sessionId}`

Close a browser session and release its engine resources.

- **Producer operationId:** `closeSession`
- **Auth:** Product bearer — an access token minted for audience `tempo`, or a central `tp_` API key.
- **Call as:** TypeScript `client.tempo.closeSession()` · Python `client.tempo.close_session()` · Rust `build_request("tempo", "close_session", params)` · Go `BuildRequest("tempo", "closeSession", params)` · C `tempera_build_request(..., "tempo", "closeSession", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `sessionId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.tempo.closeSession({
  sessionId: "<sessionId>",
});
```

```python Python
result = client.tempo.close_session({
    "sessionId": "<sessionId>",
})
```

```rust Rust
let spec = client.build_request("tempo", "close_session", &[
    ("sessionId", "<sessionId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("tempo", "closeSession", map[string]any{
    "sessionId": "<sessionId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"sessionId", "<sessionId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "tempo", "closeSession",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### grantConfirmation

`POST /v1/sessions/{sessionId}/confirmations/{confirmationId}:grant`

Grant a pending policy confirmation and receive a single-use grant token.

- **Producer operationId:** `grantSessionConfirmation`
- **Auth:** Product bearer — an access token minted for audience `tempo`, or a central `tp_` API key.
- **Call as:** TypeScript `client.tempo.grantConfirmation()` · Python `client.tempo.grant_confirmation()` · Rust `build_request("tempo", "grant_confirmation", params)` · Go `BuildRequest("tempo", "grantConfirmation", params)` · C `tempera_build_request(..., "tempo", "grantConfirmation", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `sessionId` | yes | Substituted into the URL path (percent-encoded). |
| `confirmationId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.tempo.grantConfirmation({
  sessionId: "<sessionId>",
  confirmationId: "<confirmationId>",
});
```

```python Python
result = client.tempo.grant_confirmation({
    "sessionId": "<sessionId>",
    "confirmationId": "<confirmationId>",
})
```

```rust Rust
let spec = client.build_request("tempo", "grant_confirmation", &[
    ("sessionId", "<sessionId>".into()),
    ("confirmationId", "<confirmationId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("tempo", "grantConfirmation", map[string]any{
    "sessionId": "<sessionId>",
    "confirmationId": "<confirmationId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"sessionId", "<sessionId>"},
    {"confirmationId", "<confirmationId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "tempo", "grantConfirmation",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### sessionEvents

`GET /v1/sessions/{sessionId}/events`

Fetch the session's event window after a sequence number.

- **Producer operationId:** `sessionEvents`
- **Auth:** Product bearer — an access token minted for audience `tempo`, or a central `tp_` API key.
- **Call as:** TypeScript `client.tempo.sessionEvents()` · Python `client.tempo.session_events()` · Rust `build_request("tempo", "session_events", params)` · Go `BuildRequest("tempo", "sessionEvents", params)` · C `tempera_build_request(..., "tempo", "sessionEvents", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `sessionId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `afterSeq` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.tempo.sessionEvents({
  sessionId: "<sessionId>",
  afterSeq: "<afterSeq>",
});
```

```python Python
result = client.tempo.session_events({
    "sessionId": "<sessionId>",
    "afterSeq": "<afterSeq>",
})
```

```rust Rust
let spec = client.build_request("tempo", "session_events", &[
    ("sessionId", "<sessionId>".into()),
    ("afterSeq", "<afterSeq>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("tempo", "sessionEvents", map[string]any{
    "sessionId": "<sessionId>",
    "afterSeq": "<afterSeq>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"sessionId", "<sessionId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "afterSeq=<afterSeq>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "tempo", "sessionEvents",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### managerSession

`GET /v1/sessions/{sessionId}/manager`

Call GET /sessions/\{session_id\}/manager.

- **Producer operationId:** `managerSession`
- **Auth:** Product bearer — an access token minted for audience `tempo`, or a central `tp_` API key.
- **Call as:** TypeScript `client.tempo.managerSession()` · Python `client.tempo.manager_session()` · Rust `build_request("tempo", "manager_session", params)` · Go `BuildRequest("tempo", "managerSession", params)` · C `tempera_build_request(..., "tempo", "managerSession", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `sessionId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.tempo.managerSession({
  sessionId: "<sessionId>",
});
```

```python Python
result = client.tempo.manager_session({
    "sessionId": "<sessionId>",
})
```

```rust Rust
let spec = client.build_request("tempo", "manager_session", &[
    ("sessionId", "<sessionId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("tempo", "managerSession", map[string]any{
    "sessionId": "<sessionId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"sessionId", "<sessionId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "tempo", "managerSession",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### createRun

`POST /v1/sessions/{sessionId}/runs`

Start an agent run against the session with a goal, action budget, and round limit.

- **Producer operationId:** `createSessionRun`
- **Auth:** Product bearer — an access token minted for audience `tempo`, or a central `tp_` API key.
- **Call as:** TypeScript `client.tempo.createRun()` · Python `client.tempo.create_run()` · Rust `build_request("tempo", "create_run", params)` · Go `BuildRequest("tempo", "createRun", params)` · C `tempera_build_request(..., "tempo", "createRun", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `sessionId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `actions` | no | — |
| `goal` | no | — |
| `maxRounds` | no | — |
| `tokenBudget` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.tempo.createRun({
  sessionId: "<sessionId>",
  actions: "<actions>",
  goal: "<goal>",
  maxRounds: "<maxRounds>",
});
```

```python Python
result = client.tempo.create_run({
    "sessionId": "<sessionId>",
    "actions": "<actions>",
    "goal": "<goal>",
    "maxRounds": "<maxRounds>",
})
```

```rust Rust
let spec = client.build_request("tempo", "create_run", &[
    ("sessionId", "<sessionId>".into()),
    ("actions", "<actions>".into()),
    ("goal", "<goal>".into()),
    ("maxRounds", "<maxRounds>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("tempo", "createRun", map[string]any{
    "sessionId": "<sessionId>",
    "actions": "<actions>",
    "goal": "<goal>",
    "maxRounds": "<maxRounds>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"sessionId", "<sessionId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"actions\":\"<actions>\",\"goal\":\"<goal>\",\"maxRounds\":\"<maxRounds>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "tempo", "createRun",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### registerSessionSurface

`POST /v1/sessions/{sessionId}/surfaces`

Call POST /sessions/\{session_id\}/surfaces.

- **Producer operationId:** `registerSessionSurface`
- **Auth:** Product bearer — an access token minted for audience `tempo`, or a central `tp_` API key.
- **Call as:** TypeScript `client.tempo.registerSessionSurface()` · Python `client.tempo.register_session_surface()` · Rust `build_request("tempo", "register_session_surface", params)` · Go `BuildRequest("tempo", "registerSessionSurface", params)` · C `tempera_build_request(..., "tempo", "registerSessionSurface", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `sessionId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `engineTier` | no | — |
| `label` | no | — |
| `platform` | no | — |
| `profileId` | no | — |
| `storageContinuity` | no | — |
| `surfaceId` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.tempo.registerSessionSurface({
  sessionId: "<sessionId>",
  engineTier: "<engineTier>",
  label: "<label>",
  platform: "<platform>",
});
```

```python Python
result = client.tempo.register_session_surface({
    "sessionId": "<sessionId>",
    "engineTier": "<engineTier>",
    "label": "<label>",
    "platform": "<platform>",
})
```

```rust Rust
let spec = client.build_request("tempo", "register_session_surface", &[
    ("sessionId", "<sessionId>".into()),
    ("engineTier", "<engineTier>".into()),
    ("label", "<label>".into()),
    ("platform", "<platform>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("tempo", "registerSessionSurface", map[string]any{
    "sessionId": "<sessionId>",
    "engineTier": "<engineTier>",
    "label": "<label>",
    "platform": "<platform>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"sessionId", "<sessionId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"engineTier\":\"<engineTier>\",\"label\":\"<label>\",\"platform\":\"<platform>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "tempo", "registerSessionSurface",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### removeSessionSurface

`DELETE /v1/sessions/{sessionId}/surfaces/{surfaceId}`

Call DELETE /sessions/\{session_id\}/surfaces/\{surface_id\}.

- **Producer operationId:** `removeSessionSurface`
- **Auth:** Product bearer — an access token minted for audience `tempo`, or a central `tp_` API key.
- **Call as:** TypeScript `client.tempo.removeSessionSurface()` · Python `client.tempo.remove_session_surface()` · Rust `build_request("tempo", "remove_session_surface", params)` · Go `BuildRequest("tempo", "removeSessionSurface", params)` · C `tempera_build_request(..., "tempo", "removeSessionSurface", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `sessionId` | yes | Substituted into the URL path (percent-encoded). |
| `surfaceId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.tempo.removeSessionSurface({
  sessionId: "<sessionId>",
  surfaceId: "<surfaceId>",
});
```

```python Python
result = client.tempo.remove_session_surface({
    "sessionId": "<sessionId>",
    "surfaceId": "<surfaceId>",
})
```

```rust Rust
let spec = client.build_request("tempo", "remove_session_surface", &[
    ("sessionId", "<sessionId>".into()),
    ("surfaceId", "<surfaceId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("tempo", "removeSessionSurface", map[string]any{
    "sessionId": "<sessionId>",
    "surfaceId": "<surfaceId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"sessionId", "<sessionId>"},
    {"surfaceId", "<surfaceId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "tempo", "removeSessionSurface",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### actBatch

`POST /v1/sessions/{sessionId}:actBatch`

Apply a batch of semantic actions with policy gating; returns the applied diff or a policy decision.

- **Producer operationId:** `actBatchSession`
- **Auth:** Product bearer — an access token minted for audience `tempo`, or a central `tp_` API key.
- **Call as:** TypeScript `client.tempo.actBatch()` · Python `client.tempo.act_batch()` · Rust `build_request("tempo", "act_batch", params)` · Go `BuildRequest("tempo", "actBatch", params)` · C `tempera_build_request(..., "tempo", "actBatch", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `sessionId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `batch` | yes | — |
| `confirmationGrant` | no | — |
| `confirmed` | no | — |
| `idempotencyKey` | no | — |
| `inputTainted` | no | — |
| `paymentContext` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.tempo.actBatch({
  sessionId: "<sessionId>",
  batch: "<batch>",
  confirmationGrant: "<confirmationGrant>",
  confirmed: "<confirmed>",
});
```

```python Python
result = client.tempo.act_batch({
    "sessionId": "<sessionId>",
    "batch": "<batch>",
    "confirmationGrant": "<confirmationGrant>",
    "confirmed": "<confirmed>",
})
```

```rust Rust
let spec = client.build_request("tempo", "act_batch", &[
    ("sessionId", "<sessionId>".into()),
    ("batch", "<batch>".into()),
    ("confirmationGrant", "<confirmationGrant>".into()),
    ("confirmed", "<confirmed>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("tempo", "actBatch", map[string]any{
    "sessionId": "<sessionId>",
    "batch": "<batch>",
    "confirmationGrant": "<confirmationGrant>",
    "confirmed": "<confirmed>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"sessionId", "<sessionId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"batch\":\"<batch>\",\"confirmationGrant\":\"<confirmationGrant>\",\"confirmed\":\"<confirmed>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "tempo", "actBatch",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### adoptSession

`POST /v1/sessions/{sessionId}:adopt`

Let a human surface take write ownership of the session and receive an adoption lease.

- **Producer operationId:** `adoptSession`
- **Auth:** Product bearer — an access token minted for audience `tempo`, or a central `tp_` API key.
- **Call as:** TypeScript `client.tempo.adoptSession()` · Python `client.tempo.adopt_session()` · Rust `build_request("tempo", "adopt_session", params)` · Go `BuildRequest("tempo", "adoptSession", params)` · C `tempera_build_request(..., "tempo", "adoptSession", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `sessionId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `engineTier` | no | — |
| `label` | no | — |
| `platform` | no | — |
| `profileId` | no | — |
| `storageContinuity` | no | — |
| `surfaceId` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.tempo.adoptSession({
  sessionId: "<sessionId>",
  engineTier: "<engineTier>",
  label: "<label>",
  platform: "<platform>",
});
```

```python Python
result = client.tempo.adopt_session({
    "sessionId": "<sessionId>",
    "engineTier": "<engineTier>",
    "label": "<label>",
    "platform": "<platform>",
})
```

```rust Rust
let spec = client.build_request("tempo", "adopt_session", &[
    ("sessionId", "<sessionId>".into()),
    ("engineTier", "<engineTier>".into()),
    ("label", "<label>".into()),
    ("platform", "<platform>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("tempo", "adoptSession", map[string]any{
    "sessionId": "<sessionId>",
    "engineTier": "<engineTier>",
    "label": "<label>",
    "platform": "<platform>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"sessionId", "<sessionId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"engineTier\":\"<engineTier>\",\"label\":\"<label>\",\"platform\":\"<platform>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "tempo", "adoptSession",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### handoffSession

`POST /v1/sessions/{sessionId}:handoff`

Return write ownership of the session to the agent plane.

- **Producer operationId:** `handoffSession`
- **Auth:** Product bearer — an access token minted for audience `tempo`, or a central `tp_` API key.
- **Call as:** TypeScript `client.tempo.handoffSession()` · Python `client.tempo.handoff_session()` · Rust `build_request("tempo", "handoff_session", params)` · Go `BuildRequest("tempo", "handoffSession", params)` · C `tempera_build_request(..., "tempo", "handoffSession", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `sessionId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.tempo.handoffSession({
  sessionId: "<sessionId>",
});
```

```python Python
result = client.tempo.handoff_session({
    "sessionId": "<sessionId>",
})
```

```rust Rust
let spec = client.build_request("tempo", "handoff_session", &[
    ("sessionId", "<sessionId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("tempo", "handoffSession", map[string]any{
    "sessionId": "<sessionId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"sessionId", "<sessionId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "tempo", "handoffSession",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### observe

`GET /v1/sessions/{sessionId}:observe`

Fetch the session's compiled structured observation (ranked, stably-identified elements).

- **Producer operationId:** `observeSession`
- **Auth:** Product bearer — an access token minted for audience `tempo`, or a central `tp_` API key.
- **Call as:** TypeScript `client.tempo.observe()` · Python `client.tempo.observe()` · Rust `build_request("tempo", "observe", params)` · Go `BuildRequest("tempo", "observe", params)` · C `tempera_build_request(..., "tempo", "observe", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `sessionId` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.tempo.observe({
  sessionId: "<sessionId>",
});
```

```python Python
result = client.tempo.observe({
    "sessionId": "<sessionId>",
})
```

```rust Rust
let spec = client.build_request("tempo", "observe", &[
    ("sessionId", "<sessionId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("tempo", "observe", map[string]any{
    "sessionId": "<sessionId>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"sessionId", "<sessionId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "tempo", "observe",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### screenshot

`GET /v1/sessions/{sessionId}:screenshot`

Capture a PNG screenshot of the session, optionally annotated with set-of-marks.

- **Producer operationId:** `screenshotSession`
- **Auth:** Product bearer — an access token minted for audience `tempo`, or a central `tp_` API key.
- **Call as:** TypeScript `client.tempo.screenshot()` · Python `client.tempo.screenshot()` · Rust `build_request("tempo", "screenshot", params)` · Go `BuildRequest("tempo", "screenshot", params)` · C `tempera_build_request(..., "tempo", "screenshot", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `sessionId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `setOfMarks` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.tempo.screenshot({
  sessionId: "<sessionId>",
  setOfMarks: "<setOfMarks>",
});
```

```python Python
result = client.tempo.screenshot({
    "sessionId": "<sessionId>",
    "setOfMarks": "<setOfMarks>",
})
```

```rust Rust
let spec = client.build_request("tempo", "screenshot", &[
    ("sessionId", "<sessionId>".into()),
    ("setOfMarks", "<setOfMarks>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("tempo", "screenshot", map[string]any{
    "sessionId": "<sessionId>",
    "setOfMarks": "<setOfMarks>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"sessionId", "<sessionId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = "setOfMarks=<setOfMarks>";
const char *body_json = NULL;
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "tempo", "screenshot",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### transformSession

`POST /v1/sessions/{sessionId}:transform`

Call POST /sessions/\{session_id\}/transform.

- **Producer operationId:** `transformSession`
- **Auth:** Product bearer — an access token minted for audience `tempo`, or a central `tp_` API key.
- **Call as:** TypeScript `client.tempo.transformSession()` · Python `client.tempo.transform_session()` · Rust `build_request("tempo", "transform_session", params)` · Go `BuildRequest("tempo", "transformSession", params)` · C `tempera_build_request(..., "tempo", "transformSession", ...)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `sessionId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `determinism` | no | — |
| `idempotencyKey` | no | — |
| `input` | no | — |
| `lane` | yes | — |
| `source` | yes | — |
| `spans` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.tempo.transformSession({
  sessionId: "<sessionId>",
  lane: "<lane>",
  source: "<source>",
  determinism: "<determinism>",
});
```

```python Python
result = client.tempo.transform_session({
    "sessionId": "<sessionId>",
    "lane": "<lane>",
    "source": "<source>",
    "determinism": "<determinism>",
})
```

```rust Rust
let spec = client.build_request("tempo", "transform_session", &[
    ("sessionId", "<sessionId>".into()),
    ("lane", "<lane>".into()),
    ("source", "<source>".into()),
    ("determinism", "<determinism>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("tempo", "transformSession", map[string]any{
    "sessionId": "<sessionId>",
    "lane": "<lane>",
    "source": "<source>",
    "determinism": "<determinism>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param path_params[] = {
    {"sessionId", "<sessionId>"},
};
const size_t path_param_count = sizeof(path_params) / sizeof(path_params[0]);
const char *query_string = NULL;
const char *body_json = "{\"determinism\":\"<determinism>\",\"lane\":\"<lane>\",\"source\":\"<source>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "tempo", "transformSession",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>
