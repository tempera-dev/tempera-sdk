---
title: "tempera-bio"
description: "Fail-closed computational-biology artifact pipeline for source ingestion, proposal preparation, measurement verification, and decision derivation."
---

{/* Generated by scripts/gen-sdk-docs.py from surface.json -- do not hand-edit. */}

> **Private access.** Hosted Tempera services are available through design-partner onboarding.
> Use the issuer, credentials, environment, and product URLs provisioned for your workspace;
> start in staging unless Tempera explicitly approves another target.

> **Contract inventory.** This typed reference is retained for SDK compatibility.
> It is not part of the primary public workflow or evidence of general service availability.

Fail-closed computational-biology artifact pipeline for source ingestion, proposal preparation, measurement verification, and decision derivation.

| | |
|---|---|
| Repository | [tempera-bio](https://github.com/tempera-dev/tempera-bio) |
| Base URL env var | `TEMPERA_BIO_URL` (see [Environments](/environments)) |
| Audience | `tempera-bio` |
| Auth | bearer minted for audience tempera-bio with the operation's bio scope, or a central tp_ API key |
| Client | TypeScript `client.temperaBio` · Python `client.tempera_bio` · Rust `build_request("tempera_bio", ...)` |
| Typed operations | 10 |

Anything this page does not cover is reachable through the raw passthrough:
`client.temperaBio.request(path, options)` (TypeScript) /
`client.tempera_bio.request(path, ...)` (Python).

## Operations

### deriveCampaignState

`POST /v1/campaignStates:derive`

Derive campaign state.

- **Producer operationId:** `deriveCampaignState`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:decision:write`
- **Call as:** TypeScript `client.temperaBio.deriveCampaignState()` · Python `client.tempera_bio.derive_campaign_state()` · Rust `build_request("tempera_bio", "derive_campaign_state", params)` · Go `BuildRequest("temperaBio", "deriveCampaignState", params)` · C `tempera_build_request(..., "temperaBio", "deriveCampaignState", ...)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `candidateSet` | yes | — |
| `hypothesis` | yes | — |
| `program` | yes | — |
| `rounds` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.deriveCampaignState({
  candidateSet: "<candidateSet>",
  hypothesis: "<hypothesis>",
  program: "<program>",
  rounds: "<rounds>",
});
```

```python Python
result = client.tempera_bio.derive_campaign_state({
    "candidateSet": "<candidateSet>",
    "hypothesis": "<hypothesis>",
    "program": "<program>",
    "rounds": "<rounds>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "derive_campaign_state", &[
    ("candidateSet", "<candidateSet>".into()),
    ("hypothesis", "<hypothesis>".into()),
    ("program", "<program>".into()),
    ("rounds", "<rounds>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaBio", "deriveCampaignState", map[string]any{
    "candidateSet": "<candidateSet>",
    "hypothesis": "<hypothesis>",
    "program": "<program>",
    "rounds": "<rounds>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = "{\"candidateSet\":\"<candidateSet>\",\"hypothesis\":\"<hypothesis>\",\"program\":\"<program>\",\"rounds\":\"<rounds>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaBio", "deriveCampaignState",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### prepareCandidateSet

`POST /v1/candidateSets:prepare`

Prepare candidate set.

- **Producer operationId:** `prepareCandidateSet`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:proposal:write`
- **Call as:** TypeScript `client.temperaBio.prepareCandidateSet()` · Python `client.tempera_bio.prepare_candidate_set()` · Rust `build_request("tempera_bio", "prepare_candidate_set", params)` · Go `BuildRequest("temperaBio", "prepareCandidateSet", params)` · C `tempera_build_request(..., "temperaBio", "prepareCandidateSet", ...)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `candidates` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.prepareCandidateSet({
  candidates: "<candidates>",
});
```

```python Python
result = client.tempera_bio.prepare_candidate_set({
    "candidates": "<candidates>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "prepare_candidate_set", &[
    ("candidates", "<candidates>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaBio", "prepareCandidateSet", map[string]any{
    "candidates": "<candidates>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = "{\"candidates\":\"<candidates>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaBio", "prepareCandidateSet",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### prepareDatasetReleaseManifest

`POST /v1/datasetReleaseManifests:prepare`

Prepare dataset release manifest.

- **Producer operationId:** `prepareDatasetReleaseManifest`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:proposal:write`
- **Call as:** TypeScript `client.temperaBio.prepareDatasetReleaseManifest()` · Python `client.tempera_bio.prepare_dataset_release_manifest()` · Rust `build_request("tempera_bio", "prepare_dataset_release_manifest", params)` · Go `BuildRequest("temperaBio", "prepareDatasetReleaseManifest", params)` · C `tempera_build_request(..., "temperaBio", "prepareDatasetReleaseManifest", ...)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `decision` | yes | — |
| `program` | yes | — |
| `verifierReceipts` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.prepareDatasetReleaseManifest({
  decision: "<decision>",
  program: "<program>",
  verifierReceipts: "<verifierReceipts>",
});
```

```python Python
result = client.tempera_bio.prepare_dataset_release_manifest({
    "decision": "<decision>",
    "program": "<program>",
    "verifierReceipts": "<verifierReceipts>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "prepare_dataset_release_manifest", &[
    ("decision", "<decision>".into()),
    ("program", "<program>".into()),
    ("verifierReceipts", "<verifierReceipts>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaBio", "prepareDatasetReleaseManifest", map[string]any{
    "decision": "<decision>",
    "program": "<program>",
    "verifierReceipts": "<verifierReceipts>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = "{\"decision\":\"<decision>\",\"program\":\"<program>\",\"verifierReceipts\":\"<verifierReceipts>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaBio", "prepareDatasetReleaseManifest",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### deriveDecision

`POST /v1/decisions:derive`

Derive decision.

- **Producer operationId:** `deriveDecision`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:decision:write`
- **Call as:** TypeScript `client.temperaBio.deriveDecision()` · Python `client.tempera_bio.derive_decision()` · Rust `build_request("tempera_bio", "derive_decision", params)` · Go `BuildRequest("temperaBio", "deriveDecision", params)` · C `tempera_build_request(..., "temperaBio", "deriveDecision", ...)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `candidateSet` | yes | — |
| `experimentProposal` | yes | — |
| `hypothesis` | yes | — |
| `program` | yes | — |
| `verifierReceipts` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.deriveDecision({
  candidateSet: "<candidateSet>",
  experimentProposal: "<experimentProposal>",
  hypothesis: "<hypothesis>",
  program: "<program>",
  verifierReceipts: "<verifierReceipts>",
});
```

```python Python
result = client.tempera_bio.derive_decision({
    "candidateSet": "<candidateSet>",
    "experimentProposal": "<experimentProposal>",
    "hypothesis": "<hypothesis>",
    "program": "<program>",
    "verifierReceipts": "<verifierReceipts>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "derive_decision", &[
    ("candidateSet", "<candidateSet>".into()),
    ("experimentProposal", "<experimentProposal>".into()),
    ("hypothesis", "<hypothesis>".into()),
    ("program", "<program>".into()),
    ("verifierReceipts", "<verifierReceipts>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaBio", "deriveDecision", map[string]any{
    "candidateSet": "<candidateSet>",
    "experimentProposal": "<experimentProposal>",
    "hypothesis": "<hypothesis>",
    "program": "<program>",
    "verifierReceipts": "<verifierReceipts>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = "{\"candidateSet\":\"<candidateSet>\",\"experimentProposal\":\"<experimentProposal>\",\"hypothesis\":\"<hypothesis>\",\"program\":\"<program>\",\"verifierReceipts\":\"<verifierReceipts>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaBio", "deriveDecision",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### prepareExperimentProposal

`POST /v1/experimentProposals:prepare`

Prepare experiment proposal.

- **Producer operationId:** `prepareExperimentProposal`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:proposal:write`
- **Call as:** TypeScript `client.temperaBio.prepareExperimentProposal()` · Python `client.tempera_bio.prepare_experiment_proposal()` · Rust `build_request("tempera_bio", "prepare_experiment_proposal", params)` · Go `BuildRequest("temperaBio", "prepareExperimentProposal", params)` · C `tempera_build_request(..., "temperaBio", "prepareExperimentProposal", ...)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `candidateSet` | yes | — |
| `program` | yes | — |
| `selectedCandidates` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.prepareExperimentProposal({
  candidateSet: "<candidateSet>",
  program: "<program>",
  selectedCandidates: "<selectedCandidates>",
});
```

```python Python
result = client.tempera_bio.prepare_experiment_proposal({
    "candidateSet": "<candidateSet>",
    "program": "<program>",
    "selectedCandidates": "<selectedCandidates>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "prepare_experiment_proposal", &[
    ("candidateSet", "<candidateSet>".into()),
    ("program", "<program>".into()),
    ("selectedCandidates", "<selectedCandidates>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaBio", "prepareExperimentProposal", map[string]any{
    "candidateSet": "<candidateSet>",
    "program": "<program>",
    "selectedCandidates": "<selectedCandidates>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = "{\"candidateSet\":\"<candidateSet>\",\"program\":\"<program>\",\"selectedCandidates\":\"<selectedCandidates>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaBio", "prepareExperimentProposal",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### prepareExperimentProposalFromGymBatch

`POST /v1/experimentProposals:prepareFromGymBatch`

Prepare experiment proposal from gym batch.

- **Producer operationId:** `prepareExperimentProposalFromGymBatch`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:proposal:write`
- **Call as:** TypeScript `client.temperaBio.prepareExperimentProposalFromGymBatch()` · Python `client.tempera_bio.prepare_experiment_proposal_from_gym_batch()` · Rust `build_request("tempera_bio", "prepare_experiment_proposal_from_gym_batch", params)` · Go `BuildRequest("temperaBio", "prepareExperimentProposalFromGymBatch", params)` · C `tempera_build_request(..., "temperaBio", "prepareExperimentProposalFromGymBatch", ...)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `batchProposal` | yes | — |
| `campaignContext` | yes | — |
| `candidateSet` | yes | — |
| `program` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.prepareExperimentProposalFromGymBatch({
  batchProposal: "<batchProposal>",
  campaignContext: "<campaignContext>",
  candidateSet: "<candidateSet>",
  program: "<program>",
});
```

```python Python
result = client.tempera_bio.prepare_experiment_proposal_from_gym_batch({
    "batchProposal": "<batchProposal>",
    "campaignContext": "<campaignContext>",
    "candidateSet": "<candidateSet>",
    "program": "<program>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "prepare_experiment_proposal_from_gym_batch", &[
    ("batchProposal", "<batchProposal>".into()),
    ("campaignContext", "<campaignContext>".into()),
    ("candidateSet", "<candidateSet>".into()),
    ("program", "<program>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaBio", "prepareExperimentProposalFromGymBatch", map[string]any{
    "batchProposal": "<batchProposal>",
    "campaignContext": "<campaignContext>",
    "candidateSet": "<candidateSet>",
    "program": "<program>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = "{\"batchProposal\":\"<batchProposal>\",\"campaignContext\":\"<campaignContext>\",\"candidateSet\":\"<candidateSet>\",\"program\":\"<program>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaBio", "prepareExperimentProposalFromGymBatch",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### prepareHypothesis

`POST /v1/hypotheses:prepare`

Prepare hypothesis.

- **Producer operationId:** `prepareHypothesis`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:proposal:write`
- **Call as:** TypeScript `client.temperaBio.prepareHypothesis()` · Python `client.tempera_bio.prepare_hypothesis()` · Rust `build_request("tempera_bio", "prepare_hypothesis", params)` · Go `BuildRequest("temperaBio", "prepareHypothesis", params)` · C `tempera_build_request(..., "temperaBio", "prepareHypothesis", ...)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `hypothesis` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.prepareHypothesis({
  hypothesis: "<hypothesis>",
});
```

```python Python
result = client.tempera_bio.prepare_hypothesis({
    "hypothesis": "<hypothesis>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "prepare_hypothesis", &[
    ("hypothesis", "<hypothesis>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaBio", "prepareHypothesis", map[string]any{
    "hypothesis": "<hypothesis>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = "{\"hypothesis\":\"<hypothesis>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaBio", "prepareHypothesis",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### verifyMeasurement

`POST /v1/measurements:verify`

Verify measurement.

- **Producer operationId:** `verifyMeasurement`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:measurement:verify`
- **Call as:** TypeScript `client.temperaBio.verifyMeasurement()` · Python `client.tempera_bio.verify_measurement()` · Rust `build_request("tempera_bio", "verify_measurement", params)` · Go `BuildRequest("temperaBio", "verifyMeasurement", params)` · C `tempera_build_request(..., "temperaBio", "verifyMeasurement", ...)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `candidate` | yes | — |
| `experimentProposal` | yes | — |
| `hypothesis` | yes | — |
| `identitySignature` | yes | — |
| `program` | yes | — |
| `rawMeasurementBase64` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.verifyMeasurement({
  candidate: "<candidate>",
  experimentProposal: "<experimentProposal>",
  hypothesis: "<hypothesis>",
  identitySignature: "<identitySignature>",
  program: "<program>",
  rawMeasurementBase64: "<rawMeasurementBase64>",
});
```

```python Python
result = client.tempera_bio.verify_measurement({
    "candidate": "<candidate>",
    "experimentProposal": "<experimentProposal>",
    "hypothesis": "<hypothesis>",
    "identitySignature": "<identitySignature>",
    "program": "<program>",
    "rawMeasurementBase64": "<rawMeasurementBase64>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "verify_measurement", &[
    ("candidate", "<candidate>".into()),
    ("experimentProposal", "<experimentProposal>".into()),
    ("hypothesis", "<hypothesis>".into()),
    ("identitySignature", "<identitySignature>".into()),
    ("program", "<program>".into()),
    ("rawMeasurementBase64", "<rawMeasurementBase64>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaBio", "verifyMeasurement", map[string]any{
    "candidate": "<candidate>",
    "experimentProposal": "<experimentProposal>",
    "hypothesis": "<hypothesis>",
    "identitySignature": "<identitySignature>",
    "program": "<program>",
    "rawMeasurementBase64": "<rawMeasurementBase64>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = "{\"candidate\":\"<candidate>\",\"experimentProposal\":\"<experimentProposal>\",\"hypothesis\":\"<hypothesis>\",\"identitySignature\":\"<identitySignature>\",\"program\":\"<program>\",\"rawMeasurementBase64\":\"<rawMeasurementBase64>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaBio", "verifyMeasurement",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### prepareProgram

`POST /v1/programs:prepare`

Prepare program.

- **Producer operationId:** `prepareProgram`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:proposal:write`
- **Call as:** TypeScript `client.temperaBio.prepareProgram()` · Python `client.tempera_bio.prepare_program()` · Rust `build_request("tempera_bio", "prepare_program", params)` · Go `BuildRequest("temperaBio", "prepareProgram", params)` · C `tempera_build_request(..., "temperaBio", "prepareProgram", ...)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `candidateSet` | yes | — |
| `configuration` | yes | — |
| `hypothesis` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.prepareProgram({
  candidateSet: "<candidateSet>",
  configuration: "<configuration>",
  hypothesis: "<hypothesis>",
});
```

```python Python
result = client.tempera_bio.prepare_program({
    "candidateSet": "<candidateSet>",
    "configuration": "<configuration>",
    "hypothesis": "<hypothesis>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "prepare_program", &[
    ("candidateSet", "<candidateSet>".into()),
    ("configuration", "<configuration>".into()),
    ("hypothesis", "<hypothesis>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaBio", "prepareProgram", map[string]any{
    "candidateSet": "<candidateSet>",
    "configuration": "<configuration>",
    "hypothesis": "<hypothesis>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = "{\"candidateSet\":\"<candidateSet>\",\"configuration\":\"<configuration>\",\"hypothesis\":\"<hypothesis>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaBio", "prepareProgram",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>

### ingestMaveDBScoreSet

`POST /v1/sources/maveDbScoreSets:ingest`

Ingest mave d b score set.

- **Producer operationId:** `ingestMaveDBScoreSet`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:source:read`
- **Call as:** TypeScript `client.temperaBio.ingestMaveDBScoreSet()` · Python `client.tempera_bio.ingest_mave_db_score_set()` · Rust `build_request("tempera_bio", "ingest_mave_db_score_set", params)` · Go `BuildRequest("temperaBio", "ingestMaveDBScoreSet", params)` · C `tempera_build_request(..., "temperaBio", "ingestMaveDBScoreSet", ...)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `scoreSetUrn` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.ingestMaveDBScoreSet({
  scoreSetUrn: "<scoreSetUrn>",
});
```

```python Python
result = client.tempera_bio.ingest_mave_db_score_set({
    "scoreSetUrn": "<scoreSetUrn>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "ingest_mave_db_score_set", &[
    ("scoreSetUrn", "<scoreSetUrn>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

```go Go
spec, err := client.BuildRequest("temperaBio", "ingestMaveDBScoreSet", map[string]any{
    "scoreSetUrn": "<scoreSetUrn>",
})
if err != nil {
    return err
}
_ = spec // Or call client.Do with the same product, operation, and params.
```

```c C
const tempera_param *path_params = NULL;
const size_t path_param_count = 0;
const char *query_string = NULL;
const char *body_json = "{\"scoreSetUrn\":\"<scoreSetUrn>\"}";
tempera_request_spec request;
int rc = tempera_build_request(
    base_url, bearer,
    "temperaBio", "ingestMaveDBScoreSet",
    path_params, path_param_count,
    query_string, body_json, &request);
if (rc != TEMPERA_OK) {
    /* Handle bounded request-construction failure. */
}
```

</CodeGroup>
