---
title: "tempera-bio"
description: "Fail-closed computational-biology artifact pipeline for source ingestion, proposal preparation, measurement verification, and decision derivation."
---

{/* Generated by scripts/gen-sdk-docs.py from surface.json -- do not hand-edit. */}

> **Private access.** Hosted Tempera services are available through design-partner onboarding.
> Use the issuer, credentials, environment, and product URLs provisioned for your workspace;
> start in staging unless Tempera explicitly approves another target.

> **Contract inventory.** This typed reference is retained for SDK compatibility.
> It is not part of the primary public workflow or evidence of general service availability.

Fail-closed computational-biology artifact pipeline for source ingestion, proposal preparation, measurement verification, and decision derivation.

| | |
|---|---|
| Repository | [tempera-bio](https://github.com/tempera-dev/tempera-bio) |
| Base URL env var | `TEMPERA_BIO_URL` (see [Environments](/environments)) |
| Audience | `tempera-bio` |
| Auth | bearer minted for audience tempera-bio with the operation's bio scope, or a central tp_ API key |
| Client | TypeScript `client.temperaBio` · Python `client.tempera_bio` · Rust `build_request("tempera_bio", ...)` |
| Typed operations | 8 |

Anything this page does not cover is reachable through the raw passthrough:
`client.temperaBio.request(path, options)` (TypeScript) /
`client.tempera_bio.request(path, ...)` (Python).

## Operations

### prepareCandidateSet

`POST /v1/candidateSets:prepare`

Prepare candidate set.

- **Producer operationId:** `prepareCandidateSet`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:proposal:write`
- **Call as:** TypeScript `client.temperaBio.prepareCandidateSet()` · Python `client.tempera_bio.prepare_candidate_set()` · Rust `build_request("tempera_bio", "prepare_candidate_set", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `candidates` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.prepareCandidateSet({
  candidates: "<candidates>",
});
```

```python Python
result = client.tempera_bio.prepare_candidate_set({
    "candidates": "<candidates>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "prepare_candidate_set", &[
    ("candidates", "<candidates>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### prepareDatasetReleaseManifest

`POST /v1/datasetReleaseManifests:prepare`

Prepare dataset release manifest.

- **Producer operationId:** `prepareDatasetReleaseManifest`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:proposal:write`
- **Call as:** TypeScript `client.temperaBio.prepareDatasetReleaseManifest()` · Python `client.tempera_bio.prepare_dataset_release_manifest()` · Rust `build_request("tempera_bio", "prepare_dataset_release_manifest", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `decision` | yes | — |
| `program` | yes | — |
| `verifierReceipts` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.prepareDatasetReleaseManifest({
  decision: "<decision>",
  program: "<program>",
  verifierReceipts: "<verifierReceipts>",
});
```

```python Python
result = client.tempera_bio.prepare_dataset_release_manifest({
    "decision": "<decision>",
    "program": "<program>",
    "verifierReceipts": "<verifierReceipts>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "prepare_dataset_release_manifest", &[
    ("decision", "<decision>".into()),
    ("program", "<program>".into()),
    ("verifierReceipts", "<verifierReceipts>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### deriveDecision

`POST /v1/decisions:derive`

Derive decision.

- **Producer operationId:** `deriveDecision`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:decision:write`
- **Call as:** TypeScript `client.temperaBio.deriveDecision()` · Python `client.tempera_bio.derive_decision()` · Rust `build_request("tempera_bio", "derive_decision", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `candidateSet` | yes | — |
| `experimentProposal` | yes | — |
| `hypothesis` | yes | — |
| `program` | yes | — |
| `verifierReceipts` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.deriveDecision({
  candidateSet: "<candidateSet>",
  experimentProposal: "<experimentProposal>",
  hypothesis: "<hypothesis>",
});
```

```python Python
result = client.tempera_bio.derive_decision({
    "candidateSet": "<candidateSet>",
    "experimentProposal": "<experimentProposal>",
    "hypothesis": "<hypothesis>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "derive_decision", &[
    ("candidateSet", "<candidateSet>".into()),
    ("experimentProposal", "<experimentProposal>".into()),
    ("hypothesis", "<hypothesis>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### prepareExperimentProposal

`POST /v1/experimentProposals:prepare`

Prepare experiment proposal.

- **Producer operationId:** `prepareExperimentProposal`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:proposal:write`
- **Call as:** TypeScript `client.temperaBio.prepareExperimentProposal()` · Python `client.tempera_bio.prepare_experiment_proposal()` · Rust `build_request("tempera_bio", "prepare_experiment_proposal", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `candidateSet` | yes | — |
| `program` | yes | — |
| `selectedCandidates` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.prepareExperimentProposal({
  candidateSet: "<candidateSet>",
  program: "<program>",
  selectedCandidates: "<selectedCandidates>",
});
```

```python Python
result = client.tempera_bio.prepare_experiment_proposal({
    "candidateSet": "<candidateSet>",
    "program": "<program>",
    "selectedCandidates": "<selectedCandidates>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "prepare_experiment_proposal", &[
    ("candidateSet", "<candidateSet>".into()),
    ("program", "<program>".into()),
    ("selectedCandidates", "<selectedCandidates>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### prepareHypothesis

`POST /v1/hypotheses:prepare`

Prepare hypothesis.

- **Producer operationId:** `prepareHypothesis`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:proposal:write`
- **Call as:** TypeScript `client.temperaBio.prepareHypothesis()` · Python `client.tempera_bio.prepare_hypothesis()` · Rust `build_request("tempera_bio", "prepare_hypothesis", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `hypothesis` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.prepareHypothesis({
  hypothesis: "<hypothesis>",
});
```

```python Python
result = client.tempera_bio.prepare_hypothesis({
    "hypothesis": "<hypothesis>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "prepare_hypothesis", &[
    ("hypothesis", "<hypothesis>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### verifyMeasurement

`POST /v1/measurements:verify`

Verify measurement.

- **Producer operationId:** `verifyMeasurement`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:measurement:verify`
- **Call as:** TypeScript `client.temperaBio.verifyMeasurement()` · Python `client.tempera_bio.verify_measurement()` · Rust `build_request("tempera_bio", "verify_measurement", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `candidate` | yes | — |
| `experimentProposal` | yes | — |
| `hypothesis` | yes | — |
| `identitySignature` | yes | — |
| `program` | yes | — |
| `rawMeasurementBase64` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.verifyMeasurement({
  candidate: "<candidate>",
  experimentProposal: "<experimentProposal>",
  hypothesis: "<hypothesis>",
});
```

```python Python
result = client.tempera_bio.verify_measurement({
    "candidate": "<candidate>",
    "experimentProposal": "<experimentProposal>",
    "hypothesis": "<hypothesis>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "verify_measurement", &[
    ("candidate", "<candidate>".into()),
    ("experimentProposal", "<experimentProposal>".into()),
    ("hypothesis", "<hypothesis>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### prepareProgram

`POST /v1/programs:prepare`

Prepare program.

- **Producer operationId:** `prepareProgram`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:proposal:write`
- **Call as:** TypeScript `client.temperaBio.prepareProgram()` · Python `client.tempera_bio.prepare_program()` · Rust `build_request("tempera_bio", "prepare_program", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `candidateSet` | yes | — |
| `configuration` | yes | — |
| `hypothesis` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.prepareProgram({
  candidateSet: "<candidateSet>",
  configuration: "<configuration>",
  hypothesis: "<hypothesis>",
});
```

```python Python
result = client.tempera_bio.prepare_program({
    "candidateSet": "<candidateSet>",
    "configuration": "<configuration>",
    "hypothesis": "<hypothesis>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "prepare_program", &[
    ("candidateSet", "<candidateSet>".into()),
    ("configuration", "<configuration>".into()),
    ("hypothesis", "<hypothesis>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### ingestMaveDBScoreSet

`POST /v1/sources/maveDbScoreSets:ingest`

Ingest mave d b score set.

- **Producer operationId:** `ingestMaveDBScoreSet`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:source:read`
- **Call as:** TypeScript `client.temperaBio.ingestMaveDBScoreSet()` · Python `client.tempera_bio.ingest_mave_d_b_score_set()` · Rust `build_request("tempera_bio", "ingest_mave_d_b_score_set", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `scoreSetUrn` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.ingestMaveDBScoreSet({
  scoreSetUrn: "<scoreSetUrn>",
});
```

```python Python
result = client.tempera_bio.ingest_mave_d_b_score_set({
    "scoreSetUrn": "<scoreSetUrn>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "ingest_mave_d_b_score_set", &[
    ("scoreSetUrn", "<scoreSetUrn>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>
