---
title: "tempera-bio"
description: "Fail-closed computational-biology artifact pipeline for source ingestion, proposal preparation, measurement verification, and decision derivation."
---

{/* Generated by scripts/gen-sdk-docs.py from surface.json -- do not hand-edit. */}

> **Private access.** Hosted Tempera services are available through design-partner onboarding.
> Use the issuer, credentials, environment, and product URLs provisioned for your workspace;
> start in staging unless Tempera explicitly approves another target.

> **Contract inventory.** This typed reference is retained for SDK compatibility.
> It is not part of the primary public workflow or evidence of general service availability.

Fail-closed computational-biology artifact pipeline for source ingestion, proposal preparation, measurement verification, and decision derivation.

| | |
|---|---|
| Repository | [tempera-bio](https://github.com/tempera-dev/tempera-bio) |
| Base URL env var | `TEMPERA_BIO_URL` (see [Environments](/environments)) |
| Audience | `tempera-bio` |
| Auth | bearer minted for audience tempera-bio with the operation's bio scope, or a central tp_ API key |
| Client | TypeScript `client.temperaBio` · Python `client.tempera_bio` · Rust `build_request("tempera_bio", ...)` |
| Typed operations | 20 |

Anything this page does not cover is reachable through the raw passthrough:
`client.temperaBio.request(path, options)` (TypeScript) /
`client.tempera_bio.request(path, ...)` (Python).

## Operations

### deriveCampaignState

`POST /v1/campaignStates:derive`

Derive campaign state.

- **Producer operationId:** `deriveCampaignState`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:decision:write`
- **Call as:** TypeScript `client.temperaBio.deriveCampaignState()` · Python `client.tempera_bio.derive_campaign_state()` · Rust `build_request("tempera_bio", "derive_campaign_state", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `candidateSet` | yes | — |
| `hypothesis` | yes | — |
| `program` | yes | — |
| `rounds` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.deriveCampaignState({
  candidateSet: "<candidateSet>",
  hypothesis: "<hypothesis>",
  program: "<program>",
  rounds: "<rounds>",
});
```

```python Python
result = client.tempera_bio.derive_campaign_state({
    "candidateSet": "<candidateSet>",
    "hypothesis": "<hypothesis>",
    "program": "<program>",
    "rounds": "<rounds>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "derive_campaign_state", &[
    ("candidateSet", "<candidateSet>".into()),
    ("hypothesis", "<hypothesis>".into()),
    ("program", "<program>".into()),
    ("rounds", "<rounds>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### prepareCandidateSet

`POST /v1/candidateSets:prepare`

Prepare candidate set.

- **Producer operationId:** `prepareCandidateSet`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:proposal:write`
- **Call as:** TypeScript `client.temperaBio.prepareCandidateSet()` · Python `client.tempera_bio.prepare_candidate_set()` · Rust `build_request("tempera_bio", "prepare_candidate_set", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `candidates` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.prepareCandidateSet({
  candidates: "<candidates>",
});
```

```python Python
result = client.tempera_bio.prepare_candidate_set({
    "candidates": "<candidates>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "prepare_candidate_set", &[
    ("candidates", "<candidates>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### prepareComputationalExperimentPlan

`POST /v1/computationalExperimentPlans:prepare`

Prepare computational experiment plan.

- **Producer operationId:** `prepareComputationalExperimentPlan`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:proposal:write`
- **Call as:** TypeScript `client.temperaBio.prepareComputationalExperimentPlan()` · Python `client.tempera_bio.prepare_computational_experiment_plan()` · Rust `build_request("tempera_bio", "prepare_computational_experiment_plan", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `baselineManifestDigest` | yes | — |
| `claimCeiling` | yes | — |
| `discipline` | yes | — |
| `domainTask` | yes | — |
| `execution` | yes | — |
| `executionClass` | yes | — |
| `experimentId` | yes | — |
| `hiddenTargetCommitmentDigest` | yes | — |
| `inputManifestDigest` | yes | — |
| `metricDefinitionDigest` | yes | — |
| `schemaVersion` | yes | — |
| `sourceSnapshots` | yes | — |
| `splitDefinitionDigest` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.prepareComputationalExperimentPlan({
  baselineManifestDigest: "<baselineManifestDigest>",
  claimCeiling: "<claimCeiling>",
  discipline: "<discipline>",
  domainTask: "<domainTask>",
  execution: "<execution>",
  executionClass: "<executionClass>",
  experimentId: "<experimentId>",
  hiddenTargetCommitmentDigest: "<hiddenTargetCommitmentDigest>",
  inputManifestDigest: "<inputManifestDigest>",
  metricDefinitionDigest: "<metricDefinitionDigest>",
  schemaVersion: "<schemaVersion>",
  sourceSnapshots: "<sourceSnapshots>",
  splitDefinitionDigest: "<splitDefinitionDigest>",
});
```

```python Python
result = client.tempera_bio.prepare_computational_experiment_plan({
    "baselineManifestDigest": "<baselineManifestDigest>",
    "claimCeiling": "<claimCeiling>",
    "discipline": "<discipline>",
    "domainTask": "<domainTask>",
    "execution": "<execution>",
    "executionClass": "<executionClass>",
    "experimentId": "<experimentId>",
    "hiddenTargetCommitmentDigest": "<hiddenTargetCommitmentDigest>",
    "inputManifestDigest": "<inputManifestDigest>",
    "metricDefinitionDigest": "<metricDefinitionDigest>",
    "schemaVersion": "<schemaVersion>",
    "sourceSnapshots": "<sourceSnapshots>",
    "splitDefinitionDigest": "<splitDefinitionDigest>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "prepare_computational_experiment_plan", &[
    ("baselineManifestDigest", "<baselineManifestDigest>".into()),
    ("claimCeiling", "<claimCeiling>".into()),
    ("discipline", "<discipline>".into()),
    ("domainTask", "<domainTask>".into()),
    ("execution", "<execution>".into()),
    ("executionClass", "<executionClass>".into()),
    ("experimentId", "<experimentId>".into()),
    ("hiddenTargetCommitmentDigest", "<hiddenTargetCommitmentDigest>".into()),
    ("inputManifestDigest", "<inputManifestDigest>".into()),
    ("metricDefinitionDigest", "<metricDefinitionDigest>".into()),
    ("schemaVersion", "<schemaVersion>".into()),
    ("sourceSnapshots", "<sourceSnapshots>".into()),
    ("splitDefinitionDigest", "<splitDefinitionDigest>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### prepareDatasetReleaseManifest

`POST /v1/datasetReleaseManifests:prepare`

Prepare dataset release manifest.

- **Producer operationId:** `prepareDatasetReleaseManifest`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:proposal:write`
- **Call as:** TypeScript `client.temperaBio.prepareDatasetReleaseManifest()` · Python `client.tempera_bio.prepare_dataset_release_manifest()` · Rust `build_request("tempera_bio", "prepare_dataset_release_manifest", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `decision` | yes | — |
| `program` | yes | — |
| `verifierReceipts` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.prepareDatasetReleaseManifest({
  decision: "<decision>",
  program: "<program>",
  verifierReceipts: "<verifierReceipts>",
});
```

```python Python
result = client.tempera_bio.prepare_dataset_release_manifest({
    "decision": "<decision>",
    "program": "<program>",
    "verifierReceipts": "<verifierReceipts>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "prepare_dataset_release_manifest", &[
    ("decision", "<decision>".into()),
    ("program", "<program>".into()),
    ("verifierReceipts", "<verifierReceipts>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### deriveDecision

`POST /v1/decisions:derive`

Derive decision.

- **Producer operationId:** `deriveDecision`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:decision:write`
- **Call as:** TypeScript `client.temperaBio.deriveDecision()` · Python `client.tempera_bio.derive_decision()` · Rust `build_request("tempera_bio", "derive_decision", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `candidateSet` | yes | — |
| `experimentProposal` | yes | — |
| `hypothesis` | yes | — |
| `program` | yes | — |
| `verifierReceipts` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.deriveDecision({
  candidateSet: "<candidateSet>",
  experimentProposal: "<experimentProposal>",
  hypothesis: "<hypothesis>",
  program: "<program>",
  verifierReceipts: "<verifierReceipts>",
});
```

```python Python
result = client.tempera_bio.derive_decision({
    "candidateSet": "<candidateSet>",
    "experimentProposal": "<experimentProposal>",
    "hypothesis": "<hypothesis>",
    "program": "<program>",
    "verifierReceipts": "<verifierReceipts>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "derive_decision", &[
    ("candidateSet", "<candidateSet>".into()),
    ("experimentProposal", "<experimentProposal>".into()),
    ("hypothesis", "<hypothesis>".into()),
    ("program", "<program>".into()),
    ("verifierReceipts", "<verifierReceipts>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### verifyDomainMaterializationSubmission

`POST /v1/domainMaterializations:verifySubmission`

Verify domain materialization submission.

- **Producer operationId:** `verifyDomainMaterializationSubmission`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:measurement:verify`
- **Call as:** TypeScript `client.temperaBio.verifyDomainMaterializationSubmission()` · Python `client.tempera_bio.verify_domain_materialization_submission()` · Rust `build_request("tempera_bio", "verify_domain_materialization_submission", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `materializationRequest` | yes | — |
| `rawSubmissionReceiptBase64` | yes | — |
| `submissionIdentitySignature` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.verifyDomainMaterializationSubmission({
  materializationRequest: "<materializationRequest>",
  rawSubmissionReceiptBase64: "<rawSubmissionReceiptBase64>",
  submissionIdentitySignature: "<submissionIdentitySignature>",
});
```

```python Python
result = client.tempera_bio.verify_domain_materialization_submission({
    "materializationRequest": "<materializationRequest>",
    "rawSubmissionReceiptBase64": "<rawSubmissionReceiptBase64>",
    "submissionIdentitySignature": "<submissionIdentitySignature>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "verify_domain_materialization_submission", &[
    ("materializationRequest", "<materializationRequest>".into()),
    ("rawSubmissionReceiptBase64", "<rawSubmissionReceiptBase64>".into()),
    ("submissionIdentitySignature", "<submissionIdentitySignature>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### prepareExperimentProposal

`POST /v1/experimentProposals:prepare`

Prepare experiment proposal.

- **Producer operationId:** `prepareExperimentProposal`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:proposal:write`
- **Call as:** TypeScript `client.temperaBio.prepareExperimentProposal()` · Python `client.tempera_bio.prepare_experiment_proposal()` · Rust `build_request("tempera_bio", "prepare_experiment_proposal", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `candidateSet` | yes | — |
| `program` | yes | — |
| `selectedCandidates` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.prepareExperimentProposal({
  candidateSet: "<candidateSet>",
  program: "<program>",
  selectedCandidates: "<selectedCandidates>",
});
```

```python Python
result = client.tempera_bio.prepare_experiment_proposal({
    "candidateSet": "<candidateSet>",
    "program": "<program>",
    "selectedCandidates": "<selectedCandidates>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "prepare_experiment_proposal", &[
    ("candidateSet", "<candidateSet>".into()),
    ("program", "<program>".into()),
    ("selectedCandidates", "<selectedCandidates>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### prepareExperimentProposalFromGymBatch

`POST /v1/experimentProposals:prepareFromGymBatch`

Prepare experiment proposal from gym batch.

- **Producer operationId:** `prepareExperimentProposalFromGymBatch`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:proposal:write`
- **Call as:** TypeScript `client.temperaBio.prepareExperimentProposalFromGymBatch()` · Python `client.tempera_bio.prepare_experiment_proposal_from_gym_batch()` · Rust `build_request("tempera_bio", "prepare_experiment_proposal_from_gym_batch", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `batchProposal` | yes | — |
| `campaignContext` | yes | — |
| `candidateSet` | yes | — |
| `program` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.prepareExperimentProposalFromGymBatch({
  batchProposal: "<batchProposal>",
  campaignContext: "<campaignContext>",
  candidateSet: "<candidateSet>",
  program: "<program>",
});
```

```python Python
result = client.tempera_bio.prepare_experiment_proposal_from_gym_batch({
    "batchProposal": "<batchProposal>",
    "campaignContext": "<campaignContext>",
    "candidateSet": "<candidateSet>",
    "program": "<program>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "prepare_experiment_proposal_from_gym_batch", &[
    ("batchProposal", "<batchProposal>".into()),
    ("campaignContext", "<campaignContext>".into()),
    ("candidateSet", "<candidateSet>".into()),
    ("program", "<program>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### prepareGraphProjection

`POST /v1/graphProjections:prepare`

Prepare graph projection.

- **Producer operationId:** `prepareGraphProjection`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:graph:project`
- **Auth availability:** Blocked on central scope registration; see [Authentication](/authentication#known-scope-gaps).
- **Call as:** TypeScript `client.temperaBio.prepareGraphProjection()` · Python `client.tempera_bio.prepare_graph_projection()` · Rust `build_request("tempera_bio", "prepare_graph_projection", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `asOf` | yes | — |
| `knownAt` | yes | — |
| `scientificClaim` | yes | — |
| `snapshotDigest` | yes | — |
| `sourceSnapshots` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.prepareGraphProjection({
  asOf: "<asOf>",
  knownAt: "<knownAt>",
  scientificClaim: "<scientificClaim>",
  snapshotDigest: "<snapshotDigest>",
  sourceSnapshots: "<sourceSnapshots>",
});
```

```python Python
result = client.tempera_bio.prepare_graph_projection({
    "asOf": "<asOf>",
    "knownAt": "<knownAt>",
    "scientificClaim": "<scientificClaim>",
    "snapshotDigest": "<snapshotDigest>",
    "sourceSnapshots": "<sourceSnapshots>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "prepare_graph_projection", &[
    ("asOf", "<asOf>".into()),
    ("knownAt", "<knownAt>".into()),
    ("scientificClaim", "<scientificClaim>".into()),
    ("snapshotDigest", "<snapshotDigest>".into()),
    ("sourceSnapshots", "<sourceSnapshots>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### prepareHypothesis

`POST /v1/hypotheses:prepare`

Prepare hypothesis.

- **Producer operationId:** `prepareHypothesis`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:proposal:write`
- **Call as:** TypeScript `client.temperaBio.prepareHypothesis()` · Python `client.tempera_bio.prepare_hypothesis()` · Rust `build_request("tempera_bio", "prepare_hypothesis", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `hypothesis` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.prepareHypothesis({
  hypothesis: "<hypothesis>",
});
```

```python Python
result = client.tempera_bio.prepare_hypothesis({
    "hypothesis": "<hypothesis>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "prepare_hypothesis", &[
    ("hypothesis", "<hypothesis>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### verifyMeasurement

`POST /v1/measurements:verify`

Verify measurement.

- **Producer operationId:** `verifyMeasurement`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:measurement:verify`
- **Call as:** TypeScript `client.temperaBio.verifyMeasurement()` · Python `client.tempera_bio.verify_measurement()` · Rust `build_request("tempera_bio", "verify_measurement", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `candidate` | yes | — |
| `experimentProposal` | yes | — |
| `hypothesis` | yes | — |
| `identitySignature` | yes | — |
| `program` | yes | — |
| `rawMeasurementBase64` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.verifyMeasurement({
  candidate: "<candidate>",
  experimentProposal: "<experimentProposal>",
  hypothesis: "<hypothesis>",
  identitySignature: "<identitySignature>",
  program: "<program>",
  rawMeasurementBase64: "<rawMeasurementBase64>",
});
```

```python Python
result = client.tempera_bio.verify_measurement({
    "candidate": "<candidate>",
    "experimentProposal": "<experimentProposal>",
    "hypothesis": "<hypothesis>",
    "identitySignature": "<identitySignature>",
    "program": "<program>",
    "rawMeasurementBase64": "<rawMeasurementBase64>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "verify_measurement", &[
    ("candidate", "<candidate>".into()),
    ("experimentProposal", "<experimentProposal>".into()),
    ("hypothesis", "<hypothesis>".into()),
    ("identitySignature", "<identitySignature>".into()),
    ("program", "<program>".into()),
    ("rawMeasurementBase64", "<rawMeasurementBase64>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### prepareNeuralResponseTask

`POST /v1/neuralResponseTasks:prepare`

Prepare neural response task.

- **Producer operationId:** `prepareNeuralResponseTask`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:proposal:write`
- **Call as:** TypeScript `client.temperaBio.prepareNeuralResponseTask()` · Python `client.tempera_bio.prepare_neural_response_task()` · Rust `build_request("tempera_bio", "prepare_neural_response_task", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `featureManifestDigest` | yes | — |
| `groupKeys` | yes | — |
| `hiddenResponseCommitmentDigest` | yes | — |
| `nwbSemanticsDigest` | yes | — |
| `physicalAction` | yes | — |
| `schemaVersion` | yes | — |
| `sourceSnapshotDigest` | yes | — |
| `stimulusProtocolDigest` | yes | — |
| `taskBoundary` | yes | — |
| `taskId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.prepareNeuralResponseTask({
  featureManifestDigest: "<featureManifestDigest>",
  groupKeys: "<groupKeys>",
  hiddenResponseCommitmentDigest: "<hiddenResponseCommitmentDigest>",
  nwbSemanticsDigest: "<nwbSemanticsDigest>",
  physicalAction: "<physicalAction>",
  schemaVersion: "<schemaVersion>",
  sourceSnapshotDigest: "<sourceSnapshotDigest>",
  stimulusProtocolDigest: "<stimulusProtocolDigest>",
  taskBoundary: "<taskBoundary>",
  taskId: "<taskId>",
});
```

```python Python
result = client.tempera_bio.prepare_neural_response_task({
    "featureManifestDigest": "<featureManifestDigest>",
    "groupKeys": "<groupKeys>",
    "hiddenResponseCommitmentDigest": "<hiddenResponseCommitmentDigest>",
    "nwbSemanticsDigest": "<nwbSemanticsDigest>",
    "physicalAction": "<physicalAction>",
    "schemaVersion": "<schemaVersion>",
    "sourceSnapshotDigest": "<sourceSnapshotDigest>",
    "stimulusProtocolDigest": "<stimulusProtocolDigest>",
    "taskBoundary": "<taskBoundary>",
    "taskId": "<taskId>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "prepare_neural_response_task", &[
    ("featureManifestDigest", "<featureManifestDigest>".into()),
    ("groupKeys", "<groupKeys>".into()),
    ("hiddenResponseCommitmentDigest", "<hiddenResponseCommitmentDigest>".into()),
    ("nwbSemanticsDigest", "<nwbSemanticsDigest>".into()),
    ("physicalAction", "<physicalAction>".into()),
    ("schemaVersion", "<schemaVersion>".into()),
    ("sourceSnapshotDigest", "<sourceSnapshotDigest>".into()),
    ("stimulusProtocolDigest", "<stimulusProtocolDigest>".into()),
    ("taskBoundary", "<taskBoundary>".into()),
    ("taskId", "<taskId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### prepareProgram

`POST /v1/programs:prepare`

Prepare program.

- **Producer operationId:** `prepareProgram`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:proposal:write`
- **Call as:** TypeScript `client.temperaBio.prepareProgram()` · Python `client.tempera_bio.prepare_program()` · Rust `build_request("tempera_bio", "prepare_program", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `candidateSet` | yes | — |
| `configuration` | yes | — |
| `hypothesis` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.prepareProgram({
  candidateSet: "<candidateSet>",
  configuration: "<configuration>",
  hypothesis: "<hypothesis>",
});
```

```python Python
result = client.tempera_bio.prepare_program({
    "candidateSet": "<candidateSet>",
    "configuration": "<configuration>",
    "hypothesis": "<hypothesis>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "prepare_program", &[
    ("candidateSet", "<candidateSet>".into()),
    ("configuration", "<configuration>".into()),
    ("hypothesis", "<hypothesis>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### prepareProspectiveExperimentProtocol

`POST /v1/prospectiveExperimentProtocols:prepare`

Prepare prospective experiment protocol.

- **Producer operationId:** `prepareProspectiveExperimentProtocol`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:proposal:write`
- **Call as:** TypeScript `client.temperaBio.prepareProspectiveExperimentProtocol()` · Python `client.tempera_bio.prepare_prospective_experiment_protocol()` · Rust `build_request("tempera_bio", "prepare_prospective_experiment_protocol", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `configuration` | yes | — |
| `experimentProposal` | yes | — |
| `program` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.prepareProspectiveExperimentProtocol({
  configuration: "<configuration>",
  experimentProposal: "<experimentProposal>",
  program: "<program>",
});
```

```python Python
result = client.tempera_bio.prepare_prospective_experiment_protocol({
    "configuration": "<configuration>",
    "experimentProposal": "<experimentProposal>",
    "program": "<program>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "prepare_prospective_experiment_protocol", &[
    ("configuration", "<configuration>".into()),
    ("experimentProposal", "<experimentProposal>".into()),
    ("program", "<program>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### verifyProspectiveMeasurement

`POST /v1/prospectiveMeasurements:verify`

Verify prospective measurement.

- **Producer operationId:** `verifyProspectiveMeasurement`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:measurement:verify`
- **Call as:** TypeScript `client.temperaBio.verifyProspectiveMeasurement()` · Python `client.tempera_bio.verify_prospective_measurement()` · Rust `build_request("tempera_bio", "verify_prospective_measurement", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `candidate` | yes | — |
| `experimentProposal` | yes | — |
| `hypothesis` | yes | — |
| `measurementIdentitySignature` | yes | — |
| `program` | yes | — |
| `prospectiveProtocol` | yes | — |
| `providerResultEnvelope` | yes | — |
| `providerResultIdentitySignature` | yes | — |
| `rawChainOfCustodyBase64` | yes | — |
| `rawMeasurementBase64` | yes | — |
| `rawSubmissionReceiptBase64` | yes | — |
| `submissionIdentitySignature` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.verifyProspectiveMeasurement({
  candidate: "<candidate>",
  experimentProposal: "<experimentProposal>",
  hypothesis: "<hypothesis>",
  measurementIdentitySignature: "<measurementIdentitySignature>",
  program: "<program>",
  prospectiveProtocol: "<prospectiveProtocol>",
  providerResultEnvelope: "<providerResultEnvelope>",
  providerResultIdentitySignature: "<providerResultIdentitySignature>",
  rawChainOfCustodyBase64: "<rawChainOfCustodyBase64>",
  rawMeasurementBase64: "<rawMeasurementBase64>",
  rawSubmissionReceiptBase64: "<rawSubmissionReceiptBase64>",
  submissionIdentitySignature: "<submissionIdentitySignature>",
});
```

```python Python
result = client.tempera_bio.verify_prospective_measurement({
    "candidate": "<candidate>",
    "experimentProposal": "<experimentProposal>",
    "hypothesis": "<hypothesis>",
    "measurementIdentitySignature": "<measurementIdentitySignature>",
    "program": "<program>",
    "prospectiveProtocol": "<prospectiveProtocol>",
    "providerResultEnvelope": "<providerResultEnvelope>",
    "providerResultIdentitySignature": "<providerResultIdentitySignature>",
    "rawChainOfCustodyBase64": "<rawChainOfCustodyBase64>",
    "rawMeasurementBase64": "<rawMeasurementBase64>",
    "rawSubmissionReceiptBase64": "<rawSubmissionReceiptBase64>",
    "submissionIdentitySignature": "<submissionIdentitySignature>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "verify_prospective_measurement", &[
    ("candidate", "<candidate>".into()),
    ("experimentProposal", "<experimentProposal>".into()),
    ("hypothesis", "<hypothesis>".into()),
    ("measurementIdentitySignature", "<measurementIdentitySignature>".into()),
    ("program", "<program>".into()),
    ("prospectiveProtocol", "<prospectiveProtocol>".into()),
    ("providerResultEnvelope", "<providerResultEnvelope>".into()),
    ("providerResultIdentitySignature", "<providerResultIdentitySignature>".into()),
    ("rawChainOfCustodyBase64", "<rawChainOfCustodyBase64>".into()),
    ("rawMeasurementBase64", "<rawMeasurementBase64>".into()),
    ("rawSubmissionReceiptBase64", "<rawSubmissionReceiptBase64>".into()),
    ("submissionIdentitySignature", "<submissionIdentitySignature>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### prepareProteinVariantTask

`POST /v1/proteinVariantTasks:prepare`

Prepare protein variant task.

- **Producer operationId:** `prepareProteinVariantTask`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:proposal:write`
- **Call as:** TypeScript `client.temperaBio.prepareProteinVariantTask()` · Python `client.tempera_bio.prepare_protein_variant_task()` · Rust `build_request("tempera_bio", "prepare_protein_variant_task", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `assayContextDigest` | yes | — |
| `candidateManifestDigest` | yes | — |
| `physicalAction` | yes | — |
| `schemaVersion` | yes | — |
| `sequenceManifestDigest` | yes | — |
| `sourceSnapshotDigest` | yes | — |
| `splitDefinitionDigest` | yes | — |
| `taskBoundary` | yes | — |
| `taskId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.prepareProteinVariantTask({
  assayContextDigest: "<assayContextDigest>",
  candidateManifestDigest: "<candidateManifestDigest>",
  physicalAction: "<physicalAction>",
  schemaVersion: "<schemaVersion>",
  sequenceManifestDigest: "<sequenceManifestDigest>",
  sourceSnapshotDigest: "<sourceSnapshotDigest>",
  splitDefinitionDigest: "<splitDefinitionDigest>",
  taskBoundary: "<taskBoundary>",
  taskId: "<taskId>",
});
```

```python Python
result = client.tempera_bio.prepare_protein_variant_task({
    "assayContextDigest": "<assayContextDigest>",
    "candidateManifestDigest": "<candidateManifestDigest>",
    "physicalAction": "<physicalAction>",
    "schemaVersion": "<schemaVersion>",
    "sequenceManifestDigest": "<sequenceManifestDigest>",
    "sourceSnapshotDigest": "<sourceSnapshotDigest>",
    "splitDefinitionDigest": "<splitDefinitionDigest>",
    "taskBoundary": "<taskBoundary>",
    "taskId": "<taskId>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "prepare_protein_variant_task", &[
    ("assayContextDigest", "<assayContextDigest>".into()),
    ("candidateManifestDigest", "<candidateManifestDigest>".into()),
    ("physicalAction", "<physicalAction>".into()),
    ("schemaVersion", "<schemaVersion>".into()),
    ("sequenceManifestDigest", "<sequenceManifestDigest>".into()),
    ("sourceSnapshotDigest", "<sourceSnapshotDigest>".into()),
    ("splitDefinitionDigest", "<splitDefinitionDigest>".into()),
    ("taskBoundary", "<taskBoundary>".into()),
    ("taskId", "<taskId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### prepareScientificClaim

`POST /v1/scientificClaims:prepare`

Prepare scientific claim.

- **Producer operationId:** `prepareScientificClaim`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:claim:prepare`
- **Auth availability:** Blocked on central scope registration; see [Authentication](/authentication#known-scope-gaps).
- **Call as:** TypeScript `client.temperaBio.prepareScientificClaim()` · Python `client.tempera_bio.prepare_scientific_claim()` · Rust `build_request("tempera_bio", "prepare_scientific_claim", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `scientificClaim` | yes | — |
| `sourceSnapshots` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.prepareScientificClaim({
  scientificClaim: "<scientificClaim>",
  sourceSnapshots: "<sourceSnapshots>",
});
```

```python Python
result = client.tempera_bio.prepare_scientific_claim({
    "scientificClaim": "<scientificClaim>",
    "sourceSnapshots": "<sourceSnapshots>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "prepare_scientific_claim", &[
    ("scientificClaim", "<scientificClaim>".into()),
    ("sourceSnapshots", "<sourceSnapshots>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### ingestMaveDBScoreSet

`POST /v1/sources/maveDbScoreSets:ingest`

Ingest mave d b score set.

- **Producer operationId:** `ingestMaveDBScoreSet`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:source:read`
- **Call as:** TypeScript `client.temperaBio.ingestMaveDBScoreSet()` · Python `client.tempera_bio.ingest_mave_db_score_set()` · Rust `build_request("tempera_bio", "ingest_mave_db_score_set", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `scoreSetUrn` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.ingestMaveDBScoreSet({
  scoreSetUrn: "<scoreSetUrn>",
});
```

```python Python
result = client.tempera_bio.ingest_mave_db_score_set({
    "scoreSetUrn": "<scoreSetUrn>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "ingest_mave_db_score_set", &[
    ("scoreSetUrn", "<scoreSetUrn>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### prepareStoichiometricExperiment

`POST /v1/stoichiometricExperiments:prepare`

Prepare stoichiometric experiment.

- **Producer operationId:** `prepareStoichiometricExperiment`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:proposal:write`
- **Call as:** TypeScript `client.temperaBio.prepareStoichiometricExperiment()` · Python `client.tempera_bio.prepare_stoichiometric_experiment()` · Rust `build_request("tempera_bio", "prepare_stoichiometric_experiment", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `bounds` | yes | — |
| `claimBoundary` | yes | — |
| `entries` | yes | — |
| `experimentId` | yes | — |
| `metaboliteIds` | yes | — |
| `physicalAction` | yes | — |
| `reactionIds` | yes | — |
| `schemaVersion` | yes | — |
| `sourceSnapshotDigest` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.prepareStoichiometricExperiment({
  bounds: "<bounds>",
  claimBoundary: "<claimBoundary>",
  entries: "<entries>",
  experimentId: "<experimentId>",
  metaboliteIds: "<metaboliteIds>",
  physicalAction: "<physicalAction>",
  reactionIds: "<reactionIds>",
  schemaVersion: "<schemaVersion>",
  sourceSnapshotDigest: "<sourceSnapshotDigest>",
});
```

```python Python
result = client.tempera_bio.prepare_stoichiometric_experiment({
    "bounds": "<bounds>",
    "claimBoundary": "<claimBoundary>",
    "entries": "<entries>",
    "experimentId": "<experimentId>",
    "metaboliteIds": "<metaboliteIds>",
    "physicalAction": "<physicalAction>",
    "reactionIds": "<reactionIds>",
    "schemaVersion": "<schemaVersion>",
    "sourceSnapshotDigest": "<sourceSnapshotDigest>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "prepare_stoichiometric_experiment", &[
    ("bounds", "<bounds>".into()),
    ("claimBoundary", "<claimBoundary>".into()),
    ("entries", "<entries>".into()),
    ("experimentId", "<experimentId>".into()),
    ("metaboliteIds", "<metaboliteIds>".into()),
    ("physicalAction", "<physicalAction>".into()),
    ("reactionIds", "<reactionIds>".into()),
    ("schemaVersion", "<schemaVersion>".into()),
    ("sourceSnapshotDigest", "<sourceSnapshotDigest>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### prepareStoichiometricFeasibilityReceipt

`POST /v1/stoichiometricFeasibilityReceipts:prepare`

Prepare stoichiometric feasibility receipt.

- **Producer operationId:** `prepareStoichiometricFeasibilityReceipt`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-bio`.
- **Scope:** `bio:measurement:verify`
- **Call as:** TypeScript `client.temperaBio.prepareStoichiometricFeasibilityReceipt()` · Python `client.tempera_bio.prepare_stoichiometric_feasibility_receipt()` · Rust `build_request("tempera_bio", "prepare_stoichiometric_feasibility_receipt", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `experiment` | yes | — |
| `flux` | yes | — |
| `schemaVersion` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaBio.prepareStoichiometricFeasibilityReceipt({
  experiment: "<experiment>",
  flux: "<flux>",
  schemaVersion: "<schemaVersion>",
});
```

```python Python
result = client.tempera_bio.prepare_stoichiometric_feasibility_receipt({
    "experiment": "<experiment>",
    "flux": "<flux>",
    "schemaVersion": "<schemaVersion>",
})
```

```rust Rust
let spec = client.build_request("tempera_bio", "prepare_stoichiometric_feasibility_receipt", &[
    ("experiment", "<experiment>".into()),
    ("flux", "<flux>".into()),
    ("schemaVersion", "<schemaVersion>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>
