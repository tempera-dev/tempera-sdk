---
title: "tempera-payments"
description: "Provider-neutral fiat checkout and blockchain settlement platform with source-locked producer operations."
---

{/* Generated by scripts/gen-sdk-docs.py from surface.json -- do not hand-edit. */}

> **Private access.** Hosted Tempera services are available through design-partner onboarding.
> Use the issuer, credentials, environment, and product URLs provisioned for your workspace;
> start in staging unless Tempera explicitly approves another target.

> **Contract inventory.** This typed reference is retained for SDK compatibility.
> It is not part of the primary public workflow or evidence of general service availability.

Provider-neutral fiat checkout and blockchain settlement platform with source-locked producer operations.

| | |
|---|---|
| Repository | [tempera-payments](https://github.com/tempera-dev/tempera-payments) |
| Base URL env var | `TEMPERA_PAYMENTS_URL` (see [Environments](/environments)) |
| Audience | `tempera-payments` |
| Auth | bearer minted for audience tempera-payments with the operation's payments scope, or a central tp_ API key |
| Client | TypeScript `client.temperaPayments` · Python `client.tempera_payments` · Rust `build_request("tempera_payments", ...)` |
| Typed operations | 47 |

Anything this page does not cover is reachable through the raw passthrough:
`client.temperaPayments.request(path, options)` (TypeScript) /
`client.tempera_payments.request(path, ...)` (Python).

## Operations

### getPaymentsHealth

`GET /healthz`

Report that the process is reachable.

- **Producer operationId:** `getPaymentsHealth`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.temperaPayments.getPaymentsHealth()` · Python `client.tempera_payments.get_payments_health()` · Rust `build_request("tempera_payments", "get_payments_health", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.getPaymentsHealth();
```

```python Python
result = client.tempera_payments.get_payments_health()
```

```rust Rust
let spec = client.build_request("tempera_payments", "get_payments_health", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getPaymentsReadiness

`GET /readyz`

Report durable-storage readiness and which rails this process is configured for.

- **Producer operationId:** `getPaymentsReadiness`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.temperaPayments.getPaymentsReadiness()` · Python `client.tempera_payments.get_payments_readiness()` · Rust `build_request("tempera_payments", "get_payments_readiness", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.getPaymentsReadiness();
```

```python Python
result = client.tempera_payments.get_payments_readiness()
```

```rust Rust
let spec = client.build_request("tempera_payments", "get_payments_readiness", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createPaymentIntent

`POST /v1/paymentIntents`

Create a canonical payment intent.

- **Producer operationId:** `createPaymentIntent`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:intents:write`
- **Call as:** TypeScript `client.temperaPayments.createPaymentIntent()` · Python `client.tempera_payments.create_payment_intent()` · Rust `build_request("tempera_payments", "create_payment_intent", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |
| `merchantId` | yes | — |
| `riskSubjectId` | yes | — |
| `recipient` | yes | — |
| `asset` | yes | — |
| `amount` | yes | — |
| `expiresInSeconds` | yes | — |
| `reference` | no | — |
| `customerId` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.createPaymentIntent({
  tenantId: "<tenantId>",
  merchantId: "<merchantId>",
  riskSubjectId: "<riskSubjectId>",
  recipient: "<recipient>",
  asset: "<asset>",
  amount: "<amount>",
  expiresInSeconds: "<expiresInSeconds>",
});
```

```python Python
result = client.tempera_payments.create_payment_intent({
    "tenantId": "<tenantId>",
    "merchantId": "<merchantId>",
    "riskSubjectId": "<riskSubjectId>",
    "recipient": "<recipient>",
    "asset": "<asset>",
    "amount": "<amount>",
    "expiresInSeconds": "<expiresInSeconds>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "create_payment_intent", &[
    ("tenantId", "<tenantId>".into()),
    ("merchantId", "<merchantId>".into()),
    ("riskSubjectId", "<riskSubjectId>".into()),
    ("recipient", "<recipient>".into()),
    ("asset", "<asset>".into()),
    ("amount", "<amount>".into()),
    ("expiresInSeconds", "<expiresInSeconds>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listPaymentIntents

`GET /v1/paymentIntents`

List a tenant's payment intents by consumer reference, status, or creation window.

- **Producer operationId:** `listPaymentIntents`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:intents:read`
- **Call as:** TypeScript `client.temperaPayments.listPaymentIntents()` · Python `client.tempera_payments.list_payment_intents()` · Rust `build_request("tempera_payments", "list_payment_intents", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |
| `referenceKind` | no | — |
| `referenceId` | no | — |
| `referenceConsumer` | no | — |
| `status` | no | — |
| `createdAfter` | no | — |
| `createdBefore` | no | — |
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.listPaymentIntents({
  tenantId: "<tenantId>",
  referenceKind: "<referenceKind>",
});
```

```python Python
result = client.tempera_payments.list_payment_intents({
    "tenantId": "<tenantId>",
    "referenceKind": "<referenceKind>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "list_payment_intents", &[
    ("tenantId", "<tenantId>".into()),
    ("referenceKind", "<referenceKind>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getPaymentIntent

`GET /v1/paymentIntents/{paymentIntentId}`

Read the canonical payment-intent projection.

- **Producer operationId:** `getPaymentIntent`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:intents:read`
- **Call as:** TypeScript `client.temperaPayments.getPaymentIntent()` · Python `client.tempera_payments.get_payment_intent()` · Rust `build_request("tempera_payments", "get_payment_intent", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `paymentIntentId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.getPaymentIntent({
  paymentIntentId: "<paymentIntentId>",
  tenantId: "<tenantId>",
});
```

```python Python
result = client.tempera_payments.get_payment_intent({
    "paymentIntentId": "<paymentIntentId>",
    "tenantId": "<tenantId>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "get_payment_intent", &[
    ("paymentIntentId", "<paymentIntentId>".into()),
    ("tenantId", "<tenantId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getPaymentSettlementReceipt

`GET /v1/paymentIntents/{paymentIntentId}/receipt`

Read and cryptographically re-verify the canonical settlement receipt.

- **Producer operationId:** `getPaymentSettlementReceipt`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:receipts:read`
- **Call as:** TypeScript `client.temperaPayments.getPaymentSettlementReceipt()` · Python `client.tempera_payments.get_payment_settlement_receipt()` · Rust `build_request("tempera_payments", "get_payment_settlement_receipt", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `paymentIntentId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.getPaymentSettlementReceipt({
  paymentIntentId: "<paymentIntentId>",
  tenantId: "<tenantId>",
});
```

```python Python
result = client.tempera_payments.get_payment_settlement_receipt({
    "paymentIntentId": "<paymentIntentId>",
    "tenantId": "<tenantId>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "get_payment_settlement_receipt", &[
    ("paymentIntentId", "<paymentIntentId>".into()),
    ("tenantId", "<tenantId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createStripeCheckout

`POST /v1/paymentIntents/{paymentIntentId}/stripeCheckout`

Create an idempotent hosted Stripe Checkout session for a fiat payment intent.

- **Producer operationId:** `createStripeCheckout`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Physical action:** Yes. This operation may cause an external real-world action.
- **Scope:** `payments:intents:write`
- **Call as:** TypeScript `client.temperaPayments.createStripeCheckout()` · Python `client.tempera_payments.create_stripe_checkout()` · Rust `build_request("tempera_payments", "create_stripe_checkout", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `paymentIntentId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |
| `currency` | yes | — |
| `productName` | yes | — |
| `successUrl` | yes | — |
| `cancelUrl` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.createStripeCheckout({
  paymentIntentId: "<paymentIntentId>",
  tenantId: "<tenantId>",
  currency: "<currency>",
  productName: "<productName>",
  successUrl: "<successUrl>",
  cancelUrl: "<cancelUrl>",
});
```

```python Python
result = client.tempera_payments.create_stripe_checkout({
    "paymentIntentId": "<paymentIntentId>",
    "tenantId": "<tenantId>",
    "currency": "<currency>",
    "productName": "<productName>",
    "successUrl": "<successUrl>",
    "cancelUrl": "<cancelUrl>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "create_stripe_checkout", &[
    ("paymentIntentId", "<paymentIntentId>".into()),
    ("tenantId", "<tenantId>".into()),
    ("currency", "<currency>".into()),
    ("productName", "<productName>".into()),
    ("successUrl", "<successUrl>".into()),
    ("cancelUrl", "<cancelUrl>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createCardSession

`POST /v1/paymentIntents/{paymentIntentId}/cardSession`

Create a hosted/tokenizing card session with the selected configured acquirer.

- **Producer operationId:** `createCardSession`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Physical action:** Yes. This operation may cause an external real-world action.
- **Scope:** `payments:intents:write`
- **Call as:** TypeScript `client.temperaPayments.createCardSession()` · Python `client.tempera_payments.create_card_session()` · Rust `build_request("tempera_payments", "create_card_session", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `paymentIntentId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |
| `acquirer` | yes | — |
| `successUrl` | yes | — |
| `failureUrl` | yes | — |
| `billing` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.createCardSession({
  paymentIntentId: "<paymentIntentId>",
  tenantId: "<tenantId>",
  acquirer: "<acquirer>",
  successUrl: "<successUrl>",
  failureUrl: "<failureUrl>",
  billing: "<billing>",
});
```

```python Python
result = client.tempera_payments.create_card_session({
    "paymentIntentId": "<paymentIntentId>",
    "tenantId": "<tenantId>",
    "acquirer": "<acquirer>",
    "successUrl": "<successUrl>",
    "failureUrl": "<failureUrl>",
    "billing": "<billing>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "create_card_session", &[
    ("paymentIntentId", "<paymentIntentId>".into()),
    ("tenantId", "<tenantId>".into()),
    ("acquirer", "<acquirer>".into()),
    ("successUrl", "<successUrl>".into()),
    ("failureUrl", "<failureUrl>".into()),
    ("billing", "<billing>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getMerchantWorkspace

`GET /v1/merchants/workspace`

Recover the centrally authenticated workspace and optional merchant.

- **Producer operationId:** `getMerchantWorkspace`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:merchants:read`
- **Call as:** TypeScript `client.temperaPayments.getMerchantWorkspace()` · Python `client.tempera_payments.get_merchant_workspace()` · Rust `build_request("tempera_payments", "get_merchant_workspace", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.getMerchantWorkspace();
```

```python Python
result = client.tempera_payments.get_merchant_workspace()
```

```rust Rust
let spec = client.build_request("tempera_payments", "get_merchant_workspace", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getWorkspaceMerchant

`GET /v1/merchants`

Read the merchant onboarding projection for one workspace.

- **Producer operationId:** `getWorkspaceMerchant`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:merchants:read`
- **Call as:** TypeScript `client.temperaPayments.getWorkspaceMerchant()` · Python `client.tempera_payments.get_workspace_merchant()` · Rust `build_request("tempera_payments", "get_workspace_merchant", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.getWorkspaceMerchant({
  tenantId: "<tenantId>",
});
```

```python Python
result = client.tempera_payments.get_workspace_merchant({
    "tenantId": "<tenantId>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "get_workspace_merchant", &[
    ("tenantId", "<tenantId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createMerchant

`POST /v1/merchants`

Create the single US/USD merchant record for a workspace.

- **Producer operationId:** `createMerchant`
- **Auth:** Human OAuth bearer for audience `tempera-payments`; `payments:merchants:write` requires an owner or admin. Central `tp_` API keys cannot use this operation.
- **Scope:** `payments:merchants:write`
- **Idempotency:** Required. Persist and send an `Idempotency-Key` for the same request on recovery; do not generate a replacement while the outcome is unknown.
- **Call as:** TypeScript `client.temperaPayments.createMerchant()` · Python `client.tempera_payments.create_merchant()` · Rust `build_request("tempera_payments", "create_merchant", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |
| `country` | yes | — |
| `currency` | yes | — |
| `category` | yes | — |
| `platformFeeBps` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.createMerchant({
  tenantId: "<tenantId>",
  country: "<country>",
  currency: "<currency>",
  category: "<category>",
}, { headers: { "Idempotency-Key": persistedKey } });
```

```python Python
result = client.tempera_payments.create_merchant({
    "tenantId": "<tenantId>",
    "country": "<country>",
    "currency": "<currency>",
    "category": "<category>",
}, headers={"Idempotency-Key": persisted_key})
```

```rust Rust
let mut spec = client.build_request("tempera_payments", "create_merchant", &[
    ("tenantId", "<tenantId>".into()),
    ("country", "<country>".into()),
    ("currency", "<currency>".into()),
    ("category", "<category>".into()),
])?;
spec.headers.push(("Idempotency-Key".into(), persisted_key.into()));
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getMerchant

`GET /v1/merchants/{merchantId}`

Read a merchant onboarding projection.

- **Producer operationId:** `getMerchant`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:merchants:read`
- **Call as:** TypeScript `client.temperaPayments.getMerchant()` · Python `client.tempera_payments.get_merchant()` · Rust `build_request("tempera_payments", "get_merchant", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `merchantId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.getMerchant({
  merchantId: "<merchantId>",
  tenantId: "<tenantId>",
});
```

```python Python
result = client.tempera_payments.get_merchant({
    "merchantId": "<merchantId>",
    "tenantId": "<tenantId>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "get_merchant", &[
    ("merchantId", "<merchantId>".into()),
    ("tenantId", "<tenantId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### refreshMerchantEligibility

`POST /v1/merchants/{merchantId}/refresh`

Retrieve and store a fresh merchant eligibility observation.

- **Producer operationId:** `refreshMerchantEligibility`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:merchants:read`
- **Call as:** TypeScript `client.temperaPayments.refreshMerchantEligibility()` · Python `client.tempera_payments.refresh_merchant_eligibility()` · Rust `build_request("tempera_payments", "refresh_merchant_eligibility", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `merchantId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.refreshMerchantEligibility({
  merchantId: "<merchantId>",
  tenantId: "<tenantId>",
});
```

```python Python
result = client.tempera_payments.refresh_merchant_eligibility({
    "merchantId": "<merchantId>",
    "tenantId": "<tenantId>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "refresh_merchant_eligibility", &[
    ("merchantId", "<merchantId>".into()),
    ("tenantId", "<tenantId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createMerchantOnboardingLink

`POST /v1/merchants/{merchantId}/onboarding`

Create a short-lived Stripe-hosted merchant onboarding link.

- **Producer operationId:** `createMerchantOnboardingLink`
- **Auth:** Human OAuth bearer for audience `tempera-payments`; `payments:merchants:write` requires an owner or admin. Central `tp_` API keys cannot use this operation.
- **Physical action:** Yes. This operation may cause an external real-world action.
- **Scope:** `payments:merchants:write`
- **Idempotency:** Required. Persist and send an `Idempotency-Key` for the same request on recovery; do not generate a replacement while the outcome is unknown.
- **Call as:** TypeScript `client.temperaPayments.createMerchantOnboardingLink()` · Python `client.tempera_payments.create_merchant_onboarding_link()` · Rust `build_request("tempera_payments", "create_merchant_onboarding_link", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `merchantId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.createMerchantOnboardingLink({
  merchantId: "<merchantId>",
  tenantId: "<tenantId>",
}, { headers: { "Idempotency-Key": persistedKey } });
```

```python Python
result = client.tempera_payments.create_merchant_onboarding_link({
    "merchantId": "<merchantId>",
    "tenantId": "<tenantId>",
}, headers={"Idempotency-Key": persisted_key})
```

```rust Rust
let mut spec = client.build_request("tempera_payments", "create_merchant_onboarding_link", &[
    ("merchantId", "<merchantId>".into()),
    ("tenantId", "<tenantId>".into()),
])?;
spec.headers.push(("Idempotency-Key".into(), persisted_key.into()));
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### receiveStripeWebhook

`POST /v1/webhooks/stripe/callback`

Verify and durably deduplicate a Stripe webhook using its raw body.

- **Producer operationId:** `receiveStripeWebhook`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.temperaPayments.receiveStripeWebhook()` · Python `client.tempera_payments.receive_stripe_webhook()` · Rust `build_request("tempera_payments", "receive_stripe_webhook", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.receiveStripeWebhook();
```

```python Python
result = client.tempera_payments.receive_stripe_webhook()
```

```rust Rust
let spec = client.build_request("tempera_payments", "receive_stripe_webhook", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createRefund

`POST /v1/paymentIntents/{paymentIntentId}/refunds`

Refund a settled payment intent.

- **Producer operationId:** `createRefund`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:refunds:write`
- **Call as:** TypeScript `client.temperaPayments.createRefund()` · Python `client.tempera_payments.create_refund()` · Rust `build_request("tempera_payments", "create_refund", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `paymentIntentId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |
| `idempotencyKey` | yes | — |
| `amount` | no | — |
| `reason` | no | — |
| `metadata` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.createRefund({
  paymentIntentId: "<paymentIntentId>",
  tenantId: "<tenantId>",
  idempotencyKey: "<idempotencyKey>",
  amount: "<amount>",
});
```

```python Python
result = client.tempera_payments.create_refund({
    "paymentIntentId": "<paymentIntentId>",
    "tenantId": "<tenantId>",
    "idempotencyKey": "<idempotencyKey>",
    "amount": "<amount>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "create_refund", &[
    ("paymentIntentId", "<paymentIntentId>".into()),
    ("tenantId", "<tenantId>".into()),
    ("idempotencyKey", "<idempotencyKey>".into()),
    ("amount", "<amount>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listRefunds

`GET /v1/paymentIntents/{paymentIntentId}/refunds`

List the refunds of one payment intent.

- **Producer operationId:** `listRefunds`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:refunds:read`
- **Auth availability:** Blocked on central scope registration; see [Authentication](/authentication#known-scope-gaps).
- **Call as:** TypeScript `client.temperaPayments.listRefunds()` · Python `client.tempera_payments.list_refunds()` · Rust `build_request("tempera_payments", "list_refunds", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `paymentIntentId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.listRefunds({
  paymentIntentId: "<paymentIntentId>",
  tenantId: "<tenantId>",
  pageSize: "<pageSize>",
});
```

```python Python
result = client.tempera_payments.list_refunds({
    "paymentIntentId": "<paymentIntentId>",
    "tenantId": "<tenantId>",
    "pageSize": "<pageSize>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "list_refunds", &[
    ("paymentIntentId", "<paymentIntentId>".into()),
    ("tenantId", "<tenantId>".into()),
    ("pageSize", "<pageSize>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getRefund

`GET /v1/paymentIntents/{paymentIntentId}/refunds/{refundId}`

Read one refund.

- **Producer operationId:** `getRefund`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:refunds:read`
- **Auth availability:** Blocked on central scope registration; see [Authentication](/authentication#known-scope-gaps).
- **Call as:** TypeScript `client.temperaPayments.getRefund()` · Python `client.tempera_payments.get_refund()` · Rust `build_request("tempera_payments", "get_refund", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `paymentIntentId` | yes | Substituted into the URL path (percent-encoded). |
| `refundId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.getRefund({
  paymentIntentId: "<paymentIntentId>",
  refundId: "<refundId>",
  tenantId: "<tenantId>",
});
```

```python Python
result = client.tempera_payments.get_refund({
    "paymentIntentId": "<paymentIntentId>",
    "refundId": "<refundId>",
    "tenantId": "<tenantId>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "get_refund", &[
    ("paymentIntentId", "<paymentIntentId>".into()),
    ("refundId", "<refundId>".into()),
    ("tenantId", "<tenantId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createPayPalCheckout

`POST /v1/paymentIntents/{paymentIntentId}/paypalCheckout`

Create an idempotent hosted PayPal order for a fiat payment intent.

- **Producer operationId:** `createPayPalCheckout`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Physical action:** Yes. This operation may cause an external real-world action.
- **Scope:** `payments:intents:write`
- **Call as:** TypeScript `client.temperaPayments.createPayPalCheckout()` · Python `client.tempera_payments.create_pay_pal_checkout()` · Rust `build_request("tempera_payments", "create_pay_pal_checkout", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `paymentIntentId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |
| `returnUrl` | yes | — |
| `cancelUrl` | yes | — |
| `idempotencyKey` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.createPayPalCheckout({
  paymentIntentId: "<paymentIntentId>",
  tenantId: "<tenantId>",
  returnUrl: "<returnUrl>",
  cancelUrl: "<cancelUrl>",
  idempotencyKey: "<idempotencyKey>",
});
```

```python Python
result = client.tempera_payments.create_pay_pal_checkout({
    "paymentIntentId": "<paymentIntentId>",
    "tenantId": "<tenantId>",
    "returnUrl": "<returnUrl>",
    "cancelUrl": "<cancelUrl>",
    "idempotencyKey": "<idempotencyKey>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "create_pay_pal_checkout", &[
    ("paymentIntentId", "<paymentIntentId>".into()),
    ("tenantId", "<tenantId>".into()),
    ("returnUrl", "<returnUrl>".into()),
    ("cancelUrl", "<cancelUrl>".into()),
    ("idempotencyKey", "<idempotencyKey>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### receivePayPalWebhook

`POST /v1/webhooks/paypal/callback`

Verify and durably deduplicate a PayPal webhook using its raw body.

- **Producer operationId:** `receivePayPalWebhook`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.temperaPayments.receivePayPalWebhook()` · Python `client.tempera_payments.receive_pay_pal_webhook()` · Rust `build_request("tempera_payments", "receive_pay_pal_webhook", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.receivePayPalWebhook();
```

```python Python
result = client.tempera_payments.receive_pay_pal_webhook()
```

```rust Rust
let spec = client.build_request("tempera_payments", "receive_pay_pal_webhook", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createCoinbaseCheckout

`POST /v1/paymentIntents/{paymentIntentId}/coinbaseCheckout`

Create an idempotent hosted Coinbase Commerce charge for a fiat payment intent.

- **Producer operationId:** `createCoinbaseCheckout`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Physical action:** Yes. This operation may cause an external real-world action.
- **Scope:** `payments:intents:write`
- **Call as:** TypeScript `client.temperaPayments.createCoinbaseCheckout()` · Python `client.tempera_payments.create_coinbase_checkout()` · Rust `build_request("tempera_payments", "create_coinbase_checkout", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `paymentIntentId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |
| `redirectUrl` | yes | — |
| `cancelUrl` | yes | — |
| `idempotencyKey` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.createCoinbaseCheckout({
  paymentIntentId: "<paymentIntentId>",
  tenantId: "<tenantId>",
  redirectUrl: "<redirectUrl>",
  cancelUrl: "<cancelUrl>",
  idempotencyKey: "<idempotencyKey>",
});
```

```python Python
result = client.tempera_payments.create_coinbase_checkout({
    "paymentIntentId": "<paymentIntentId>",
    "tenantId": "<tenantId>",
    "redirectUrl": "<redirectUrl>",
    "cancelUrl": "<cancelUrl>",
    "idempotencyKey": "<idempotencyKey>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "create_coinbase_checkout", &[
    ("paymentIntentId", "<paymentIntentId>".into()),
    ("tenantId", "<tenantId>".into()),
    ("redirectUrl", "<redirectUrl>".into()),
    ("cancelUrl", "<cancelUrl>".into()),
    ("idempotencyKey", "<idempotencyKey>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### receiveCoinbaseWebhook

`POST /v1/webhooks/coinbase/callback`

Verify and durably deduplicate a Coinbase Commerce webhook using its raw body.

- **Producer operationId:** `receiveCoinbaseWebhook`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.temperaPayments.receiveCoinbaseWebhook()` · Python `client.tempera_payments.receive_coinbase_webhook()` · Rust `build_request("tempera_payments", "receive_coinbase_webhook", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.receiveCoinbaseWebhook();
```

```python Python
result = client.tempera_payments.receive_coinbase_webhook()
```

```rust Rust
let spec = client.build_request("tempera_payments", "receive_coinbase_webhook", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createWebhookEndpoint

`POST /v1/webhookEndpoints`

Register a destination for signed payment events.

- **Producer operationId:** `createWebhookEndpoint`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:webhooks:write`
- **Call as:** TypeScript `client.temperaPayments.createWebhookEndpoint()` · Python `client.tempera_payments.create_webhook_endpoint()` · Rust `build_request("tempera_payments", "create_webhook_endpoint", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |
| `url` | yes | — |
| `eventTypes` | yes | — |
| `description` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.createWebhookEndpoint({
  tenantId: "<tenantId>",
  url: "<url>",
  eventTypes: "<eventTypes>",
});
```

```python Python
result = client.tempera_payments.create_webhook_endpoint({
    "tenantId": "<tenantId>",
    "url": "<url>",
    "eventTypes": "<eventTypes>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "create_webhook_endpoint", &[
    ("tenantId", "<tenantId>".into()),
    ("url", "<url>".into()),
    ("eventTypes", "<eventTypes>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listWebhookEndpoints

`GET /v1/webhookEndpoints`

List the webhook endpoints of one workspace.

- **Producer operationId:** `listWebhookEndpoints`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:webhooks:read`
- **Auth availability:** Blocked on central scope registration; see [Authentication](/authentication#known-scope-gaps).
- **Call as:** TypeScript `client.temperaPayments.listWebhookEndpoints()` · Python `client.tempera_payments.list_webhook_endpoints()` · Rust `build_request("tempera_payments", "list_webhook_endpoints", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.listWebhookEndpoints({
  tenantId: "<tenantId>",
  pageSize: "<pageSize>",
});
```

```python Python
result = client.tempera_payments.list_webhook_endpoints({
    "tenantId": "<tenantId>",
    "pageSize": "<pageSize>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "list_webhook_endpoints", &[
    ("tenantId", "<tenantId>".into()),
    ("pageSize", "<pageSize>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getWebhookEndpoint

`GET /v1/webhookEndpoints/{webhookEndpointId}`

Read one webhook endpoint.

- **Producer operationId:** `getWebhookEndpoint`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:webhooks:read`
- **Auth availability:** Blocked on central scope registration; see [Authentication](/authentication#known-scope-gaps).
- **Call as:** TypeScript `client.temperaPayments.getWebhookEndpoint()` · Python `client.tempera_payments.get_webhook_endpoint()` · Rust `build_request("tempera_payments", "get_webhook_endpoint", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `webhookEndpointId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.getWebhookEndpoint({
  webhookEndpointId: "<webhookEndpointId>",
  tenantId: "<tenantId>",
});
```

```python Python
result = client.tempera_payments.get_webhook_endpoint({
    "webhookEndpointId": "<webhookEndpointId>",
    "tenantId": "<tenantId>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "get_webhook_endpoint", &[
    ("webhookEndpointId", "<webhookEndpointId>".into()),
    ("tenantId", "<tenantId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### deleteWebhookEndpoint

`DELETE /v1/webhookEndpoints/{webhookEndpointId}`

Stop delivering to a webhook endpoint.

- **Producer operationId:** `deleteWebhookEndpoint`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:webhooks:write`
- **Call as:** TypeScript `client.temperaPayments.deleteWebhookEndpoint()` · Python `client.tempera_payments.delete_webhook_endpoint()` · Rust `build_request("tempera_payments", "delete_webhook_endpoint", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `webhookEndpointId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.deleteWebhookEndpoint({
  webhookEndpointId: "<webhookEndpointId>",
  tenantId: "<tenantId>",
});
```

```python Python
result = client.tempera_payments.delete_webhook_endpoint({
    "webhookEndpointId": "<webhookEndpointId>",
    "tenantId": "<tenantId>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "delete_webhook_endpoint", &[
    ("webhookEndpointId", "<webhookEndpointId>".into()),
    ("tenantId", "<tenantId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listWebhookDeliveries

`GET /v1/webhookEndpoints/{webhookEndpointId}/deliveries`

List delivery attempts for one webhook endpoint.

- **Producer operationId:** `listWebhookDeliveries`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:webhooks:read`
- **Auth availability:** Blocked on central scope registration; see [Authentication](/authentication#known-scope-gaps).
- **Call as:** TypeScript `client.temperaPayments.listWebhookDeliveries()` · Python `client.tempera_payments.list_webhook_deliveries()` · Rust `build_request("tempera_payments", "list_webhook_deliveries", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `webhookEndpointId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.listWebhookDeliveries({
  webhookEndpointId: "<webhookEndpointId>",
  tenantId: "<tenantId>",
  pageSize: "<pageSize>",
});
```

```python Python
result = client.tempera_payments.list_webhook_deliveries({
    "webhookEndpointId": "<webhookEndpointId>",
    "tenantId": "<tenantId>",
    "pageSize": "<pageSize>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "list_webhook_deliveries", &[
    ("webhookEndpointId", "<webhookEndpointId>".into()),
    ("tenantId", "<tenantId>".into()),
    ("pageSize", "<pageSize>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createPaymentMethodCharge

`POST /v1/paymentIntents/{paymentIntentId}/paymentMethodCharges`

Charge a saved payment method off-session for an authorized payment intent.

- **Producer operationId:** `createPaymentMethodCharge`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Physical action:** Yes. This operation may cause an external real-world action.
- **Scope:** `payments:intents:write`
- **Call as:** TypeScript `client.temperaPayments.createPaymentMethodCharge()` · Python `client.tempera_payments.create_payment_method_charge()` · Rust `build_request("tempera_payments", "create_payment_method_charge", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `paymentIntentId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |
| `paymentMethodId` | yes | — |
| `idempotencyKey` | yes | — |
| `offSession` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.createPaymentMethodCharge({
  paymentIntentId: "<paymentIntentId>",
  tenantId: "<tenantId>",
  paymentMethodId: "<paymentMethodId>",
  idempotencyKey: "<idempotencyKey>",
});
```

```python Python
result = client.tempera_payments.create_payment_method_charge({
    "paymentIntentId": "<paymentIntentId>",
    "tenantId": "<tenantId>",
    "paymentMethodId": "<paymentMethodId>",
    "idempotencyKey": "<idempotencyKey>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "create_payment_method_charge", &[
    ("paymentIntentId", "<paymentIntentId>".into()),
    ("tenantId", "<tenantId>".into()),
    ("paymentMethodId", "<paymentMethodId>".into()),
    ("idempotencyKey", "<idempotencyKey>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createCustomer

`POST /v1/customers`

Create a customer for one workspace.

- **Producer operationId:** `createCustomer`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:customers:write`
- **Auth availability:** Blocked on central scope registration; see [Authentication](/authentication#known-scope-gaps).
- **Call as:** TypeScript `client.temperaPayments.createCustomer()` · Python `client.tempera_payments.create_customer()` · Rust `build_request("tempera_payments", "create_customer", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |
| `externalId` | no | — |
| `email` | no | — |
| `name` | no | — |
| `metadata` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.createCustomer({
  tenantId: "<tenantId>",
  externalId: "<externalId>",
  email: "<email>",
});
```

```python Python
result = client.tempera_payments.create_customer({
    "tenantId": "<tenantId>",
    "externalId": "<externalId>",
    "email": "<email>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "create_customer", &[
    ("tenantId", "<tenantId>".into()),
    ("externalId", "<externalId>".into()),
    ("email", "<email>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listCustomers

`GET /v1/customers`

List customers of one workspace by an exact externalId or email.

- **Producer operationId:** `listCustomers`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:customers:read`
- **Auth availability:** Blocked on central scope registration; see [Authentication](/authentication#known-scope-gaps).
- **Call as:** TypeScript `client.temperaPayments.listCustomers()` · Python `client.tempera_payments.list_customers()` · Rust `build_request("tempera_payments", "list_customers", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |
| `externalId` | no | — |
| `email` | no | — |
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.listCustomers({
  tenantId: "<tenantId>",
  externalId: "<externalId>",
});
```

```python Python
result = client.tempera_payments.list_customers({
    "tenantId": "<tenantId>",
    "externalId": "<externalId>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "list_customers", &[
    ("tenantId", "<tenantId>".into()),
    ("externalId", "<externalId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getCustomer

`GET /v1/customers/{customerId}`

Read one customer.

- **Producer operationId:** `getCustomer`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:customers:read`
- **Auth availability:** Blocked on central scope registration; see [Authentication](/authentication#known-scope-gaps).
- **Call as:** TypeScript `client.temperaPayments.getCustomer()` · Python `client.tempera_payments.get_customer()` · Rust `build_request("tempera_payments", "get_customer", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `customerId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.getCustomer({
  customerId: "<customerId>",
  tenantId: "<tenantId>",
});
```

```python Python
result = client.tempera_payments.get_customer({
    "customerId": "<customerId>",
    "tenantId": "<tenantId>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "get_customer", &[
    ("customerId", "<customerId>".into()),
    ("tenantId", "<tenantId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createSetupSession

`POST /v1/customers/{customerId}/setupSessions`

Create a provider-hosted session that saves a card for this customer.

- **Producer operationId:** `createSetupSession`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Physical action:** Yes. This operation may cause an external real-world action.
- **Scope:** `payments:customers:write`
- **Auth availability:** Blocked on central scope registration; see [Authentication](/authentication#known-scope-gaps).
- **Call as:** TypeScript `client.temperaPayments.createSetupSession()` · Python `client.tempera_payments.create_setup_session()` · Rust `build_request("tempera_payments", "create_setup_session", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `customerId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |
| `successUrl` | yes | — |
| `cancelUrl` | yes | — |
| `idempotencyKey` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.createSetupSession({
  customerId: "<customerId>",
  tenantId: "<tenantId>",
  successUrl: "<successUrl>",
  cancelUrl: "<cancelUrl>",
  idempotencyKey: "<idempotencyKey>",
});
```

```python Python
result = client.tempera_payments.create_setup_session({
    "customerId": "<customerId>",
    "tenantId": "<tenantId>",
    "successUrl": "<successUrl>",
    "cancelUrl": "<cancelUrl>",
    "idempotencyKey": "<idempotencyKey>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "create_setup_session", &[
    ("customerId", "<customerId>".into()),
    ("tenantId", "<tenantId>".into()),
    ("successUrl", "<successUrl>".into()),
    ("cancelUrl", "<cancelUrl>".into()),
    ("idempotencyKey", "<idempotencyKey>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listPaymentMethods

`GET /v1/customers/{customerId}/paymentMethods`

List the payment methods saved for one customer.

- **Producer operationId:** `listPaymentMethods`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:customers:read`
- **Auth availability:** Blocked on central scope registration; see [Authentication](/authentication#known-scope-gaps).
- **Call as:** TypeScript `client.temperaPayments.listPaymentMethods()` · Python `client.tempera_payments.list_payment_methods()` · Rust `build_request("tempera_payments", "list_payment_methods", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `customerId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.listPaymentMethods({
  customerId: "<customerId>",
  tenantId: "<tenantId>",
  pageSize: "<pageSize>",
});
```

```python Python
result = client.tempera_payments.list_payment_methods({
    "customerId": "<customerId>",
    "tenantId": "<tenantId>",
    "pageSize": "<pageSize>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "list_payment_methods", &[
    ("customerId", "<customerId>".into()),
    ("tenantId", "<tenantId>".into()),
    ("pageSize", "<pageSize>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### deletePaymentMethod

`DELETE /v1/customers/{customerId}/paymentMethods/{paymentMethodId}`

Detach a saved payment method.

- **Producer operationId:** `deletePaymentMethod`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Physical action:** Yes. This operation may cause an external real-world action.
- **Scope:** `payments:customers:write`
- **Auth availability:** Blocked on central scope registration; see [Authentication](/authentication#known-scope-gaps).
- **Call as:** TypeScript `client.temperaPayments.deletePaymentMethod()` · Python `client.tempera_payments.delete_payment_method()` · Rust `build_request("tempera_payments", "delete_payment_method", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `customerId` | yes | Substituted into the URL path (percent-encoded). |
| `paymentMethodId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.deletePaymentMethod({
  customerId: "<customerId>",
  paymentMethodId: "<paymentMethodId>",
  tenantId: "<tenantId>",
});
```

```python Python
result = client.tempera_payments.delete_payment_method({
    "customerId": "<customerId>",
    "paymentMethodId": "<paymentMethodId>",
    "tenantId": "<tenantId>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "delete_payment_method", &[
    ("customerId", "<customerId>".into()),
    ("paymentMethodId", "<paymentMethodId>".into()),
    ("tenantId", "<tenantId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listPaymentIntentDisputes

`GET /v1/paymentIntents/{paymentIntentId}/disputes`

List the disputes of one payment intent.

- **Producer operationId:** `listPaymentIntentDisputes`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:disputes:read`
- **Auth availability:** Blocked on central scope registration; see [Authentication](/authentication#known-scope-gaps).
- **Call as:** TypeScript `client.temperaPayments.listPaymentIntentDisputes()` · Python `client.tempera_payments.list_payment_intent_disputes()` · Rust `build_request("tempera_payments", "list_payment_intent_disputes", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `paymentIntentId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.listPaymentIntentDisputes({
  paymentIntentId: "<paymentIntentId>",
  tenantId: "<tenantId>",
  pageSize: "<pageSize>",
});
```

```python Python
result = client.tempera_payments.list_payment_intent_disputes({
    "paymentIntentId": "<paymentIntentId>",
    "tenantId": "<tenantId>",
    "pageSize": "<pageSize>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "list_payment_intent_disputes", &[
    ("paymentIntentId", "<paymentIntentId>".into()),
    ("tenantId", "<tenantId>".into()),
    ("pageSize", "<pageSize>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listDisputes

`GET /v1/disputes`

List the disputes of one workspace.

- **Producer operationId:** `listDisputes`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:disputes:read`
- **Auth availability:** Blocked on central scope registration; see [Authentication](/authentication#known-scope-gaps).
- **Call as:** TypeScript `client.temperaPayments.listDisputes()` · Python `client.tempera_payments.list_disputes()` · Rust `build_request("tempera_payments", "list_disputes", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |
| `status` | no | — |
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.listDisputes({
  tenantId: "<tenantId>",
  status: "<status>",
});
```

```python Python
result = client.tempera_payments.list_disputes({
    "tenantId": "<tenantId>",
    "status": "<status>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "list_disputes", &[
    ("tenantId", "<tenantId>".into()),
    ("status", "<status>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getDispute

`GET /v1/disputes/{disputeId}`

Read one dispute.

- **Producer operationId:** `getDispute`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:disputes:read`
- **Auth availability:** Blocked on central scope registration; see [Authentication](/authentication#known-scope-gaps).
- **Call as:** TypeScript `client.temperaPayments.getDispute()` · Python `client.tempera_payments.get_dispute()` · Rust `build_request("tempera_payments", "get_dispute", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `disputeId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.getDispute({
  disputeId: "<disputeId>",
  tenantId: "<tenantId>",
});
```

```python Python
result = client.tempera_payments.get_dispute({
    "disputeId": "<disputeId>",
    "tenantId": "<tenantId>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "get_dispute", &[
    ("disputeId", "<disputeId>".into()),
    ("tenantId", "<tenantId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createSubscription

`POST /v1/subscriptions`

Create a recurring charge schedule against a saved payment method.

- **Producer operationId:** `createSubscription`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:subscriptions:write`
- **Auth availability:** Blocked on central scope registration; see [Authentication](/authentication#known-scope-gaps).
- **Call as:** TypeScript `client.temperaPayments.createSubscription()` · Python `client.tempera_payments.create_subscription()` · Rust `build_request("tempera_payments", "create_subscription", params)`

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |
| `customerId` | yes | — |
| `paymentMethodId` | yes | — |
| `merchantId` | yes | — |
| `riskSubjectId` | yes | — |
| `recipient` | yes | — |
| `amount` | yes | — |
| `asset` | yes | — |
| `expiresInSeconds` | yes | — |
| `interval` | yes | — |
| `startAt` | no | — |
| `consumer` | no | — |
| `metadata` | no | — |
| `idempotencyKey` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.createSubscription({
  tenantId: "<tenantId>",
  customerId: "<customerId>",
  paymentMethodId: "<paymentMethodId>",
  merchantId: "<merchantId>",
  riskSubjectId: "<riskSubjectId>",
  recipient: "<recipient>",
  amount: "<amount>",
  asset: "<asset>",
  expiresInSeconds: "<expiresInSeconds>",
  interval: "<interval>",
  idempotencyKey: "<idempotencyKey>",
});
```

```python Python
result = client.tempera_payments.create_subscription({
    "tenantId": "<tenantId>",
    "customerId": "<customerId>",
    "paymentMethodId": "<paymentMethodId>",
    "merchantId": "<merchantId>",
    "riskSubjectId": "<riskSubjectId>",
    "recipient": "<recipient>",
    "amount": "<amount>",
    "asset": "<asset>",
    "expiresInSeconds": "<expiresInSeconds>",
    "interval": "<interval>",
    "idempotencyKey": "<idempotencyKey>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "create_subscription", &[
    ("tenantId", "<tenantId>".into()),
    ("customerId", "<customerId>".into()),
    ("paymentMethodId", "<paymentMethodId>".into()),
    ("merchantId", "<merchantId>".into()),
    ("riskSubjectId", "<riskSubjectId>".into()),
    ("recipient", "<recipient>".into()),
    ("amount", "<amount>".into()),
    ("asset", "<asset>".into()),
    ("expiresInSeconds", "<expiresInSeconds>".into()),
    ("interval", "<interval>".into()),
    ("idempotencyKey", "<idempotencyKey>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listSubscriptions

`GET /v1/subscriptions`

List the subscriptions of one workspace.

- **Producer operationId:** `listSubscriptions`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:subscriptions:read`
- **Auth availability:** Blocked on central scope registration; see [Authentication](/authentication#known-scope-gaps).
- **Call as:** TypeScript `client.temperaPayments.listSubscriptions()` · Python `client.tempera_payments.list_subscriptions()` · Rust `build_request("tempera_payments", "list_subscriptions", params)`

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |
| `customerId` | no | — |
| `status` | no | — |
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.listSubscriptions({
  tenantId: "<tenantId>",
  customerId: "<customerId>",
});
```

```python Python
result = client.tempera_payments.list_subscriptions({
    "tenantId": "<tenantId>",
    "customerId": "<customerId>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "list_subscriptions", &[
    ("tenantId", "<tenantId>".into()),
    ("customerId", "<customerId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getSubscription

`GET /v1/subscriptions/{subscriptionId}`

Read one subscription.

- **Producer operationId:** `getSubscription`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:subscriptions:read`
- **Auth availability:** Blocked on central scope registration; see [Authentication](/authentication#known-scope-gaps).
- **Call as:** TypeScript `client.temperaPayments.getSubscription()` · Python `client.tempera_payments.get_subscription()` · Rust `build_request("tempera_payments", "get_subscription", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `subscriptionId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.getSubscription({
  subscriptionId: "<subscriptionId>",
  tenantId: "<tenantId>",
});
```

```python Python
result = client.tempera_payments.get_subscription({
    "subscriptionId": "<subscriptionId>",
    "tenantId": "<tenantId>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "get_subscription", &[
    ("subscriptionId", "<subscriptionId>".into()),
    ("tenantId", "<tenantId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### cancelSubscription

`POST /v1/subscriptions/{subscriptionId}/cancel`

Cancel a subscription.

- **Producer operationId:** `cancelSubscription`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:subscriptions:write`
- **Auth availability:** Blocked on central scope registration; see [Authentication](/authentication#known-scope-gaps).
- **Call as:** TypeScript `client.temperaPayments.cancelSubscription()` · Python `client.tempera_payments.cancel_subscription()` · Rust `build_request("tempera_payments", "cancel_subscription", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `subscriptionId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.cancelSubscription({
  subscriptionId: "<subscriptionId>",
  tenantId: "<tenantId>",
});
```

```python Python
result = client.tempera_payments.cancel_subscription({
    "subscriptionId": "<subscriptionId>",
    "tenantId": "<tenantId>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "cancel_subscription", &[
    ("subscriptionId", "<subscriptionId>".into()),
    ("tenantId", "<tenantId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### pauseSubscription

`POST /v1/subscriptions/{subscriptionId}/pause`

Pause a subscription.

- **Producer operationId:** `pauseSubscription`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:subscriptions:write`
- **Auth availability:** Blocked on central scope registration; see [Authentication](/authentication#known-scope-gaps).
- **Call as:** TypeScript `client.temperaPayments.pauseSubscription()` · Python `client.tempera_payments.pause_subscription()` · Rust `build_request("tempera_payments", "pause_subscription", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `subscriptionId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.pauseSubscription({
  subscriptionId: "<subscriptionId>",
  tenantId: "<tenantId>",
});
```

```python Python
result = client.tempera_payments.pause_subscription({
    "subscriptionId": "<subscriptionId>",
    "tenantId": "<tenantId>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "pause_subscription", &[
    ("subscriptionId", "<subscriptionId>".into()),
    ("tenantId", "<tenantId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### resumeSubscription

`POST /v1/subscriptions/{subscriptionId}/resume`

Resume a paused subscription.

- **Producer operationId:** `resumeSubscription`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:subscriptions:write`
- **Auth availability:** Blocked on central scope registration; see [Authentication](/authentication#known-scope-gaps).
- **Call as:** TypeScript `client.temperaPayments.resumeSubscription()` · Python `client.tempera_payments.resume_subscription()` · Rust `build_request("tempera_payments", "resume_subscription", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `subscriptionId` | yes | Substituted into the URL path (percent-encoded). |

**Body parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.resumeSubscription({
  subscriptionId: "<subscriptionId>",
  tenantId: "<tenantId>",
});
```

```python Python
result = client.tempera_payments.resume_subscription({
    "subscriptionId": "<subscriptionId>",
    "tenantId": "<tenantId>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "resume_subscription", &[
    ("subscriptionId", "<subscriptionId>".into()),
    ("tenantId", "<tenantId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listSubscriptionPeriods

`GET /v1/subscriptions/{subscriptionId}/periods`

List the billing periods of one subscription.

- **Producer operationId:** `listSubscriptionPeriods`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:subscriptions:read`
- **Auth availability:** Blocked on central scope registration; see [Authentication](/authentication#known-scope-gaps).
- **Call as:** TypeScript `client.temperaPayments.listSubscriptionPeriods()` · Python `client.tempera_payments.list_subscription_periods()` · Rust `build_request("tempera_payments", "list_subscription_periods", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `subscriptionId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.listSubscriptionPeriods({
  subscriptionId: "<subscriptionId>",
  tenantId: "<tenantId>",
  pageSize: "<pageSize>",
});
```

```python Python
result = client.tempera_payments.list_subscription_periods({
    "subscriptionId": "<subscriptionId>",
    "tenantId": "<tenantId>",
    "pageSize": "<pageSize>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "list_subscription_periods", &[
    ("subscriptionId", "<subscriptionId>".into()),
    ("tenantId", "<tenantId>".into()),
    ("pageSize", "<pageSize>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getMerchantBalance

`GET /v1/merchants/{merchantId}/balance`

Read one merchant's balance.

- **Producer operationId:** `getMerchantBalance`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:merchants:read`
- **Call as:** TypeScript `client.temperaPayments.getMerchantBalance()` · Python `client.tempera_payments.get_merchant_balance()` · Rust `build_request("tempera_payments", "get_merchant_balance", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `merchantId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.getMerchantBalance({
  merchantId: "<merchantId>",
  tenantId: "<tenantId>",
});
```

```python Python
result = client.tempera_payments.get_merchant_balance({
    "merchantId": "<merchantId>",
    "tenantId": "<tenantId>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "get_merchant_balance", &[
    ("merchantId", "<merchantId>".into()),
    ("tenantId", "<tenantId>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listMerchantPayouts

`GET /v1/merchants/{merchantId}/payouts`

List the payouts observed for one merchant.

- **Producer operationId:** `listMerchantPayouts`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-payments`.
- **Scope:** `payments:merchants:read`
- **Call as:** TypeScript `client.temperaPayments.listMerchantPayouts()` · Python `client.tempera_payments.list_merchant_payouts()` · Rust `build_request("tempera_payments", "list_merchant_payouts", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `merchantId` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `tenantId` | yes | — |
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.listMerchantPayouts({
  merchantId: "<merchantId>",
  tenantId: "<tenantId>",
  pageSize: "<pageSize>",
});
```

```python Python
result = client.tempera_payments.list_merchant_payouts({
    "merchantId": "<merchantId>",
    "tenantId": "<tenantId>",
    "pageSize": "<pageSize>",
})
```

```rust Rust
let spec = client.build_request("tempera_payments", "list_merchant_payouts", &[
    ("merchantId", "<merchantId>".into()),
    ("tenantId", "<tenantId>".into()),
    ("pageSize", "<pageSize>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### receiveStripeConnectWebhook

`POST /v1/webhooks/stripe/connect/callback`

Verify a Stripe Connect account event and record an observed payout.

- **Producer operationId:** `receiveStripeConnectWebhook`
- **Auth:** None (public endpoint).
- **Call as:** TypeScript `client.temperaPayments.receiveStripeConnectWebhook()` · Python `client.tempera_payments.receive_stripe_connect_webhook()` · Rust `build_request("tempera_payments", "receive_stripe_connect_webhook", params)`

<CodeGroup>

```typescript TypeScript
const result = await client.temperaPayments.receiveStripeConnectWebhook();
```

```python Python
result = client.tempera_payments.receive_stripe_connect_webhook()
```

```rust Rust
let spec = client.build_request("tempera_payments", "receive_stripe_connect_webhook", &[])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>
