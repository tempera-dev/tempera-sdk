---
title: "tempera-investigations"
description: "Long-running investigations with profiles, agentic and deterministic runs, findings, reviews and a replayable dossier."
---

{/* Generated by scripts/gen-sdk-docs.py from surface.json -- do not hand-edit. */}

> **Private access.** Hosted Tempera services are available through design-partner onboarding.
> Use the issuer, credentials, environment, and product URLs provisioned for your workspace;
> start in staging unless Tempera explicitly approves another target.

> **Contract inventory.** This typed reference is retained for SDK compatibility.
> It is not part of the primary public workflow or evidence of general service availability.

Long-running investigations with profiles, agentic and deterministic runs, findings, reviews and a replayable dossier.

| | |
|---|---|
| Repository | [tempera-investigations](https://github.com/tempera-dev/tempera-investigations) |
| Base URL env var | `TEMPERA_INVESTIGATIONS_URL` (see [Environments](/environments)) |
| Audience | `tempera-investigations` |
| Auth | bearer minted for audience tempera-investigations with the operation's investigation:read/investigation:write/investigation:run/investigation:review scope |
| Client | TypeScript `client.temperaInvestigations` · Python `client.tempera_investigations` · Rust `build_request("tempera_investigations", ...)` |
| Typed operations | 24 |

Anything this page does not cover is reachable through the raw passthrough:
`client.temperaInvestigations.request(path, options)` (TypeScript) /
`client.tempera_investigations.request(path, ...)` (Python).

## Operations

### createInvestigationProfile

`POST /v1/projects/{project}/investigationProfiles`

Create investigation profile.

- **Producer operationId:** `createInvestigationProfile`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-investigations`.
- **Scope:** `investigation:write`
- **Call as:** TypeScript `client.temperaInvestigations.createInvestigationProfile()` · Python `client.tempera_investigations.create_investigation_profile()` · Rust `build_request("tempera_investigations", "create_investigation_profile", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaInvestigations.createInvestigationProfile({
  project: "<project>",
});
```

```python Python
result = client.tempera_investigations.create_investigation_profile({
    "project": "<project>",
})
```

```rust Rust
let spec = client.build_request("tempera_investigations", "create_investigation_profile", &[
    ("project", "<project>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listInvestigationProfiles

`GET /v1/projects/{project}/investigationProfiles`

List investigation profiles.

- **Producer operationId:** `listInvestigationProfiles`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-investigations`.
- **Scope:** `investigation:read`
- **Call as:** TypeScript `client.temperaInvestigations.listInvestigationProfiles()` · Python `client.tempera_investigations.list_investigation_profiles()` · Rust `build_request("tempera_investigations", "list_investigation_profiles", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaInvestigations.listInvestigationProfiles({
  project: "<project>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.tempera_investigations.list_investigation_profiles({
    "project": "<project>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("tempera_investigations", "list_investigation_profiles", &[
    ("project", "<project>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getInvestigationProfile

`GET /v1/projects/{project}/investigationProfiles/{profile}`

Get investigation profile.

- **Producer operationId:** `getInvestigationProfile`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-investigations`.
- **Scope:** `investigation:read`
- **Call as:** TypeScript `client.temperaInvestigations.getInvestigationProfile()` · Python `client.tempera_investigations.get_investigation_profile()` · Rust `build_request("tempera_investigations", "get_investigation_profile", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `profile` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaInvestigations.getInvestigationProfile({
  project: "<project>",
  profile: "<profile>",
});
```

```python Python
result = client.tempera_investigations.get_investigation_profile({
    "project": "<project>",
    "profile": "<profile>",
})
```

```rust Rust
let spec = client.build_request("tempera_investigations", "get_investigation_profile", &[
    ("project", "<project>".into()),
    ("profile", "<profile>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### updateInvestigationProfile

`PATCH /v1/projects/{project}/investigationProfiles/{profile}`

Update investigation profile.

- **Producer operationId:** `updateInvestigationProfile`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-investigations`.
- **Scope:** `investigation:write`
- **Call as:** TypeScript `client.temperaInvestigations.updateInvestigationProfile()` · Python `client.tempera_investigations.update_investigation_profile()` · Rust `build_request("tempera_investigations", "update_investigation_profile", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `profile` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `updateMask` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaInvestigations.updateInvestigationProfile({
  project: "<project>",
  profile: "<profile>",
});
```

```python Python
result = client.tempera_investigations.update_investigation_profile({
    "project": "<project>",
    "profile": "<profile>",
})
```

```rust Rust
let spec = client.build_request("tempera_investigations", "update_investigation_profile", &[
    ("project", "<project>".into()),
    ("profile", "<profile>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### deleteInvestigationProfile

`DELETE /v1/projects/{project}/investigationProfiles/{profile}`

Delete investigation profile.

- **Producer operationId:** `deleteInvestigationProfile`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-investigations`.
- **Scope:** `investigation:write`
- **Call as:** TypeScript `client.temperaInvestigations.deleteInvestigationProfile()` · Python `client.tempera_investigations.delete_investigation_profile()` · Rust `build_request("tempera_investigations", "delete_investigation_profile", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `profile` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `etag` | yes | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaInvestigations.deleteInvestigationProfile({
  project: "<project>",
  profile: "<profile>",
  etag: "<etag>",
});
```

```python Python
result = client.tempera_investigations.delete_investigation_profile({
    "project": "<project>",
    "profile": "<profile>",
    "etag": "<etag>",
})
```

```rust Rust
let spec = client.build_request("tempera_investigations", "delete_investigation_profile", &[
    ("project", "<project>".into()),
    ("profile", "<profile>".into()),
    ("etag", "<etag>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createInvestigation

`POST /v1/projects/{project}/investigations`

Create investigation.

- **Producer operationId:** `createInvestigation`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-investigations`.
- **Scope:** `investigation:write`
- **Call as:** TypeScript `client.temperaInvestigations.createInvestigation()` · Python `client.tempera_investigations.create_investigation()` · Rust `build_request("tempera_investigations", "create_investigation", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaInvestigations.createInvestigation({
  project: "<project>",
});
```

```python Python
result = client.tempera_investigations.create_investigation({
    "project": "<project>",
})
```

```rust Rust
let spec = client.build_request("tempera_investigations", "create_investigation", &[
    ("project", "<project>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listInvestigations

`GET /v1/projects/{project}/investigations`

List investigations.

- **Producer operationId:** `listInvestigations`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-investigations`.
- **Scope:** `investigation:read`
- **Call as:** TypeScript `client.temperaInvestigations.listInvestigations()` · Python `client.tempera_investigations.list_investigations()` · Rust `build_request("tempera_investigations", "list_investigations", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaInvestigations.listInvestigations({
  project: "<project>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.tempera_investigations.list_investigations({
    "project": "<project>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("tempera_investigations", "list_investigations", &[
    ("project", "<project>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getInvestigation

`GET /v1/projects/{project}/investigations/{investigation}`

Get investigation.

- **Producer operationId:** `getInvestigation`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-investigations`.
- **Scope:** `investigation:read`
- **Call as:** TypeScript `client.temperaInvestigations.getInvestigation()` · Python `client.tempera_investigations.get_investigation()` · Rust `build_request("tempera_investigations", "get_investigation", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `investigation` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaInvestigations.getInvestigation({
  project: "<project>",
  investigation: "<investigation>",
});
```

```python Python
result = client.tempera_investigations.get_investigation({
    "project": "<project>",
    "investigation": "<investigation>",
})
```

```rust Rust
let spec = client.build_request("tempera_investigations", "get_investigation", &[
    ("project", "<project>".into()),
    ("investigation", "<investigation>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### evaluateInvestigationDeterministically

`POST /v1/projects/{project}/investigations/{investigation}:evaluateDeterministically`

Run deterministic investigation.

- **Producer operationId:** `evaluateInvestigationDeterministically`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-investigations`.
- **Scope:** `investigation:run`
- **Call as:** TypeScript `client.temperaInvestigations.evaluateInvestigationDeterministically()` · Python `client.tempera_investigations.evaluate_investigation_deterministically()` · Rust `build_request("tempera_investigations", "evaluate_investigation_deterministically", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `investigation` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaInvestigations.evaluateInvestigationDeterministically({
  project: "<project>",
  investigation: "<investigation>",
});
```

```python Python
result = client.tempera_investigations.evaluate_investigation_deterministically({
    "project": "<project>",
    "investigation": "<investigation>",
})
```

```rust Rust
let spec = client.build_request("tempera_investigations", "evaluate_investigation_deterministically", &[
    ("project", "<project>".into()),
    ("investigation", "<investigation>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### runInvestigationAgentic

`POST /v1/projects/{project}/investigations/{investigation}:runAgentic`

Run agentic investigation.

- **Producer operationId:** `runInvestigationAgentic`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-investigations`.
- **Scope:** `investigation:run`
- **Call as:** TypeScript `client.temperaInvestigations.runInvestigationAgentic()` · Python `client.tempera_investigations.run_investigation_agentic()` · Rust `build_request("tempera_investigations", "run_investigation_agentic", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `investigation` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaInvestigations.runInvestigationAgentic({
  project: "<project>",
  investigation: "<investigation>",
});
```

```python Python
result = client.tempera_investigations.run_investigation_agentic({
    "project": "<project>",
    "investigation": "<investigation>",
})
```

```rust Rust
let spec = client.build_request("tempera_investigations", "run_investigation_agentic", &[
    ("project", "<project>".into()),
    ("investigation", "<investigation>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### runInvestigation

`POST /v1/projects/{project}/investigations/{investigation}:run`

Run hybrid investigation.

- **Producer operationId:** `runInvestigation`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-investigations`.
- **Scope:** `investigation:run`
- **Call as:** TypeScript `client.temperaInvestigations.runInvestigation()` · Python `client.tempera_investigations.run_investigation()` · Rust `build_request("tempera_investigations", "run_investigation", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `investigation` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaInvestigations.runInvestigation({
  project: "<project>",
  investigation: "<investigation>",
});
```

```python Python
result = client.tempera_investigations.run_investigation({
    "project": "<project>",
    "investigation": "<investigation>",
})
```

```rust Rust
let spec = client.build_request("tempera_investigations", "run_investigation", &[
    ("project", "<project>".into()),
    ("investigation", "<investigation>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### cancelInvestigation

`POST /v1/projects/{project}/investigations/{investigation}:cancel`

Cancel investigation.

- **Producer operationId:** `cancelInvestigation`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-investigations`.
- **Scope:** `investigation:write`
- **Call as:** TypeScript `client.temperaInvestigations.cancelInvestigation()` · Python `client.tempera_investigations.cancel_investigation()` · Rust `build_request("tempera_investigations", "cancel_investigation", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `investigation` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaInvestigations.cancelInvestigation({
  project: "<project>",
  investigation: "<investigation>",
});
```

```python Python
result = client.tempera_investigations.cancel_investigation({
    "project": "<project>",
    "investigation": "<investigation>",
})
```

```rust Rust
let spec = client.build_request("tempera_investigations", "cancel_investigation", &[
    ("project", "<project>".into()),
    ("investigation", "<investigation>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### replayInvestigation

`POST /v1/projects/{project}/investigations/{investigation}:replay`

Replay investigation.

- **Producer operationId:** `replayInvestigation`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-investigations`.
- **Scope:** `investigation:run`
- **Call as:** TypeScript `client.temperaInvestigations.replayInvestigation()` · Python `client.tempera_investigations.replay_investigation()` · Rust `build_request("tempera_investigations", "replay_investigation", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `investigation` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaInvestigations.replayInvestigation({
  project: "<project>",
  investigation: "<investigation>",
});
```

```python Python
result = client.tempera_investigations.replay_investigation({
    "project": "<project>",
    "investigation": "<investigation>",
})
```

```rust Rust
let spec = client.build_request("tempera_investigations", "replay_investigation", &[
    ("project", "<project>".into()),
    ("investigation", "<investigation>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listDeterministicEvaluations

`GET /v1/projects/{project}/investigations/{investigation}/deterministicEvaluations`

List deterministic evaluations.

- **Producer operationId:** `listDeterministicEvaluations`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-investigations`.
- **Scope:** `investigation:read`
- **Call as:** TypeScript `client.temperaInvestigations.listDeterministicEvaluations()` · Python `client.tempera_investigations.list_deterministic_evaluations()` · Rust `build_request("tempera_investigations", "list_deterministic_evaluations", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `investigation` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaInvestigations.listDeterministicEvaluations({
  project: "<project>",
  investigation: "<investigation>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.tempera_investigations.list_deterministic_evaluations({
    "project": "<project>",
    "investigation": "<investigation>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("tempera_investigations", "list_deterministic_evaluations", &[
    ("project", "<project>".into()),
    ("investigation", "<investigation>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listAgenticRuns

`GET /v1/projects/{project}/investigations/{investigation}/agenticRuns`

List agentic runs.

- **Producer operationId:** `listAgenticRuns`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-investigations`.
- **Scope:** `investigation:read`
- **Call as:** TypeScript `client.temperaInvestigations.listAgenticRuns()` · Python `client.tempera_investigations.list_agentic_runs()` · Rust `build_request("tempera_investigations", "list_agentic_runs", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `investigation` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaInvestigations.listAgenticRuns({
  project: "<project>",
  investigation: "<investigation>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.tempera_investigations.list_agentic_runs({
    "project": "<project>",
    "investigation": "<investigation>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("tempera_investigations", "list_agentic_runs", &[
    ("project", "<project>".into()),
    ("investigation", "<investigation>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listFindings

`GET /v1/projects/{project}/investigations/{investigation}/findings`

List findings.

- **Producer operationId:** `listFindings`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-investigations`.
- **Scope:** `investigation:read`
- **Call as:** TypeScript `client.temperaInvestigations.listFindings()` · Python `client.tempera_investigations.list_findings()` · Rust `build_request("tempera_investigations", "list_findings", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `investigation` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaInvestigations.listFindings({
  project: "<project>",
  investigation: "<investigation>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.tempera_investigations.list_findings({
    "project": "<project>",
    "investigation": "<investigation>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("tempera_investigations", "list_findings", &[
    ("project", "<project>".into()),
    ("investigation", "<investigation>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listInvestigationEvents

`GET /v1/projects/{project}/investigations/{investigation}/events`

List investigation events.

- **Producer operationId:** `listInvestigationEvents`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-investigations`.
- **Scope:** `investigation:read`
- **Call as:** TypeScript `client.temperaInvestigations.listInvestigationEvents()` · Python `client.tempera_investigations.list_investigation_events()` · Rust `build_request("tempera_investigations", "list_investigation_events", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `investigation` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaInvestigations.listInvestigationEvents({
  project: "<project>",
  investigation: "<investigation>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.tempera_investigations.list_investigation_events({
    "project": "<project>",
    "investigation": "<investigation>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("tempera_investigations", "list_investigation_events", &[
    ("project", "<project>".into()),
    ("investigation", "<investigation>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### listInvestigationReviews

`GET /v1/projects/{project}/investigations/{investigation}/reviews`

List investigation reviews.

- **Producer operationId:** `listInvestigationReviews`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-investigations`.
- **Scope:** `investigation:read`
- **Call as:** TypeScript `client.temperaInvestigations.listInvestigationReviews()` · Python `client.tempera_investigations.list_investigation_reviews()` · Rust `build_request("tempera_investigations", "list_investigation_reviews", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `investigation` | yes | Substituted into the URL path (percent-encoded). |

**Query parameters**

| Parameter | Required | Notes |
|---|---|---|
| `pageSize` | no | — |
| `pageToken` | no | — |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaInvestigations.listInvestigationReviews({
  project: "<project>",
  investigation: "<investigation>",
  pageSize: "<pageSize>",
  pageToken: "<pageToken>",
});
```

```python Python
result = client.tempera_investigations.list_investigation_reviews({
    "project": "<project>",
    "investigation": "<investigation>",
    "pageSize": "<pageSize>",
    "pageToken": "<pageToken>",
})
```

```rust Rust
let spec = client.build_request("tempera_investigations", "list_investigation_reviews", &[
    ("project", "<project>".into()),
    ("investigation", "<investigation>".into()),
    ("pageSize", "<pageSize>".into()),
    ("pageToken", "<pageToken>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### createInvestigationReview

`POST /v1/projects/{project}/investigations/{investigation}/reviews`

Create append-only review.

- **Producer operationId:** `createInvestigationReview`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-investigations`.
- **Scope:** `investigation:review`
- **Call as:** TypeScript `client.temperaInvestigations.createInvestigationReview()` · Python `client.tempera_investigations.create_investigation_review()` · Rust `build_request("tempera_investigations", "create_investigation_review", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `investigation` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaInvestigations.createInvestigationReview({
  project: "<project>",
  investigation: "<investigation>",
});
```

```python Python
result = client.tempera_investigations.create_investigation_review({
    "project": "<project>",
    "investigation": "<investigation>",
})
```

```rust Rust
let spec = client.build_request("tempera_investigations", "create_investigation_review", &[
    ("project", "<project>".into()),
    ("investigation", "<investigation>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getDeterministicEvaluation

`GET /v1/projects/{project}/investigations/{investigation}/deterministicEvaluations/{evaluation}`

Get deterministic evaluation.

- **Producer operationId:** `getDeterministicEvaluation`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-investigations`.
- **Scope:** `investigation:read`
- **Call as:** TypeScript `client.temperaInvestigations.getDeterministicEvaluation()` · Python `client.tempera_investigations.get_deterministic_evaluation()` · Rust `build_request("tempera_investigations", "get_deterministic_evaluation", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `investigation` | yes | Substituted into the URL path (percent-encoded). |
| `evaluation` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaInvestigations.getDeterministicEvaluation({
  project: "<project>",
  investigation: "<investigation>",
  evaluation: "<evaluation>",
});
```

```python Python
result = client.tempera_investigations.get_deterministic_evaluation({
    "project": "<project>",
    "investigation": "<investigation>",
    "evaluation": "<evaluation>",
})
```

```rust Rust
let spec = client.build_request("tempera_investigations", "get_deterministic_evaluation", &[
    ("project", "<project>".into()),
    ("investigation", "<investigation>".into()),
    ("evaluation", "<evaluation>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getAgenticRun

`GET /v1/projects/{project}/investigations/{investigation}/agenticRuns/{run}`

Get agentic run.

- **Producer operationId:** `getAgenticRun`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-investigations`.
- **Scope:** `investigation:read`
- **Call as:** TypeScript `client.temperaInvestigations.getAgenticRun()` · Python `client.tempera_investigations.get_agentic_run()` · Rust `build_request("tempera_investigations", "get_agentic_run", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `investigation` | yes | Substituted into the URL path (percent-encoded). |
| `run` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaInvestigations.getAgenticRun({
  project: "<project>",
  investigation: "<investigation>",
  run: "<run>",
});
```

```python Python
result = client.tempera_investigations.get_agentic_run({
    "project": "<project>",
    "investigation": "<investigation>",
    "run": "<run>",
})
```

```rust Rust
let spec = client.build_request("tempera_investigations", "get_agentic_run", &[
    ("project", "<project>".into()),
    ("investigation", "<investigation>".into()),
    ("run", "<run>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getInvestigationDossier

`GET /v1/projects/{project}/investigations/{investigation}/dossier`

Get cited investigation dossier.

- **Producer operationId:** `getInvestigationDossier`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-investigations`.
- **Scope:** `investigation:read`
- **Call as:** TypeScript `client.temperaInvestigations.getInvestigationDossier()` · Python `client.tempera_investigations.get_investigation_dossier()` · Rust `build_request("tempera_investigations", "get_investigation_dossier", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `project` | yes | Substituted into the URL path (percent-encoded). |
| `investigation` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaInvestigations.getInvestigationDossier({
  project: "<project>",
  investigation: "<investigation>",
});
```

```python Python
result = client.tempera_investigations.get_investigation_dossier({
    "project": "<project>",
    "investigation": "<investigation>",
})
```

```rust Rust
let spec = client.build_request("tempera_investigations", "get_investigation_dossier", &[
    ("project", "<project>".into()),
    ("investigation", "<investigation>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### getOperation

`GET /v1/operations/{operation}`

Get long-running operation.

- **Producer operationId:** `getOperation`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-investigations`.
- **Scope:** `investigation:read`
- **Call as:** TypeScript `client.temperaInvestigations.getOperation()` · Python `client.tempera_investigations.get_operation()` · Rust `build_request("tempera_investigations", "get_operation", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `operation` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaInvestigations.getOperation({
  operation: "<operation>",
});
```

```python Python
result = client.tempera_investigations.get_operation({
    "operation": "<operation>",
})
```

```rust Rust
let spec = client.build_request("tempera_investigations", "get_operation", &[
    ("operation", "<operation>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>

### cancelOperation

`POST /v1/operations/{operation}:cancel`

Cancel long-running operation.

- **Producer operationId:** `cancelOperation`
- **Auth:** Resource bearer — an OAuth access token or central `tp_` API key for audience `tempera-investigations`.
- **Scope:** `investigation:run`
- **Call as:** TypeScript `client.temperaInvestigations.cancelOperation()` · Python `client.tempera_investigations.cancel_operation()` · Rust `build_request("tempera_investigations", "cancel_operation", params)`

**Path parameters**

| Parameter | Required | Notes |
|---|---|---|
| `operation` | yes | Substituted into the URL path (percent-encoded). |

<CodeGroup>

```typescript TypeScript
const result = await client.temperaInvestigations.cancelOperation({
  operation: "<operation>",
});
```

```python Python
result = client.tempera_investigations.cancel_operation({
    "operation": "<operation>",
})
```

```rust Rust
let spec = client.build_request("tempera_investigations", "cancel_operation", &[
    ("operation", "<operation>".into()),
])?;
// Send spec.method / spec.full_url() / spec.headers / spec.body_json
// with your own HTTP client.
```

</CodeGroup>
